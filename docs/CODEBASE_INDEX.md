# XCodeAgent Codebase Index

Last updated: 2026-08-26

Use this file before broad code exploration. It is a routing map, not full documentation: read the matching row, then inspect only the listed entry files and nearby code.

## Update Policy

Update this index in the same change when a code change:

- Adds, removes, renames, or moves a directory or major module.
- Adds a new feature slice that changes ownership boundaries.
- Changes public API routes, Electron IPC channels, storage formats, AG-UI payloads, or workspace tool contracts.
- Changes validation/build expectations for an area.

For small local edits that do not change structure or ownership, leave the index alone and mention that no index update was needed.

## Top-Level Map

| Path                                | Responsibility                                                                                                                                             | Read first                                                                                                                        |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `AGENTS.md`                         | Repository-wide agent instructions, validation rules, and architecture constraints.                                                                        | Always read before edits.                                                                                                         |
| `docs/CODEBASE_INDEX.md`            | Directory/function routing map for future agents.                                                                                                          | Read before broad search or planning.                                                                                             |
| `contracts/authorization-api.v1.yaml` | 权限管理前后端共享的本地 OpenAPI 唯一事实源；后端 `auth` 分支副本必须与其 SHA-256 一致，前端只生成类型并通过既有 `service.ts` 发起请求。 | Read before changing authorization management APIs, auth-template generation, generated authorization types, or service wrappers. |
| `docs/WORKFLOW.md`                  | Design notes for the LangGraph workflow that coordinates requirement confirmation, project planning, code generation, integration testing, and acceptance. | Read before changing workflow phases, agent boundaries, Graph State shape, build/test subgraphs, or context/persistence strategy. |
| `docs/DAG_TASK_GENERATION_AND_VALIDATION_MININAL_CHANGE_PLAN.md` | Current minimal-change contract for Build DAG generation, deterministic validation, platform-owned regeneration, latest JSON confirmation, and the boundary between template initialization, runtime context, and Build execution. | Read before changing task preparation, DAG validation, Build gates, task-confirmation actions, or menu/route/page-placeholder ownership. |
| `docs/APPLICATION_REVISION_WORKFLOW.md` | Canonical design for post-creation Change lifecycle: stable change identity, target-aware impact analysis, candidate artifacts, confirmation gates, invalidation closure, Build DAG execution, acceptance, promotion, rollback, resource locking, and AG-UI projection. | Read first before changing secondary modification, acceptance adjustment, revision events/cards, candidate promotion, or quick-modification completion semantics. |
| `docs/APPLICATION_DEVELOPMENT_PLANNING.md` | Workbench page/API selection, development-readiness gating, independent EntitySourceBinding, Build DAG confirmation, and numbered development-task planning. | Read before changing workbench target selection, entity-binding readiness, development planning, or `menus.developmentTasks`. |
| `docs/PRODUCT_UI_TECHNICAL_PLANNING.md` | Canonical creation design: RequirementSpec → ProductPlan → React UiDesign → explicit planning-stage entry → TechnicalPlan, including Endpoint implementation semantics, PageImplementationContract, EntitySourceBinding boundary, and 128k context budget. | Read first when changing creation planning phases, ProductPlan/TechnicalPlan storage, UI authority, page implementation contracts, or Endpoint semantics. |
| `docs/DESIGN_CONVERSATION_AGENT.md` | Canonical design for the bottom Design Conversation Agent: earliest-node RequirementSpec/ProductPlan/UiDesign routing, incremental revision, live regeneration projection, confirmation gates, and the hard stop before TechnicalPlan. | Read first when changing design-phase bottom chat, design artifact rollback/invalidation, regeneration status UI, or the independent design conversation AG-UI endpoint. |
| `scripts/start-backend.sh`          | Standard macOS/POSIX backend dev-server command for agents and humans.                                                                                       | Run this on macOS when asked to start the backend service.                                                                        |
| `scripts/start-backend.ps1`         | Standard Windows PowerShell backend dev-server command with Python 3.12 virtual-environment discovery.                                                       | Run this on Windows when asked to start the backend service.                                                                      |
| `scripts/build-backend-mac.sh`      | macOS PyInstaller build and architecture-specific staging script for the packaged backend executable, required built-in skills, and `.env`.                 | Run with `x64` on Intel or `arm64` on Apple Silicon before matching `Frontend` mac packaging; staging stops on architecture or bundled-skill validation failures. |
| `docs/CROSS_PLATFORM_SUPPORT.md`    | Supported Windows/macOS architectures, development commands, packaging flow, signing boundary, and reference-architecture mapping.                          | Read before changing platform adapters, process lifecycle, or desktop packaging.                                                  |
| `scripts/build-backend-win.ps1`     | Windows-only PyInstaller build and staging script for the packaged backend exe, required built-in skills, and `.env`.                                       | Run on a Windows CI/VM before `Frontend` Windows packaging; staging stops when bundled skill validation fails.                    |
| `Backend/`                          | FastAPI backend, agent runtime, planning/orchestration, and local workspace tools.                                                                          | `Backend/app/main.py`, then the relevant module below.                                                                            |
| `Frontend/`                         | Electron + React desktop client with Vite, Ant Design, local session/app storage, AG-UI frontend clients.                                                  | `Frontend/src/main/index.ts`, `Frontend/src/renderer/src/pages/AppEntryPage.tsx`.                                                 |

## Backend

```text
Backend/app/
├── main.py
├── config.py
├── graph/          # LangGraph workflow lifecycle, state, nodes, and subgraphs
├── agents/         # First-class Deep Agent runtimes: code_analyze, code_review_repair, frontend, data_source, database, test, repair_planner, small_task, workspace_assistant
├── domain/         # Framework-independent core data models and workflow contracts
├── services/       # Deterministic business rules, normalization, scheduling, and quality gates
├── tools/          # Controlled tools exposed to Deep Agents
├── protocols/      # Protocol integration
├── workspace/      # Project workspace documents and sandboxed local file/tool access
├── middleware/     # Cross-cutting Deep Agent controls
├── persistence/    # Business and runtime persistence
├── observability/  # Runtime events and observability
└── utils/          # Shared dependency-light parsing and utility helpers
```

| Path                                                    | Responsibility                                                                                                                                                                                                                                                                                                                                                                                     | Read first / common edits                                                                                                                                                                                           |
| ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Backend/app/main.py`                                   | FastAPI app setup and route registration for `/health`, primary `/workflow/run`, optionally page/API-targeted `/conversation/run`, standalone product AG-UI actions, controlled `/tools/*` endpoints (including database metadata), approvals, git/workspace tools, and local preview controls `/api/projects/launch` and `/api/projects/stop`. Its lifespan clears main, creation-planning, and conversation Graph caches before closing shared checkpoint storage. | Add or change HTTP endpoints here, then update the corresponding runtime/tool module. |
| `Backend/app/config.py`                                 | Environment-backed model provider, endpoint, credentials, and runtime settings, including `BUILD_TASK_PLAN_MAX_RETRIES` (default `2`) for platform-owned Build DAG candidate regeneration.                                                                                                                                                                                                    | Read when changing `.env` variables, model/client configuration, or DAG generation retry policy.                                                                                                                    |
| `Backend/packaging/xcodeagent-backend.spec`             | PyInstaller onedir definition that bundles the Backend application, all built-in skills under `_internal/app/builtin_skills`, package data, and hidden imports.                                                                                                                                                                                                                                     | Read when changing packaged Python/data dependencies or frozen resource layout.                                                                                                                                    |
| `Backend/packaging/verify_bundled_skills.py`            | Cross-platform post-PyInstaller validator used by both Backend staging scripts to reject bundles missing required built-in skill files.                                                                                                                                                                                                                                                            | Update with required built-in skill files or supported PyInstaller resource layouts.                                                                                                                               |
| `Backend/packaging/backend_server.py`                   | Frozen Backend entry point: loads packaged configuration, validates required built-in skills before Uvicorn starts, and launches the API server.                                                                                                                                                                                                                                                   | Read when changing packaged Backend startup, frozen resource discovery, or fail-fast requirements.                                                                                                                  |
| `Backend/app/graph/workflow.py`                         | Primary LangGraph workflow declaration: page/API development starts at `development_readiness_gate`; missing entity bindings end with a manual EntitySourceBinding instruction, while ready targets continue through `inspect_workspace -> prepare_build_tasks -> DAG confirmation -> build -> test_phase_confirmation -> integration_test -> launch/acceptance/finalize`. EntitySourceBinding is an independent terminal branch. Build success stops at the structured test-phase confirmation gate; only `clarificationAnswers.test_phase_confirmation.action=confirm` resumes testing, while failed/incomplete Build states route to `handle_failure`. | Read when adding, removing, renaming, rerouting workflow phases, or changing workflow checkpoint behavior. Keep node logic in `graph/nodes/` or subgraphs. |
| `Backend/app/graph/direct_modification_workflow.py`     | Independent optionally targeted conversation Graph: always creates a bounded read-only workspace/code-graph snapshot before intent classification, then routes casual chat to a tool-less ChatModel, workspace questions to a read-only Agent, localized code/docs/config changes to bounded SmallTask execution, and formal changes to confirmed `/workflow/run` handoff. Current page/API target is included in classification context. Code changes retain backend-first fullstack execution, shared integration tests, preview, and the bounded `integration_test -> direct_modification_repair -> integration_test` loop (maximum 3 repair iterations); ordinary workspace-file edits use precise paths and skip preview. Checkpoints use a `conversation:` thread prefix. | Read with `graph/nodes/direct_modification.py`, `graph/nodes/direct_repair.py`, `agents/direct_modification.py`, `agents/workspace_assistant/`, and `protocols/direct_modification.py` when changing free conversation. |
| `Backend/app/graph/nodes/direct_repair.py`            | Dedicated free-conversation repair node: normalizes test evidence, gives a read-only RepairPlanner only the current real frontend/backend diff paths, executes bounded shared SmallTask tasks, persists the internal repair plan, and returns successful fixes to independent integration testing; stops at 3 iterations or on scope/contract/database escalation. | Read with `graph/direct_modification_workflow.py`, `services/small_task.py`, `agents/repair_planner/`, and `protocols/direct_modification_projection.py` when changing free-chat repair. |
| `Backend/app/graph/state.py`                            | `ProjectState` TypedDict for compact Graph State: project/request/workspace metadata, selected targets, EntitySourceBinding submission, formal artifact paths, checkpoint-internal TechnicalPlan repair candidate/errors, runtime `project_plan` context, Build DAG confirmation/progress, `test_phase_confirmation`, stable `test_target`, build/test summaries, unit-test generation context/mapping/change sets, quality gate, SmallTask state, code-review scan/repair/build state, preview, acceptance, failures, and timeline. Internal TechnicalPlan repair fields are excluded from AG-UI projection and never become formal artifacts; `project_plan` is a runtime projection for the current Build scope. Workspace `build-task-plan.json` remains the DAG confirmation authority. Build-gate/DAG-generation retry returns to `prepare_build_tasks`; ordinary task retry resumes `build`; completed Build pauses at `test_phase_confirmation` before `integration_test`. | Read when changing workflow state keys, persisted file references, readiness/binding resume contracts, DAG/test confirmation, code-review repair recovery, stale-checkpoint recovery, node contracts, or planning artifacts. |
| `Backend/app/graph/nodes/`                              | Thin phase nodes for classification, requirements, planning/detail confirmation, task preparation, direct modification, build/testing wrappers, and lifecycle phases. Task preparation checks confirmed formal artifacts, template readiness, scope detail, and API consistency before compiling a seven-stage DAG-generation snapshot through the shared progress service. Platform-owned DAG validation failures route to bounded model regeneration or `handle_failure`; they are not exposed as user task-splitting decisions. | Read the file matching the phase before changing node behavior; keep deterministic routing/state updates here and delegate reusable rules to `services/`.                                                           |
| `Backend/app/graph/nodes/tasks.py`                      | `prepare_build_tasks` is the DAG boundary: it consumes confirmed upstream artifacts, template manifest evidence, runtime `project_plan` context, Unit/target context, the current PageImplementationContract, TechnicalPlan Endpoint semantics, and confirmed EntitySourceBinding; compiles and validates the candidate; writes only the pending `build-task-plan.json`; and returns the structured confirmation payload. `task_graph.validation` is produced by task compilation/validation and read here before persistence, while confirmation later reloads the latest workspace JSON rather than trusting a checkpoint copy. Confirmation accepts only `confirm`, `patch`, and `regenerate`; patching is limited to task `title`/`description`, and platform boundary errors trigger automatic regeneration instead of manual correction. | Read when changing DAG prerequisites, Unit/context generation, task-graph validation, pending/confirmed status, workspace-authoritative recovery, confirmation actions, or the `project_plan` read-only boundary. |
| `Backend/app/graph/subgraphs/build.py`                  | Build Subgraph behind `build`: first reads the latest workspace `build-task-plan.json` and requires `build-dag.v3`, `status=ready`, `task_graph.validation.is_valid=true`, `confirmation_status=confirmed`, and matching Build scope before execution; stale checkpoint plans cannot bypass this gate. It then dispatches owners in bounded batches, attributes concurrent file changes per task authorization for code owners, lets database owner use database execution evidence, restores only explicitly requested `retry` failures to `pending` while preserving prior results, counts real repair dispatches, and exposes bounded repair-scope/database-approval confirmations. It does not normalize away invalid menu/route/page-boundary tasks or use a Markdown DAG artifact. | Read when changing task dispatch, owner concurrency, latest-JSON Build gates, confirmation recovery, owner result collection, explicit retry recovery, scheduler coordination after code generation, database approval pauses, or repair planning. |
| `Backend/app/graph/subgraphs/testing.py`                | Testing Subgraph behind `integration_test`: runs dependency installation, frontend/backend Build checks, pauses through an AG-UI confirmation for the optional advisory frontend performance test, launches the user `frontend` dev server with `frontend_performance_runner`, records the non-blocking Lighthouse check, and performs the integration quality gate; it no longer calls TestGenerationAgent or executes frontend/backend unit tests. Any blocking integration check routes to the independent `small_task_repair` loop; API contracts are validated before build and repair plans never route directly to `build`. | Read when changing integration build/performance confirmation, quality gate rules, test evidence shape (including `performanceScores`/`performanceMetrics`), revision requests, integration repair budgets, repair scope, or repair-task routing. |
| `Backend/app/graph/subgraphs/code_review.py`             | Code Review Subgraph behind the top-level `code_review` node: runs one read-only scan, pauses on `code_review_repair_confirmation`, performs bounded source-only repair, and runs independent frontend-install/frontend-build/backend-build checks with a three-round repair loop. It preserves the original scan result and emits bounded repair/build projections before `launch_project`. | Read when changing one-click review repair, source-only Agent scope, independent review build logs, repair iteration limits, or review AG-UI progress. |
| `Backend/app/graph/subgraphs/unit_testing.py`            | Development Unit Testing Subgraph behind `unit_test`: captures the first Build `code_changes/code_change_sets`, derives the bounded `unit_test_generation_context.code_diff`, handles the existing run/skip confirmation, calls TestGenerationAgent, validates generated files, executes affected frontend/backend unit tests, writes an independent unit report/quality gate, and plans the bounded `unit_test_repair` loop. Generated-test and repair changes merge into the development Diff while the original Build Diff remains stable for every retry. | Read when changing unit-test target mapping, generation context, run/skip recovery, TestGenerationAgent input, unit execution, unit reports, unit repair budget, or `unit_test.checks` projection. |
| `Backend/app/graph/nodes/small_task.py`                 | Main-graph SmallTask execution node: selects bounded parallel batches, calls the shared coding Agent, attributes real workspace diffs, pauses for scope/workflow confirmation, and returns successful local fixes to the explicit `repair_return_node` (`unit_test` or `integration_test`). | Read when changing repair execution, escalation targets, parallelism, or SmallTask state transitions. |
| `Backend/app/deprecated/graph_agent.py`                 | Deprecated, fully commented legacy/main chat runtime formerly at `graph/agent.py`; retained only as reference for future workflow parity work.                                                                                                                                                                                                                                                     | Do not import from main flow. Read only when intentionally backporting old tool/approval/diff behavior into workflow nodes.                                                                                         |
| `Backend/app/deprecated/graph_orchestrator.py`          | Deprecated, fully commented earlier development orchestration runtime formerly at `graph/orchestrator.py`.                                                                                                                                                                                                                                                                                         | Do not import from main flow. Read only when comparing old orchestrator behavior with workflow.                                                                                                                     |
| `Backend/app/agents/registry.py`                        | Creates and groups the coding, planning, repair, workspace-assistant and internal TestGeneration Deep Agents; the latter is best-effort and may be unavailable without blocking the workflow. The registry force-appends validated selected `SKILL.md` documents to prompts and caches bundles by normalized workspace, skill whitelist, immutable user-skill revision, and AGENTS.md memory revision. | Read when adding/removing first-class agents or changing selected-skill prompt injection and workspace/skill/memory-aware Agent caching. |
| `Backend/app/agents/code_analyze/`                       | Read-only CodeAnalyze DeepAgent wrapper and deterministic result normalizer. It exposes only list/search/read operations, loads exactly the frontend-code-scan and backend-code-scan built-in Skills, enforces `frontend/src` and `backend/src/main/java` workspace boundaries, rejects writes/commands/delegation, validates relative issue paths, de-duplicates and caps findings at 100, and retries malformed or incomplete Agent output once. | Read when changing review-scan Skill loading, workspace security scope, review result shape, or failure/retry policy. |
| `Backend/app/agents/test_generation/`                   | Internal TestGeneration Agent, generator, mapping-cache persistence, deterministic test-file validation, explicit read/list/search delegation, and hard backend write scope. It may read bounded workspace/project artifacts and the current Build diff but can write only flat `frontend/tests/*.test.ts(x)` or mirrored `backend/src/test/java/**/*.java`; LangGraph checkpoint churn is excluded from workspace-change acceptance while formal `.xcodeagent` artifacts remain protected. | Read when changing unit-test naming, source/test mapping, generation failure policy, checkpoint filtering, backend delegation, or test-only filesystem permissions. |
| `Backend/app/agents/workspace_scope.py`                 | Normalizes `workspaceRoot`, creates DeepAgents filesystem/Composite backends, mounts packaged built-in skills plus immutable user-skill and AGENTS.md memory snapshots at read-only virtual paths, and defines workspace-confined filesystem permissions including the test-generation-only write mode. | Read when changing DeepAgents workspace access, skill/memory mounts, virtual filesystem behavior, or sensitive-file policy. |
| `Backend/app/middleware/direct_modification.py`         | Request-sensitive policy installed on shared coding Agents. It recognizes direct-chat and SmallTask markers and removes task/todo orchestration only for those bounded runs, while inheriting the normal model, retry, timeout, recursion, and command runtime configuration; for every model request it also restores the factory-bound `execute` tool if upstream middleware rebuilt the tool list, without restoring broader tools that another policy intentionally removed. | Read with `agents/direct_modification.py`, `agents/small_task/`, both coding Agent factories, and `agents/tool_activity_stream.py` when changing quick-run or formal-run tool availability. |
| `Backend/app/agents/model_factory.py`                   | Builds LangChain chat models for direct reasoning calls and Deep Agents from backend settings.                                                                                                                                                                                                                                                                                                     | Read when changing model provider wiring for direct ChatModel or Deep Agents.                                                                                                                                       |
| `Backend/app/agents/messages.py`                        | Graph-independent helpers for extracting text from DeepAgent result messages without importing workflow nodes.                                                                                                                                                                                                                                                                                     | Read when changing shared Agent result parsing or avoiding Agent-to-Graph dependency cycles.                                                                                                                        |
| `Backend/app/agents/direct_modification.py`             | Unified free-conversation classifier that can return the final tool-less casual response in the same model call, with a fallback responder, plus the read-only workspace-question wrapper and frontend/backend/workspace SmallTask wrappers. Formal classification and Agent escalation produce confirmation cards instead of silently entering planning. | Read when changing conversation intent/owner semantics, SmallTask reuse, frontend Skill prerequisites, backend-to-frontend handoff, or response/result schemas. |
| `Backend/app/agents/workspace_assistant/`               | Read-only Deep Agent for questions that require current workspace evidence. It can progressively read workspace files plus selected Skill/AGENTS snapshots, but cannot write, execute commands, plan todos, or spawn subagents. | Read when changing workspace-question evidence, permissions, or natural-language answer behavior. |
| `Backend/app/agents/small_task/`                        | Shared bounded coding Agent declaration, source-first runner, and scoped filesystem backend. It receives a compact task packet containing source candidates, allowed paths, failure evidence, confirmed context, and acceptance criteria; unscoped search starts at the narrow task source root, installed dependencies/caches/build outputs are unreadable and unwritable, and results use completed/already-satisfied/escalation/failed JSON without planning, subagents, or formal/database mutation. | Read when changing SmallTask prompts, read/write scope, source-candidate priority, result normalization, free-chat reuse, or the 128k context boundary. |
| `Backend/app/agents/tool_activity_stream.py` and `tool_activity_projection.py` | Streams root/delegated Deep Agent messages, returns the authoritative root result, and projects filesystem, execute, task, and todo calls into bounded safe activity records without exposing command arguments, prompts, file contents, or host paths. | Read when changing Agent progress visibility, namespace/call-id handling, execution budgets, or AG-UI tool step semantics. |
| `Backend/app/agents/main/`                              | Direct ChatModel workflow reasoning boundaries for requirements, ProductPlan/TechnicalPlan revision, task preparation, and user-edited Markdown-to-JSON synchronization. TechnicalPlan request bodies follow operation semantics rather than HTTP Method; validation retries first project and replace only implicated API Contracts, then fall back to full-plan revision when no Contract can be located. It does not declare or create a Main DeepAgent and contains no page/API detail-design generator. `task_preparer_prompt.py` owns the scoped eight-section Build-task prompts, bounded WorkspaceSnapshot projections, an independent source-specific Skill Injection section, fixed database/external task algorithms, stable structural task-ID guidance, module resolution, and final forbidden-output rules. `task_preparer.py` owns the model call, acceptance-field reset, deterministic DAG validation feedback, and bounded automatic regeneration (default two retries, three total attempts). | Read the matching file before changing direct reasoning or document-sync boundaries; keep these calls free of workspace tools and keep the outer LangGraph lifecycle in nodes.                                      |
| `Backend/app/agents/repair_planner/`                    | Dedicated read-only RepairPlanner DeepAgent node plus prompt wrappers for build-failure and testing repair planning. It consumes deterministic command-failure evidence only after a quality-gate failure and must not edit files or mutate workflow state. | Read when changing RepairPlanner prompts, DeepAgent permissions, RepairPlannerInput/RepairPlan behavior, or testing/build repair planning calls. |
| `Backend/app/agents/code_analyze/`                      | Read-only CodeAnalyzeAgent and result normalizer. It force-loads the two built-in scan Skills, validates tool activity, normalizes bounded issue/target output, drops unsupported frontend findings when no rules are configured, and rejects paths or Skills outside the current contract. | Read when changing scan output normalization, Skill-read verification, source directory boundaries, or the no-rescan contract. |
| `Backend/app/agents/code_review_repair/`                | CodeReviewRepairAgent declaration and bounded repair wrapper. It exposes only source read/search/write tools, accepts the current issue packet plus bounded build evidence, and returns a strict repair result that is checked against real workspace Diff capture. | Read when changing one-click repair prompts, allowed source roots, repair result validation, or Agent tool filtering. |
| `Backend/app/agents/frontend/`                          | Frontend Generation Agent declaration and generator wrapper for approved frontend tasks. The real workspace is passed only to the Agent bundle/backend; model prompts expose the virtual root `/`.                                                                                                                                                                                                 | Read when changing page/component generation responsibilities, allowed paths, frontend task reports, or frontend agent permissions.                                                                                 |
| `Backend/app/agents/data_source/`                       | Internal Data Source Generation runtime whose model role is the Java Backend Coding Agent. It expands task-specific database/external-API Skill references, emits bootstrap or endpoint `implementation_contract` packets, and invokes approved backend tasks without exposing unrelated ProjectPlan context. A separate Backend Workspace Context reads `WorkspaceSnapshot.backend.dir_structure` and exposes both the virtual Java project root and the complete backend directory tree as trusted path-navigation metadata; listed relevant files may be read for implementation context, while current filesystem reads remain authoritative and task paths remain the write boundary. The context is never sourced from BuildTaskPlan or embedded in task packets and implementation contracts. Bootstrap packets contain only Java/Maven/Spring/MyBatis/MySQL policy; endpoint packets contain only the scoped API contract, confirmed EndpointDetail behavior, and confirmed entity source bindings. The internal registry/audit identity remains `data-source-generation-agent`; model paths use virtual root `/` and do not use the code graph. | Read when changing Java backend model responsibilities, WorkspaceSnapshot projection, source-to-Skill/reference routing, implementation-contract projection, allowed paths, reports, or agent permissions. |
| `Backend/app/agents/database/`                          | Database Change Agent declaration and generator wrapper for database owner tasks. It re-reads real MySQL schema summaries, asks a read-only Deep Agent for an exact SQL plan, runs deterministic risk classification, pauses high-risk plans through `ApprovalStore`, and executes approved/low-risk plans through controlled MySQL execution instead of editing code. | Read when changing database task execution, SQL plan schema, high-risk approval behavior, latest-schema refresh, or database agent permissions. |
| `Backend/app/deprecated/agents_requirement_planner.py`  | Deprecated, fully commented requirement planner runtime formerly at `agents/requirement_planner.py`.                                                                                                                                                                                                                                                                                               | Do not import from main flow. Read only for reference while moving planner parity into workflow.                                                                                                                    |
| `Backend/app/agents/subagents.py`                       | Subagent input/result contracts and direct-write guards.                                                                                                                                                                                                                                                                                                                                           | Read when wiring scout/build/verifier agents or changing subagent payloads.                                                                                                                                         |
| `Backend/app/domain/development_contract.py`            | Framework-independent Development Contract models and normalization used by the earlier orchestrator and frontend typings.                                                                                                                                                                                                                                                                         | Read when changing SDD, features, API contracts, task graph, or verification plan payloads.                                                                                                                         |
| `Backend/app/domain/acceptance_adjustment.py`          | Typed acceptance-adjustment contract and safe recovery-node mapping for local fixes, detail redesign, data-source changes, and ProjectPlan changes.                                                                                                                                                                                                                                                  | Read when changing post-acceptance routing, adjustment payload validation, or confirmation-gate recovery.                                                                                                            |
| `Backend/app/domain/models.py`                          | Framework-independent workflow document/task models such as RequirementSpec, ProjectPlan, PageSpec, BuildTask, v3 database/backend/frontend task ownership, database planning context, and report shapes.                                                                                                                                                                                                                                                                  | Read when changing spec/plan/task/test schemas used by the new workflow.                                                                                                                                            |
| `Backend/app/domain/application_lifecycle.py`               | Strict Pydantic schema for `.xcodeagent/application-lifecycle.json` without a schema-version field: top-level `application` is the sole workspace business identity, while `initialization` is the application-initialization gate and temporarily owns its checkpoint `threadId`; entering `ready_for_workbench/completed` clears that thread. Run-indexed workbench executions separately own page/API/data-source locks, revision-bound pending interactions, run references, and errors; no duplicate project identity, root pending interaction, or delivery projection is stored. | Read before changing the workspace application-initialization schema, creation gate, plan-execution lock, or interaction contracts. |
| `Backend/app/services/request_complexity.py`            | Legacy model-backed simple/complex classifier retained outside the current main Graph. | Read only when intentionally changing the legacy complexity boundary. |
| `Backend/app/services/direct_modification.py`           | Deterministic quick-modification rules: bounded SmallTask JSON/handoff normalization, authoritative snapshot file lists, frontend/backend path ownership checks, and per-run dynamic authorization of exact existing files outside normal source roots without a file-type whitelist. Dynamic paths must remain inside the current owner and workspace and cannot enter sensitive, dependency, generated, migration, schema, or formal-artifact locations. It also owns no-diff handling, recoverable partial-change/tool-error warnings, bounded rolling summary, final user message, and test-log references. | Read with the direct Agent wrapper and Graph nodes when changing quick result validation, dynamic path authorization, final acceptance, or context limits. |
| `Backend/app/services/direct_repair.py`                 | Deterministic free-conversation repair support: extracts exact parent-authorized frontend/backend diff paths, normalizes test owners and RepairPlanner tasks, persists internal plans, and builds scope/formal-workflow handoffs without allowing guessed paths. | Read with `graph/nodes/direct_repair.py`, `services/small_task.py`, and `agents/repair_planner/` when changing free-chat repair scope or escalation. |
| `Backend/app/services/small_task.py` and `small_task_scope.py` | Deterministic SmallTask packet construction, capped parallel scheduling, dependency/path/resource conflict checks, preflight escalation, confirmation scope application, batch snapshot attribution, unauthorized-change detection, and bounded handoff payloads. Directory-level repair authorization such as `frontend` or `backend` covers nested workspace source files consistently in both attribution and batch acceptance, so successful bounded fixes return to the explicit unit-test or integration-test node. | Read when changing repair-task execution, concurrency, scope safety, or workflow escalation. |
| `Backend/app/services/agent_file_documents.py`          | Owns the environment-scoped `~/.xcodeagent*/AGENTS.md` default template, safe creation/read/save rules, UTF-8 and 32 KiB validation, atomic replacement, and revision conflict checks. | Read with `protocols/agent_files.py` and `services/agent_memory_runtime.py` when changing AGENTS.md storage, validation, or save semantics. |
| `Backend/app/services/code_change_revert.py`            | Validates historical workspace change sets and safely reverts their complete text patches through Git, including repository/workspace path confinement, staged-file rejection, temporary reverse simulation, conflict preflight, and atomic application. | Read with `workspace/code_changes.py` and `protocols/code_changes.py` when changing historical change rollback behavior or safety rules. |
| `Backend/app/services/version_control.py`               | Owns deterministic Git inspection and explicit small-scope commits after a revision has been explicitly accepted. It requires the workspace to be its own repository root, filters actual dirty files to the run-associated paths, fingerprints HEAD/status/diffs, rejects stale reviews, sensitive/internal paths, unborn repositories, and pre-existing staged changes, then commits only user-selected files. | Read with `protocols/version_control.py`, `docs/APPLICATION_REVISION_WORKFLOW.md`, and the frontend version-control client/reminder before changing commit safety, fingerprints, or revision commit behavior. |
| `Backend/app/services/agent_memory_runtime.py`          | Converts the mutable environment AGENTS.md into a one-file immutable, read-only DeepAgents memory snapshot mounted at `/.xcodeagent/agent-memory/AGENTS.md`; its content revision invalidates cached Agent bundles so the next run loads saved instructions. | Read with `agents/registry.py` and `agents/workspace_scope.py` when changing AGENTS.md runtime injection, virtual mounts, or snapshot consistency. |
| `Backend/app/services/entity_definitions.py`            | Shared deterministic entity model: RequirementSpec entities keep display-only fields, while confirmed technical/entity designs add normalized field names and semantic types; data source selection belongs to the entity-design stage. It derives JSON Schema/API bindings and MySQL target tables without exposing credentials. | Read before changing entity normalization, schema derivation, table mapping, or field validation. |
| `Backend/app/services/entity_design.py`                | Deterministic workbench entity-design service covering database, external API, and static data sources; validates bindings and rules, persists confirmed entity artifacts, and executes approved database table operations with evidence. | Read with `entity_design_assist.py` and `tools/database_tools.py` when changing entity design or database metadata browsing. |
| `Backend/app/services/entity_design_assist.py`         | Bounded AI assistance for entity table selection, field bindings, mappings, seed data, rules, relationships, acceptance, and risks; suggestions never bypass deterministic confirmation. | Read when changing entity-assist prompts or suggestion schemas. |
| `Backend/app/tools/database_tools.py`                  | Controlled read-only database metadata endpoints used by entity design. | Read when changing table/column browsing contracts. |
| `Backend/app/services/requirement_spec.py`              | Deterministic RequirementSpec builder and draft-save service. It consolidates original requests with clarification answers and normalizes only business permission semantics, without page/entity/operation/resource bindings. RBAC rules carry hidden UUID `ruleId`, non-empty `defaultGrantedRoleIds`, source references, and data rules additionally carry stable `dataRuleKey` plus `includes/excludes`; page/operation unauthorized behavior is fixed and not stored. The requirements analyzer first extracts and raw-validates explicit role/permission facts before any clarification persists; `isSystemRole/isInitialAdminRole` and `initialAdminRoleId` become mandatory only at confirmation. Markdown sync protects hidden rule/data-key markers. | Read when changing RequirementSpec fields, staged permission clarification, default role grants, initial-admin selection, hidden rule/data identity, editor saves, normalization, or product-versus-engineering boundaries. |
| `Backend/app/services/application_authorization_config.py` | Current schema v5 application-permission configuration service. It validates the database and real initial-administrator subject prerequisites, then atomically enables `auth.enable` and `authorization` in `.xcodeagent/application.json`; it is only called after the application-planning AG-UI conflict clarification is answered. | Read with `graph/nodes/requirements.py` and `agents/main/requirements_analyzer.py` when changing the authorization-configuration conflict gate or application.json permission persistence. |
| `Backend/app/services/product_plan.py`                  | Canonical `product-plan.v5` model-output validator and normalizer. It preserves RequirementSpec page identity, requires stable lower_snake_case `pageId/actionId`, owns product-visible page behavior and acceptance, deterministically maps confirmed page/operation `ruleId` values to stable page/action targets, rejects roles, role assignments, authorization/resource fields, the fixed `/roles` page, and historical formats, allows display-only pages without actions, and closes navigation targets. | Read with `agents/main/product_planner.py`, `workspace/product_plan_documents.py`, and UI generation when changing ProductPlan JSON, stable identifiers, permission-target mapping, field semantics, Markdown projection, or confirmation gates. |
| `Backend/app/services/ui_design_manifest.py`            | Canonical `ui-manifest.v3` builder, persistence reducer, ProductPlan control/local-effect inspector, and deterministic UI ownership validator; formal manifests omit duplicated product facts and support explicit UI skip. | Read with UI confirmation/generation, PageImplementationContract code, and `docs/PRODUCT_UI_TECHNICAL_PLANNING.md`. |
| `Backend/app/services/project_plan.py`, `authorization_manifest.py` | Own the compact current TechnicalPlan and its deterministic `authorization-manifest.v1`: three-part architecture, RequirementSpec-derived top-level entity field definitions, API contracts bound only by `entity_ids` with `entity_field_ref` Schema links, fixed system/business resource keys, endpoint data/operation bindings, upstream hashes, and `pages[{pageId,references}]`. Runtime PageImplementationContracts compile later and are not persisted here. | Read when changing TechnicalPlan, API/schema/entity contracts, resource manifests, stack constraints, or page references. |
| `Backend/app/graph/application_planning_workflow.py`    | New-application planning Graph: normal creation runs `requirements -> product_planning -> ui_confirmation -> planning_stage_entry -> technical_planning`; RequirementSpec and ProductPlan use the joint `requirement_document_review`, UI confirmation or skip always stops at the entry interrupt, and only `enter_planning` starts TechnicalPlan. Unlocked bottom input reuses the same Graph/thread through `design_intent_analysis`; design input routes to the earliest affected artifact and follows every downstream gate again. | Read with `docs/PRODUCT_UI_TECHNICAL_PLANNING.md`, `docs/DESIGN_CONVERSATION_AGENT.md`, `agents/design_conversation/router.py`, `services/application_planning_persistence.py`, and the Welcome creation components. |
| `Backend/app/graph/application_planning_revision.py` | Focused design-revision adapter for the original application-planning Graph: intent classification, earliest confirmed-node guard, original-request propagation, pre-run artifact-presence tracking for first-generation/regeneration copy, applied-node tracking, UI incremental action preparation, and controlled lifecycle rewind. | Read with `application_planning_workflow.py` when changing bottom-input design revisions; do not create a second Graph or thread. |
| `Backend/app/services/ui_design_generation_pool.py`     | Process-global decoupled worker pool (方案 B) that generates page design TSX outside the LangGraph sync path. `ui_confirmation` only enqueues per-page `regenerate`/`select_template` intents; N workers (`Settings.ui_design_concurrency`, default 3) run the LLM generation in threads, persist `.xcodeagent/ui-design/pages/<PageKey>/index.tsx`, and merge `queued → generating → confirmed/generation_failed` statuses into `.xcodeagent/specs/ui-designs.json`. Frontend progress arrives by polling a no-op AG-UI resume that re-reads the manifest. | Read with `graph/nodes/ui_confirmation.py`, `services/ui_design_generator.py`, `services/ui_design_manifest.py`, and `workspace/spec_documents.py` when changing decoupled generation, concurrency, dedup, recovery, or page status semantics. |
| `Backend/app/services/application_planning_persistence.py` | Atomically validates the current RequirementSpec/ProductPlan/UiManifest/TechnicalPlan artifacts during creation, accepting confirmed or explicitly skipped UI design, then projects menus, APIs, schemas, entities and data sources for workbench/template generation. | Read when changing the new-application persisted schema, storage path, deferred application fields, menu projection, or TechnicalPlan-to-application mapping. |
| `Backend/app/services/frontend_page_tree.py`            | Shared helpers for `ProjectPlan.frontend_pages`: identifies menu nodes vs page leaves, flattens leaf pages for execution and validation, preserves or rebuilds menu hierarchy during normalization, and provides stable menu/page lookup utilities across workflow, persistence, and workbench projections. Route normalization propagates parent menu paths to descendants, prevents menu-enabled homepage-like pages from collapsing to the bare root route, avoids auto-creating parent menus when a module has only one visible page plus dynamic/detail routes, and demotes menu routes that equal a direct page route into route-less grouping nodes. | Read when changing `frontend_pages` storage shape, menu-node rules, leaf-page execution semantics, or tree rebuild/update behavior. |
| `Backend/app/services/application_lifecycle.py`             | Owns the strict v1.4 application-initialization state machine plus separately persisted workbench executions. Initialization advances through requirement, ProductPlan, UI design, and TechnicalPlan confirmation gates, keeps its thread only until workbench entry, and rejects unconfirmed formal artifacts. Workbench executions can pause at typed `unit_test_confirmation`, `frontend_performance_confirmation`, `test_phase_confirmation`, and `review_phase_confirmation` gates; performance selection resumes only in the same test thread, while the two stage-confirmation gates perform the bounded cross-thread handoff into fresh test/review conversations. Only the TechnicalPlan-confirmation transition can enter template generation; template failure is terminal and ready/failed states cannot reopen initialization. Template completion also invokes the manifest/real-file gate; lifecycle revision/CAS behavior remains the sole version control for this stage. Workbench running/HITL/failure/acceptance update `activeExecutions` and resource locks without reopening initialization. | Read with `domain/application_lifecycle.py`, `services/application_template_generation.py`, `workspace/run_lease.py`, planning protocols, and `protocols/workflow/lifecycle.py` when changing initialization confirmation, template readiness, page-scope concurrency, plan execution, acceptance, or lifecycle persistence. |
| `Backend/app/services/application_template_generation.py`, `frontend_scaffold.py` | Own the post-TechnicalPlan template readiness boundary and the only page-placeholder/menu initialization path. They normalize frontend/backend download results, read the latest confirmed ProductPlan v4 plus confirmed/skipped UiDesign v3, derive stable PageKeys, run page-placeholder and menu injection in a controlled two-worker pool, never overwrite existing pages or menus, atomically persist `.xcodeagent/template-generation-manifest.json`, and gate completion against manifest plus real files. DAG preparation only consumes this readiness evidence; it does not create or modify the scaffold, menu, route, or placeholder files. | Read with `services/application_lifecycle.py`, `protocols/application_lifecycle.py`, `services/ui_design_generator.py`, and `docs/APPLICATION_TEMPLATE_GENERATION_STATUS_AND_PLAN.md` when changing template download, page/menu initialization, manifest schema, or readiness checks. |
| `Backend/app/protocols/application_lifecycle.py`             | Dedicated `/application-lifecycle/run` AG-UI adapter for lifecycle create/get, structured template preparation, and template-completion actions. `prepare_template_generation` receives per-target download status/attempt/error and runs filesystem work off the event loop. Lifecycle actions are not accepted by the application-planning endpoint. | Read with `services/application_lifecycle.py`, `services/application_template_generation.py`, and frontend `service/applicationLifecycle.ts` when changing lifecycle action payloads, events, or cold-start calibration. |
| `Backend/app/protocols/ag_ui_action_stream.py`          | Shared AG-UI lifecycle wrapper for deterministic product actions. Owns run/message start and finish events, custom events, state snapshots, optional progress-to-text projection, streamed assistant text deltas, structured success envelopes, and structured error envelopes; product adapters retain their own action dispatch and payload schemas.                                                                                                                       | Read when adding or changing a simple workflow-independent AG-UI product action. Do not force the streaming LangGraph workflow through this helper.                                                                 |
| `Backend/app/protocols/application_page_planning.py`    | AG-UI adapter exposing the design stages, explicit `planning_stage_entry`, TechnicalPlan stage, design-change intent entry, non-confirming `requirementSpecDraft.save`, and read-only checkpoint recovery. All requests reuse the original planning thread and publish the `planning_stage_entry_confirmation` plus formal artifact modes. | Read when changing new-application planning transport, design revision handoff, cold-start checkpoint recovery, editable artifact save/confirmation contracts, or capability metadata. |
| `Backend/app/protocols/code_changes.py`                 | Workflow-independent AG-UI adapter for confirmed historical code-change rollback at `/code-changes/run`, exposing `code-changes` custom events and `codeChangesAction` state snapshots. | Read with `services/code_change_revert.py` and frontend `service/codeChanges.ts` when changing rollback payloads, lifecycle, or errors. |
| `Backend/app/protocols/version_control.py`              | Workflow-independent `/version-control/run` AG-UI adapter for Git `inspect` and user-confirmed `commit` actions, exposing `version-control` custom events and `versionControl` state snapshots without putting commit logic into the main Workflow. | Read with `services/version_control.py` and frontend `service/versionControl.ts` when changing action payloads, lifecycle, or commit errors. |
| `Backend/app/protocols/direct_modification.py` and `direct_modification_projection.py` | Dedicated `/conversation/run` AG-UI runtime plus bounded projection. They validate `forwardedProps.conversation`, run the isolated `conversation:` checkpoint thread, acquire a workspace lease only for write intent, project node/tool/test progress, preserve handoff confirmation, and expose `conversation` custom events/snapshots. | Read with the conversation Graph and frontend `service/agUiAgent.ts` before changing request, progress, cancellation, handoff, or result contracts. |
| `Backend/app/services/page_dependencies.py`             | Canonical ProjectPlan page dependency normalization and validation. Owns unique leaf-page id/path checks plus the immutable `frontend_pages[].references` projection (`permissions`, `endpoint_dependencies`, `navigation_targets`) consumed by runtime PageImplementationContract and Build context compilation; menu nodes are preserved but never treated as executable pages. It derives data-source task context from endpoint IDs. | Read when changing page dependency schema, menu-vs-page validation rules, endpoint-bound task context, route/id validation, or ProjectPlan-to-runtime-contract ownership. |
| `Backend/app/services/database_crypto.py`               | Owns the environment-scoped persistent RSA-3072 platform key, strict 0700/0600 atomic storage, public-key health metadata, versioned RSA-OAEP-SHA256 ciphertext parsing, and password decryption. | Read before changing database credential algorithms, key location/version/rotation, secret envelope format, startup behavior, or public metadata. Never project private-key material. |
| `Backend/app/services/database_credentials.py`          | Reads one workspace's `.xcodeagent/application.json`, maps `datasource.db.plantMode` to a strong `MySQLConnectionConfig`, decrypts versioned passwords, validates connection fields, builds the shared Spring JDBC URL, and read-compatibly accepts legacy plaintext without rewriting files. | Read with `database_crypto.py`, `tools/mysql_info.py`, and `backend_project_launcher.py` when changing application-level MySQL configuration. Keep plaintext out of errors, logs, Graph State, and tool inputs. |
| `Backend/app/services/database_requirement_schema.py` and `database_schema_diff.py` | Deterministic database requirement and gap compiler retained for entity-design and dedicated database execution flows. It compiles confirmed structured operations into target tables/columns, compares them with actual MySQL metadata, ignores extra actual objects, and emits bounded differences without interpreting prose. | Read when changing entity-to-table compilation, missing database/table/column/type/nullability/default handling, or dedicated database execution verification. |
| `Backend/app/services/database_schema_summary.py`       | Shared MySQL schema-summary helper used by entity design, database tools, and the Database Agent: requires a workspace root, calls `get_mysql_table_info_for_workspace`, truncates schema output, and normalizes target/connection-failure summaries. | Read when changing common MySQL inspection, workspace-bound application credential resolution, summary truncation, or table/column/index/foreign-key projection. |
| `Backend/app/services/database_execution.py`            | Controlled database execution helper for database owner tasks: fingerprints latest schema and SQL plans, classifies high-risk SQL/task intent, creates or consumes `database.execute` approvals, resolves the current workspace's application-level MySQL credentials, creates a missing target database when executing approved create-database work, and records MySQL DDL evidence without promising full rollback. | Read when changing database high-risk policy, application-level connection resolution, approval fingerprints, SQL execution evidence, rollback planning, or the future AG-UI database-connection input flow. |
| `Backend/app/services/backend_project_launcher.py`      | Graph-independent Java backend launch service: `launch_backend_project(workspace_path)` accepts only a normal `str | Path` workspace, derives `.xcodeagent/runtime/launch/`, resolves the current application's database environment while removing inherited global database variables, serializes each workspace launch, stops the previously managed Java process before Maven, runs `mvn clean install`, selects one non-auxiliary `*-SNAPSHOT.jar`, automatically runs `mvn -B package spring-boot:repackage` when that JAR lacks `Main-Class`, starts and registers `java -jar`, and accepts readiness only while the process lives and current-launch stdout/stderr contains `Spring Boot Version` or `ZA21 Version`. Cleanup and synchronous command failures become structured results instead of escaping the Workflow. | Read when changing the public backend launch contract, application-level runtime environment injection, prebuild cleanup ordering/evidence, Maven/JAR discovery, Java startup, readiness markers, logs, timeout behavior, or rollback. |
| `Backend/app/services/backend_process_registry.py`      | Workspace-scoped Java preview process registry and lock manager. Owns in-memory `Popen` registration, `backend.pid` recovery after Backend restart, current-workspace `java -jar` command identity validation, graceful/forced cross-platform termination, stale PID cleanup, and fail-closed cleanup evidence. | Read when changing backend preview process ownership, PID persistence/recovery, process identity checks, concurrent launches, termination timeouts, or Windows/POSIX stop behavior. |
| `Backend/app/services/frontend_project_launcher.py`     | Graph-independent generated-project frontend launch/stop service: `launch_frontend_project(workspace_path)` accepts only a normal `str | Path` workspace, derives `.xcodeagent/runtime/launch/`, finds frontend `package.json`, resolves full npm/pnpm/yarn executable paths for Windows `.cmd` support, installs dependencies, applies framework-compatible host rules, reuses a prior ready frontend service, and supervises a background dev server; `stop_frontend_project(workspace_path)` stops the recorded frontend dev-server process. Synchronous install/stop `OSError` values become structured failures. Explicit script ports are honored; missing ports resolve to `http://127.0.0.1:80`. | Read when changing the public frontend launch/stop contract, cross-platform command resolution, package-manager detection, dev-script selection, preview URL inference, process/readiness handling, frontend preview shutdown, logs, or acceptance metadata. |
| `Backend/app/services/project_launcher.py`              | Thin generated-project preview facade that preserves stable imports for Workflow nodes and FastAPI routes. It re-exports backend/frontend launch helpers and combines `stop_frontend_project` plus `stop_workspace_backend_project` into `stop_project_preview(workspace_path)`. | Read when changing the preview facade, combined `/api/projects/stop` behavior, or backwards-compatible launcher imports. |
| `Backend/app/services/api_contracts.py`                  | Canonical compact API-contract normalization, schema references, endpoint dependencies, and page response-field bindings. API contracts bind TechnicalPlan entities through non-empty `entity_ids`; Entity data sources resolve from confirmed EntitySourceBinding artifacts. | Read when changing schemas, endpoint ids, entity bindings, parameter/request/response references, or exact page-to-endpoint field mapping. |
| `Backend/app/services/api_contract_validation.py`       | Deterministic pre-build validation that rejects contracts with empty or unknown `entity_ids`, unresolved schema/endpoint references, invalid backend endpoint shapes, and page fields absent from API responses. Data-source bindings are not part of API Contract validation.                                                                                                                                                         | Read when changing contract consistency rules or the code-generation safety gate.                                                                                                                                   |
| `Backend/app/services/build_task_planner.py` and `engineering_acceptance.py` | Compile workspace-aware model candidates into `build-dag.v3`, including structural task normalization, file scopes, dependencies, owner semantics, execution batches, and deterministic pure-engineering `acceptance_checks`. Structural normalization covers field aliases, path/operation defaults, `target_files` plus `allowed_paths` fallback, exact duplicate-task merging, Unit compilation, and fingerprints; it must not rewrite semantic task boundaries. Model-returned business acceptance text is discarded; the compiler derives stable file-operation, scope, API/Spring contract, database-gap, and approval checks, then projects their descriptions to the backwards-compatible `acceptance_criteria` field. Template/menu/route/page-entry, Unit/owner, database, topology, and required database-bootstrap omissions remain in the task registry and are recorded in `task_graph.validation.errors` with task/path context, so invalid candidates can trigger automatic regeneration instead of being silently trimmed. `reconcile_live_page_paths` only maps a non-existing candidate page path to one already-existing unique semantic directory without changing its declared operation; Unit metadata and cross-Unit dependency compilation live in `build_unit_compiler.py`, which derives task entity scope from fixed task IDs and filtered entity designs. | Read when changing task ids, structural normalization, file scopes, owners, database task gating, internal acceptance storage, DAG validation, or automatic regeneration. Read `engineering_acceptance_verifier.py` with scheduler changes, `build_task_menu.py` for read-only page-path reconciliation, and `build_unit_compiler.py` for Unit dependencies and fingerprints. |
| `Backend/app/services/build_task_menu.py` | Narrow template-boundary helper that only reconciles a page candidate to an already-existing unique live page directory. It does not parse menu registrations or create, inject, remove, prune, or rewrite menu, route, hidden-route, or page-placeholder tasks, and path reconciliation preserves the candidate's explicit `add`/`modify` operation. | Read when changing live page-path evidence; do not treat it as a DAG task-generation or menu-acceptance owner. |
| `Backend/app/services/build_task_progress.py` | Owns the fixed seven-stage `prepare_build_tasks` progress snapshot (`unit_skeleton`, `build_context`, `contract_validation`, `model_planning`, `task_compilation`, `dag_validation`, `artifact_persistence`). It emits complete bounded `prepare_build_tasks.progress` custom-stream snapshots, projects safe task/validation/artifact details, and exposes `build-task-plan.json` with its pending/confirmed status without model text, host paths, or internal JSON paths. | Read when changing DAG progress events, `dag_generation`/`agent-process` projection, stage output limits, task confirmation cards, or session re-entry history. |
| `Backend/app/services/build_unit_skeleton.py`           | Deterministically creates/reuses the v3 application/backend/frontend/page Unit graph from TechnicalPlan and WorkspaceSnapshot. Confirmed entity source bindings drive backend/static-data unit selection; only database-backed endpoints receive the `backend:bootstrap` Maven/datasource/MyBatis prerequisite, while external API-only and static-only scopes omit it. Database table operations completed during entity confirmation do not become ordinary Build tasks. Menu, route, hidden-route, and page-placeholder capabilities are template-owned and do not become Build Units. | Read when changing Unit hierarchy, entity source handling, endpoint→page dependencies, bootstrap prerequisites, or reuse policy. |
| `Backend/app/services/build_context_resolver.py`        | Resolves page or endpoint targets into bounded context using runtime PageImplementationContract, TechnicalPlan Endpoint semantics, API contracts, entity IDs, and compact confirmed EntitySourceBinding summaries. | Read when changing target validation, implementation-contract slicing, entity projection, or Unit subsets. |
| `Backend/app/services/execution_resource_scope.py`      | Resolves a server-authoritative page/application/data-source execution scope into stable page, API-contract, and data-source resource claims from the confirmed ProjectPlan. | Read with lifecycle persistence and run leases when changing cross-page locking, dependency concurrency, or repair-scope expansion. |
| `Backend/app/services/build_scheduler.py`, `engineering_acceptance_verifier.py`, and `engineering_contract_verifier.py` | Deterministic BuildScheduler and engineering harness for execution slicing, task-result normalization, actual diff attribution, and repair-attempt changes. `engineering_acceptance_verifier.py` and contract parsers remain reusable metadata/audit services, but ordinary owner-task execution no longer invokes per-task `engineering_acceptance` or rejects extra workspace build artifacts; project-level correctness is deferred to `integration_test`. Backend contract verification keeps write scope unchanged while boundedly following workspace-local Java DTO references as read-only evidence for nested response schemas. | Read when changing dispatch, target-file attribution, contract metadata, nested DTO references, serialization naming, database verification, or terminal states. |
| `Backend/app/services/build_repair_planner.py`          | Scheduler-bounded RepairPlanner contract service: builds `RepairPlannerInput`, accepts an exact parent-authorized repair `change_scope`, recompiles attempt-specific file checks, inherits only parent outcome checks such as menu/API contracts, handles `requires_user_confirmation` / `terminal_failure`, appends repairs to the runtime Build DAG, and closes parents after verified written or `already_satisfied` repairs. | Read when changing build failure repair inputs, repair task boundaries, acceptance inheritance, repair depth budget, repair-plan persistence shape, or how repair tasks close failed parents. |
| `Backend/app/services/workspace_inspector.py`           | Deterministic, cacheable workspace snapshot service for the `inspect_workspace` and direct `scan_workspace_code` nodes: computes workspace revision, lists safe files, detects stack/entrypoints/routes/components/contracts and common frontend/backend config files, and delegates explicit-workspace code graph indexing. Snapshot schema `1.2.0` includes the expanded deterministic frontend/Spring entrypoint set. | Read when changing workspace snapshot schema, cache invalidation, safe source/config inventory, or code graph progress integration. |
| `Backend/app/services/code_graph/`                     | Embedded `code-review-graph` adapter, per-workspace cache manager, bounded execution-Agent context resolver, and stable models. Stores `graph.sqlite3` plus `index.json` under the user workspace `.xcodeagent/cache/code-graph/v1`; `index.json` persists bounded node/edge distributions, language and warning metadata, while the graph remains the query fact source. Scan nodes build or refresh the index but do not create request-scoped navigation state; only Frontend and DataSource execution Agents query it through the workspace-bound `code_graph_context` tool. Never auto-discover the XCodeAgent repository or expose MCP/CRG tool-server APIs. | Read `manager.py`, `adapter.py`, `context.py`, and `tools/code_graph_context.py` when changing full/incremental indexing, cache summaries, 30-second fallback, SQLite isolation, execution-Agent query bounds/fallback, packaging, or workspace security. |
| `Backend/app/services/build_result_coordinator.py`      | Parses per-task specialist `task_results`, performs one deterministic repair for clearly unescaped quotes inside JSON strings, and classifies unrecoverable malformed reports as `invalid_structured_response`. Its opt-in strict schema rejects extra top-level/result fields, duplicate/unknown/missing task IDs, missing satisfaction evidence, incomplete failures, and invalid change-request placement; DataSource enables this mode while other specialist protocols retain their current parsing behavior. | Read when changing the specialist final-report schema, strict validation policy, compatibility/recovery behavior, build result structure, task status updates, or plan consistency checks. |
| `Backend/app/services/integration_test_runner.py`       | Deterministic fail-fast real-command integration test runner: discovers frontend package scripts and Maven projects, runs package-manager install and frontend build first, and stops later commands on the first blocking failure; a frontend failure records backend build/test as not-executed skips and routes directly toward quality-gate repair instead of starting Maven. When frontend checks pass, it runs the split Maven production build (`-Dmaven.test.skip=true`) plus conditional `test`; Jest/Surefire run only when affected source has corresponding tests, and parseable passed/total counts are projected as structured UI evidence. Logs live under `.xcodeagent/runtime/tests/`; Python projects and pytest remain out of scope. | Read when changing actual integration test execution order, fail-fast behavior, package script discovery, test-count parsing, command timeouts, skipped-check semantics, log references, or failure categories. |
| `Backend/app/services/frontend_performance_runner.py`   | Advisory Lighthouse performance runner: detects the user `frontend` package, starts it through `frontend_project_launcher` with `skip_install=True`, resolves the real `preview_url`, runs `@lhci/cli@0.7.2` via `npx`, saves HTML/JSON reports under `.xcodeagent/runtime/tests/frontend_performance/`, parses Lighthouse scores/metrics, and stops only servers it started itself. Results are `blocking=False` and never enter repair planning. | Read when changing performance test discovery, LHCI command/config, report parsing, URL resolution, server reuse/stop semantics, or advisory result fields. |
| `Backend/app/services/test_validation.py`               | Deterministic testing and quality-gate rules plus repair-task plan creation. Only `blocking` failures (default) affect `passed`/`needs_revision`/revision requests; advisory performance checks are counted in the report summary but never block or repair.                                                                                                                                       | Read when changing test result evaluation, advisory semantics, revision requests, or failure-to-repair mapping.                                                                                                      |
| `Backend/app/deprecated/services_requirement_intake.py` | Deprecated, fully commented simple/complex intake rules for the older AG-UI/orchestrator flow. Workflow routing now starts in `services/request_complexity.py`.                                                                                                                                                                                                                                    | Do not import from main flow.                                                                                                                                                                                       |
| `Backend/app/services/task_scheduler.py`                | Deterministic serial/parallel batching and assignment: executable tasks with exact targets reach their bounded owner runner, while shared/public/overlapping paths serialize. | Read when changing task isolation, execution modes, target conflicts, or scheduling rules. |
| `Backend/app/services/llm_client.py`                    | Provider-neutral model response contract backed by OpenAI-compatible Chat Completions, including request/response conversion and tools.                                                                                                                                                                                                                                                            | Read when changing provider selection, request/response conversion, tools, auth, or base URL behavior.                                                                                                              |
| `Backend/app/services/builtin_skills.py`                | Resolves built-in skill data in source and PyInstaller-frozen layouts, validates required files (including progressively disclosed references), reports availability, exposes sanitized read-only catalog summaries, and defines the stable read-only Deep Agent virtual source. Source-specific generation skills include MyBatis database, external API integration, and frontend static data modules. | Read when adding/wiring built-in skills, changing packaged paths, catalog metadata, reference routing, or startup validation.                                                                                                           |
| `Backend/app/services/user_skills.py`                   | Owns user-skill catalog discovery and shared PyYAML-based metadata parsing. Resolves the environment-specific root (`~/.xcodeagent_dev/skills`, `~/.xcodeagent_st/skills`, `~/.xcodeagent_uat/skills`, or `~/.xcodeagent/skills`), scans direct-child directories only, merges persisted enablement, ignores symlinks, and returns sorted summaries plus non-fatal issues without exposing absolute host paths. | Read when changing environment mapping, catalog discovery depth, YAML/frontmatter fields, enablement merging, sorting, skipped-entry diagnostics, or public path labels; document editing and ZIP import reuse this parser. |
| `Backend/app/services/user_skill_settings.py`           | Persists versioned per-environment user-skill enablement in `skill-settings.json` as a sorted disabled-path set. Missing files default to all enabled; malformed or unreadable files fail closed. Writes use a process lock, temporary file, fsync, and atomic replacement. | Read when changing enablement defaults, storage schema/path, failure policy, atomicity, or stale-setting cleanup.                                                                                                    |
| `Backend/app/services/user_skill_documents.py`          | Owns complete `SKILL.md` persistence for the catalog editor: safely creates direct-child skill directories, reads documents, atomically saves with file-mode preservation, and deletes the containing skill directory. Enforces canonical relative paths, snake_case names on create, duplicate/symlink rejection, UTF-8 and 512 KiB limits, valid frontmatter, SHA-256 revisions, and optimistic concurrency. | Read when changing create/get/save/delete request models, editor validation, path confinement, duplicate handling, revision conflicts, atomic replacement, or deletion behavior.                                      |
| `Backend/app/services/user_skill_imports.py`            | Owns deterministic user-skill ZIP import from Base64 AG-UI input. Validates archive/path/file types, entry and expanded-size limits, exactly one root or single-wrapper `SKILL.md`, shared YAML metadata, duplicate skill conflicts, and atomically renames a fully validated staging directory into the environment-specific skills root without overwriting existing data. | Read when changing ZIP request/result models, archive safety limits, accepted package layout, import-name rules, conflict behavior, staging cleanup, or atomic installation semantics.                                 |
| `Backend/app/services/user_skill_runtime.py`            | Converts only enabled entries from the mutable user catalog into bounded immutable runtime snapshots for Deep Agents. Explicit selections validate an exact whitelist, copy only selected directories to the read-only `/.xcodeagent/user-skills/` mount, and reject disabled, unavailable, or oversized sets atomically; empty selection preserves enabled-catalog on-demand discovery. | Read when changing user-skill enablement/selection, forced prompt injection, snapshot limits and consistency, supporting-resource rules, virtual mounts, bundle-cache invalidation, or runtime diagnostics. |
| `Backend/app/tools/ask_user.py`                         | Generic ask-user tool for direct models and Agents that need human-in-the-loop clarification. Normalizes Gemini-style question payloads, automatically adds “其他” to selectable questions, and extracts ask-user tool calls from model or agent messages. It does not carry RequirementSpec business facts. | Read when changing user-question payloads, “其他” supplemental-answer behavior, HITL clarification behavior, or which model boundaries can ask users questions.                                                        |
| `Backend/app/tools/delete_file.py`                      | DeepAgent `delete_file` tool for deleting one regular file inside the selected `workspaceRoot` using virtual absolute paths, while refusing traversal, symlinks, directories, sensitive files, and paths that repeat the real host workspace root.                                                                                                                                                 | Read when changing workspace-confined deletion behavior or which writable DeepAgents can delete files.                                                                                                              |
| `Backend/app/tools/mysql_info.py`                       | MySQL metadata/config/DDL tools. The workspace-bound `create_get_mysql_table_info_tool(workspace_root)`, `create_get_mysql_config_tool(workspace_root)`, and `create_execute_mysql_ddl_tool(workspace_root)` resolve the current application's encrypted `plantMode` credentials while keeping model-visible arguments limited to table names or SQL statements; unbound compatibility entries fail closed. | Read with `services/database_credentials.py`, database Agent construction, data-source generation, and schema-summary callers. Preserve `mysql_table_info`/config/DDL result JSON and do not add credentials/workspace paths to model-visible arguments. |
| `Backend/app/tools/page_selection.py`                   | Controlled page-selection payload helper for detail confirmation.                                                                                                                                                                                                                                                                                                                                  | Read when changing page selection UX payloads or selected-page defaults.                                                                                                                                            |
| `Backend/app/deprecated/protocols_ag_ui.py`             | Deprecated, fully commented AG-UI adapter for old agent-mode routing, approval events, and workspace code-change events.                                                                                                                                                                                                                                                                           | Do not import from main flow. Use `protocols/workflow/` for active AG-UI workflow events.                                                                                                                            |
| `Backend/app/protocols/workflow/`                       | Self-contained AG-UI protocol package for the main `/workflow/run` flow. It normalizes resume/debug/control actions, validates structured `acceptance_adjustment`, `unit_test_confirmation` run/skip, Build DAG confirmation, and `test_phase_confirmation` actions, projects stable node/HITL boundaries including Build DAG pending/patch/regenerate, the development unit-test gate and the Build→test confirmation gate, validates selected skills, and separates execution, cancellation, plan control, frame encoding, visualization, and observability concerns. Successful runs end with `RUN_FINISHED`; unhandled Workflow/model failures emit their structured snapshot followed by `RUN_ERROR` and never emit a normal finish event. | Import public Workflow protocol functions from `app.protocols.workflow`; read `request.py`, `runtime.py`, `lifecycle.py`, `run_control.py`, `projection.py`, and `definition.py` together when changing resume, phase, Build DAG/unit-test/test-phase confirmation, plan-execution lock, stop/end, repair, acceptance, summary, selected-skill, or terminal-event contracts. |
| `Backend/app/protocols/workflow/request.py`            | Normalizes AG-UI workflow recovery inputs and maps `build_task_plan_confirmation`, `test_phase_confirmation={action:confirm}`, and `review_phase_confirmation={action:confirm}` to their exact resume nodes. Review handoff validates the existing execution scope/target while allowing an atomic transfer to the new review thread; the resume payload preserves bounded code-review state and current workspace artifacts. Unsupported actions are rejected or discarded according to the current contract. | Read when changing DAG/test/review confirmation actions, resume-node precedence, cross-thread lifecycle recovery, camelCase/snake_case state aliases, or public Workflow request validation. |
| `Backend/app/protocols/user_skills.py`                  | Workflow-independent AG-UI action adapter for `/skills/run`. Dispatches user catalog/document/import actions plus `set-enabled`, returns user and sanitized built-in summaries from `list`, exposes the `skill-catalog` custom event and `skillCatalog` snapshot/result key, and preserves the common run/message/success/error SSE lifecycle. | Read when changing skill action routing, capabilities, AG-UI event/state names, request validation, assistant messages, enablement, or catalog/document/import results. |
| `Backend/app/workspace/workspace.py`                    | Sandboxed workspace/file/search/terminal/Git operations, sensitive-file checks, approval gates, and code-change payloads.                                                                                                                                                                                                                                                                          | Read when changing workspace management or agent-facing workspace tool contracts.                                                                                                                                   |
| `Backend/app/workspace/code_changes.py`                 | Workspace snapshot diff capture for DeepAgent file changes, producing frontend-compatible `WorkspaceCodeChangeSet` payloads with workspace-root name, normalized relative file paths, and code-only changed diff lines while skipping ignored dirs, sensitive files, symlinks, and binary text leakage.                                                                                              | Read when changing workflow code-change capture, workspace path metadata, diff truncation, or AG-UI `codeChanges` output.                                                                                          |
| `Backend/app/workspace/run_lease.py`                    | Process-local activity registry used by `/workflow/run` to track and clean up active Graph runs without rejecting overlapping workspaces, pages, or resource claims.                                                                                                                                                                                                                                | Read when changing workflow concurrency, active-run cleanup, or reintroducing an explicit concurrency policy.                                                                                                      |
| `Backend/app/workspace/virtual_paths.py`                | Shared virtual-workspace path instructions plus helpers that recognize host `workspaceRoot` aliases accidentally supplied as DeepAgent virtual paths.                                                                                                                                                                                                                                              | Read when changing the model-visible `/` root contract or repeated-host-path safeguards.                                                                                                                            |
| `Backend/app/workspace/spec_documents.py`               | Writes product-facing RequirementSpec Markdown/internal JSON, including display-only entity information while omitting technical data-source and engineering-acceptance sections; detects Markdown edits before confirmation. | Read when changing RequirementSpec persistence, entity rendering, synchronization, or artifact roots. |
| `Backend/app/workspace/plan_documents.py`               | Writes current TechnicalPlan Markdown/JSON and synchronizes developer edits; renders three-part architecture, API/schema/entity bindings, and page references. | Read with `product_plan_documents.py` when changing formal planning persistence. |
| `Backend/app/workspace/detail_design_documents.py`      | Persists only EntitySourceBinding JSON/Markdown under `.xcodeagent/plans/entities/` and stores `source_binding` references in TechnicalPlan. No page/API artifact is written or hydrated. | Read when changing entity binding artifact names, references, hydration, or task-context assembly. |
| `Backend/app/workspace/task_documents.py`               | Writes only the current BuildTaskPlan JSON and the independent repair task plan JSON under the shared `.xcodeagent/` workflow artifact root; DAG confirmation and the Build gate consume the latest `build-task-plan.json` projection. It does not create or read a `BUILD_TASK_DAG.md` artifact.                                                                                                                                 | Read when changing JSON task-plan persistence, confirmation projection, or repair-plan artifacts.                                                          |
| `Backend/app/workspace/workspace_snapshot_documents.py` | Helpers for reading workspace snapshot JSON and resolving the `.xcodeagent/cache` artifact root used by `inspect_workspace`.                                                                                                                                                                                                                                                                        | Read when changing workspace snapshot persistence paths or cache loading.                                                                                                                                           |
| `Backend/app/workspace/test_documents.py`               | Writes structured test reports under the shared workflow artifact root.                                                                                                                                                                                                                                                                                                                            | Read when changing test report persistence or artifact paths.                                                                                                                                                       |
| `Backend/app/middleware/approvals.py`                   | Approval grants, reusable operation rules, and protected-operation fingerprints.                                                                                                                                                                                                                                                                                                                   | Read when changing approval lifecycle, scopes, or risk gating.                                                                                                                                                      |
| `Backend/app/persistence/run_store.py`                  | Writes `.xcodeagent/runs/<runId>` artifacts and JSONL run events.                                                                                                                                                                                                                                                                                                                                  | Read when changing run artifact lifecycle, formats, or retention.                                                                                                                                                   |
| `Backend/app/persistence/checkpoints.py`                | Owns workspace-scoped LangGraph SQLite checkpointers for the main workflow, including `.xcodeagent/checkpoints/checkpoints.sqlite` path creation, optional global `XCODEAGENT_CHECKPOINT_DB` override, conservative retention cleanup, and connection shutdown.                                                                                                                                                                                                                    | Read when changing workflow state persistence, checkpoint retention, recovery behavior, or `XCODEAGENT_CHECKPOINT_DB` handling.                                                                                     |
| `Backend/app/observability/agent_events.py`             | Helpers for append-oriented agent run event payloads.                                                                                                                                                                                                                                                                                                                                              | Read when changing event names or JSONL fields.                                                                                                                                                                     |
| `Backend/app/utils/model_output.py`                     | Shared best-effort JSON object extraction for direct model outputs, including fenced or commentary-wrapped JSON.                                                                                                                                                                                                                                                                                   | Read when changing model JSON parsing used by requirements, product/technical planning, task preparation, or entity-source binding.                                                                                |

## Current Build DAG Contract

The current Build path is `confirmed formal artifacts + template readiness + PageImplementationContract + confirmed EntitySourceBinding → development_readiness_gate → inspect_workspace → prepare_build_tasks → DAG confirmation → build`. The following boundaries are part of the current contract and should be checked together when changing this path:

| Concern | Current contract | Read first |
| --- | --- | --- |
| Runtime context | `project_plan` is a read-only runtime projection used to assemble the current Build context. It is not a new editable formal artifact, and DAG generation/confirmation must not write it back into TechnicalPlan Markdown or JSON. | `Backend/app/graph/nodes/tasks.py`, `Backend/app/services/build_context_resolver.py`, `Backend/app/workspace/plan_documents.py` |
| DAG prerequisites | `RequirementSpec`, `ProductPlan`, `UiManifest` (confirmed or explicitly skipped), `TechnicalPlan`, template-generation manifest/real-file readiness, current PageImplementationContract, Endpoint semantics, confirmed EntitySourceBinding, and API consistency must pass before candidate generation. | `Backend/app/graph/nodes/tasks.py`, `Backend/app/services/application_template_generation.py`, `Backend/app/services/development_readiness.py`, `Backend/app/services/build_context_resolver.py` |
| Generation and validation | The platform owns Unit/task boundaries. Candidate structural normalization may fill deterministic fields and merge exact duplicates, but must not delete or rewrite semantic menu/route/page-boundary tasks. Violations stay in `task_graph.validation.errors`; `task_preparer.py` feeds them back for bounded automatic regeneration, and exhausted attempts become platform failure rather than a user task-splitting request. | `Backend/app/services/build_task_planner.py`, `Backend/app/agents/main/task_preparer_prompt.py`, `Backend/app/agents/main/task_preparer.py`, `Backend/app/services/build_task_menu.py` |
| Artifact authority | The only current DAG artifact is `.xcodeagent/plans/build-task-plan.json`. Generation saves it as `confirmation_status=pending`; confirmation and the Build gate reload the latest workspace JSON, validate `schema_version`, `status`, `task_graph.validation`, `confirmation_status`, and scope, and never treat a stale checkpoint copy as authoritative. | `Backend/app/workspace/task_documents.py`, `Backend/app/graph/nodes/tasks.py`, `Backend/app/graph/subgraphs/build.py`, `Backend/app/protocols/workflow/request.py` |
| Ownership boundary | Template initialization owns page placeholders, menus, routes, and hidden routes; Normal Build does not create `database:*` Units or database-owner tasks. DAG only plans page/API/backend implementation work and consumes the initialized scaffold. | `Backend/app/services/application_template_generation.py`, `Backend/app/services/build_task_menu.py`, `Backend/app/graph/nodes/tasks.py` |
| User confirmation | The AG-UI `build_task_plan_confirmation` mode supports only `confirm`, `patch`, and `regenerate`. Users may patch task `title`/`description`; platform validation/generation failures are not manually corrected, and there is no standalone “暂不执行” action. | `Backend/app/protocols/workflow/request.py`, `Backend/app/protocols/workflow/definition.py`, `Frontend/src/renderer/src/components/AiChatPanel/components/WorkflowRunCard/BuildTaskPlanConfirmation.tsx`, `docs/DAG_TASK_GENERATION_AND_VALIDATION_MININAL_CHANGE_PLAN.md` |

## Frontend

| Path                                                                                                          | Responsibility                                                                                                                                                                                                                                                                                                  | Read first / common edits                                                                                                                                                              |
| ------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Frontend/src/main/index.ts`                                                                                  | Electron main process: app/window/auth/preview lifecycle, storage and workspace IPC, controlled application deletion, template clone/retry, local sessions, and planning-artifact inspection. The `windows:open-planning` IPC creates one planning window per application (or focuses the existing one), theme-matches its native background, and passes explicit Graph-checkpoint plus independent conversation thread identifiers through preload launch context. Current inspection merges ProductPlan pages with TechnicalPlan references and projects API plus entity outlines (including field summaries/design status) for the workbench; endpoint status is merged from the real Markdown artifact through `planningArtifactStatus.ts`, never inferred from TechnicalPlan membership. | Read with `Frontend/src/main/planningArtifactStatus.ts` when changing Electron lifecycle/IPC, phase windows, template download, planning inspection, endpoint design-document status, app/session storage, preview, or filesystem-backed behavior. |
| `Frontend/src/main/planningArtifactStatus.ts`                                                                 | Filesystem-backed endpoint design status: resolves the current `.xcodeagent/plans/endpoints/endpoint--<contractId>--<endpointId>.md` path and reports designed only for a non-empty Markdown document. | Read when changing workbench API design status or endpoint document naming. |
| `Frontend/src/main/filesystem.ts`                                                                            | Small Electron main-process filesystem helpers for idempotent `lstat`/recursive directory removal: an absent path is a successful no-op, while permission, type, symlink, and other filesystem errors remain visible to the caller. | Read when changing safe project deletion or other main-process filesystem operations that need explicit `ENOENT` semantics. |
| `Frontend/src/main/applicationSettings.ts`                                                                    | Versioned application-wide settings persisted atomically at Electron `app.getPath('userData')/settings.json`; currently owns the light/dark appearance theme, validates IPC writes, and broadcasts changes to every open Electron window. | Read when changing the application settings schema, theme persistence/defaults, settings IPC, or cross-window synchronization. |
| `Frontend/src/main/auth.ts`                                                                                   | Environment-scoped desktop authentication state: CMB Device Flow placeholder, current-process `auth.json` `{ access_token }` persistence, main-process memory access, and shared startup/reauthentication/quit cleanup. Cold starts delete any residual file before backend startup; ordinary window close only hides the window.                                                | Read when changing login, token persistence/memory semantics, environment auth paths, cold-start policy, or logout behavior.                                                            |
| `Frontend/src/main/backendService.ts`                                                                         | Packaged Windows/macOS backend service lifecycle: locating the PyInstaller executable, checking the generic built-in-skills resource directory, selecting a local port, health polling, renderer base URL, and process shutdown. Backend Python remains the authority for required skill names and files.        | Read when changing bundled backend startup, packaged resource structure, ports, health checks, or quit cleanup.                                                                         |
| `Frontend/tsconfig.node.json`                                                                                 | TypeScript project for Electron configuration, main-process, and preload sources; loads `electron-vite/node` declarations for resource queries such as `?asset`.                                                                                                       | Keep `src/main/**/*` and `src/preload/**/*` included so `pnpm typecheck` validates desktop runtime code and resolves Electron Vite asset modules.                                        |
| `Frontend/src/preload/index.ts` and `Frontend/src/preload/index.d.ts`                                         | Context-bridge API exposed as `window.xcodeAgent` plus preload typings, including authentication status/login/token access/reauthentication, application-settings load/theme-save/change subscription, session workspace history calls, planning-window launch context, and the controlled `windows.openPlanning` bridge.                                                                                                                                | Update with every new/changed Electron IPC channel or renderer launch parameter.                                                                                                                                    |
| `Frontend/src/renderer/src/window.d.ts`                                                                       | Renderer-side `window.xcodeAgent` type declarations.                                                                                                                                                                                                                                                            | Update with preload API changes.                                                                                                                                                       |
| `Frontend/src/renderer/src/main.tsx`                                                                          | React renderer entry; mounts the global authentication-failure gate outside the workbench provider so every product page receives the same blocking 401/403 UI.                                                                                                                                                 | Read for app bootstrapping or global authentication-failure UI issues.                                                                                                                 |
| `Frontend/src/renderer/src/login.tsx` and `login.less`                                                        | Dedicated frameless Electron authentication window: custom close control, draggable themed security-workbench layout, Device Flow entry, login loading/error states, and technology-oriented system/terminal presentation.                                                                                       | Read with `Frontend/src/main/index.ts` when changing login interaction, window frame/dimensions, authentication messaging, or light/dark visual treatment.                             |
| `Frontend/src/renderer/src/pages/AppEntryPage.tsx`                                                            | Top-level renderer routing between welcome flow and workbench.                                                                                                                                                                                                                                                  | Read when changing app open/create lifecycle.                                                                                                                                          |
| `Frontend/src/renderer/src/pages/WelcomePage.tsx`                                                             | Thin composition page for the welcome hero and create/open application actions.                                                                                                                                                                                                                                 | Read with `components/Welcome/` when changing the welcome screen layout.                                                                                                               |
| `Frontend/src/renderer/src/components/Welcome/`                                                               | Welcome and new-application planning UI. Creation requires a new/empty independent project directory, writes lifecycle state, keeps the platform database default hidden, conditionally collects one external database mode, encrypts stored credentials, and renders RequirementSpec/ProductPlan/UI/TechnicalPlan confirmation and retry states. | Read the creation form, planning modal, summaries, constants, template service, and credential helpers when changing initialization. |
| `Frontend/src/renderer/src/pages/AppEntryPage.tsx`                                                            | Root renderer surface switcher for welcome, planning, template readiness and workbench. New applications rerun template readiness before development; workspace switching preserves preview cleanup and background planning isolation. An Electron planning launch filters mounting to the requested application plus `graphThreadId`, shows a planning-specific recovery first frame, and hands the independent `conversationThreadId` to the Planning Agent session. | Read with lifecycle/template services when changing application opening or planning-to-workbench handoff. |
| `Frontend/src/renderer/src/pages/WorkbenchPage.tsx`                                                           | Workbench shell; reads the complete page outline from RequirementSpec `pages`, hydrates designed status plus menu-tree `pageTree` from ProjectPlan `frontend_pages`, optionally synchronizes workspace `application.json`, starts the current frontend preview server, registers successful preview workspaces with Electron for explicit-exit cleanup, and keeps its runtime origin/error in memory for page-level preview URL composition. Planning-stage windows render the shell immediately and skip the generic “entering workbench” overlay while background artifact inspection continues. | Read when changing workbench page selection, menu hierarchy projection, workspace-backed application synchronization, stage-window entry, or frontend preview startup/registration state. |
| `Frontend/src/renderer/src/components/AiChatPanel/`                                                           | Main Workflow chat UI split by responsibility. Creation uses separate Design and Planning workbench phases: Design owns RequirementSpec/ProductPlan/UiDesign; the green `PlanningStageEntryCard` opens or focuses the application's separate Electron window with explicit `graphThreadId` and new `conversationThreadId`, then that planning window restores the original Graph checkpoint, submits explicit `enter_planning`, and owns the structured TechnicalPlan tab plus template preparation and development entry. The TechnicalPlan tab reads the current structured JSON snapshot and renders architecture, entity, API/Schema, and page-binding review surfaces under `components/DocPanel/` instead of presenting Markdown. During an active lifecycle execution, only the bottom Chat Composer is replaced by `PlanExecutionDock`; it derives running/HITL/failure/acceptance controls from the backend lifecycle while preserving the message stream, progress cards, sidebar, and preview layout. Pending endpoints and unbound entities reuse the locked `DetailConfirmationPageSelector` start-design card; page/API actions enter `development_readiness_gate`, where missing bindings instruct the user to enter the independent EntitySourceBinding chat/card flow. EntitySourceBinding supports database, external API, static data, mapping, and DDL approval UI and ends independently. Build DAG confirmation is rendered by `WorkflowRunCard/BuildTaskPlanConfirmation.tsx`; the Build→test gate is rendered by `WorkflowRunCard/TestPhaseConfirmationCard.tsx` and submits only structured `test_phase_confirmation.action=confirm`. Confirming creates a target-bound empty test session with its own AG-UI thread, persists the “开始测试…” user message there, and resumes without copying development history. `MessageList` resolves each assistant header from that message's Workflow node, so historical development messages remain 研发 Agent after the test phase starts; test and review use distinct identities and avatars. Acceptance adjustment uses a typed selector for bounded local fixes versus page/endpoint/data-source/project-plan redesign, while hooks submit the selected type and feedback through AG-UI. Hooks also own persisted sessions (page/endpoint/entity-bound identities), AG-UI run/stop/end/retry/adjust/accept actions, the test-confirmation user message and resume, SmallTask scope/formal-workflow handoff confirmation, and authentication rollback. | Read `AiChatPanel.tsx`, `components/DetailConfirmationPageSelector/`, `components/DocPanel/`, `components/PlanExecutionDock/`, `planExecutionMode.ts`, `components/ChatComposer/`, `components/MessageList/`, `components/WorkflowRunCard/PlanningStageEntryCard.tsx`, `components/WorkflowRunCard/TestPhaseConfirmationCard.tsx`, `components/WorkflowRunCard/BuildTaskPlanConfirmation.tsx`, `components/WorkflowRunCard/EntityDesignChatCard.tsx`, `components/EntityInfoPanel/`, and `useWorkflowConversation.ts` when changing phase locking, planning-stage entry, artifact presentation, readiness/binding, endpoint start-design entry, test confirmation, final result presentation, workflow chat, history, AG-UI sending, sessions, or preview controls. |
| `Frontend/src/renderer/src/components/AiChatPanel/components/WorkflowRunCard/BuildTaskPlanConfirmation.tsx` | Structured Build DAG confirmation card. It displays the current task title/description, owner/scope/dependency/read-only fields and validation errors; it can submit only `confirm`, title/description `patch`, or `regenerate` through AG-UI. Platform task-boundary errors remain non-editable failure feedback, and there is no independent “暂不执行” action. | Read with `useWorkflowConversation.ts`, `typings/workflow.ts`, backend `protocols/workflow/request.py`, and `graph/nodes/tasks.py` when changing DAG confirmation UX or action payloads. |
| `Frontend/src/renderer/src/components/SkillsPage/`                                                          | Categorized skill management UI with user/built-in segmented filtering, request-safe manual refresh, read-only built-in cards, per-user-skill enable switches, create/edit/delete flows, and themed ZIP upload. Catalog helpers also filter enabled skills and remove stale chat selections. | Read with `service/userSkills.ts`, `typings/userSkills.ts`, `AiChatPanel`, and `Frontend/package.json` when changing catalog categories, refresh, enablement, editor/import interaction, chat selection cleanup, or light/dark styles. |
| `Frontend/src/renderer/src/components/AgentFilesPage/`                                                     | Environment-level AGENTS.md file page: one fixed `AGENTS.md` list item plus a plain textarea, save state, revision-conflict feedback, and dual-theme styling; it has no tabs, preview, file switches, or additional files. | Read with `service/agentFiles.ts`, `typings/agentFiles.ts`, and `AiChatPanel`/`SessionSidebar` when changing the file-management UI or navigation. |
| `Frontend/src/renderer/src/deprecated/components/RequirementPlannerPanel/`                                    | Deprecated, fully commented old planner UI for collecting answers and rendering structured development plans.                                                                                                                                                                                                   | Do not import from main flow.                                                                                                                                                          |
| `Frontend/src/renderer/src/deprecated/components/OrchestrationPanel/`                                         | Deprecated, fully commented old structured orchestration card for complex requirements, task graph summary, and plan confirmation.                                                                                                                                                                              | Do not import from main flow.                                                                                                                                                          |
| `Frontend/src/renderer/src/components/BrowserPreviewPanel/`                                                   | Embedded browser preview panel with in-memory address/history navigation, optional Workflow-driven navigation, and a versioned cross-origin `postMessage` element-inspection bridge. Inspection readiness and selection messages are validated against the active iframe window/origin; selected source locations are logged in the renderer and shown through Ant Design message feedback, while `AiChatPanel` masks the non-preview region during inspection. Page preview URLs are rebuilt on every preview action from the latest runtime frontend origin/port plus the selected ProjectPlan page path; preview URLs and launch errors are never persisted to localStorage. | Read with `AiChatPanel`, `WorkbenchPage`, workflow typings, and `utils/previewUrl.ts` when changing in-app preview, element inspection, page-route composition, or post-launch preview handoff. |
| `Frontend/src/renderer/src/deprecated/components/ProtectedToolPanel/`                                         | Deprecated, fully commented old UI for protected tool approvals.                                                                                                                                                                                                                                                | Do not import from main flow.                                                                                                                                                          |
| `Frontend/src/renderer/src/components/GlobalConfigPanel/`                                                     | Global configuration UI.                                                                                                                                                                                                                                                                                        | Read when changing app-wide settings UI.                                                                                                                                               |
| `Frontend/src/renderer/src/components/EditorPanel/`                                                           | Displays assistant output bridged into editor-facing panels.                                                                                                                                                                                                                                                    | Read when changing editor output presentation.                                                                                                                                         |
| `Frontend/src/renderer/src/components/LeftPanel/`                                                             | Left workbench panel composition.                                                                                                                                                                                                                                                                               | Read when changing panel layout or which assistant panels are mounted.                                                                                                                 |
| `Frontend/src/renderer/src/components/ActivityBar/`, `ResizeHandle/`, `PlaceholderPanel/`, `MarkdownContent/` | Shared UI pieces for navigation, resizing, empty states, and markdown rendering.                                                                                                                                                                                                                                | Read only for UI work touching those widgets.                                                                                                                                          |
| `Frontend/src/renderer/src/service/agUiAgent.ts`                                                              | Shared AG-UI chat client for `/workflow/run` and `/conversation/run`. It builds the targetless `forwardedProps.conversation` envelope plus explicit workflow actions, reconciles workflow or conversation custom/snapshot/result payloads into the existing message model, preserves the final Workflow snapshot on `RUN_ERROR`, merges process/tool steps, and keeps cancellation on the active endpoint. | Read when changing workflow/conversation request props, AG-UI parsing, process/tool rendering, code-change payloads, failure handling, or thread/session handling. |
| `Frontend/src/renderer/src/service/authentication.ts` and `components/AuthenticationFailureGate/`            | Shared AG-UI `HttpAgent` factory and `onRunFailed` subscriber for transport-level 401/403 detection, deduplicated authentication-failure notification, error classification, and the light/dark full-window blocking reauthentication modal.                                                                    | Read when changing Python AG-UI authentication-failure handling, subscriber composition, or the reauthentication UI.                                                                  |
| `Frontend/src/renderer/src/service/cloudApi.ts`                                                               | Generic non-streaming cloud Java JSON client; reads the main-process in-memory access token through preload IPC, forces the Bearer header, and routes missing-token/401/403 failures through the shared reauthentication gate.                                                                                   | Read when adding cloud Java calls or changing their token/header/error contract.                                                                                                       |
| `Frontend/src/renderer/src/service/databaseCredentialCrypto.ts`                                               | Fetches the Backend `/health` public database-encryption metadata, validates/imports the RSA key with WebCrypto, encrypts `plantMode.pwd` using RSA-OAEP-SHA256, and returns a copied persistence schema containing the versioned base64url secret envelope. | Read before changing application creation credential persistence. Never log/store plaintext, silently fall back to plaintext, or send it through AG-UI. |
| `Frontend/src/renderer/src/service/userSkills.ts`                                                           | Workflow-independent `@ag-ui/client` wrapper for `/skills/run`. Sends list/get/create/save/delete/import/set-enabled actions, reconciles custom events/snapshots/final results, validates user and built-in catalog shapes, and converts failed AG-UI runs into UI-facing errors. | Read when changing skill action payloads, transport, result reconciliation, catalog schemas, enablement acknowledgement, revision/deletion/import acknowledgement, or frontend error handling. |
| `Frontend/src/renderer/src/service/agentFiles.ts`                                                           | Workflow-independent `@ag-ui/client` wrapper for `/agent-files/run`; sends `get` and revision-protected `save` actions, reconciles the `agent-files` custom event, `agentFiles` snapshots, and final result into the single editable AGENTS.md document. | Read with `services/agent_file_documents.py` and `protocols/agent_files.py` when changing the AGENTS.md product contract. |
| `Frontend/src/renderer/src/service/codeChanges.ts`                                                          | Workflow-independent `@ag-ui/client` wrapper for `/code-changes/run`; submits confirmed historical change sets and reconciles custom events, state snapshots, and final rollback results. | Read with `AiChatPanel/components/CodeChangeCard`, Backend `protocols/code_changes.py`, and `services/code_change_revert.py` when changing review or rollback behavior. |
| `Frontend/src/renderer/src/service/versionControl.ts` and `components/AiChatPanel/components/VersionCommitReminder/` | Workflow-independent `@ag-ui/client` wrapper and dual-theme revision commit reminder. A successful reminder remains dormant until the Workflow emits a completed `application-revision` event after final acceptance; failed quick changes only offer review and repair guidance. | Read with Backend `protocols/version_control.py`, `services/version_control.py`, `MessageList`, and `docs/APPLICATION_REVISION_WORKFLOW.md` when changing revision commit timing or UI. |
| `Frontend/src/renderer/src/deprecated/service/orchestratorAgent.ts`                                           | Deprecated, fully commented thin re-export service for old development orchestrator AG-UI sessions.                                                                                                                                                                                                             | Do not import from main flow.                                                                                                                                                          |
| `Frontend/src/renderer/src/service/chatSessions.ts`                                                           | Renderer chat session model, summary/workspace normalization, code-change message persistence, Electron/localStorage persistence fallback.                                                                                                                                                                      | Read when changing chat history/session behavior or persisted message shape.                                                                                                           |
| `Frontend/src/renderer/src/service/applicationStorage.ts`                                                     | Stored application list load/save/removal fallback, controlled local-project transfer to the OS trash through Electron IPC, design-stage deletion that moves the workspace `.xcodeagent` directory plus the environment chat-session root to the OS trash and clears renderer fallback/runtime caches, full workspace `application.json` reads, and confirmed specs/plans inspection with both ProjectPlan leaf-page options and menu-tree `pageTree` through Electron IPC. | Read when changing saved applications or workspace application/planning-artifact synchronization. |
| `Frontend/src/renderer/src/components/ProjectPlanPageTreePreview/`                                            | Shared menu-tree preview component that converts `ProjectPlan.frontend_pages` into a visual hierarchy, including menu `unique_path` and leaf-page descriptions. The ProjectPlan confirmation surface reuses it inside a JSON-driven modular summary; Markdown remains the formal artifact but does not decide which confirmation modules are displayed. | Read before changing ProjectPlan confirmation modules, menu preview rendering, or tree-node conversion helpers. |
| `Frontend/src/renderer/src/service/applicationSettings.ts` and `hooks/useApplicationTheme.tsx`                | Renderer application-theme client and root Provider. It loads Electron `userData/settings.json` once before initial render, keeps theme state shared across welcome/planning/workbench/authentication surfaces, receives cross-window updates, and uses memory-only behavior in non-Electron browser development without localStorage fallback. | Read when changing application-wide appearance settings, initial theme loading, theme persistence, or cross-window theme behavior. |
| `Frontend/src/renderer/src/service/applicationLifecycle.ts`, `templateApi.ts`, `activeApplicationPlanning.ts`, and `applicationPagePlanning.ts` | Dedicated lifecycle AG-UI client with template readiness orchestration, plus the in-memory active-planning view and creation-planning Graph client. Template readiness asks Electron only for structured clone results, submits them through `prepare_template_generation`, and finishes through the backend manifest gate; the only caller is the TechnicalPlan confirmation callback. Welcome-page opening, application restart, workbench entry, and template-generation failure do not start or resume readiness. Business stages come directly from the projected lifecycle; no old localStorage, application-index, or checkpoint stage migration is supported. | Read when changing creation planning confirmation, lifecycle AG-UI payloads, template download/readiness, homepage status, or workbench entry gating. |
| `Frontend/src/renderer/src/service/workspaceTools.ts`                                                         | Frontend client helpers for backend workspace tools and approval approve/reject calls.                                                                                                                                                                                                                          | Read when changing workspace tool invocation or approval API usage from the UI.                                                                                                        |
| `Frontend/src/renderer/src/context/WorkbenchContext.tsx`                                                      | Shared workbench state for assistant messages published to editor panels.                                                                                                                                                                                                                                       | Read when changing cross-panel message flow.                                                                                                                                           |
| `Frontend/src/renderer/src/typings/`                                                                          | Shared TypeScript domain types for applications, workbench mode, and plans. `application.ts` includes the saved app metadata plus the app-creation JSON schema emitted by the new-app form, including `DatasourceEnum` and the built-in/external database connection contract. | Update when payload shape changes; never persist both `datasource.db.dbidMode` and `datasource.db.plantMode`. |
| `Frontend/src/renderer/src/constants/`                                                                        | Static workbench constants.                                                                                                                                                                                                                                                                                     | Read when changing mode/navigation constants.                                                                                                                                          |
| `Frontend/src/renderer/src/utils/`                                                                            | Class name helper, layout helpers, preview URL/open helpers.                                                                                                                                                                                                                                                    | Read for shared renderer utilities.                                                                                                                                                    |
| `Frontend/src/renderer/src/styles/global.less` and component `.less` files                                    | Global and component-scoped styling.                                                                                                                                                                                                                                                                            | Keep style changes near the component unless truly global.                                                                                                                             |
| `Frontend/scripts/verify-backend-resource.mjs`                                                                | Generic preflight check used by Windows/macOS Electron package scripts to ensure platform-specific `resources/backend/<platform>` contains the staged PyInstaller backend, `.env`, `_internal`, and `builtin_skills` directory. It intentionally does not duplicate Backend skill manifests.                       | Read when changing frontend desktop packaging or packaged resource structure.                                                                                                           |

## Generated Or External-Like Areas

| Path                                                        | Notes                                                                                                                                                                                                          |
| ----------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Frontend/node_modules/`, `Frontend/out/`, `Frontend/dist/` | Generated/dependency output; do not inspect for normal feature work.                                                                                                                                           |
| `Frontend/resources/backend/`                               | Generated platform backend staging output from `scripts/build-backend-win.ps1` and `scripts/build-backend-mac.sh`; contains PyInstaller support files and copied `.env`. Do not commit or inspect secrets.     |
| `Backend/build/`, `Backend/dist/`                           | Generated PyInstaller build output.                                                                                                                                                                            |
| `Backend/.venv/`, `Backend/.pycache/`                       | Environment/cache output; do not inspect for normal feature work.                                                                                                                                              |
| `var/workspaces/`                                           | Runtime workflow workspace artifacts such as generated specs, plans, JSON `build-task-plan.json` / `repair-task-plan.json`, reports, previews, and other local run output. Default workflow writes resolve here from the repository root. Do not treat as source unless a task explicitly asks to inspect a run artifact. |
| `.pnpm-store/`                                              | Package store; do not inspect.                                                                                                                                                                                 |

## Common Change Routes

| Task                                                   | Start here                                             | Usually also touch                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------ | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Add or change backend API route                        | `Backend/app/main.py`                                  | Runtime/tool module, frontend service caller, validation docs if behavior changes.                                                                                                                                                                                                                                                                                                             |
| Add or change workspace/file/git/terminal tool         | `Backend/app/workspace/workspace.py`                   | `Backend/app/main.py`, `Backend/app/tools/*` when exposed to DeepAgents, affected `Backend/app/agents/*`, frontend tool UI/service if exposed.                                                                                                                                                                                                                                                 |
| Change app-generation workflow phase order/routing     | `docs/WORKFLOW.md`, `Backend/app/graph/workflow.py`    | `Backend/app/graph/state.py`, matching `graph/nodes/*`, `graph/subgraphs/unit_testing.py`, `graph/subgraphs/testing.py`, `protocols/workflow/`, `protocols/workflow_visualization.py`, frontend `workbenchPhase.ts`, `WorkbenchTopBar.tsx`, `MessageList`, `WorkflowRunCard`, and workflow debug UI when node selection changes. The current workbench order is design → development (Build → unit_test → unit_test_repair) → test (integration/repair/review confirmation) → review (code review → launch → acceptance → finalize), with separate unit-test/test-phase/review confirmations and independent development/test/review sessions. |
| Change workflow state or persisted artifact references | `Backend/app/graph/state.py`                           | Matching node, `workspace/*_documents.py`, `domain/models.py`, `workflow.md` if design semantics change.                                                                                                                                                                                                                                                                                       |
| Change requirement confirmation in workflow            | `Backend/app/graph/nodes/requirements.py`              | `agents/main/requirements_analyzer.py`, `services/requirement_spec.py`, `services/entity_definitions.py`, `tools/ask_user.py`, and `workspace/spec_documents.py`. Requirement entities are display-only and never choose data sources; later product/technical/entity stages own those decisions. |
| Change product/technical planning, readiness, or entity binding | `Backend/app/graph/nodes/product_planning.py`, `Backend/app/graph/nodes/planning.py`, `Backend/app/graph/nodes/development_readiness.py` | ProductPlan owns visible behavior, UiManifest owns controls/local effects, TechnicalPlan owns API/schema and Endpoint implementation semantics, and EntitySourceBinding independently owns data-source/source bindings and database operations. |
| Change workflow workspace inspection or code graph scan UI | `Backend/app/graph/nodes/workspace_inspection.py`      | `services/workspace_inspector.py`, `services/code_graph/`, `workspace/workspace_snapshot_documents.py`, `protocols/workflow/`, `protocols/direct_modification.py`, frontend `ProcessSteps/WorkspaceInspectionPanel.tsx`, `processStepHistory.ts`, `Backend/app/graph/state.py`; preserve explicit user `workspaceRoot` boundaries, bounded `workspace_inspection.progress`/AG-UI projections, cache/incremental/full statuses, CRG node/edge distributions, 30-second file-search fallback, and no request-scoped navigation state, host absolute paths, source bodies, MCP server, or standalone graph endpoint. |
| Change workflow task DAG preparation                   | `Backend/app/graph/nodes/tasks.py`                     | `agents/main/task_preparer_prompt.py`, `agents/main/task_preparer.py`, `services/build_unit_skeleton.py`, `services/build_context_resolver.py`, `services/build_task_planner.py`, `services/build_unit_compiler.py`, `services/build_task_progress.py`, `services/task_scheduler.py`, `workspace/task_documents.py`, `services/workspace_inspector.py`; preparation rechecks EntitySourceBinding and exposes PageImplementationContract, TechnicalPlan Endpoint/API facts, and compact entity bindings. |
| Change workflow code generation/build execution        | `Backend/app/graph/subgraphs/build.py`                 | `services/build_scheduler.py`, `services/build_repair_planner.py`, `services/build_tool_activity.py`, `agents/tool_activity_stream.py`, `agents/messages.py`, `agents/code_graph_guidance.py`, `agents/repair_planner/`, `agents/frontend/`, `agents/data_source/`, `agents/database/`, `tools/code_graph_context.py`, `services/database_execution.py`, `services/build_result_coordinator.py`, `workspace/code_changes.py`, `workspace/plan_documents.py`, `workspace/task_documents.py`. Frontend uses graph-first navigation with task-scoped file-search fallback; DataSource uses exact task paths, task-level source Skill routing, targeted contract/design context, and a standalone backend working-directory projection from the inspection-stage WorkspaceSnapshot; Database/RepairPlanner do not receive the graph tool. All code owners read real source before edits. Tool activity consumes root and delegated streams and falls back to root message chunks when values omit messages; code owner reports are verified against actual file changes, while database owner reports are verified by database execution evidence and may pause for approval before SQL writes. |
| Change workflow unit testing or integration quality gate | `Backend/app/graph/subgraphs/unit_testing.py`, `Backend/app/graph/subgraphs/testing.py` | `graph/workflow.py`, `graph/nodes/small_task.py`, `services/small_task.py`, `protocols/workflow/`, `services/integration_test_runner.py`, `services/frontend_performance_runner.py`, `services/frontend_project_launcher.py`, `agents/test_generation/`, `agents/repair_planner/`, `agents/small_task/`, `services/test_validation.py`, `workspace/test_documents.py`, `workspace/task_documents.py`; `unit_testing.py` owns Build Diff capture, TestGenerationAgent, generation validation, frontend/backend unit execution, the unit quality gate and independent three-round `unit_test_repair`. `testing.py` owns dependency installation, frontend/backend Build checks, frontend performance, integration quality gate and independent `small_task_repair`; it must not generate or execute unit tests. TestGenerationAgent always receives `unit_test_generation_context.code_diff` derived from the first Build snapshot; generated-test and repair changes merge into the development Diff before the test-phase confirmation. Unit and integration reports, next actions, repair plans and counters remain separate. Runtime progress uses `unit_test.checks` for the development matrix and `integration_test.checks` for the test matrix; the integration matrix has no unit-test rows. |
| Change launch/preview after tests pass                 | `Backend/app/graph/nodes/lifecycle.py`                 | `services/backend_project_launcher.py`, `services/project_launcher.py`, `protocols/workflow/projection.py`, `protocols/workflow_visualization.py`, frontend preview/acceptance UI if launch metadata changes. Detect Maven projects from workspace-root `backend/Backend/pom.xml`; preserve backend-before-frontend ordering when detected, frontend-only launch with `launch_result.backend.status=skipped` otherwise, the `launch_result.backend` / `frontend` / `failed_stage` projection, and the failure contract that copies the launch reason into all `preview_url` projections while status remains `failed`. |
| Change workflow debug UI                               | `Frontend/src/renderer/src/components/AiChatPanel/components/ChatComposer/index.tsx` | `Frontend/src/renderer/src/typings/workflow.ts`, `Frontend/src/renderer/src/service/agUiAgent.ts`, `Backend/app/protocols/workflow_request.py`, `Backend/app/protocols/workflow_visualization.py`, `Backend/README.md`.                                                                                                                                                  |
| Change AG-UI workflow chat behavior                    | `Frontend/src/renderer/src/service/agUiAgent.ts`       | `Backend/app/protocols/workflow/`, `AiChatPanel`, `ProcessSteps`, `WorkflowRunCard`, `processStepHistory.ts`, `chatSessions.ts`, workflow typings. Readiness may return `entity_source_binding_required`, `missing_entities`, and `development_target`; EntitySourceBinding uses an independent submission and terminal result. DAG, inspection, testing, tool activity, failure, and history projections remain shared. |
| Change free-conversation routing or Prompt wrappers   | `Backend/app/graph/direct_modification_workflow.py`   | `agents/direct_modification.py`, `agents/workspace_assistant/`, `agents/small_task/`, `graph/nodes/direct_modification.py`, `protocols/direct_modification.py`, shared testing/lifecycle nodes, frontend `service/agUiAgent.ts`, `conversationMode.ts`, `AiChatPanel.tsx`, `components/AiChatPanel/components/ChatComposer/`, and `useWorkflowConversation.ts`. Preserve page/API input's explicit “设计修改 / 自由协作” mode routing, optional current page/API target, permission separation between tool-less/read-only/write branches, write-only lease acquisition, backend-first fullstack order, precise workspace paths, 4000-character summary, confirmation before formal Workflow, and main Workflow isolation. |
| Change revision version reminders or commits | `Backend/app/services/version_control.py`             | `protocols/version_control.py`, `docs/APPLICATION_REVISION_WORKFLOW.md`, frontend `service/versionControl.ts`, `typings/versionControl.ts`, `AiChatPanel/components/VersionCommitReminder/`, `MessageList`, and `CodeChangeCard`. Preserve final acceptance as the only success-trigger boundary, real Git status as authority, independent repository roots, fingerprint revalidation, rejection of existing staged work, exact selected paths, explicit user confirmation, failed-run review-only behavior, and AG-UI transport. |
| Change SmallTask repair routing or free-chat handoff  | `Backend/app/graph/nodes/small_task.py`                  | `services/small_task.py`, `services/small_task_scope.py`, `agents/small_task/`, `graph/workflow.py`, `graph/state.py`, `protocols/workflow/`, `graph/nodes/direct_modification.py`, and `useWorkflowConversation.ts`; preserve no direct `integration_test -> build`, max concurrency 2 (hard cap 3), exact-path diff attribution, user confirmation before scope/workflow escalation, and return to the explicit `repair_return_node` after local fixes. |
| Change workbench plan-execution mode or input locking  | `Frontend/src/renderer/src/components/AiChatPanel/components/PlanExecutionDock/` | `pages/AppEntryPage.tsx`, `hooks/useApplicationLifecycleStore.ts`, `AiChatPanel.tsx`, `hooks/useSessionRuntimeStore.ts`, `components/SessionSidebar/`, `planExecutionMode.ts`, `useWorkflowConversation.ts`, `service/agUiAgent.ts`, workflow typings, Backend `services/execution_resource_scope.py`, `protocols/workflow/lifecycle.py`, `protocols/workflow/request.py`, `run_control.py`, `runtime.py`, `workspace/run_lease.py`, and application lifecycle service/schema. Page executions persist their ProjectPlan-derived page/API/data-source resource set in `resourceLocks`; endpoint executions carry `buildExecutionScope.type=endpoint`, `targetId`, and `apiContractId`, persist endpoint/API/data-source/page dependency claims under `resourceLocks.endpoints` plus related groups, and prepare only the selected endpoint Unit. Endpoint `prepare_build_tasks` debug resumes reuse the API Contract ID from the snapshot or infer it only when the ProjectPlan match is unique. Failed Build controls use `buildSummary.recovery_available`/`recovery_task_ids` (with retry metadata retained for compatibility) and send `forwardedProps.workflowAction=retry_failed_tasks`; the action first retries transient failures and otherwise executes a ready RepairPlanner task set, while confirmation-required failures keep the confirmation path. If a public failure snapshot omits internal plans, the request adapter restores the persisted Build/Repair JSON plans from the current workspace before dispatch. The current phase treats locks as observation metadata only: overlapping claims and process-local leases do not block new runs, and the latest writer owns each displayed key. Every successful lifecycle write emits `application-lifecycle` before the related Workflow projection; the application-entry store merges events and cold/reconnect calibration by revision and supplies the composer, page outline, API outline, and return-home guard without polling the file. The application-entry session runtime provider and mounted workbench remain alive while the welcome page is visible, so AG-UI streams and in-memory UI state continue without persistence-based recovery. Continue uses the old runId only as a validated same-thread/same-target recovery token while live AG-UI state and the LangGraph checkpoint remain the current-thread Graph source. Only replace the bottom composer visually; preserve all other workbench regions and keep product actions on AG-UI. |
| Change per-message Workflow skill selection            | `Frontend/src/renderer/src/components/AiChatPanel/components/ChatComposer/` | `skillSelection.ts`, `useSessionRuntimeStore.ts`, `useWorkflowConversation.ts`, `service/agUiAgent.ts`, `service/chatSessions.ts`, Electron `src/main/index.ts`, Backend `protocols/workflow/`, `services/user_skill_runtime.py`, and `agents/registry.py`. Preserve AG-UI transport, message snapshots, resume conflict checks, whitelist isolation, and forced prompt loading. |
| Change user-skill catalog, enablement, editor, ZIP import, or runtime injection | `Backend/app/services/user_skills.py` | `services/user_skill_settings.py`, `services/user_skill_documents.py`, `services/user_skill_imports.py`, `services/user_skill_runtime.py`, `services/builtin_skills.py`, `protocols/user_skills.py`, `agents/registry.py`, `agents/workspace_scope.py`, frontend `service/userSkills.ts`, `components/SkillsPage/`, `AiChatPanel`, and user-skill typings. Keep client preflight aligned with authoritative backend checks, fail-closed settings, and immutable per-run Agent snapshots. |
| Recover old requirement planning behavior              | `Backend/app/deprecated/agents_requirement_planner.py` | Port only the needed behavior into workflow nodes or services; do not re-import deprecated files into main flow.                                                                                                                                                                                                                                                                               |
| Recover old orchestration/verification behavior        | `Backend/app/deprecated/graph_orchestrator.py`         | Port only the needed behavior into `graph/workflow.py`, `graph/nodes/`, or `graph/subgraphs/`; do not re-import deprecated files into main flow.                                                                                                                                                                                                                                               |
| Change Development Contract payload                    | `Backend/app/domain/development_contract.py`           | `Frontend/src/renderer/src/typings/developmentContract.ts`; deprecated planner/orchestrator files are reference-only.                                                                                                                                                                                                                                                                          |
| Change local chat history                              | `Frontend/src/main/index.ts`                           | `preload` typings, `chatSessions.ts`, `AiChatPanel`, and `SessionSidebar/PageSessionHistory.tsx`. Page sessions persist `pageId`, endpoint sessions persist `apiContractId`/`endpointId`, and entity-design sessions persist `entityId`/`entityLabel`; each target owns filtered history with a distinct AG-UI `threadId`. Entering test creates another target-bound session/thread with empty message history while preserving the development session as a separate history entry. Environment session directories use normalized absolute `workspaceRoot`; permanent managed-project deletion removes them with renderer caches, index-only removal preserves history, and deleting an unfinished design-stage plan moves its `.xcodeagent` data and session root to the OS trash. |
| Change saved applications/workspace open flow          | `Frontend/src/renderer/src/components/Welcome/`        | `WelcomePage.tsx`, `applicationStorage.ts`, Electron workspace/app storage IPC, `Frontend/src/main/managedWorkspace.ts`, `Frontend/src/renderer/src/typings/application.ts`. The open-workspace list only admits applications already present in the application index; orphan session workspaces never become implicit applications. Adding a local folder validates a real non-symlink `.xcodeagent` directory and a readable `application.json` before creating the index. New applications persist `planningConfirmedAt` after application-plan confirmation; once present it permanently admits the application to the workbench, while later page planning and workbench execution lifecycle state remains independent. The welcome page may host up to three unfinished application plans at once and disables creation only when all three slots are occupied. |
| Change new-app planning or lifecycle recovery | `docs/PRODUCT_UI_TECHNICAL_PLANNING.md`, `docs/APPLICATION_LIFECYCLE.md` | Start with `domain/application_lifecycle.py`, `domain/application_planning_interaction.py`, `services/application_lifecycle.py`, `graph/application_planning_workflow.py`, `graph/application_planning_interrupts.py`, `graph/nodes/requirements.py`, `graph/nodes/product_planning.py`, `graph/nodes/ui_confirmation.py`, `graph/nodes/planning.py`, `protocols/workflow/runtime.py`, `protocols/application_page_planning.py`, frontend `ApplicationPagePlanningModal.tsx`, `ApplicationPlanningQuestionPanel.tsx`, and workflow typings. RequirementSpec and ProductPlan are one jointly confirmed requirement document: ProductPlan may consume only the validated RequirementSpec draft in that node; both Markdown/JSON pairs are atomically promoted and hash-bound before UI can run. Do not create a requirement-document manifest. Preserve explicit action-driven node decisions, per-thread atomic resume validation, the optional UI skip, stale-card rejection before started projection, formal Markdown synchronization, and server-checkpoint authority. TechnicalPlan confirmation, not UI confirmation, moves initialization to template generation. |
| Change design-stage bottom conversation or artifact rollback | `docs/DESIGN_CONVERSATION_AGENT.md`, `Backend/app/graph/application_planning_revision.py` | `graph/application_planning_workflow.py`, `agents/design_conversation/router.py`, `protocols/application_page_planning.py`, shared RequirementSpec/ProductPlan/UiDesign/TechnicalPlan nodes, workflow request/projection/runtime, `ApplicationPagePlanningModal.tsx`, and `AiChatPanel`. Preserve earliest-node routing, the original planning thread/checkpoint, existing-artifact incremental updates, and every downstream formal confirmation gate. |
| Change workbench page/API/entity outline, selection, or application development planning | `docs/APPLICATION_DEVELOPMENT_PLANNING.md` | Use Electron planning inspection, AiChatPanel/SessionSidebar, workflow hooks, request protocol, readiness node, build-context resolver, and EntitySourceBinding services. Page/API selection enters `development_readiness_gate`; entity selection enters the independent binding flow. |
| Change frontend preview                                | `AiChatPanel` preview actions                          | `BrowserPreviewPanel`, `utils/previewUrl.ts`, Electron browser IPC.                                                                                                                                                                                                                                                                                                                            |
| Change shared domain types                             | `Frontend/src/renderer/src/typings/`                   | All service/UI/backend payload producers that consume the shape.                                                                                                                                                                                                                                                                                                                               |

## Validation Routes

| Change type                                   | Expected validation                                                                              |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| Documentation/instruction/index only          | No backend health, Vite check, build, or compile required unless explicitly requested.           |
| Frontend TypeScript/UI/Electron behavior      | `pnpm build` from `Frontend`; if a dev server is running, check the active Vite URL.             |
| Windows Electron package with bundled backend | Run `scripts/build-backend-win.ps1` on Windows first, then `pnpm build:win:dev` from `Frontend`. |
| macOS Electron package with bundled backend   | Run `scripts/build-backend-mac.sh x64` or `arm64` on the matching macOS host, then the matching `pnpm build:mac:<arch>:dev` from `Frontend`. |
| Backend Python behavior                       | Focused `python3 -m py_compile` or narrower test for changed Python files, plus `/health`.       |
| Backend API/agent behavior                    | `/health`; add focused endpoint/manual checks when behavior changes.                             |
| Application-level database credential encryption | From `Backend`, run `.venv/bin/python -m pytest tests/test_database_credentials.py tests/test_entity_design.py tests/test_entity_detail_plan.py` plus focused `py_compile`; from `Frontend`, run `pnpm test:database-crypto`, focused ESLint, and `pnpm build`; then verify `/health.database_encryption` contains only public metadata. |
| User-skill catalog, enablement, runtime, and ZIP import | From `Backend`, run `python3 -m unittest tests.test_user_skill_settings tests.test_user_skills tests.test_user_skill_documents tests.test_user_skill_imports tests.test_user_skill_runtime tests.test_builtin_skills` plus focused `py_compile`; from `Frontend`, run `pnpm test:chat-skills`, `pnpm typecheck`, and `pnpm build`; then check `/health` and the active Vite URL when present. |
