# XCodeAgent Codebase Index

Last updated: 2026-09-10

Use this file before broad code exploration. It is a routing map, not full documentation: read the matching row, then inspect only the listed entry files and nearby code.

> 当前旅程规则：Endpoint 的自包含字段映射由独立 `/endpoint-designs/run` AG-UI 配置流负责；页面和接口开发必须先进入主 `/workflow/run` 的 `api_design_readiness_gate`。页面门禁检查全部关联 Endpoint，接口门禁只检查自身；缺失时默认只展示清单，用户主动打开配置弹窗并保存后只更新当前会话的本地已配置状态，可继续配置其他 Endpoint；用户点击门禁“确认”后统一检测，检测通过后回显完整结果并等待继续开发确认。保存不会自动推进开发。旧版 `development_readiness_gate` 与 EntitySourceBinding 仅保留为独立旧入口。

## Update Policy

Update this index in the same change when a code change:

- Adds, removes, renames, or moves a directory or major module.
- Adds a new feature slice that changes ownership boundaries.
- Changes public API routes, Electron IPC channels, storage formats, AG-UI payloads, or workspace tool contracts.
- Changes validation/build expectations for an area.

For small local edits that do not change structure or ownership, leave the index alone and mention that no index update was needed.

## Top-Level Map

| Path                                | Responsibility                                                                                                                                             | Read first                                                                                                                        |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `AGENTS.md`                         | Repository-wide agent instructions, validation rules, and architecture constraints.                                                                        | Always read before edits.                                                                                                         |
| `docs/CODEBASE_INDEX.md`            | Directory/function routing map for future agents.                                                                                                          | Read before broad search or planning.                                                                                             |
| `contracts/authorization-api.v1.yaml` | 权限管理前后端共享的本地 OpenAPI 唯一事实源；后端 `auth` 分支副本必须与其 SHA-256 一致，前端只生成类型并通过既有 `service.ts` 发起请求。 | Read before changing authorization management APIs, auth-template generation, generated authorization types, or service wrappers. |
| `docs/WORKFLOW.md`                  | Design notes for the LangGraph workflow that coordinates requirement confirmation, project planning, code generation, integration testing, and acceptance. | Read before changing workflow phases, agent boundaries, Graph State shape, build/test subgraphs, or context/persistence strategy. |
| `docs/DAG_TASK_GENERATION_AND_VALIDATION_MININAL_CHANGE_PLAN.md` | Current minimal-change contract for Build DAG generation, deterministic validation, separate Pending/Formal authority, Confirm/Abandon/Regenerate actions, application-level Planning exclusivity, and the boundary between template initialization, runtime context, and Build execution. | Read before changing task preparation, DAG validation, Build gates, task-confirmation actions, Planning cancellation/refresh, or menu/route/page-placeholder ownership. |
| `docs/DAG_TASK_GENERATION_UNIT_CANDIDATE_PLAN.md` | Target architecture for refactoring Scope-level combined task generation into current-PlanningRun UnitCandidate generation, local validation/retry, confirmed-DAG-based reuse, serialized Scope assembly, and attributable global validation. | Read after the minimal-change plan and before designing or implementing PlanningRun state, per-Unit model requests, candidate persistence, reuse facts, progress events, or local retry orchestration. |
| `docs/APPLICATION_REVISION_WORKFLOW.md` | Canonical design for post-creation Change lifecycle: stable change identity, target-aware impact analysis, candidate artifacts, confirmation gates, invalidation closure, Build DAG execution, acceptance, promotion, rollback, resource locking, and AG-UI projection. | Read first before changing secondary modification, acceptance adjustment, revision events/cards, candidate promotion, or quick-modification completion semantics. |
| `docs/APPLICATION_DEVELOPMENT_PLANNING.md` | Workbench page/API selection, Endpoint API-design/readiness gating, retained independent legacy EntitySourceBinding, Build DAG confirmation, and numbered development-task planning. | Read before changing workbench target selection, API-design readiness, development planning, or `menus.developmentTasks`. |
| `docs/PRODUCT_UI_TECHNICAL_PLANNING.md` | Canonical creation/design boundary: RequirementSpec → ProductPlan → React UiDesign → explicit planning-stage entry → TechnicalPlan → per-Endpoint API Design, including PageImplementationContract, field-source snapshots, retained legacy EntitySourceBinding boundary, and 128k context budget. | Read first when changing planning phases, ProductPlan/TechnicalPlan storage, UI authority, page implementation contracts, API design, or Endpoint semantics. |
| `docs/DESIGN_CONVERSATION_AGENT.md` | Canonical design for the bottom Design Conversation Agent: earliest-node RequirementSpec/ProductPlan/UiDesign routing, incremental revision, live regeneration projection, confirmation gates, and the hard stop before TechnicalPlan. | Read first when changing design-phase bottom chat, design artifact rollback/invalidation, regeneration status UI, or the independent design conversation AG-UI endpoint. |
| `scripts/start-backend.sh`          | Standard macOS/POSIX backend dev-server command for agents and humans.                                                                                       | Run this on macOS when asked to start the backend service.                                                                        |
| `scripts/start-backend.ps1`         | Standard Windows PowerShell backend dev-server command with Python 3.12 virtual-environment discovery.                                                       | Run this on Windows when asked to start the backend service.                                                                      |
| `scripts/build-backend-mac.sh`      | macOS PyInstaller build and architecture-specific staging script for the packaged backend executable, required built-in skills, and `.env`.                 | Run with `x64` on Intel or `arm64` on Apple Silicon before matching `Frontend` mac packaging; staging stops on architecture or bundled-skill validation failures. |
| `docs/CROSS_PLATFORM_SUPPORT.md`    | Supported Windows/macOS architectures, development commands, packaging flow, signing boundary, and reference-architecture mapping.                          | Read before changing platform adapters, process lifecycle, or desktop packaging.                                                  |
| `scripts/build-backend-win.ps1`     | Windows-only PyInstaller build and staging script for the packaged backend exe, required built-in skills, and `.env`.                                       | Run on a Windows CI/VM before `Frontend` Windows packaging; staging stops when bundled skill validation fails.                    |
| `Backend/`                          | FastAPI backend, agent runtime, planning/orchestration, and local workspace tools.                                                                          | `Backend/app/main.py`, then the relevant module below.                                                                            |
| `Frontend/`                         | Electron + React desktop client with Vite, Ant Design, local session/app storage, AG-UI frontend clients.                                                  | `Frontend/src/main/index.ts`, `Frontend/src/renderer/src/pages/AppEntryPage.tsx`.                                                 |

## Backend

### Initial development completion and test entry

- Initial development registration and continuation phases use the current `api_design_readiness_gate` entry. A retained confirmed API mapping may occupy `WorkflowRunCard` only at that gate; downstream unit-test confirmation must render its skip/run actions. Build completion alone never increments the lifecycle completion count: `test_phase_confirmation` requires the unit-test gate to pass or be explicitly skipped first.
- `Backend/app/domain/development_artifacts.py` defines the strict page/Endpoint/entity identity, initial progress, catalog availability, and derived `TestEntryGate` contracts. `services/development_artifacts.py` owns confirmed ProductPlan/TechnicalPlan catalog reconciliation, sticky first completion, and the single application-wide test-entry rule. Entities count in this gate: their progress comes from the current persisted EntitySourceBinding confirmation and active data_source executions. QuickTaskGuide and ApplicationOutline consume this lifecycle status for card text, dots, and counts.
- `.xcodeagent/application-lifecycle.json.developmentArtifacts` is the sole completion authority. Every lifecycle write reconciles the current confirmed catalog under the existing lock; the AG-UI lifecycle `get` calibrates cold/reconnected reads. `testEntryGate` is projection-only. Execution `developmentPurpose`/`developmentTarget` are server-owned and preserved across initial-development continuations, while revisions never backfill completion.
- Downstream `unit_test`, `unit_test_repair`, and `test_phase_confirmation` lifecycle registration reads the same-thread server checkpoint’s `build_execution_slice.scope`; debug UI defaults cannot replace a completed page/Endpoint Build target with application scope. Runtime keeps Graph and execution targets aligned without replaying reducer fields. For an unfinished target outside a formal revision, that verified Build continuation can establish initial-development purpose; completion still requires Build and unit gates. Regression: `tests.test_development_completion_resume` exercises actual AG-UI/checkpoint flows and 1/2 counts.
- `graph/nodes/lifecycle.py::test_phase_confirmation` records the selected target only after Build and the development unit-test gate pass, before checking all targets. Workflow lifecycle startup and `graph/subgraphs/testing.py::integration_test` recheck the same gate; test interaction consumption and execution replacement share one atomic write. Blocked runs emit a structured `development_artifacts_incomplete` result and finish the AG-UI lifecycle without marking development failed.
- Frontend `developmentArtifacts.ts`, `context/WorkbenchPhaseContext.tsx` and `context/workbenchPhaseState.ts` own shared phase access and live gate consumption. `ApplicationOutline/DevelopmentStatusDot` and `ApiOutlineGroup` render initial status and unfiltered counts; `WorkbenchTopBar` and historical `TestPhaseConfirmationCard` consume the same application lifecycle. Test browsing creates no session; actual confirmation changes phase only after server execution registration, and an unaccepted test session is discarded.
- `AiChatPanel/workflowDebugResume.ts` binds downstream node debugging to the most recent Build thread in the current conversation that is still registered in lifecycle. Both send-button debugging and paused-plan debugging resume the execution, never the chat thread. Server checkpoint validation remains authoritative; a newer failed Build cannot fall back to an earlier success. Regression coverage is in `Frontend/tests/workflowPreview.test.ts`.
- Regression entry points: Backend `tests.test_development_artifacts`, `tests.test_workflow_test_phase_confirmation`, lifecycle/continuation stream tests; Frontend `pnpm test:development-artifacts`, existing workflow/stage tests, targeted ESLint and `pnpm build`. Runtime UI verification must use Electron.
- `ApplicationOutline/DevelopmentStatusDot.less` owns the purple in-progress pulse (with reduced-motion support); `styles/global.less` defines dedicated light/dark completion greens. `WorkflowRunCard/TestPhaseConfirmationCard.less` owns the themed confirmation layout: blocked cards list each remaining artifact with its type and count, while test target and action appear only after the live gate allows entry.
- `Frontend/src/renderer/src/workbenchPhaseNavigation.ts` owns the shared phase order and per-application furthest-reached browsing record (`xcodeagent:workbench-reached-phase:<applicationId>`). `WorkbenchPhaseContext` keeps this separate from the current view and active execution; `useChatSessions` restores browsing reach from the current nonempty stage-session directory after its stale-load check. `WorkbenchTopBar` uses this record for revisiting stages, while test entry still obeys the live server gate. Application deletion clears the record. Regression coverage lives in `Frontend/tests/workbenchPhaseNavigation.test.tsx`, included by `pnpm test:development-artifacts`.

```text
Backend/app/
├── main.py
├── config.py
├── graph/          # LangGraph workflow lifecycle, state, nodes, and subgraphs
├── agents/         # First-class Deep Agent runtimes plus the read-only change-impact analyzer
├── domain/         # Framework-independent core data models and workflow contracts
├── services/       # Deterministic business rules, normalization, scheduling, and quality gates
├── tools/          # Controlled tools exposed to Deep Agents
├── protocols/      # Protocol integration
├── workspace/      # Project workspace documents and sandboxed local file/tool access
├── middleware/     # Cross-cutting Deep Agent controls
├── persistence/    # Business and runtime persistence
├── observability/  # Runtime events and observability
└── utils/          # Shared dependency-light parsing and utility helpers
```

| Path                                                    | Responsibility                                                                                                                                                                                                                                                                                                                                                                                     | Read first / common edits                                                                                                                                                                                           |
| ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Backend/app/main.py`                                   | FastAPI app setup and route registration for `/health`, primary `/workflow/run`, optionally page/API-targeted `/conversation/run`, standalone product AG-UI actions, Electron-only application deletion preparation/completion, dedicated data-source and Endpoint API-design routers, controlled `/tools/*` endpoints (including database metadata), approvals, git/workspace tools, and local preview controls `/api/projects/launch` and `/api/projects/stop`. Its lifespan clears main, creation-planning, and conversation Graph caches before closing shared checkpoint storage. | Add or change shared HTTP endpoints here; data-source HTTP changes belong in `app/routes/data_sources.py`, and Endpoint API-design HTTP changes belong in `app/routes/endpoint_designs.py`. |
| `Backend/app/config.py`                                 | Environment-backed model/runtime settings. DAG Unit token budget defaults to 4096; platform Worker Pool concurrency defaults to 3 and is hard-capped at 3 even when configuration is larger. Local=3 and Global=2 are fixed read-only fields; production request/session/tool-read protection budgets are constructed centrally by the async Planning adapter. `XCODEAGENT_DAG_BUSINESS_SELF_CHECK_ENABLED` independently controls Build-stage business self-check execution. | Read with `Backend/.env.example`, `graph/nodes/task_planning_adapter.py`, `services/unit_generation_scheduler.py`, and `Backend/tests/test_dag_settings.py` when changing DAG budgets, platform-owned parallelism, or Build business self-check behavior. |
| `Backend/packaging/xcodeagent-backend.spec`             | PyInstaller onedir definition that bundles the Backend application, all built-in skills under `_internal/app/builtin_skills`, package data, and hidden imports.                                                                                                                                                                                                                                     | Read when changing packaged Python/data dependencies or frozen resource layout.                                                                                                                                    |
| `Backend/packaging/verify_bundled_skills.py`            | Cross-platform post-PyInstaller validator used by both Backend staging scripts to reject bundles missing required built-in skill files.                                                                                                                                                                                                                                                            | Update with required built-in skill files or supported PyInstaller resource layouts.                                                                                                                               |
| `Backend/packaging/backend_server.py`                   | Frozen Backend entry point: loads packaged configuration, validates required built-in skills before Uvicorn starts, and launches the API server.                                                                                                                                                                                                                                                   | Read when changing packaged Backend startup, frozen resource discovery, or fail-fast requirements.                                                                                                                  |
| `Backend/app/graph/workflow.py`                         | Primary LangGraph workflow declaration: page/API development enters `api_design_readiness_gate`, which checks current Endpoint API Design versions before `inspect_workspace -> prepare_build_tasks`. Production task planning uses the async Planning adapter and stops at a PendingPlan confirmation gate; exact Confirm promotes Formal and resumes Build, Regenerate consumes the old Pending and creates a new PlanningRun, and Abandon ends the matching execution. Legacy EntitySourceBinding nodes remain registered only as an independent flow. Later phases retain explicit test/review/acceptance gates and failures route to `handle_failure`. | Read when adding, removing, renaming, rerouting workflow phases, changing workflow checkpoint behavior, or selecting planning authority. Keep node logic in `graph/nodes/` or subgraphs. |
| `Backend/app/graph/direct_modification_workflow.py`     | Independent optionally targeted conversation Graph: always creates a bounded read-only workspace/code-graph snapshot before intent classification, then routes casual chat to a tool-less ChatModel, workspace questions to a read-only Agent, localized code/docs/config changes to bounded SmallTask execution, and formal changes to confirmed `/workflow/run` handoff. Current page/API target is included in classification context. Code changes retain backend-first fullstack execution, then `validate_direct_fix` derives frontend/backend Build/static checks from the real diff and runs only affected layers without generating or executing unit tests; successful quick modifications finalize without starting a preview, while the bounded repair loop returns to the same scoped validation. Ordinary workspace-file edits use precise paths and also skip preview. Checkpoints use a `conversation:` thread prefix. | Read with `graph/nodes/direct_modification.py`, `graph/nodes/direct_repair.py`, `agents/direct_modification.py`, `agents/workspace_assistant/`, and `protocols/direct_modification.py` when changing free conversation. |
| `Backend/app/graph/nodes/direct_repair.py`            | Dedicated free-conversation repair node: normalizes scoped validation evidence, gives a read-only RepairPlanner only the current real frontend/backend diff paths, executes bounded shared SmallTask tasks, persists the internal repair plan, and returns successful fixes to `validate_direct_fix`; missing or placeholder paths fail with exact evidence and cannot become a formal revision, while an explicit `formal_revision` escalation keeps the confirmation gate. | Read with `graph/direct_modification_workflow.py`, `services/small_task.py`, `agents/repair_planner/`, and `protocols/direct_modification_projection.py` when changing free-chat repair. |
| `Backend/app/graph/state.py`                            | `ProjectState` TypedDict for compact Graph State: server-owned target/scope, API-design gate action/readiness/result, retained legacy EntitySourceBinding submission, formal artifact paths, runtime Build context, PlanningRun/PendingPlan confirmation and progress, build/test/review/acceptance state, failures, and timeline. Endpoint drafts live in the independent editor; confirmed Endpoint JSON/Markdown are workspace artifacts. Pending tasks never become downstream Build state until exact promotion to Formal. | Read when changing state keys, API-design/readiness recovery, DAG/test/acceptance confirmation, stale-checkpoint recovery, node contracts, or planning artifacts. |
| `Backend/app/graph/nodes/`                              | Thin phase nodes for deterministic API readiness, workspace inspection, production async task planning, direct modification, Build/testing wrappers, lifecycle and confirmation phases. `api_design.py` owns the normal readiness gate; `task_planning_adapter.py` owns Planning/Confirm/Regenerate and reuses bounded prerequisite/context helpers from `tasks.py`; independent Endpoint configuration lives outside the main Graph. | Read the file matching the phase before changing node behavior; keep deterministic routing/state updates here and delegate reusable rules to `services/`. |
| `Backend/app/domain/api_design.py`                      | Current-only Pydantic contract for self-contained Endpoint field mappings, embedded source fields, source snapshots, drafts, and confirmed `endpoint-field-mapping.v3` design documents. Source mappings use `sourceFields` plus `processingType` (`direct`, `single_field_description`, `multi_field_description`); formal mappings also allow pure `business_description`, while drafts additionally allow `unconfigured`. | Read before changing `fieldMappings`, mapping modes, Endpoint/source references, or source snapshot shape. |
| `Backend/app/services/api_design.py`                    | Deterministic Endpoint API-design service: creates immutable Endpoint field snapshots, enumerates real-time source candidates, validates complete self-contained `fieldMappings`, handles business descriptions without fake sources, creates redacted source snapshots, and evaluates current design readiness. TechnicalPlan global entity semantics remain outside Endpoint↔Source mappings. | Read with the domain model, data-source catalog service, Endpoint document store, and node before changing design behavior. |
| `Backend/app/services/api_design_mapping_rules.py`      | Shared current-v3 mapping rules: iterates `sourceFields`, builds database/Operation source identities, rejects duplicate sources, and keeps source-based processing descriptions distinct from pure `business_description`. | Read with `domain/api_design.py` and `services/api_design.py` when changing mapping cardinality, processing types, or source identity validation. |
| `Backend/app/workspace/endpoint_design_documents.py`   | Reads and atomically writes `.xcodeagent/plans/endpoints/endpoint--<contractId>--<endpointId>.json/.md`, computes the TechnicalPlan contract fingerprint, and requires the confirmed dual-file pair for readiness. | Read before changing Endpoint artifact naming, fingerprinting, Markdown rendering, or persistence guarantees. |
| `Backend/app/graph/nodes/api_design.py`                 | Implements deterministic `api_design_readiness_gate`; independent mapping save writes Endpoint artifacts before this gate. Page readiness checks every `requiredEndpointId`, Endpoint readiness checks only the selected Contract/Endpoint, and confirmation rechecks the complete displayed revision set before entering `inspect_workspace`. | Read with workflow request/lifecycle/projection/runtime adapters and the frontend API-design panel. |
| `Backend/app/graph/nodes/tasks.py`                      | Legacy combined generator plus the current shared prerequisite, Build scope/context, Endpoint API Design guard, confirmation projection, confirmed-result, and Build-compatibility helpers. It is no longer the production generation authority; current Planning writes only Pending through the async adapter. | Read when changing shared DAG prerequisites, Endpoint-design inputs, retained ownership, confirmation read models, or intentionally changing/removing the legacy implementation. |
| `Backend/app/graph/nodes/task_planning_adapter.py`, `task_planning_inputs.py` | Production async Graph adapter and formal-contract projection: reuse formal-artifact/template/scope/API prerequisites, freeze `MainlinePlanningInputs`, await `run_mainline_planning`, project PendingPlan/DraftIdentity/PlanningRun progress, route exact Confirm through lifecycle promotion, and route exact Regenerate through `dag_planning_regeneration` back into a newly allocated PlanningRun. Generation and Regenerate never write Formal. | Read with `tasks.py`, `graph/workflow.py`, `services/build_task_planning_service.py`, `services/dag_planning_regeneration.py`, `Backend/tests/test_async_workflow_planning_adapter.py`, and `test_dag_confirm_authority_cutover.py`. |
| `Backend/app/graph/subgraphs/build.py`                  | Build Subgraph behind `build`: at Build Run start it reads the current confirmed workspace `build-task-plan.json`, requires `build-dag.v3`, `status=ready`, `task_graph.validation.is_valid=true`, and matching Build scope, then binds a read-only per-run copy by canonical SHA-256. Before every leaf dispatch it checks that the planning authority still has that digest; drift terminates the Run rather than switching plans. Before dispatching any leaf task, it applies the confirmed platform-owned RouteGuard projections and the auth template's fixed `AuthConstants.java` managed region; a missing target, marker, branch mismatch, or constant drift blocks Build. Each ready Task is first dispatched by its independent `execution_strategy`: legacy/missing values default to `agent`, whose domain-specific runner is then selected by `owner`; `deterministic` requires an allowlisted and registered `platform_executor`. `authorization.frontend_resources` now executes through that registry inside the unchanged ready batch and returns through the same result/lifecycle normalization; contract, registry, executor, or validation failures fail closed without reaching an LLM. Agent results combine the task's authorized snapshot paths with recorded write/delete tool paths, and check scope only against the resulting task-owned change set so concurrent tasks cannot inherit each other's violations. Engineering acceptance always runs; business acceptance runs independently only when `XCODEAGENT_DAG_BUSINESS_SELF_CHECK_ENABLED=true`, otherwise it records an explicit skipped status before the aggregated Repair decision. Database tasks continue to use database execution evidence. Explicit `retry` recovery, real repair counting, and repair-scope/database-approval confirmations remain bounded. It does not normalize away invalid menu/route/page-boundary tasks or use a Markdown DAG artifact. | Read when changing task dispatch, execution strategy, deterministic executor registration, concurrency, task-owned file attribution, Build Run plan binding, RouteGuard/AuthConstants projection, acceptance aggregation, confirmation recovery, owner result collection, explicit retry recovery, scheduler coordination after code generation, database approval pauses, or repair planning. |
| `Backend/app/graph/subgraphs/testing.py`                | Testing Subgraph behind `integration_test`: runs dependency installation, frontend/backend Build checks, pauses through an AG-UI confirmation for the optional advisory frontend performance test, launches the user `frontend` dev server with `frontend_performance_runner`, records the non-blocking Lighthouse check, and performs the integration quality gate. Every quality-gate pass overwrites the internal JSON and user-facing Markdown test reports so repair retries always publish the latest result; it no longer calls TestGenerationAgent or executes frontend/backend unit tests. Any blocking integration check routes to the independent `small_task_repair` loop; API contracts are validated before build and repair plans never route directly to `build`. | Read when changing integration build/performance confirmation, quality gate rules, test evidence shape (including `performanceScores`/`performanceMetrics`), test-report refresh, revision requests, integration repair budgets, repair scope, or repair-task routing. |
| `Backend/app/graph/subgraphs/code_review.py`             | Code Review Subgraph behind the top-level `code_review` node: runs one read-only scan, persists the immutable latest Markdown snapshot through `workspace/code_review_documents.py`, pauses on `code_review_repair_confirmation`, performs bounded Skill-driven repair, requires real dedicated pnpm-install evidence for declared dependency actions, and runs independent frontend/backend build checks with a three-round repair loop. Scan/repair model failures publish the bounded `codeReviewRetry` target consumed by `workflowAction=retry_code_review`; repair retries reuse the issue snapshot and failed request's iteration, while build/budget failures remain non-retryable through this action. It preserves the original scan result/report, captures authorized project and generated lockfile Diff while excluding node_modules, reuses successful Agent install evidence, and emits bounded repair/build projections on one stable process-step identity before `acceptance_phase_confirmation`. | Read when changing review reports, one-click review repair, model-request retry recovery, frontend project scope, dedicated package-install evidence, independent review build logs, repair iteration limits, or review AG-UI progress. |
| `Backend/app/graph/subgraphs/acceptance.py`              | Acceptance Subgraph mounted directly as the top-level `acceptance` runnable: reuses the deterministic project launcher as `launch_project`, streams its custom progress through the parent AG-UI run, skips launch when a successful preview snapshot is already present, and enters `acceptance_review` to project `page_acceptance` with preview/launch/request evidence. Launch failures end the subgraph without an acceptance interaction; accepted/finalize behavior remains available at the main graph boundary. | Read when changing acceptance-phase launch, preview recovery, `page_acceptance` lifecycle mapping, or acceptance AG-UI state projection. |
| `Backend/app/graph/subgraphs/unit_testing.py`            | Development Unit Testing Subgraph behind `unit_test`: captures the first Build `code_changes/code_change_sets`, derives the bounded `unit_test_generation_context.code_diff`, and gives TestGenerationAgent only changed sources/existing tests, the confirmed Build DAG, current TechnicalPlan JSON, and the active Build scope/slice. It handles the existing run/skip confirmation, validates generated files, executes affected frontend/backend unit tests, writes an independent unit report/quality gate, and plans the bounded `unit_test_repair` loop. Generated-test and repair changes merge into the development Diff while the original Build Diff remains stable for every retry. | Read when changing unit-test target mapping, generation context, run/skip recovery, TestGenerationAgent input, unit execution, unit reports, unit repair budget, or `unit_test.checks` projection. |
| `Backend/app/graph/nodes/small_task.py`                 | Main-graph SmallTask execution node: selects bounded parallel batches, calls the shared coding Agent, attributes real workspace diffs, pauses for scope/workflow confirmation, and returns successful local fixes to the explicit `repair_return_node` (`unit_test` or `integration_test`). | Read when changing repair execution, escalation targets, parallelism, or SmallTask state transitions. |
| `Backend/app/deprecated/graph_agent.py`                 | Deprecated, fully commented legacy/main chat runtime formerly at `graph/agent.py`; retained only as reference for future workflow parity work.                                                                                                                                                                                                                                                     | Do not import from main flow. Read only when intentionally backporting old tool/approval/diff behavior into workflow nodes.                                                                                         |
| `Backend/app/deprecated/graph_orchestrator.py`          | Deprecated, fully commented earlier development orchestration runtime formerly at `graph/orchestrator.py`.                                                                                                                                                                                                                                                                                         | Do not import from main flow. Read only when comparing old orchestrator behavior with workflow.                                                                                                                     |
| `Backend/app/agents/registry.py`                        | Creates and groups the coding, planning, repair, workspace-assistant and internal TestGeneration Deep Agents; the latter is best-effort and may be unavailable without blocking the workflow. The registry force-appends validated selected `SKILL.md` documents to prompts and caches bundles by normalized workspace, skill whitelist, immutable user-skill revision, and AGENTS.md memory revision. | Read when adding/removing first-class agents or changing selected-skill prompt injection and workspace/skill/memory-aware Agent caching. |
| `Backend/app/agents/code_analyze/`                       | Read-only CodeAnalyze DeepAgent wrapper and deterministic result normalizer. It exposes only list/search/read operations, loads exactly the frontend-code-scan and backend-code-scan built-in Skills, keeps the tool-level `frontend/**` (excluding node_modules/sensitive files) and `backend/src/main/java` read boundary, drops model-declared out-of-scope targets or issue paths without failing the whole scan, normalizes safe workspace-relative and side scan-root-relative issue paths plus the internal finite `repair_actions` contract, rejects writes/commands/delegation, de-duplicates and caps findings at 100, and retries malformed or incomplete Agent output once. | Read when changing review-scan Skill loading, workspace security scope, internal repair actions, review result shape, or failure/retry policy. |
| `Backend/app/services/unit_test_repair_budget.py` | Server-owned unit-test repair accounting by check ID: ten attempts per generation/execution check, charged before actual SmallTask dispatch, including failed attempts; deduplicates same-plan tasks and scope-confirmation resumes. Current failed checks alone determine exhaustion. `services/unit_test_repair_recovery.py` sends `invalid_agent_output` failures back to real unit checks and replanning within that same budget, preserving diffs; exhausted and non-protocol failures stop. The shared SmallTask result parser rejects empty/tool-only/incomplete JSON and exposes optional `failureCode` in `smallTaskResults`. Exposes read-only `unitTestRepairAttempts`; integration/review budgets remain separate. | Read with `graph/subgraphs/unit_testing.py`, `graph/nodes/small_task.py` and `tests/test_unit_test_repair_budget.py`. Test-phase confirmation is a one-shot current-request action: `protocols/workflow/request.py`, `unit_testing.py` and `nodes/lifecycle.py` clear stale confirmations on debug/rerun and after consumption. |
| `Backend/app/agents/frontend_test_instructions.py` | Shared TestGeneration/SmallTask system-prompt rules for Jest default-export mock interoperability. Requires reading actual test configuration and exports, uses `__esModule: true` for ES-module-shaped factories under CommonJS transforms, distinguishes mock setup exceptions from business failures, and preserves assertions and execution/path boundaries. | Read with both Agent factories and `tests/test_frontend_test_instructions.py` when changing frontend test generation or local mock repair guidance. |
| `Backend/app/agents/test_generation/`                   | Internal TestGeneration Agent, generator, mapping-cache persistence, deterministic test-file validation, explicit read/list/search delegation, and hard backend write scope. It reads the frozen current Build diff first, then changed sources/related tests, the confirmed `build-task-plan.json`, current `technical-plan.json`, and only necessary direct dependencies; obsolete ProjectPlan, RequirementSpec, detail-plan, and code-graph inputs are excluded. It can write only flat `frontend/tests/*.test.ts(x)` or mirrored `backend/src/test/java/**/*.java`; LangGraph checkpoint churn is excluded from workspace-change acceptance while formal `.xcodeagent` artifacts remain protected. | Read when changing unit-test naming, source/test mapping, generation failure policy, checkpoint filtering, backend delegation, or test-only filesystem permissions. |
| `Backend/app/agents/workspace_scope.py`                 | Normalizes `workspaceRoot`, creates DeepAgents filesystem/Composite backends, mounts packaged built-in skills plus immutable user-skill and AGENTS.md memory snapshots at read-only virtual paths, and defines workspace-confined filesystem permissions including the test-generation-only write mode. | Read when changing DeepAgents workspace access, skill/memory mounts, virtual filesystem behavior, or sensitive-file policy. |
| `Backend/app/middleware/direct_modification.py`         | Request-sensitive policy installed on shared coding Agents. It recognizes direct-chat and SmallTask markers and removes task/todo orchestration only for those bounded runs, while inheriting the normal model, retry, timeout, recursion, and command runtime configuration; for every model request it also restores the factory-bound `execute` tool if upstream middleware rebuilt the tool list, without restoring broader tools that another policy intentionally removed. | Read with `agents/direct_modification.py`, `agents/small_task/`, both coding Agent factories, and `agents/tool_activity_stream.py` when changing quick-run or formal-run tool availability. |
| `Backend/app/agents/model_factory.py` | Builds LangChain chat models from global Settings with optional per-call `max_tokens_override`, `max_retries_override`, and `timeout_seconds_override`. None preserves global behavior; retry=0 reaches the SDK. Unit generation uses these overrides through `services/unit_generation.py`, with its frozen Policy supplying the DAG-only token, retry and request-timeout values. | Read with `Backend/tests/test_model_factory.py`, `test_unit_generation.py`, and `services/unit_generation.py` when changing model configuration overrides. |
| `Backend/app/agents/messages.py`                        | Graph-independent helpers for extracting text from DeepAgent result messages without importing workflow nodes.                                                                                                                                                                                                                                                                                     | Read when changing shared Agent result parsing or avoiding Agent-to-Graph dependency cycles.                                                                                                                        |
| `Backend/app/agents/direct_modification.py`             | Unified free-conversation classifier that can return the final tool-less casual response in the same model call, with a fallback responder, plus the read-only workspace-question wrapper and frontend/backend/workspace SmallTask wrappers. Formal classification and Agent escalation produce confirmation cards instead of silently entering planning. | Read when changing conversation intent/owner semantics, SmallTask reuse, frontend Skill prerequisites, backend-to-frontend handoff, or response/result schemas. |
| `Backend/app/agents/change_impact_analyzer.py`          | Read-only ChangeImpactAnalyzer. It compares only the four current confirmed JSON artifacts, normalizes atomic `invalidates/preserves/unknown` facts, verifies artifact key/JSON Pointer/file hash evidence, and never emits Workflow routing fields or reads Markdown. Code-scan evidence is attached only by the server after all relevant contracts are preserved. | Read when changing secondary-revision contract evidence, analyzer normalization, or the contract-first/code-second boundary. |
| `Backend/app/agents/workspace_assistant/`               | Read-only Deep Agent for questions that require current workspace evidence. It can progressively read workspace files plus selected Skill/AGENTS snapshots, but cannot write, execute commands, plan todos, or spawn subagents. | Read when changing workspace-question evidence, permissions, or natural-language answer behavior. |
| `Backend/app/agents/small_task/`                        | Shared bounded coding Agent declaration, source-first runner, and scoped filesystem backend. It receives source candidates, allowed paths, failure evidence, confirmed context, and structured engineering/business checks; unscoped search starts at the narrow task source root, installed dependencies/caches/build outputs are unreadable and unwritable, and results use completed/already-satisfied/escalation/failed JSON without planning, subagents, or formal/database mutation. | Read when changing SmallTask prompts, read/write scope, source-candidate priority, structured check context, free-chat reuse, or the 128k context boundary. |
| `Backend/app/agents/tool_activity_stream.py` and `tool_activity_projection.py` | Streams root/delegated Deep Agent messages, returns the authoritative root result, and projects filesystem, execute, task, and todo calls into bounded safe activity records without exposing command arguments, prompts, file contents, or host paths. | Read when changing Agent progress visibility, namespace/call-id handling, execution budgets, or AG-UI tool step semantics. |
| `Backend/app/agents/main/`                              | Direct ChatModel boundaries for requirements and ProductPlan/TechnicalPlan work, plus single-Unit DAG prompt rules. `unit_task_prompt.py`, `unit_task_rules.py`, and `backend_unit_task_rules.py` build one frozen Unit prompt from GenerationRequirements, a metadata-only contract catalog, bounded WorkspaceSnapshot facts, retained-owner constraints, exact Endpoint API Design references, and structured Local/Global feedback. The model returns only the current Unit's `tasks`; it cannot assemble Scope, choose other Units, create platform tasks, or write acceptance metadata. Model execution and FrozenContractReader turns live in `services/unit_generation.py`; validation/retry/scheduling remain service responsibilities. The deleted legacy `task_preparer.py` is not a production entry. | Read the matching planner file plus `services/unit_generation.py`, `unit_candidate_validator.py`, and `dag_planning_orchestrator.py` before changing direct reasoning or Unit task generation. |
| `Backend/app/agents/repair_planner/`                    | Dedicated read-only RepairPlanner DeepAgent node plus prompt wrappers for build-failure and testing repair planning. It consumes deterministic command-failure evidence only after a quality-gate failure and must not edit files or mutate workflow state. | Read when changing RepairPlanner prompts, DeepAgent permissions, RepairPlannerInput/RepairPlan behavior, or testing/build repair planning calls. |
| `Backend/app/agents/code_analyze/`                      | Read-only CodeAnalyzeAgent and result normalizer. It force-loads the two built-in scan Skills, validates tool activity, normalizes bounded issue/target output plus finite internal repair actions, scans safe frontend project files when rules exist, keeps node_modules and sensitive paths inaccessible to tools, and silently drops model-declared out-of-scope targets/issues instead of failing the scan. | Read when changing scan output normalization, Skill-read verification, project directory boundaries, or the no-rescan contract. |
| `Backend/app/agents/code_review_repair/`                | CodeReviewRepairAgent declaration and bounded repair wrapper. Its default capability exposes only safe frontend-project/backend-source file tools; the zero-argument `pnpm_install_frontend` tool is registered only on a separate capability selected when the normalized issue packet explicitly declares `repair_actions=["pnpm_install"]`. It accepts bounded build evidence, verifies fresh install evidence, and returns a strict repair result checked against real workspace Diff capture. | Read with `tools/code_review_pnpm.py` when changing one-click repair prompts, frontend project scope, package-install authorization/execution, repair result validation, or Agent tool filtering. |
| `Backend/app/agents/frontend/`                          | Frontend Generation Agent declaration and generator wrapper for approved frontend tasks. The real workspace is passed only to the Agent bundle/backend; model prompts expose the virtual root `/`. For real backend APIs, the shared `frontend:api-client` adapter owns `ResponseEntity<T>` validation and unwrapping: API modules type HTTP payloads with the envelope but expose only business `Promise<T>` values to pages; static data modules bypass the HTTP adapter. | Read when changing page/component generation responsibilities, business API transport handling, allowed paths, frontend task reports, or frontend agent permissions.                                                                                 |
| `Backend/app/agents/data_source/`                       | Internal Data Source Generation runtime whose model role is the Java Backend Coding Agent. It loads the unified `springboot-backend-generate` Skill once and expands task-specific references first by `database`/`external_api`, then by bootstrap/endpoint mode; mixed-source tasks load both selected references in stable database-first order. It emits bootstrap or endpoint `implementation_contract` packets with an explicit task `stage` plus bounded numbered `execution_steps`. External API packets reject missing/duplicate current-Endpoint operation matches and consume only structural request/response shapes, mapped entity paths, and confirmed mappings. A separate Backend Workspace Context reads `WorkspaceSnapshot.backend.dir_structure` and exposes both the virtual Java project root and the complete backend directory tree as trusted path-navigation metadata; listed relevant files may be read for implementation context, while current filesystem reads remain authoritative and task paths remain the write boundary. The context is never sourced from BuildTaskPlan or embedded in task packets and implementation contracts. Bootstrap packets expose only the source-selected Java/Maven/Spring capability policy: MyBatis/MySQL for database and Spring Cloud OpenFeign for external API, including the template Boot/Cloud dependency baseline; endpoint packets contain only the scoped confirmed TechnicalPlan API/operation behavior and the current Endpoint API Design projected into `implementation_contract.api_design`. New external Clients prefer OpenFeign, while an already-satisfying existing project HTTP abstraction remains valid. The internal registry/audit identity remains `data-source-generation-agent`; model paths use virtual root `/` and do not use the code graph. | Read when changing Java backend model responsibilities, WorkspaceSnapshot projection, source-to-Skill/reference routing, implementation-contract projection, allowed paths, reports, or agent permissions. |
| `Backend/app/agents/database/`                          | Database Change Agent declaration and generator wrapper for database owner tasks. It re-reads real MySQL schema summaries, asks a read-only Deep Agent for an exact SQL plan, runs deterministic risk classification, pauses high-risk plans through `ApprovalStore`, and executes approved/low-risk plans through controlled MySQL execution instead of editing code. | Read when changing database task execution, SQL plan schema, high-risk approval behavior, latest-schema refresh, or database agent permissions. |
| `Backend/app/deprecated/agents_requirement_planner.py`  | Deprecated, fully commented requirement planner runtime formerly at `agents/requirement_planner.py`.                                                                                                                                                                                                                                                                                               | Do not import from main flow. Read only for reference while moving planner parity into workflow.                                                                                                                    |
| `Backend/app/agents/subagents.py`                       | Subagent input/result contracts and direct-write guards.                                                                                                                                                                                                                                                                                                                                           | Read when wiring scout/build/verifier agents or changing subagent payloads.                                                                                                                                         |
| `Backend/app/domain/development_contract.py`            | Framework-independent Development Contract models and normalization used by the earlier orchestrator and frontend typings.                                                                                                                                                                                                                                                                         | Read when changing SDD, features, API contracts, task graph, or verification plan payloads.                                                                                                                         |
| `Backend/app/domain/models.py`                          | Framework-independent workflow document/task models such as RequirementSpec, ProjectPlan, PageSpec, and BuildTask. BuildTask keeps `owner` as database/backend/frontend domain ownership, defaults legacy tasks to `execution_strategy=agent`, and validates deterministic `platform_executor` values against the explicit allowlist. | Read when changing spec/plan/task/test schemas or Build Task execution contracts used by the new workflow. |
| `Backend/app/domain/application_lifecycle.py`               | Strict Pydantic schema for `.xcodeagent/application-lifecycle.json` without a schema-version field: top-level `application` is the sole workspace business identity, while `initialization` owns the original planning checkpoint `threadId` and retains it after `ready_for_workbench/completed` solely for controlled design-stage revisions. Run-indexed workbench executions separately own page/API/data-source locks, revision-bound pending interactions, run references, and errors; `pendingRevisionImpact` and `activeFormalRevision` bind the current application revision without reopening initialization. | Read before changing the workspace application-initialization schema, creation gate, plan-execution lock, formal revision lease, or interaction contracts. |
| `Backend/app/domain/application_revision.py`               | Current-only Pydantic contract for five top-level revision routes, two formal branches, targets, read-only impact confirmation, active formal revision, TechnicalPlan draft metadata/actions, and one-time TechnicalPlan continuation. Workbench continuation additionally binds the planning execution that must be transferred to development. It accepts no client Graph node or historical adjustment alias. | Read with `services/revision_routing.py`, `services/application_revision_lifecycle.py`, and both planning/workflow protocol adapters before changing application revision payloads or branch semantics. |
| `Backend/app/services/request_complexity.py`            | Legacy model-backed simple/complex classifier retained outside the current main Graph. | Read only when intentionally changing the legacy complexity boundary. |
| `Backend/app/services/direct_modification.py`           | Deterministic quick-modification rules: bounded SmallTask JSON/handoff normalization, authoritative snapshot file lists, frontend/backend path ownership checks, and per-run dynamic authorization of exact existing files outside normal source roots without a file-type whitelist. Dynamic paths must remain inside the current owner and workspace and cannot enter sensitive, dependency, generated, migration, schema, or formal-artifact locations. It also owns no-diff handling, recoverable partial-change/tool-error warnings, bounded rolling summary, final user message, and test-log references. | Read with the direct Agent wrapper and Graph nodes when changing quick result validation, dynamic path authorization, final acceptance, or context limits. |
| `Backend/app/services/change_contracts.py` and `contract_evidence.py` | Read-only index and adapter for the four current confirmed JSON contract artifacts. It emits artifact key, JSON Pointer, selector, and current file SHA-256 evidence; it intentionally has no global Contract ID registry and never loads Markdown. | Read when changing contract search/read scope or evidence provenance. |
| `Backend/app/services/change_code_scan.py`                      | Bounded target-oriented source scanner used only after contract preservation. It returns relative file paths, symbols, line ranges, and nearby code without classifying a bug or writing files. | Read when changing implementation evidence scope, candidate-path safety, or code-scan limits. |
| `Backend/app/services/revision_routing.py`             | Deterministic Router for Analyzer facts and SmallTask escalation. It maps verified earliest contract stage to formal revision branches, maps preserved contracts plus code findings to implementation fixes, and falls back to clarification when evidence or ownership is insufficient. | Read with `agents/change_impact_analyzer.py`, `graph/nodes/direct_modification.py`, and revision lifecycle code when changing secondary-modification routing. |
| `Backend/app/services/direct_repair.py`                 | Deterministic free-conversation repair support: extracts exact parent-authorized frontend/backend diff paths, normalizes test owners and RepairPlanner tasks, persists internal plans, and builds scope/formal-workflow handoffs without allowing guessed paths. | Read with `graph/nodes/direct_repair.py`, `services/small_task.py`, and `agents/repair_planner/` when changing free-chat repair scope or escalation. |
| `Backend/app/services/small_task.py` and `small_task_scope.py` | Deterministic SmallTask packet construction, capped parallel scheduling, dependency/path/resource conflict checks, preflight escalation, confirmation scope application, batch snapshot attribution, unauthorized-change detection, and bounded handoff payloads. Directory-level repair authorization such as `frontend` or `backend` covers nested workspace source files consistently in both attribution and batch acceptance, so successful bounded fixes return to the explicit unit-test or integration-test node. | Read when changing repair-task execution, concurrency, scope safety, or workflow escalation. |
| `Backend/app/services/agent_file_documents.py`          | Owns the environment-scoped `~/.xcodeagent*/AGENTS.md` default template, safe creation/read/save rules, UTF-8 and 32 KiB validation, atomic replacement, and revision conflict checks. | Read with `protocols/agent_files.py` and `services/agent_memory_runtime.py` when changing AGENTS.md storage, validation, or save semantics. |
| `Backend/app/services/data_sources.py`                  | Owns the workspace-scoped `.xcodeagent/datasource` catalog, strict database/external-API validation, one-database limit, password redaction, direct-database connection checks, and API-design-only runtime resolution of decrypted direct-MySQL connection information. Persistence stores catalog/index/domain/operation files separately. External operations persist current request/response JSON Schema fragments and Path/Query parameters. API design may read direct-MySQL metadata and the latest saved Operation Schema; it never persists credentials or performs external network calls. Build resolves required credentials later by `sourceId`. | Read with `persistence/data_sources.py`, `protocols/data_sources.py`, `database_crypto.py`, `services/api_design.py`, and the frontend data-source service/page when changing the catalog or API-design source boundary. |
| `Backend/app/services/data_source_json_fields.py` | Current JSON Schema node model, complete sample-to-structure normalization, and stored-schema validation. Uses only standard type/properties/items/description keywords, merges all array elements, preserves matching number/integer declarations and field descriptions, omits unused keywords, and never reads retired field maps. | Read with `services/data_sources.py` when changing field metadata, limits, or sample consistency rules. |
| `Backend/app/persistence/data_sources.py`              | Splits the independent data-source catalog into `.xcodeagent/datasource/index.json`, database files, domain `source.json` files, and per-operation JSON files. The index keeps only list-ready summaries: databases contain `id/type/name/mode/hasPassword`, external APIs contain `id/type/name/baseUrl/directories`, directory entries contain `id/name/operations`, and operation summaries contain `id/name/method/path`; full resource files are loaded only for detail or mutation reads. It validates safe IDs and references, assembles the current public service shape on reads, writes changed files atomically, and cleans stale files after successful updates. It has no historical `data-sources.json` migration or compatibility reader. | Read with `services/data_sources.py` when changing data-source storage layout or write failure recovery. |
| `Backend/app/services/code_change_revert.py`            | Validates historical workspace change sets and safely reverts their complete text patches through Git, including repository/workspace path confinement, staged-file rejection, temporary reverse simulation, conflict preflight, and atomic application. | Read with `workspace/code_changes.py` and `protocols/code_changes.py` when changing historical change rollback behavior or safety rules. |
| `Backend/app/services/version_control.py`               | Owns deterministic Git inspection and explicit small-scope commits after a revision has been explicitly accepted. It requires the workspace to be its own repository root, filters actual dirty files to the run-associated paths, fingerprints HEAD/status/diffs, rejects stale reviews, sensitive/internal paths, unborn repositories, and pre-existing staged changes, then commits only user-selected files. | Read with `protocols/version_control.py`, `docs/APPLICATION_REVISION_WORKFLOW.md`, and the frontend version-control client/reminder before changing commit safety, fingerprints, or revision commit behavior. |
| `Backend/app/services/agent_memory_runtime.py`          | Converts the mutable environment AGENTS.md into a one-file immutable, read-only DeepAgents memory snapshot mounted at `/.xcodeagent/agent-memory/AGENTS.md`; its content revision invalidates cached Agent bundles so the next run loads saved instructions. | Read with `agents/registry.py` and `agents/workspace_scope.py` when changing AGENTS.md runtime injection, virtual mounts, or snapshot consistency. |
| `Backend/app/services/entity_definitions.py`            | Shared deterministic entity model: RequirementSpec entities remain product-stage display facts and are not consumed by TechnicalPlan; TechnicalPlan entities are generated from confirmed ProductPlan context with normalized field names and semantic types, while data source selection belongs to the entity-design stage. It derives JSON Schema/API bindings and MySQL target tables without exposing credentials; external API Build summaries project shared connection plus only the current page/Endpoint-linked operations, replacing complete samples with bounded `request_shape`/`response_shape`, preserving response semantics and mappings, and deriving `mapped_entity_path` from the mappings' common array prefix. | Read before changing entity normalization, schema derivation, table mapping, field validation, or external API build context. |
| `Backend/app/services/entity_design.py`                | Deterministic workbench entity-design service covering database, external API, and static data sources; validates bindings and rules, normalizes only the current `connection + operations[]` external API contract, persists confirmed entity artifacts, and executes approved database table operations with evidence. External API validation covers operation limits/stable IDs, per-operation unique Endpoint assignment (允许实体按 Endpoint 分批确认), URL/override pairing, placeholder/parameter/header safety, entity/non-entity response semantics, pagination references, real response paths, and per-operation required mappings. It contains no reader or migration for the removed single-API shape. | Read with `entity_design_assist.py`, `entity_source_binding.py`, and `tools/database_tools.py` when changing entity design, external API bindings, or database metadata browsing. |
| `Backend/app/services/entity_source_binding.py`       | 独立旧能力 EntitySourceBinding 的 AG-UI 投影与确认入口；无论实体是否已有 EntityDetail，实体摘要都投射当前实体关联的 `api_contract_id + endpoint_id` 清单，供旧外部 API 多操作选择器建立唯一 endpoint_refs 绑定。其结果不参与当前 Endpoint API 设计或 Build。 | Read with `entity_design.py` and `graph/nodes/planning.py` when changing the retained legacy entity-binding payload or old external API Endpoint association. |
| `Backend/app/services/entity_design_assist.py`         | Bounded AI assistance for entity table selection, field bindings, mappings, seed data, rules, relationships, acceptance, and risks; suggestions never bypass deterministic confirmation. | Read when changing entity-assist prompts or suggestion schemas. |
| `Backend/app/tools/database_tools.py`                  | Controlled read-only database metadata endpoints used by entity design. | Read when changing table/column browsing contracts. |
| `Backend/app/services/requirement_spec.py`              | Deterministic RequirementSpec builder and draft-save service. It consolidates original requests with clarification answers and normalizes page/operation business permission semantics. Each restricted-page rule carries hidden UUID `ruleId` plus required `targetPageId` referencing the same RequirementSpec page catalogue; ProductPlan must consume that stable binding rather than match display names. Rules also require non-empty `defaultGrantedRoleIds` and source references; explicit data authorization becomes `DATA_AUTHORIZATION_NOT_SUPPORTED` and blocks confirmation. The requirements analyzer first extracts and raw-validates explicit role/permission facts against the generated page catalogue before any clarification persists; `isSystemRole/isInitialAdminRole` and `initialAdminRoleId` become mandatory only at confirmation. Markdown sync protects hidden rule markers. | Read when changing RequirementSpec fields, staged permission clarification, page-target bindings, default role grants, initial-admin selection, hidden rule identity, editor saves, normalization, or product-versus-engineering boundaries. |
| `Backend/app/services/application_authorization_config.py` | Current schema v5 application-permission configuration service. It validates the database and real initial-administrator subject prerequisites, then atomically enables `auth.enable` and `authorization` in `.xcodeagent/application.json`; it is only called after the application-planning AG-UI conflict clarification is answered. | Read with `graph/nodes/requirements.py` and `agents/main/requirements_analyzer.py` when changing the authorization-configuration conflict gate or application.json permission persistence. |
| `Backend/app/services/product_plan.py`                  | Canonical `product-plan.v5` model-output validator and normalizer. It preserves RequirementSpec page identity, requires stable lower_snake_case `pageId/actionId`, owns product-visible page behavior and acceptance, deterministically maps page rules from RequirementSpec `targetPageId` to `pageRules[{ruleId,pageId}]` and operation rules to `operationRules[{ruleId,pageId,actionId}]`, rejects roles, resource/policy fields, the fixed `/roles` page and historical formats, and validates page/action/system resource-key candidates for global collisions before joint confirmation. | Read with `agents/main/product_planner.py`, `workspace/product_plan_documents.py`, and UI generation when changing ProductPlan JSON, stable identifiers, permission-target mapping, field semantics, Markdown projection, or confirmation gates. |
| `Backend/app/services/ui_design_manifest.py`            | Canonical `ui-manifest.v3` builder, persistence reducer, ProductPlan control/local-effect inspector, and deterministic UI ownership validator. It only consumes validated business ProductPlan pages, formal manifests omit duplicated product/authorization facts, and explicit UI skip remains supported. | Read with UI confirmation/generation, PageImplementationContract code, and `docs/PRODUCT_UI_TECHNICAL_PLANNING.md`. |
| `Backend/app/services/project_plan.py`, `authorization_manifest.py`, `authorization_deliverability.py` | Own the compact current TechnicalPlan, deterministic `authorization-manifest.v2`, and the Step 4E authorization deliverability gate. They validate Resource/Page/Action/Endpoint references, Action→Endpoint→Resource closure, Controller-Task ANY-OF guard deliverability, mixed control, Initial Admin system-resource grant, fingerprint, and PageImplementationContract projection before TechnicalPlan confirmation; only failures are surfaced through the existing technical-plan generation-error flow, and no second permissions fact is persisted. | Read when changing TechnicalPlan, API/schema/entity contracts, resource manifests, stack constraints, page references, or the TechnicalPlan confirmation gate. |
| `Backend/app/services/authorization_resource_catalog.py` | T2.5A canonical full resource catalog compiled from confirmed manifest source data, including system/page/operation resources. Owns immutable `ResourceCatalog`, existing frontend constant naming/collision rules, and a 64-character SHA-256 over resource key/type/target identity and constant references. Rejects duplicate resources; ignores routes, descriptions, grants, Tasks and generated-file formatting. | `compile_frontend_resource_catalog(manifest)` and `resource_catalog_fingerprint(catalog)` are shared by the resources-only frontend projection and `unit_generation_requirement_targets.py`, which retains the TechnicalPlan confirmation gate. Tests: `tests/test_authorization_resource_catalog.py` and R-AUTH. `resources.ts` write ownership belongs only to the deterministic auth-guard Task. |
| `Backend/app/services/page_implementation_contract.py`, `technical_action_binding_repair.py` | Own ProductPlan-derived business Action/sequence-step expectations, structured missing/incomplete binding issues, strict Action Binding Patch validation, and an idempotent merge that can modify only targeted `pages[].references.action_implementations`. The scoped repair accepts only existing Endpoint IDs; dependency closure remains owned by `page_dependencies.py`, and the full validator remains authoritative after merge. | Read with `agents/main/planner.py` and `graph/nodes/planning.py` when changing Action→Endpoint validation, scoped repair routing, Patch shape, or PageImplementationContract compilation. |
| `Backend/app/services/authorization_overlay.py`, `authorization_frontend_projection.py`, `authorization_constants_projection.py`, `authorization_bootstrap.py`, `authorization_platform_projection.py`, `authorization_edd.py` | Compile confirmed `authorization-manifest.v2` into independent frontend resources/routes projections, page/action/Endpoint slices, and deduplicated non-system Endpoint `AuthConstants`. The combined frontend compile shape remains in Build Plans, while auth Build pre-projection writes only the managed regions of `frontend/src/constants/routes.tsx` and the fixed backend `AuthConstants.java` region; `frontend/src/constants/resources.ts` is reserved for the deterministic auth-guard Task. Split read-only verification still composes into complete EDD. Missing markers, route/resource drift, branch mismatch, or constant drift blocks Build. | Read with `graph/nodes/tasks.py`, `graph/nodes/authorization_bootstrap.py`, `graph/subgraphs/build.py`, `services/deterministic_unit_candidates.py`, `services/build_unit_compiler.py`, and `services/build_repair_planner.py` when changing permission Bootstrap, overlays, projection ownership, repair inheritance, or EDD authority. |
| `Backend/app/graph/application_planning_workflow.py`    | New-application planning Graph: normal creation runs `requirements -> product_planning -> ui_confirmation -> planning_stage_entry -> technical_planning`; RequirementSpec and ProductPlan use the joint `requirement_document_review`, UI confirmation or skip always stops at the entry interrupt, and only `enter_planning` starts TechnicalPlan. Product-stage free input reuses the same Graph/thread through `design_intent_analysis`; only Policy-approved requirement/product-behavior/UI changes enter formal nodes, while chat/read-only/clarification/out-of-scope replies return to the current review gate without artifact or lifecycle mutation. | Read with `docs/PRODUCT_UI_TECHNICAL_PLANNING.md`, `docs/DESIGN_CONVERSATION_AGENT.md`, `agents/design_conversation/models.py`, `agents/design_conversation/router.py`, `agents/design_conversation/fallback.py`, `agents/design_conversation/policy.py`, `services/application_planning_persistence.py`, and the Welcome creation components. |
| `Backend/app/graph/application_planning_revision.py` | Focused design-revision adapter for the original application-planning Graph: maps Coordinator semantics through a three-pair Product Policy whitelist, treats an approved formal revision lifecycle target as authoritative over classification, keeps non-mutating intents isolated, and limits completed-application product input to a Coordinator reply until the existing formal revision impact flow is used. It also propagates the original request, freezes pre-run artifact presence, prepares incremental UI actions, and controls lifecycle rewind plus TechnicalPlan invalidation. | Read with `application_planning_workflow.py` and `protocols/application_page_planning.py` when changing bottom-input, completed-application product conversation, or TechnicalPlan confirmation revisions; do not create a second Graph or thread. |
| `Backend/app/services/ui_design_generation_pool.py`     | Process-global decoupled worker pool (方案 B) that generates page design TSX outside the LangGraph sync path. `ui_confirmation` only enqueues per-page `regenerate`/`select_template` intents; N workers (`Settings.ui_design_concurrency`, default 3) run the LLM generation in threads, persist `.xcodeagent/ui-design/pages/<PageKey>/index.tsx`, and merge `queued → generating → confirmed/generation_failed` statuses into `.xcodeagent/specs/ui-designs.json`. Application deletion fences new tasks, drops queued work, and waits active thread calls to reach the no-late-write cancellation boundary. Frontend progress arrives by polling a no-op AG-UI resume that re-reads the manifest. | Read with `graph/nodes/ui_confirmation.py`, `services/ui_design_generator.py`, `services/ui_design_manifest.py`, `workspace/spec_documents.py`, and `protocols/application_deletion.py` when changing decoupled generation, concurrency, dedup, recovery, deletion, or page status semantics. |
| `Backend/app/services/application_planning_persistence.py` | Atomically validates the current RequirementSpec/ProductPlan/UiManifest/TechnicalPlan artifacts during creation, accepting confirmed or explicitly skipped UI design, then projects menus, APIs, schemas, entities and data sources for workbench/template generation. | Read when changing the new-application persisted schema, storage path, deferred application fields, menu projection, or TechnicalPlan-to-application mapping. |
| `Backend/app/services/frontend_page_tree.py`            | Shared helpers for `ProjectPlan.frontend_pages`: identifies menu nodes vs page leaves, flattens leaf pages for execution and validation, preserves or rebuilds menu hierarchy during normalization, and provides stable menu/page lookup utilities across workflow, persistence, and workbench projections. Route normalization propagates parent menu paths to descendants, prevents menu-enabled homepage-like pages from collapsing to the bare root route, avoids auto-creating parent menus when a module has only one visible page plus dynamic/detail routes, and demotes menu routes that equal a direct page route into route-less grouping nodes. | Read when changing `frontend_pages` storage shape, menu-node rules, leaf-page execution semantics, or tree rebuild/update behavior. |
| `Backend/app/services/application_lifecycle.py`             | Owns the strict application-initialization state machine and separately persisted workbench executions. It enforces formal artifact gates, explicit template retry, typed revision/unit-test/performance/test/review/code-review interactions, lifecycle revision/CAS, and server-owned execution/resource locks. Pending DAG confirmation is mandatory closeout: ordinary stop/end or session navigation cannot consume it; Confirm/Abandon/Regenerate use the exact DraftIdentity. | Read with `domain/application_lifecycle.py`, `services/application_revision_lifecycle.py`, `services/planning_refresh_recovery.py`, planning protocols, and `protocols/workflow/lifecycle.py` when changing initialization, revision recovery, Planning locks, confirmation, or acceptance. |
| `Backend/app/services/application_template_generation.py`, `frontend_scaffold.py` | Own the post-TechnicalPlan template boundary. The manifest derives `templateVariant` only from matching frontend/backend download branches: main retains page-placeholder and `BIZ_MENUS` initialization; auth validates resources/routes markers, Build platform owns route/AuthConstants projection, and the deterministic auth-guard Task exclusively owns resources registration. Template failure remains recoverable only through the explicit template-stage retry path; mixed branches fail closed. Deletion fencing prevents late template/UI writes. | Read with `services/application_lifecycle.py`, lifecycle/deletion protocols, `Frontend/src/main/index.ts`, and `services/ui_design_generator.py` when changing template generation, manifest schema, readiness, retry, or deletion safety. |
| `Backend/app/services/application_revision_lifecycle.py`, `revision_routing.py`, `revision_drafts.py` | Own the deterministic application-revision boundary: register/approve impact, retain one active formal revision, issue/consume a TechnicalPlan-bound continuation, let independent `application_planning` continuations start a fresh development execution (while preserving optional main-Workflow execution handoff), route formal changes, and keep a single isolated Markdown/JSON draft whose confirmation atomically replaces current canonical files after lifecycle and upstream-hash checks. | Read with `graph/nodes/application_revision.py`, `protocols/workflow/revision.py`, `protocols/workflow/lifecycle.py`, and `workspace/revision_draft_documents.py` before changing revision routing, draft confirmation, continuation, execution handoff, or cleanup. |
| `Backend/app/graph/nodes/application_revision.py` | Main Workflow executor for current workbench formal revisions. It generates or revises one TechnicalPlan draft, synchronizes user-edited Markdown into JSON, validates current upstreams and page implementation contracts, then returns to `api_design_readiness_gate` before the existing Build DAG path. It does not generate page-detail artifacts or Endpoint API-design artifacts; changed Endpoint contracts become stale through fingerprint checks. | Read with `graph/workflow.py`, `services/revision_drafts.py`, `services/api_design.py`, and `agents/main/planner.py` when changing formal revision execution. |
| `Backend/app/protocols/application_lifecycle.py`             | Dedicated `/application-lifecycle/run` AG-UI adapter for lifecycle create/get, explicit template-stage begin/retry, structured template preparation, and completion actions. `get` attaches a non-persisted `extensions.planningRefresh` resolved from the unique PendingPlan, authoritative abandon marker, live process registry, PlanningRun projection, and ConfirmedPlan; it never advances persistent lifecycle revision or revives confirmation from chat history. | Read with `services/application_lifecycle.py`, `services/planning_refresh_recovery.py`, `services/application_template_generation.py`, and frontend lifecycle store when changing lifecycle actions, Planning refresh priority, events, or cold-start calibration. |
| `Backend/app/protocols/application_deletion.py` | Dedicated `/application-deletion/run` AG-UI destruction boundary plus Electron-only `/application-deletion/prepare` and `/application-deletion/complete` handshake. It validates the managed workspace/application identity, fences and cancels every workspace run, UI-design/template worker and registered command process, stops all preview variants, removes target checkpoint rows, closes local SQLite, evicts target Graph caches, releases workspace locks, and returns `readyForTrash` only after no late writer remains. The path fences remain active until Electron has moved the project to trash and `complete` verifies both the bound `applicationId` and that the path no longer exists, after which the same path can host a new application. It deliberately preserves environment-wide credentials/settings, user skills/agent files, external databases, remote traces, and Git remotes. | Read with `protocols/workflow/run_control.py`, `services/workspace_process_registry.py`, checkpoint/Graph cache owners, Electron application deletion, and `docs/APPLICATION_LIFECYCLE.md` before changing full-app destruction or its preservation boundary. |
| `Backend/app/protocols/ag_ui_action_stream.py`          | Shared AG-UI lifecycle wrapper for deterministic product actions. Owns run/message start and finish events, custom events, state snapshots, optional progress-to-text projection, streamed assistant text deltas, structured success envelopes, and structured error envelopes; product adapters retain their own action dispatch and payload schemas.                                                                                                                       | Read when adding or changing a simple workflow-independent AG-UI product action. Do not force the streaming LangGraph workflow through this helper.                                                                 |
| `Backend/app/services/workspace_process_registry.py` | Workspace-keyed subprocess runner used by product flows that execute Git, package-manager, build, test, inspection, Bootstrap, and command-tool processes. Each command owns a process group; `managed_process` additionally binds temporary startup checks to a run, fences cancelled runs and waits for graceful/forced cleanup without touching other preview processes. Workflow run cancellation signals only that run’s registered processes. Application deletion fences new commands, sends a graceful termination, then force-kills and verifies remaining PIDs. | Use instead of bare `subprocess.run` for any new command that operates on an application workspace and may overlap deletion. |
| `Backend/app/protocols/application_page_planning.py`    | AG-UI adapter exposing the design stages, explicit `planning_stage_entry`, TechnicalPlan stage, design-change intent entry, controlled `product_stage_conversation`/`start_design_revision`/`start_technical_revision`, non-confirming `requirementSpecDraft.save`, and read-only checkpoint recovery. Recovery and Graph writers share `application_planning_run_lock.py` by thread, so the checkpoint plus lifecycle snapshot is read only after an in-flight writer releases the barrier; a missing checkpoint has a stable action error code. Completed-application product conversation validates `ready_for_workbench`, server-selects the original planning thread, and opens a Coordinator-only reply round; design revision validates the lifecycle-bound impact before restoring that thread. Direct TechnicalPlan revision clears stale paths/derived state without overwriting the checkpoint baseline, and confirmation can issue the one-time main-Workflow continuation. | Read when changing new-application planning transport, completed-application product boundaries, TechnicalPlan baseline revision, design revision handoff, cold-start checkpoint recovery, editable artifact save/confirmation contracts, or capability metadata. |
| `Backend/app/protocols/code_changes.py`                 | Workflow-independent AG-UI adapter for confirmed historical code-change rollback at `/code-changes/run`, exposing `code-changes` custom events and `codeChangesAction` state snapshots. | Read with `services/code_change_revert.py` and frontend `service/codeChanges.ts` when changing rollback payloads, lifecycle, or errors. |
| `Backend/app/protocols/version_control.py`              | Workflow-independent `/version-control/run` AG-UI adapter for Git `inspect` and user-confirmed `commit` actions, exposing `version-control` custom events and `versionControl` state snapshots without putting commit logic into the main Workflow. | Read with `services/version_control.py` and frontend `service/versionControl.ts` when changing action payloads, lifecycle, or commit errors. |
| `Backend/app/protocols/direct_modification.py` and `direct_modification_projection.py` | Dedicated `/conversation/run` AG-UI runtime plus bounded projection. They validate `forwardedProps.conversation`, including the optional DOM `elementContext`, safely resolve `/src/` preview locations to real frontend workspace files, run the isolated `conversation:` checkpoint thread, acquire a workspace lease only for write intent, project node/tool/test progress, preserve handoff confirmation and DOM context across confirmation, and turn formal changes into lifecycle-bound revision impact confirmation rather than exposing a Graph node. | Read with the conversation Graph, `services/revision_routing.py`, and frontend `service/agUiAgent.ts` before changing request, progress, cancellation, handoff, or result contracts. |
| `Backend/app/services/page_dependencies.py`             | Canonical ProjectPlan/TechnicalPlan page dependency normalization and validation. It deterministically closes known Endpoint selections from `action_implementations` and business `stepBindings` into `endpoint_dependencies` without masking unknown Endpoint IDs, and owns unique leaf-page id/path checks plus the immutable `frontend_pages[].references` projection consumed by runtime PageImplementationContract and Build context compilation; menu nodes are preserved but never treated as executable pages. It derives data-source task context from endpoint IDs. | Read when changing page dependency schema, action-to-endpoint closure, menu-vs-page validation rules, endpoint-bound task context, route/id validation, or ProjectPlan-to-runtime-contract ownership. |
| `Backend/app/services/database_crypto.py`               | Owns the environment-scoped persistent RSA-3072 platform key, strict 0700/0600 atomic storage, public-key health metadata, versioned RSA-OAEP-SHA256 ciphertext parsing, and password decryption. | Read before changing database credential algorithms, key location/version/rotation, secret envelope format, startup behavior, or public metadata. Never project private-key material. |
| `Backend/app/services/database_credentials.py`          | Reads one workspace's `.xcodeagent/application.json`, maps `datasource.db.plantMode` to a strong `MySQLConnectionConfig`, decrypts versioned passwords, validates connection fields, builds the shared Spring JDBC URL, and read-compatibly accepts legacy plaintext without rewriting files. | Read with `database_crypto.py`, `tools/mysql_info.py`, and `backend_project_launcher.py` when changing application-level MySQL configuration. Keep plaintext out of errors, logs, Graph State, and tool inputs. |
| `Backend/app/services/database_requirement_schema.py` and `database_schema_diff.py` | Deterministic database requirement and gap compiler retained for entity-design and dedicated database execution flows. It compiles confirmed structured operations into target tables/columns, compares them with actual MySQL metadata, ignores extra actual objects, and emits bounded differences without interpreting prose. | Read when changing entity-to-table compilation, missing database/table/column/type/nullability/default handling, or dedicated database execution verification. |
| `Backend/app/services/database_schema_summary.py`       | Shared MySQL schema-summary helper used by entity design, database tools, and the Database Agent: requires a workspace root, calls `get_mysql_table_info_for_workspace`, truncates schema output, and normalizes target/connection-failure summaries. | Read when changing common MySQL inspection, workspace-bound application credential resolution, summary truncation, or table/column/index/foreign-key projection. |
| `Backend/app/services/database_execution.py`            | Controlled database execution helper for database owner tasks: fingerprints latest schema and SQL plans, classifies high-risk SQL/task intent, creates or consumes `database.execute` approvals, resolves the current workspace's application-level MySQL credentials, creates a missing target database when executing approved create-database work, and records MySQL DDL evidence without promising full rollback. | Read when changing database high-risk policy, application-level connection resolution, approval fingerprints, SQL execution evidence, rollback planning, or the future AG-UI database-connection input flow. |
| `Backend/app/services/backend_project_launcher.py`      | Graph-independent Java backend launch service: `launch_backend_project(workspace_path)` accepts only a normal `str | Path` workspace, derives `.xcodeagent/runtime/launch/`, resolves the current application's database environment while removing inherited global database variables, serializes each workspace launch, stops the previously managed Java process before Maven, runs `mvn clean install`, selects one non-auxiliary `*-SNAPSHOT.jar`, automatically runs `mvn -B package spring-boot:repackage` when that JAR lacks `Main-Class`, starts and registers `java -jar`, and accepts readiness only while the process lives and current-launch stdout/stderr contains `Spring Boot Version` or `ZA21 Version`. Cleanup and synchronous command failures become structured results instead of escaping the Workflow. | Read when changing the public backend launch contract, application-level runtime environment injection, prebuild cleanup ordering/evidence, Maven/JAR discovery, Java startup, readiness markers, logs, timeout behavior, or rollback. |
| `Backend/app/services/backend_process_registry.py`      | Workspace-scoped Java preview process registry and lock manager. Owns in-memory `Popen` registration, `backend.pid` recovery after Backend restart, current-workspace `java -jar` command identity validation, graceful/forced cross-platform termination, stale PID cleanup, and fail-closed cleanup evidence. | Read when changing backend preview process ownership, PID persistence/recovery, process identity checks, concurrent launches, termination timeouts, or Windows/POSIX stop behavior. |
| `Backend/app/services/frontend_project_launcher.py`     | Graph-independent generated-project frontend launch/stop service: `launch_frontend_project(workspace_path)` accepts only a normal `str | Path` workspace, derives `.xcodeagent/runtime/launch/`, finds frontend `package.json`, resolves full npm/pnpm/yarn executable paths for Windows `.cmd` support, installs dependencies, applies framework-compatible host rules, reuses a prior ready frontend service, and supervises a background dev server; `stop_frontend_project(workspace_path)` stops the recorded frontend dev-server process. Synchronous install/stop `OSError` values become structured failures. Explicit script ports are honored; missing ports resolve to `http://127.0.0.1:80`. | Read when changing the public frontend launch/stop contract, cross-platform command resolution, package-manager detection, dev-script selection, preview URL inference, process/readiness handling, frontend preview shutdown, logs, or acceptance metadata. |
| `Backend/app/services/project_launcher.py`              | Thin generated-project preview facade that preserves stable imports for Workflow nodes and FastAPI routes. It re-exports backend/frontend launch helpers and combines `stop_frontend_project` plus `stop_workspace_backend_project` into `stop_project_preview(workspace_path)`. | Read when changing the preview facade, combined `/api/projects/stop` behavior, or backwards-compatible launcher imports. |
| `Backend/app/services/api_contracts.py`                  | Canonical compact API-contract normalization, schema references, endpoint dependencies, and page response-field bindings. API contracts reference TechnicalPlan entities through non-empty `entity_ids`; physical data fields resolve only from confirmed per-Endpoint API designs. | Read when changing schemas, endpoint ids, entity semantic references, parameter/request/response references, or exact page-to-endpoint field mapping. |
| `Backend/app/services/api_contract_validation.py`       | Deterministic pre-build validation that rejects contracts with empty or unknown `entity_ids`, unresolved schema/endpoint references, invalid backend endpoint shapes, and page fields absent from API responses. Data-source bindings are not part of API Contract validation.                                                                                                                                                         | Read when changing contract consistency rules or the code-generation safety gate.                                                                                                                                   |
| `Backend/app/services/build_task_planner.py`, `engineering_acceptance.py`, and `business_acceptance.py` | Compile workspace-aware model candidates into `build-dag.v3`, including structural task normalization, cross-platform file scopes, dependencies, owner semantics, execution strategy, execution batches, deterministic engineering `acceptance_checks`, deliverables, and formal-source-bound business checks. Structural normalization defaults legacy Tasks to `execution_strategy=agent`, preserves `platform_executor`, rejects unsupported strategies or non-allowlisted deterministic executors in DAG validation, and includes both execution fields in exact-duplicate identity so Agent and deterministic work cannot be merged. It also covers field aliases, path/operation defaults, `target_files` plus `allowed_paths` fallback, Unit compilation, and fingerprints; it must not rewrite semantic task boundaries. After business-check compilation, `build_task_planner.py` enforces one normal frontend implementation owner per stable `api_contract_id + endpoint_id`; a permission-bound endpoint requires a `backend.endpoint_controller` deliverable only when its existing backend endpoint Unit belongs to the current Build scope—permissions never create a separate Unit. Conflicts stay in `task_graph.validation.errors`, while repair tasks inherit their parent ownership rather than creating a second owner. Model-returned acceptance text and verification commands are discarded. Engineering checks cover file/scope, frontend page structure, platform-sliced page Action permissions, and platform-sliced Endpoint permissions: a target Controller handler located by exact HTTP method/path must carry exactly one `RequireAnyResource` containing precisely the supplied `AuthConstants` ANY-OF symbols. `business_acceptance.py` compiles the nine deterministic business kinds with stable formal-artefact hashes and aggregates sibling deliverables of the same kind into one bounded target-path set, so composite Entity/PO/DTO/Converter or Repository/Mapper implementations are verified together rather than per file. Deliverable ownership, path, Unit, source, and verifier errors remain in `task_graph.validation.errors` for bounded regeneration. `reconcile_live_page_paths` only maps a non-existing candidate page path to one already-existing unique semantic directory without changing its declared operation; Unit metadata and cross-Unit dependency compilation live in `build_unit_compiler.py`, which also rewrites every page-scoped backend Unit to its own Endpoint target and filters external API operations to that exact Endpoint before task compilation. | Read when changing task ids, structural normalization, file scopes, owners, execution strategy/executor fields, endpoint implementation ownership, deliverables, engineering/business check compilation, DAG validation, or automatic regeneration. Read `engineering_acceptance_verifier.py` and `business_acceptance_verifier.py` with scheduler changes, `build_task_menu.py` for read-only page-path reconciliation, and `build_unit_compiler.py` for Unit dependencies and fingerprints. |
| `Backend/app/services/planning_issues.py` | Deeply frozen ValidationIssue with strict retry-target validation, detached JSON serialization, stable first-wins deduplication, and explicit retry-Unit grouping. Identity uses structured fields; message/details never determine routing. ID sequences are tuples and nested details are read-only; copy updates revalidate. | Read with `planning_frozen.py` and `Backend/tests/test_planning_issues.py`. No existing validator, retry, PlanningRun or Scheduler integration. |
| `Backend/app/services/planning_run.py`, `planning_run_contracts.py` | T5.1 pure in-memory PlanningRun/UnitRunState state machine. Explicit phase, Unit, Candidate and expected-Attempt guards return frozen snapshots with revision+1 and caller-supplied timestamps. Local=3, Global=2; Global reopen consumes a complete T4.1 GlobalRepairDecision, rejects blocked/empty decisions and raw Issue subsets, and atomically supersedes only its total targets. Deterministic Candidates spend no model budget. Fatal/cancel revoke unfinished identities. PlanningRunProjection shares lightweight invariants without Candidate bodies; full PlanningRun additionally validates Candidate references. | Read with `unit_generation_contracts.py`, `planning_issues.py`, `global_issue_attribution.py`, `Backend/tests/test_planning_run.py`, `test_planning_run_transitions.py`, and `test_global_repair_controller.py`. Callers keep the returned snapshot as current authority; no production caller, IO, model invocation, Scheduler, progress emit, PendingPlan write or confirmation integration. Run R-PLAN. |
| `Backend/app/workspace/planning_run_documents.py` | T5.3 lightweight `.xcodeagent/plans/planning-run.json` persistence for refresh projection. Atomic write, load and Controller publication use the same strict PlanningRunProjection schema without changing the JSON shape. All root/Unit state fields must be present; wrong types, unknown fields, inconsistent scope/status/Attempt identity and Candidate bodies are rejected. Missing files return None; invalid JSON/schema is reported without rewriting evidence. | Read with `services/planning_run_contracts.py`, `services/planning_run_controller.py`, `workspace/json_documents.py`, and `Backend/tests/test_planning_run_projection.py`. Loaded mappings are display/recovery evidence only and cannot resume Scheduler execution. Workers must not invoke the writer. |
| `Backend/app/services/planning_refresh_recovery.py` | T11.4/T11.5 read-only Planning/Confirmation refresh resolver. The Backend half resolves Pending → authoritative Abandon tombstone → process-active PlanningRun → ConfirmedPlan and exposes a Pending's page-conversation `ownerSessionId`; frontend treats that projection as authoritative and does not restore historical confirmation/generation output after Pending disappears. It treats a disk-active Run without a matching process registry entry as `planning_run_interrupted`, rejects same-run Pending/planning residues after Formal promotion, and keeps an abandoned Draft suppressed across reload and late progress. It emits only a compact `dag-generation.v1` snapshot and the existing safe confirmation read model; Candidate bodies remain private and no Scheduler is resumed. | Read with `services/application_lifecycle.py`, `workspace/planning_run_documents.py`, `workspace/task_documents.py`, `protocols/application_lifecycle.py`, workflow `run_control.py`, frontend `stageOutputState.ts`, `Backend/tests/test_planning_refresh_recovery.py`, and `test_build_task_plan_abandon.py` when changing refresh priority, Pending session ownership, Abandon recovery, restart interruption, or promotion-residue handling. |
| `Backend/app/services/planning_run_controller.py`, `planning_run_events.py` | T5.4/T9.4/T9.5 single-writer Controller with frozen, allowlisted internal events and `await apply(event)`. GlobalRepairStarted carries the complete T4.1 decision unchanged, never a naked Issue sequence. An async lock covers T5.1 transition/revision, strict T5.3 atomic persistence, optional readonly projection publication, Attempt result acceptance, and idempotent `cancel(at=...)`. Persistence failure preserves the committed state/revision; publication failure reports an already committed snapshot. Cancellation drains in-flight IO/publication before unlocking; stale or duplicate result events are no-write no-ops. | Read with `planning_run.py`, `workspace/planning_run_documents.py`, `Backend/tests/test_planning_run_controller.py`, `test_planning_run_controller_cancellation.py`, `test_attempt_result_acceptance.py`, and `test_global_repair_controller.py`. Orchestration must retain one Controller per active Run, submit the complete T4.1 result, and give Workers only Jobs/event submission. No frontend or new AG-UI payload. Run R-PERSIST and R-PROGRESS. |
| `Backend/app/services/global_issue_attribution.py`, `global_planning_validation.py` | T4.1 Global Issue attribution plus T6.2 pure Candidate completeness. The completeness API requires every model/deterministic Unit to be round-terminal, ignores reuse/prerequisite/structural Units, and returns frozen ready/missing IDs with one attributable Global generation Issue per exhausted Unit. Existing ownership/provenance validation still blocks malformed or absent Candidate facts before issue attribution. | Read `docs/GLOBAL_ISSUE_ATTRIBUTION.md`, `planning_issues.py`, `planning_run_contracts.py`, `build_task_reuse_contracts.py`, `unit_generation_contracts.py`, `Backend/tests/test_global_issue_attribution.py`, `test_global_planning_validation.py`, and `test_candidate_completeness_barrier.py`. No full DAG Validator, Assembly, Candidate mutation, Global repair execution, model call or round consumption. Run R-PLAN. |
| `Backend/app/services/scope_assembly.py` | T4.2/T4.3 pure append-only Scope Assembly. It deep-copies every confirmed Task, requires ReuseFacts to match the complete formal registry, accepts exactly one current valid CandidateAttempt for each Unit with non-empty generation requirements, rejects retained/Candidate and Candidate/Candidate ID collisions before registry construction, compiles acceptance only for Candidate Tasks, and rebuilds cumulative Unit/task DAG metadata without replacement, auto-rename, or exact-duplicate merging. The returned assembled draft has compile `status` only and explicitly removes inherited `confirmation_status` / `confirmed_at`; only a later Global-success Controller/persistence boundary may create a formal PendingPlan. For an auth-protected Page, Assembly forwards the complete `ReuseFacts.external_capabilities` snapshot and enables exact current `frontend.auth.resources:<R>` dependency compilation instead of inheriting every historical auth-guard Task. | Read with `build_task_planner.py`, `build_unit_compiler.py`, `authorization_capability_dependency.py`, `build_task_reuse_contracts.py`, `unit_generation_contracts.py`, `Backend/tests/test_scope_assembly.py`, and `Backend/tests/test_global_planning_integration.py`. It does not select Candidates, retry, persist PendingPlan, or change Build execution. Run R-AUTH, R-PLAN, and R-ACCEPT. |
| `Backend/app/services/authorization_capability_dependency.py` | T4.3 pure authorization capability helper. It derives the current full-catalog `frontend.auth.resources:<R>`, indexes explicit Task/deliverable providers in `frontend:auth-guard`, recognizes only an exact matching external capability, returns the unique dependency provider (or no Task edge for external satisfaction), and emits structured Global issues for missing or duplicate providers. | Read with `scope_assembly.py`, `build_unit_compiler.py`, `authorization_resource_catalog.py`, and `Backend/tests/test_scope_assembly.py`. It does not inspect the workspace, execute the auth writer, change Scheduler behavior, or generalize other cross-Unit dependency rules. |
| `Backend/app/services/build_task_reuse.py`, `build_task_reuse_contracts.py` | Pure, frozen ReuseFacts from confirmed DAG, current Unit skeleton, full formal Endpoint catalog, and explicit workspace evidence. Retains pending/failed/completed Task identities equally; maps exact capability IDs to provider Tasks without treating bare Unit IDs as full reuse; checks case-sensitive formal Endpoint ownership and emits non-retryable platform issues for baseline conflicts. Template readiness can prove external `frontend.shell.ready`. T2.5B prioritizes current auth R providers, then accepts exact workspace resource evidence via `auth_resource_inspection`; old R Tasks remain retained. | Read `docs/BUILD_TASK_REUSE.md`, `Backend/tests/test_build_task_reuse.py`, `test_build_task_reuse_workspace.py`, and `test_build_task_reuse_auth.py`. Consumed by the independent generation-requirements service; T2.4 connects only `resolve_template_prerequisite_facts` to the existing Planning entry. Candidate generation, Assembly, and Scheduler remain separate. See `docs/FRONTEND_SHELL_PREREQUISITE.md`. |
| `Backend/app/services/authorization_resource_inspection.py` | T2.5B read-only `inspect_authorization_resource_catalog(workspace, catalog, workspace_revision=...)` compares the entire resources.ts byte content with the existing deterministic renderer, without routes, TypeScript execution or writes. `verified_auth_resource_capability` binds satisfied evidence to current canonical R, snapshot revision, exact path and projection/content hashes; stale evidence cannot become external capability. | Read with `authorization_resource_catalog.py`, `authorization_frontend_projection.py`, `build_task_reuse.py`, `docs/BUILD_TASK_REUSE.md`, and `tests/test_build_task_reuse_auth.py`. Run R-AUTH and R-PLAN; no writer, Build execution or Page dependency changes. |
| `Backend/app/services/unit_generation_requirements.py`, `unit_generation_requirement_targets.py`, `unit_generation_requirements_contracts.py` | Computes frozen per-Unit missing responsibilities, generation strategies and planning IDs from required Units, exact Scope, confirmed formal targets and ReuseFacts. Structural/shell Units never generate; shared API client and bootstrap retain existing duties while adding missing ones; auth-guard uses a SHA-256 of the full existing resource projection for deterministic requirements. Empty model requirements never enter planning; errors carry non-retryable T1.1 issues. | Read `docs/UNIT_GENERATION_REQUIREMENTS.md`, `Backend/tests/test_unit_generation_requirements.py`, and `test_unit_generation_requirements_auth.py`. New Candidates must preserve the declared capability identities. No model call, Candidate generation, Retry, Scheduler, Assembly, file IO, or old Planning-flow switch. |
| `Backend/app/services/deterministic_unit_candidates.py` | T2.5C pure `build_auth_guard_candidate` consumes the T2.3 missing-requirements result plus the current ResourceCatalog/fingerprint. Returns no candidate when satisfied; otherwise builds one frontend Task with a full-fingerprint ID, deterministic `authorization.frontend_resources` executor, exact capability and resources.ts-only paths. Validates source refs and never calls a model, allocates an Attempt, enters a Worker, writes files or changes execution counters. | Read `docs/DETERMINISTIC_AUTH_CANDIDATE.md`, `platform_task_executors/authorization_frontend_resources.py`, and `Backend/tests/test_deterministic_unit_candidates.py`. Output is a tasks payload, not a validated CandidateAttempt; Task normalization preserves and fail-closed validates the execution contract, and Build dispatch resolves the executor through the explicit registry. Run R-AUTH/R-PLAN. |
| `Backend/app/services/platform_task_executors/` | Explicit deterministic Build Task registry plus the T8.3 `authorization.frontend_resources` executor. Registry keys must exactly match the domain allowlist. The executor accepts one exact auth-guard Task plus `workspace` and a confirmed `formal_plan`, recompiles the canonical ResourceCatalog, rejects stale fingerprints and any non-resources target before IO, uses the split resources compiler/writer, then verifies the complete generated file. Identical output returns `already_satisfied`; all contract/compile/write/validation failures are terminal categories. | Read with `graph/subgraphs/build.py`, `deterministic_unit_candidates.py`, `authorization_resource_catalog.py`, and `authorization_frontend_projection.py`. Tests: `tests/test_authorization_frontend_resources_executor.py`, `tests/test_build_subgraph_scheduler.py`, and the R1/R2 Planning-to-execution regression in `tests/test_auth_deterministic_execution_integration.py`. It is registered in Build dispatch, reuses normal Task lifecycle, never falls back to an owner Agent, and never writes routes.tsx. Run R-AUTH and R-BUILD. |
| `Backend/app/services/unit_generation_contracts.py` | Frozen GenerationRequirement, Context, Policy, AttemptIdentity, UnitAttemptJob, CandidateAttempt and AttemptResult DTOs. `UnitGenerationContext.contract_catalog` contains only sorted Frozen Store refs/kinds/selectors and no inline formal body. Platform allocates Attempt identity before dispatch; Job validates Run/Unit against Context; Candidate/Result carry one nested identity. Attempt/Candidate/Task IDs are separate. Local=3 and SDK retry=0 are fixed; the explicit contract-read policy requires read count, accumulated bytes, and per-read bytes. | Read with `frozen_contract_catalog.py`, `frozen_contract_reader_contracts.py`, `planning_issues.py`, `planning_frozen.py`, `Backend/tests/test_unit_generation_contracts.py` and `test_unit_attempt_identity.py`. No Context builder, model call, Validator, Scheduler or persistence; active-Run/expected-attempt discard checks remain Controller work. |
| `Backend/app/services/unit_candidate_parser.py` | T3.1 strict parser for the single-Unit model envelope `{"tasks": [...]}`. It requires complete standard JSON, the exact top-level key, object tasks, and unique non-empty string Task IDs; failures carry retryable Unit-scoped `ValidationIssue` values and never return partial tasks. | Read with `planning_issues.py` and `Backend/tests/test_unit_candidate_parser.py`. It deliberately does not call legacy candidate extraction/normalization, validate owners or business fields, invoke a model, retry, assemble, or run global validation. |
| `Backend/app/services/unit_candidate_validator.py`, `unit_candidate_task_rules.py`, `unit_candidate_platform_rules.py` | Unit Local Validation entry point and pure rule slices. `validate_unit_candidate(context, candidate_tasks, reuse_facts=None)` first fails malformed frozen Context/ReuseFacts as non-retryable input/platform Issues, then strictly validates raw Task schema and identities, exact owner/Unit, exact requested capability coverage and per-requirement deliverable identity, target/change/deliverable scope, managed/platform files, strong rules, same-Unit dependencies/cycles, and retained Endpoint ownership. T3.2 dependency validation remains available independently. | Read with `unit_candidate_parser.py`, `unit_generation_contracts.py`, `build_task_reuse_contracts.py`, `Backend/tests/test_unit_candidate_validator.py`, and `test_unit_candidate_dependencies.py`. Model content Issues retry only the current Unit; input/platform failures never do. The validator never mutates or normalizes Candidate Tasks, executes retry, compiles cross-Unit dependencies, assembles a Scope, inspects other Candidates, or validates the global DAG. |
| `Backend/app/agents/main/unit_task_prompt.py`, `unit_task_rules.py`, `backend_unit_task_rules.py` | T3.3/T10.4 pure single-Unit prompt construction over a frozen `UnitGenerationContext`. The rule resolver deterministically projects the legacy Scope planner's Page, shared API client, static data, backend bootstrap, database endpoint, and external-API endpoint algorithms onto only the current Unit; it also renders the exact Local requirement/deliverable, owner, task-type, path, ID, and dependency contract, while optional caller rules are additive only. The prompt binds retained dependency allowlists, metadata-only contract catalogs, the sole allowed `read_frozen_contract_fragment` protocol, strict `{"tasks": [...]}` output, and replacement/platform-work prohibitions. | Read with `task_preparer_prompt.py`, `unit_generation_contracts.py`, `unit_generation_tool_session.py`, and `Backend/tests/test_unit_task_prompt.py`. Unknown model Units fail before invocation rather than receiving empty rules. These modules do not inline contract bodies, call a model or Reader, retry, validate a Candidate, assemble a Scope, or perform Global validation. |
| `Backend/app/services/unit_generation.py`, `unit_generation_tool_session.py` | T3.4/T10.4 async single-attempt Unit generation session. One identity-bound `UnitAttemptJob` may use bounded Model → Frozen Reader → Model turns while still producing exactly one unpromoted Attempt result. Each Attempt gets an isolated Reader/message history and only the catalog-bound read tool; model-turn/read-count/read-byte exhaustion and malformed tool/final output are Unit content failures. Frozen Store/catalog corruption is a platform fatal; model setup/transport/session timeout remains infrastructure fatal. | Read with `unit_generation_contracts.py`, `frozen_contract_reader.py`, `unit_task_prompt.py`, `unit_candidate_parser.py`, `Backend/tests/test_unit_generation.py`, and `test_unit_generation_tool_session.py`. The services do not schedule Local/Global retry, run Local/Global validation, create Candidate status, expose workspace tools, assemble a Scope, or persist state. |
| `Backend/app/services/unit_generation_orchestrator.py` | T6.1/T9.2/T10.4 shared Local semantics. It allocates the next identity-bound `UnitAttemptJob`, executes and commits exactly one Local Attempt even when its internal Tool Session has multiple model turns, returns pending after retryable content failure, or reaches candidate_ready/round_exhausted. Frozen-source platform fatal and model infrastructure fatal both terminate the Run without Local/Global content retry. The Barrier refuses to advance while any planning Unit is non-terminal, then commits `GlobalCheckStarted` and returns Candidate completeness. | Read with `unit_generation.py`, `unit_generation_scheduler.py`, `unit_candidate_validator.py`, `global_planning_validation.py`, `planning_run_controller.py`, `Backend/tests/test_unit_generation_orchestrator.py`, and `test_candidate_completeness_barrier.py`. It does not own the worker pool, perform Global repair/assembly, persist PendingPlan, or switch the LangGraph production entry. |
| `Backend/app/services/unit_generation_scheduler.py` | T9.2/T9.3/T9.5/T10.4 bounded FIFO `UnitAttemptJob` Queue, per-round Worker Pool, infrastructure/platform fatal propagation, and Workflow cancellation cleanup. `UnitGenerationScheduler.run_round` caps active model workers at three, queues pending model Units without DAG dependency gating, appends Local retry Jobs at the tail, and runs deterministic Units outside model workers. Fatal and cancellation both stop dispatch and drain queued Jobs. Workflow cancellation persists `PlanningRun.cancelled` before best-effort active-worker cancellation, rejects late results through T9.4, stops Local requeue/Barrier/Global progression, and re-raises `CancelledError`; normal rounds return only after the complete Barrier advances to global_check. | Read with `unit_generation_orchestrator.py`, `planning_run_controller.py`, `global_repair_orchestrator.py`, `Backend/tests/test_unit_generation_scheduler.py`, `test_unit_generation_scheduler_fatal.py`, `test_unit_generation_scheduler_cancellation.py`, `test_unit_generation_tool_session.py`, and `test_dag_planning_orchestrator.py`. No persistence-authority, Pending Abandon, or frontend Cancel UI change. |
| `Backend/app/services/global_repair_orchestrator.py` | T6.3/T9.2 `run_global_repair_loop` drives Candidate completeness, injected full Global checks and affected-only batch regeneration. Complete attributed decisions pass unchanged through `GlobalRepairStarted`; each batch consumes one of G=2, all affected Candidates are superseded before one `regenerate_round` call, and the Unit Scheduler may execute its model targets concurrently. Blockers/limit exhaustion preserve complete diagnostics. | Read with `unit_generation_scheduler.py`, `planning_run.py`, `planning_run_controller.py`, `global_issue_attribution.py::aggregate_global_repair_decision`, `docs/GLOBAL_ISSUE_ATTRIBUTION.md`, and `Backend/tests/test_global_repair_orchestrator.py`. One caller per Run; Global success returns an empty decision at active/global_check. Infrastructure fatal aborts in the Scheduler before another Global check. No Scope Assembly body, Pending persistence or production Planning switch. Run R-PLAN. |
| `Backend/app/services/dag_planning_orchestrator.py`, `dag_planning_inputs.py` | T6.4/T9.2-T9.5/T10.2/T10.4 `plan_dag_sequential` returns a frozen `ValidatedAssembledPlan` from actual Requirements → bounded model Queue/deterministic Units → Local → Barrier → append-only Assembly → Global → affected-only repair. Formal inputs freeze once into the Run Store；each Unit independently compiles an expected binding manifest and receives only the exactly matching metadata catalog before dispatch. `assemble_mainline_planning_inputs` compiles that internal binding manifest from Workflow-supplied formal inputs so the Graph adapter does not construct Scheduler state. The default generation adapter binds the same in-memory Store to a new Attempt-scoped Reader；custom injected generators remain supported. Infrastructure or source-binding/Reader platform fatal stops dispatch and bypasses Local retry, Global and Pending. | Read `frozen_contract_store.py`, `frozen_contract_catalog.py`, `frozen_contract_manifest.py`, `unit_generation_tool_session.py`, `docs/SEQUENTIAL_DAG_PLANNING.md`, `Backend/tests/test_unit_contract_catalog.py`, `test_unit_generation_tool_session.py`, `test_dag_planning_orchestrator.py`, and Scheduler cancellation/fatal tests. The orchestrator persists only lightweight `planning-run.json`; Store content remains in-memory only. Production Graph 已经通过 async adapter 调用该编排器，`tasks.py` 不再是默认生成入口。Mandatory Gate: R-PLAN, R-MODEL. |
| `Backend/app/services/build_task_planning_service.py` | T11.6.2 production-facing async Planning business facade: accepts frozen authoritative `MainlinePlanningInputs` carrying the page-conversation owner, allocates the backend-owned PlanningRun ID, delegates all Unit scheduling/retry/Assembly/Global authority to `plan_dag_sequential`, and only after validated success writes one PendingPlan whose DraftIdentity includes `owner_session_id`. It returns the validated result, self-checked PendingPlan, and DraftIdentity; failures and `CancelledError` propagate without Pending writes. | Read with `dag_planning_inputs.py`, `dag_planning_orchestrator.py`, `workspace/task_documents.py`, `Backend/tests/test_build_task_planning_service.py`, and `test_async_workflow_planning_adapter.py`. Production Graph adapter 已以该服务作为唯一首次 Planning 业务边界；Confirm/Abandon/Regenerate 已接入独立 lifecycle，Regenerate 创建新 PlanningRun 但继承原页面对话 owner。 |
| `Backend/app/services/frozen_contract_store.py` | PlanningRun-scoped in-memory store for explicit ProductPlan, TechnicalPlan, PageImplementationContract, API Contract, confirmed Endpoint API Design, and authorization slices. It recursively copies and freezes content plus structured source provenance, derives stable run-scoped `ref_id` values, rejects missing groups and duplicate source identities, and resolves exact kind/source pairs without reading workspace files or accepting Candidate buckets. EntitySourceBinding is not a store kind in the current production adapter. | Read with `graph/nodes/task_planning_inputs.py`, `planning_frozen.py`, `frozen_contract_catalog.py`, `dag_planning_inputs.py`, and `Backend/tests/test_frozen_contract_store.py`. The Store is not serialized into `planning-run.json`. |
| `Backend/app/services/frozen_contract_catalog.py`, `frozen_contract_manifest.py`, `frozen_contract_manifest_index.py` | Frozen DTOs, deterministic expected-binding compiler and `build_unit_contract_catalog`. The compiler derives requirement-level ProductPlan/TechnicalPlan/Page/API/Endpoint-API-Design/authorization grants from the current Scope and already-frozen Store; the builder compares expected and declared requirement/kind/ref/selector atoms exactly, then merges selectors by Store ref into a stable metadata-only allowlist. | Read with `frozen_contract_store.py`, `task_planning_inputs.py`, `dag_planning_inputs.py`, `unit_generation_contracts.py`, and `Backend/tests/test_unit_contract_catalog.py`. Missing, extra, wrong-kind, unknown, or cross-target bindings fail before model dispatch and Local retry. |
| `Backend/app/services/frozen_contract_reader.py`, `frozen_contract_reader_contracts.py` | T10.3/T10.4 Attempt/session-scoped `FrozenContractReader`, `read_frozen_contract_fragment`, the sole LangChain tool factory, and frozen input/output/policy/error DTOs. Exact Unit catalog refs/selectors resolve only inside the bound Frozen Store, return canonical JSON text with opaque cursor paging, and enforce caller-supplied read-count, accumulated-byte, and UTF-8-safe per-read-byte budgets (minimum 4 bytes). Structured failures cover unauthorized refs, invalid/unknown selectors, invalid cursors, malformed inputs, and exhausted budgets. | Read with `frozen_contract_store.py`, `frozen_contract_catalog.py`, `unit_generation_contracts.py`, `unit_generation_tool_session.py`, `Backend/tests/test_frozen_contract_reader.py`, and `test_unit_generation_tool_session.py`. The tool accepts no workspace path/file argument, performs no IO, and is bound to a fresh Reader for each Attempt. Run R-PLAN and R-MODEL. |
| `Backend/app/services/build_task_menu.py` | Narrow template-boundary helper that only reconciles a page candidate to an already-existing unique live page directory. It does not parse menu registrations or create, inject, remove, prune, or rewrite menu, route, hidden-route, or page-placeholder tasks, and path reconciliation preserves the candidate's explicit `add`/`modify` operation. | Read when changing live page-path evidence; do not treat it as a DAG task-generation or menu-acceptance owner. |
| `docs/FRONTEND_SHELL_PREREQUISITE.md` | T2.4 shell contract: verified template evidence produces `frontend.shell.ready`; no shell planning/Candidate or completed-task reuse; invalid existing formal DAG blocks new Planning while missing formal DAG allows first planning. | Read with `services/build_task_reuse.py`, `build_unit_skeleton.py`, `build_unit_compiler.py`, `build_task_planner.py`, `graph/nodes/tasks.py`, `agents/main/task_preparer_prompt.py`, and `tests/test_frontend_shell_prerequisite.py`. Existing template initialization and other Unit assembly policies remain unchanged. |
| `Backend/app/services/build_task_progress.py` | Owns the fixed seven-stage `prepare_build_tasks` progress snapshot (`unit_skeleton`, `build_context`, `contract_validation`, `model_planning`, `task_compilation`, `dag_validation`, `artifact_persistence`). It emits complete bounded `prepare_build_tasks.progress` custom-stream snapshots and projects safe task/validation/artifact details. Generation completion exposes PendingPlan status；only post-confirm projection may expose Formal as confirmed. | Read when changing DAG progress events, `dag_generation`/`agent-process` projection, stage output limits, task confirmation cards, or session re-entry history. |
| `Backend/app/services/planning_run_progress.py`, `build_task_progress.py` | T11.1/T11.2 pure `PlanningRunProjection` to `dag-generation.v1` projector plus the `prepare_build_tasks.progress` custom-stream bridge. Every committed PlanningRun transition emits one complete snapshot with a strictly increasing revision；Candidate bodies/identities, Issue `taskIds`, and Issue details remain private, and stream failures cannot mutate committed PlanningRun state. The async Planning adapter is the production path；the seven-stage projection remains a compatibility-facing UI shape rather than generation authority. | Read with `planning_run_contracts.py`, `planning_run_controller.py`, `dag_planning_orchestrator.py`, `protocols/workflow/runtime.py`, and `Backend/tests/test_build_task_progress.py`. Run R-PROGRESS and R-WORKFLOW. |
| `Backend/app/services/build_task_confirmation.py` | Builds the read-only Build DAG confirmation model from the current PendingPlan without mutating Formal：target identity, page acceptance provenance, conditional directly related Endpoint summaries, current-scope task instructions/paths/business acceptance without internal engineering checks, minimal reused prerequisite chains, and status-only summaries for unrelated retained tasks. | Read with `graph/nodes/task_planning_adapter.py`, `build_context_resolver.py`, frontend Workflow typings, and the confirmation card before changing confirmation content or current-versus-retained task visibility. |
| `Backend/app/services/build_unit_skeleton.py`           | Deterministically creates/reuses the v3 application/backend/frontend/page Unit graph from TechnicalPlan and WorkspaceSnapshot. `build_context_resolver.py` derives required backend/bootstrap/static responsibilities from the current confirmed Endpoint API Designs and source types; EntitySourceBinding is not a normal DAG input. Menu, route, hidden-route, and page-placeholder capabilities are template-owned. `frontend:shell` is prerequisite-only; its architecture edge never creates a Task dependency. | Read with `build_context_resolver.py`, Endpoint design services, `build_task_reuse.py`, and `tests/test_frontend_shell_prerequisite.py` when changing Unit hierarchy, endpoint-to-page dependencies, bootstrap prerequisites, or reuse policy. |
| `Backend/app/services/build_context_resolver.py`        | Resolves page or endpoint targets into bounded context using runtime PageImplementationContract, TechnicalPlan Endpoint semantics, API contracts, and current confirmed Endpoint designs. It projects field bindings, business descriptions, and confirmation-time source snapshots; it does not read global entity bindings. | Read when changing target validation, implementation-contract slicing, Endpoint-design projection, source snapshot scoping, or Unit subsets. |
| `Backend/app/services/execution_resource_scope.py`      | Resolves a server-authoritative page/application/data-source execution scope into stable page, API-contract, and data-source resource claims from the confirmed ProjectPlan. | Read with lifecycle persistence and run leases when changing cross-page locking, dependency concurrency, or repair-scope expansion. |
| `Backend/app/services/build_scheduler.py`, `engineering_acceptance_verifier.py`, `business_acceptance_verifier.py`, and `engineering_contract_verifier.py` | Deterministic BuildScheduler and acceptance harness for execution slicing, task-result normalization, actual diff attribution, cross-platform path matching, and the nine registered business verifiers. Normal Build execution records engineering acceptance as skipped and does not run `engineering_acceptance`/`acceptance_checks`; those compiled checks remain plan/audit and repair-context metadata. Owner reports are never accepted as business evidence: when the optional business self-check is enabled, Build rereads bounded target files and formal hashes for business verification; definite implementation mismatches may enter Repair, while `business_acceptance_blocked` is terminal for the current DAG and never asks a code Agent to repair verifier/formal-source uncertainty. | Read when changing dispatch, target-file attribution, engineering/business evidence, formal-source freshness, verifier registration, nested DTO references, serialization naming, database verification, repair classification, or terminal states. |
| `Backend/app/services/build_repair_planner.py`          | Scheduler-bounded RepairPlanner contract service: builds `RepairPlannerInput` with engineering and business evidence, accepts an exact parent-authorized repair `change_scope`, recompiles attempt-specific engineering checks, inherits the parent business checks and expected values exactly, handles `requires_user_confirmation` / `terminal_failure`, appends repairs to the runtime Build DAG, and closes parents after verified written or `already_satisfied` repairs. | Read when changing build failure repair inputs, structured evidence, repair task boundaries, acceptance inheritance, formal-hash freshness, repair depth budget, repair-plan persistence shape, or how repair tasks close failed parents. |
| `Backend/app/services/business_acceptance_verifiers/`    | Bounded, read-only tree-sitter TypeScript/TSX and Java/XML AST verifiers for the nine Phase 2 business-check kinds. `typescript_ast.py`/`typescript_ast_types.py` extract exports, generic client calls, import aliases, literals, and recursive type shapes; `java_ast.py` plus `java_inspection_support.py` bind Spring mappings and layer calls to concrete types/methods and read MyBatis XML structurally. External API verification checks the exact upstream method/path and HTTP call declaration without enforcing a client technology, plus the Base URL configuration key, declared Path/Query parameters, canonical root/nested array mappings, response payload-path access, and field mappings. `backend_domain.py` and `backend_domain_support.py` keep Entity/PO/DTO fields isolated per layer, verify API-Schema DTO fields, table/column bindings, and typed MapStruct/JavaBean conversion edges in the endpoint-required direction. They read only declared deliverable paths, cap file size/count, return unsupported grammar as `blocked` with a reason code, and do not run shells, browsers, network calls, semantic Agents, or runtime integration tests. | Read when changing deterministic business-check implementation, AST extraction, structural matching, target-file limits, or cross-platform workspace reads. |
| `Backend/app/services/workspace_inspector.py`           | Deterministic, cacheable workspace snapshot service for the `inspect_workspace` and direct `scan_workspace_code` nodes: computes workspace revision, lists safe files, detects stack/entrypoints/routes/components/contracts and common frontend/backend config files, and delegates explicit-workspace code graph indexing. Snapshot schema `1.2.0` includes the expanded deterministic frontend/Spring entrypoint set. | Read when changing workspace snapshot schema, cache invalidation, safe source/config inventory, or code graph progress integration. |
| `Backend/app/services/code_graph/`                     | Embedded `code-review-graph` adapter, per-workspace cache manager, bounded execution-Agent context resolver, and stable models. Stores `graph.sqlite3` plus `index.json` under the user workspace `.xcodeagent/cache/code-graph/v1`; `index.json` persists bounded node/edge distributions, language and warning metadata, while the graph remains the query fact source. Scan nodes build or refresh the index but do not create request-scoped navigation state; only Frontend and DataSource execution Agents query it through the workspace-bound `code_graph_context` tool. Never auto-discover the XCodeAgent repository or expose MCP/CRG tool-server APIs. | Read `manager.py`, `adapter.py`, `context.py`, and `tools/code_graph_context.py` when changing full/incremental indexing, cache summaries, 30-second fallback, SQLite isolation, execution-Agent query bounds/fallback, packaging, or workspace security. |
| `Backend/app/services/build_result_coordinator.py`      | Parses per-task specialist `task_results`, performs one deterministic repair for clearly unescaped quotes inside JSON strings (including fallback extraction of an inner evidence object), classifies unrecoverable malformed reports as `invalid_structured_response`, preserves completion/failure/satisfaction evidence plus independent engineering/business `acceptance_status`, and applies verified results back to ProjectPlan and BuildTaskPlan. Its opt-in strict schema rejects extra fields, duplicate/unknown/missing task IDs, missing satisfaction evidence, incomplete failures, and invalid change-request placement; DataSource enables strict mode while other specialist protocols retain their current parsing behavior. | Read when changing the specialist final-report schema, strict validation policy, compatibility/recovery behavior, acceptance status, build result structure, task status updates, or plan consistency checks. |
| `Backend/app/services/integration_test_runner.py`       | Deterministic fail-fast real-command integration test runner: discovers frontend package scripts and Maven projects, runs package-manager install and frontend build first, and stops later commands on the first blocking failure; code review may inject verified `pnpm_install_frontend` evidence so Build does not repeat the install. A frontend failure records backend build/test as not-executed skips and routes directly toward quality-gate repair instead of starting Maven. Test-stage callers opt into a required temporary `backend_startup` check after successful Maven build; failed prerequisites and non-Maven/Static projects receive explicit skips. When frontend checks pass, it runs the split Maven production build (`-Dmaven.test.skip=true`) plus conditional `test`; Jest/Surefire run only when affected source has corresponding tests, and parseable passed/total counts are projected as structured UI evidence. Logs live under `.xcodeagent/runtime/tests/`; Python projects and pytest remain out of scope. | Read when changing actual integration test execution order, verified install evidence reuse, fail-fast behavior, package script discovery, test-count parsing, command timeouts, skipped-check semantics, log references, or failure categories. |
| `Backend/app/services/backend_startup_check.py`, `backend_startup_diagnostics.py`, `backend_launch_support.py` | Test-only opt-in `backend_startup` check immediately after backend build. Shared launcher support supplies current database environment, SNAPSHOT JAR selection and optional repackage. The check owns a run-bound temporary Java process on loopback/port zero; requires initialized Started logs, a reachable HTTP port and three seconds of stability within 60 seconds, then verifies cleanup. Per-attempt logs, redacted root causes and virtual log paths survive repair retries; source fingerprints invalidate checks after edits while waiting for performance confirmation. Diagnostics also creates the SmallTask `backendStartupFailure` packet independent of overall context truncation. | Read with `graph/subgraphs/testing.py`, `services/test_validation.py`, `services/small_task.py`, `services/workspace_process_registry.py` and `tests/test_backend_startup_check.py`, `tests/test_backend_startup_integration.py` when changing startup readiness, cleanup, evidence or backend repair routing. |
| `Backend/app/services/frontend_performance_runner.py`   | Advisory Lighthouse performance runner: detects the user `frontend` package, starts it through `frontend_project_launcher` with `skip_install=True`, resolves the real `preview_url`, runs `@lhci/cli@0.7.2` via `npx`, saves HTML/JSON reports under `.xcodeagent/runtime/tests/frontend_performance/`, parses Lighthouse scores/metrics, and stops only servers it started itself. Results are `blocking=False` and never enter repair planning. | Read when changing performance test discovery, LHCI command/config, report parsing, URL resolution, server reuse/stop semantics, or advisory result fields. |
| `Backend/app/services/test_validation.py`               | Deterministic testing and quality-gate rules plus repair-task plan creation. Only `blocking` failures (default) affect `passed`/`needs_revision`/revision requests; advisory performance checks are counted in the report summary but never block or repair.                                                                                                                                       | Read when changing test result evaluation, advisory semantics, revision requests, or failure-to-repair mapping.                                                                                                      |
| `Backend/app/deprecated/services_requirement_intake.py` | Deprecated, fully commented simple/complex intake rules for the older AG-UI/orchestrator flow. Workflow routing now starts in `services/request_complexity.py`.                                                                                                                                                                                                                                    | Do not import from main flow.                                                                                                                                                                                       |
| `Backend/app/services/task_scheduler.py`                | Deterministic serial/parallel batching and assignment: executable tasks with exact targets reach their bounded owner runner, while shared/public/overlapping paths serialize. | Read when changing task isolation, execution modes, target conflicts, or scheduling rules. |
| `Backend/app/services/llm_client.py`                    | Provider-neutral model response contract backed by OpenAI-compatible Chat Completions, including request/response conversion and tools.                                                                                                                                                                                                                                                            | Read when changing provider selection, request/response conversion, tools, auth, or base URL behavior.                                                                                                              |
| `Backend/app/services/builtin_skills.py`                | Resolves built-in skill data in source and PyInstaller-frozen layouts, validates required files (including progressively disclosed references), reports availability, exposes sanitized read-only catalog summaries, and defines the stable read-only Deep Agent virtual source. Backend database and external API generation share `springboot-backend-generate`; frontend static data remains a separate module. | Read when adding/wiring built-in skills, changing packaged paths, catalog metadata, reference routing, or startup validation.                                                                                                           |
| `Backend/app/services/user_skills.py`                   | Owns user-skill catalog discovery and shared PyYAML-based metadata parsing. Resolves the environment-specific root (`~/.xcodeagent_dev/skills`, `~/.xcodeagent_st/skills`, `~/.xcodeagent_uat/skills`, or `~/.xcodeagent/skills`), scans direct-child directories only, merges persisted enablement, ignores symlinks, and returns sorted summaries plus non-fatal issues without exposing absolute host paths. | Read when changing environment mapping, catalog discovery depth, YAML/frontmatter fields, enablement merging, sorting, skipped-entry diagnostics, or public path labels; document editing and ZIP import reuse this parser. |
| `Backend/app/services/user_skill_settings.py`           | Persists versioned per-environment user-skill enablement in `skill-settings.json` as a sorted disabled-path set. Missing files default to all enabled; malformed or unreadable files fail closed. Writes use a process lock, temporary file, fsync, and atomic replacement. | Read when changing enablement defaults, storage schema/path, failure policy, atomicity, or stale-setting cleanup.                                                                                                    |
| `Backend/app/services/user_skill_documents.py`          | Owns complete `SKILL.md` persistence for the catalog editor: safely creates direct-child skill directories, reads documents, atomically saves with file-mode preservation, and deletes the containing skill directory. Enforces canonical relative paths, snake_case names on create, duplicate/symlink rejection, UTF-8 and 512 KiB limits, valid frontmatter, SHA-256 revisions, and optimistic concurrency. | Read when changing create/get/save/delete request models, editor validation, path confinement, duplicate handling, revision conflicts, atomic replacement, or deletion behavior.                                      |
| `Backend/app/services/user_skill_imports.py`            | Owns deterministic user-skill ZIP import from Base64 AG-UI input. Validates archive/path/file types, entry and expanded-size limits, exactly one root or single-wrapper `SKILL.md`, shared YAML metadata, duplicate skill conflicts, and atomically renames a fully validated staging directory into the environment-specific skills root without overwriting existing data. | Read when changing ZIP request/result models, archive safety limits, accepted package layout, import-name rules, conflict behavior, staging cleanup, or atomic installation semantics.                                 |
| `Backend/app/services/user_skill_runtime.py`            | Converts only enabled entries from the mutable user catalog into bounded immutable runtime snapshots for Deep Agents. Explicit selections validate an exact whitelist, copy only selected directories to the read-only `/.xcodeagent/user-skills/` mount, and reject disabled, unavailable, or oversized sets atomically; empty selection preserves enabled-catalog on-demand discovery. | Read when changing user-skill enablement/selection, forced prompt injection, snapshot limits and consistency, supporting-resource rules, virtual mounts, bundle-cache invalidation, or runtime diagnostics. |
| `Backend/app/tools/ask_user.py`                         | Generic ask-user tool for direct models and Agents that need human-in-the-loop clarification. Normalizes Gemini-style question payloads, automatically adds “其他” to selectable questions, and extracts ask-user tool calls from model or agent messages. It does not carry RequirementSpec business facts. | Read when changing user-question payloads, “其他” supplemental-answer behavior, HITL clarification behavior, or which model boundaries can ask users questions.                                                        |
| `Backend/app/tools/delete_file.py`                      | DeepAgent `delete_file` tool for deleting one regular file inside the selected `workspaceRoot` using virtual absolute paths, while refusing traversal, symlinks, directories, sensitive files, and paths that repeat the real host workspace root.                                                                                                                                                 | Read when changing workspace-confined deletion behavior or which writable DeepAgents can delete files.                                                                                                              |
| `Backend/app/tools/mysql_info.py`                       | MySQL metadata/config/DDL tools. The workspace-bound `create_get_mysql_table_info_tool(workspace_root)`, `create_get_mysql_config_tool(workspace_root)`, and `create_execute_mysql_ddl_tool(workspace_root)` resolve the current application's encrypted `plantMode` credentials while keeping model-visible arguments limited to table names or SQL statements; unbound compatibility entries fail closed. | Read with `services/database_credentials.py`, database Agent construction, data-source generation, and schema-summary callers. Preserve `mysql_table_info`/config/DDL result JSON and do not add credentials/workspace paths to model-visible arguments. |
| `Backend/app/tools/page_selection.py`                   | Controlled page-selection payload helper for detail confirmation.                                                                                                                                                                                                                                                                                                                                  | Read when changing page selection UX payloads or selected-page defaults.                                                                                                                                            |
| `Backend/app/deprecated/protocols_ag_ui.py`             | Deprecated, fully commented AG-UI adapter for old agent-mode routing, approval events, and workspace code-change events.                                                                                                                                                                                                                                                                           | Do not import from main flow. Use `protocols/workflow/` for active AG-UI workflow events.                                                                                                                            |
| `Backend/app/protocols/workflow/`                       | Self-contained AG-UI protocol package for `/workflow/run`. It normalizes resume/debug/control actions and projects stable lifecycle/HITL boundaries including PlanningRun progress and the read-only PendingPlan gate. Confirm/Abandon/Regenerate are exact DraftIdentity actions; Abandon preserves chat/Formal, Regenerate transfers the awaiting execution to a new run, and ordinary stop/end cannot consume a Pending confirmation. Active cancellation waits for the registered server task to exit and returns `cancelled`, `not_running`, or `cancel_timeout`; successful runs emit `RUN_FINISHED`, while unhandled failures emit their structured snapshot then `RUN_ERROR`. A reused durable execution `runId` is rejected before lifecycle creation with `DURABLE_EXECUTION_RUN_ID_CONFLICT` and does not start Graph execution. | Read `request.py`, `runtime.py`, `lifecycle.py`, `run_control.py`, `projection.py`, and `definition.py` together when changing resume, Planning cancellation, confirmation, stop/end, repair, acceptance, or terminal events. |
| `Backend/app/protocols/workflow/request.py`            | Normalizes AG-UI workflow recovery inputs, maps renderer `forwardedProps.sessionId` to the Graph's PendingPlan `owner_session_id`, and maps `build_task_plan_confirmation={action:confirm|regenerate}`, `test_phase_confirmation={action:confirm}`, and `review_phase_confirmation={action:confirm}` to their exact resume nodes. Regenerate forwards only the server-issued old Pending identity and resumes `prepare_build_tasks` rather than interpreting user text. Normal page/Endpoint development and completed planning revisions enter `api_design_readiness_gate`; the `continue_revision_build` page/Endpoint compatibility branch still resumes the legacy `development_readiness_gate`, while application scope enters workspace inspection directly. Failed-execution retry preserves the registered snapshot scope and can resume readiness, workspace inspection, DAG preparation, or Build without letting the current UI selection retarget the run. Review handoff validates the existing execution scope/target while allowing an atomic transfer to the new review thread；the resume payload preserves bounded code-review state and current workspace artifacts. Empty public `buildSummary` projections are ignored, and unit-test/confirmation debug resumes discard client `tasks`, `buildResults`, and `buildSummary`；persisted DAG tasks are initialized only at the Build entry so downstream resumes cannot overwrite server-checkpoint Build execution facts with PendingPlan tasks. Unsupported actions are rejected or discarded according to the current contract. | Read with `protocols/workflow/projection.py` when changing page-session ownership, formal-revision continuation, DAG/test/review confirmation actions, resume-node precedence, cross-thread lifecycle recovery, camelCase/snake_case state aliases, Build-summary projection, or public Workflow request validation. |
| `Backend/app/protocols/user_skills.py`                  | Workflow-independent AG-UI action adapter for `/skills/run`. Dispatches user catalog/document/import actions plus `set-enabled`, returns user and sanitized built-in summaries from `list`, exposes the `skill-catalog` custom event and `skillCatalog` snapshot/result key, and preserves the common run/message/success/error SSE lifecycle. | Read when changing skill action routing, capabilities, AG-UI event/state names, request validation, assistant messages, enablement, or catalog/document/import results. |
| `Backend/app/protocols/data_sources.py`                 | Workflow-independent AG-UI action adapter for the data-source catalog and API-design metadata endpoints. `/data-sources/list` returns index summaries, `/data-sources/detail` loads an external API operation configuration, and `/data-sources/{database-tables,database-columns,external-operation}` returns only the requested API-design metadata. All actions use the `data-sources` custom event and `dataSources` snapshot/result key; none enters `/workflow/run`. | Read with `routes/data_sources.py`, `services/data_sources.py`, `services/api_design.py`, and frontend `service/dataSources.ts` when changing the standalone catalog or API-design source boundary. |
| `Backend/app/routes/data_sources.py`                    | Dedicated FastAPI router for the independent data-source catalog and API-design metadata. Registers separate POST AG-UI SSE endpoints for list/detail/create/update/delete/validate plus database table/column and external Operation metadata, and prevents clients from overriding the action selected by the URL. | Read with `protocols/data_sources.py` and `services/data_sources.py` when changing data-source HTTP routing. |
| `Backend/app/routes/endpoint_designs.py`                | Dedicated FastAPI router for the independent `/endpoint-designs/run` AG-UI `get/prepare/save` flow. Keeps Endpoint mapping editing outside the main LangGraph while delegating lifecycle encoding to `protocols/endpoint_designs.py`. | Read with `protocols/endpoint_designs.py` and `services/endpoint_design_detail.py` when changing Endpoint mapping reads or edits. |
| `Backend/app/workspace/workspace.py`                    | Sandboxed workspace/file/search/terminal/Git operations, sensitive-file checks, approval gates, and code-change payloads.                                                                                                                                                                                                                                                                          | Read when changing workspace management or agent-facing workspace tool contracts.                                                                                                                                   |
| `Backend/app/workspace/code_changes.py`                 | Workspace snapshot diff capture for DeepAgent file changes, producing frontend-compatible `WorkspaceCodeChangeSet` payloads with workspace-root name, normalized relative file paths, and code-only changed diff lines while skipping ignored dirs, sensitive files, symlinks, and binary text leakage.                                                                                              | Read when changing workflow code-change capture, workspace path metadata, diff truncation, or AG-UI `codeChanges` output.                                                                                          |
| `Backend/app/workspace/run_lease.py`                    | Process-local activity registry used by `/workflow/run` to track and clean up active Graph runs without rejecting overlapping workspaces, pages, or resource claims.                                                                                                                                                                                                                                | Read when changing workflow concurrency, active-run cleanup, or reintroducing an explicit concurrency policy.                                                                                                      |
| `Backend/app/workspace/virtual_paths.py`                | Shared virtual-workspace path instructions plus helpers that recognize host `workspaceRoot` aliases accidentally supplied as DeepAgent virtual paths.                                                                                                                                                                                                                                              | Read when changing the model-visible `/` root contract or repeated-host-path safeguards.                                                                                                                            |
| `Backend/app/workspace/spec_documents.py`               | Writes product-facing RequirementSpec Markdown/internal JSON, including display-only entity information while omitting technical data-source and engineering-acceptance sections; detects Markdown edits before confirmation. | Read when changing RequirementSpec persistence, entity rendering, synchronization, or artifact roots. |
| `Backend/app/workspace/plan_documents.py`               | Writes current TechnicalPlan Markdown/JSON and synchronizes developer edits; renders three-part architecture, API/schema/entity semantics, and page references. Legacy EntitySourceBinding Markdown rendering remains for the independent old entry; current Endpoint API-design Markdown belongs to `endpoint_design_documents.py`. | Read with `product_plan_documents.py` and `endpoint_design_documents.py` when changing formal planning persistence. |
| `Backend/app/workspace/detail_design_documents.py`      | Persists only EntitySourceBinding JSON/Markdown under `.xcodeagent/plans/entities/` and stores `source_binding` references in TechnicalPlan. No page/API artifact is written or hydrated. External API bindings use the normalized `api_info`/`response_handling`/`field_mappings` contract. This retained legacy artifact is not read into the current DAG Planning context. | Read when changing legacy entity-binding artifact names, references, hydration, or external API contract projection. |
| `Backend/app/services/build_task_plan_lifecycle.py`, `Backend/app/services/dag_planning_regeneration.py`, `Backend/app/workspace/task_documents.py` | Own distinct Formal `.xcodeagent/plans/build-task-plan.json` and Pending `.xcodeagent/plans/build-task-plan.pending.json` paths plus frozen `DraftIdentity`. The Pending writer binds backend metadata, rejects caller identity/failed validation, verifies canonical SHA-256 excluding only its own digest, and writes atomically without touching Formal. Confirm checks exact request identity, self digest, current Formal baseline, fresh inputs, Scope and DAG gate, then atomically replaces Formal before deleting matching Pending. Abandon records the terminal marker, deletes only an exact Pending, ends the matching Workflow execution and preserves chat history/Formal. Regenerate is fully integrated：it commits old-Pending deletion, allocates a new Run ID, reloads current Formal and writes only a new Pending on success；later failures never restore the old Pending. The target concurrency contract allows only one active PlanningRun or awaiting-confirmation PendingPlan per application, and cancellation remains Workflow/PlanningRun-level rather than Unit-level. | Read `docs/BUILD_TASK_PLAN_CONFIRM_PROMOTION.md`, `docs/DAG_TASK_GENERATION_UNIT_CANDIDATE_PLAN.md`, `Backend/tests/test_build_task_plan_lifecycle.py`, `test_build_task_plan_regenerate.py`, `test_dag_confirm_authority_cutover.py`, and `test_pending_build_task_plan_documents.py` before changing promotion, identity, persistence, Regenerate, application-level exclusivity, or refresh/cancellation behavior. |
| `Backend/app/workspace/workspace_snapshot_documents.py` | Helpers for reading workspace snapshot JSON and resolving the `.xcodeagent/cache` artifact root used by `inspect_workspace`.                                                                                                                                                                                                                                                                        | Read when changing workspace snapshot persistence paths or cache loading.                                                                                                                                           |
| `Backend/app/workspace/test_documents.py`               | Atomically writes internal quality-gate data to `.xcodeagent/reports/test-report.json` and renders the current user-facing `.xcodeagent/reports/test-report.md`. The Markdown always summarizes frontend/backend Build checks, conditionally includes bounded Lighthouse scores/metrics, and strips full logs, HTML bodies, and host absolute paths. | Read with Testing Subgraph and workflow projection when changing test report persistence, rendering, sanitization, or artifact paths. |
| `Backend/app/workspace/code_review_documents.py`        | Renders and atomically overwrites `.xcodeagent/reports/code-review.md` from the normalized scan snapshot. It publishes only front/back scan roots and counts, warnings, and bounded issue locations; scan inventories, repair actions, logs, and host absolute paths are excluded. | Read with the Code Review Subgraph and workflow projection when changing the review report contract or persistence path. |
| `Backend/app/middleware/approvals.py`                   | Approval grants, reusable operation rules, and protected-operation fingerprints.                                                                                                                                                                                                                                                                                                                   | Read when changing approval lifecycle, scopes, or risk gating.                                                                                                                                                      |
| `Backend/app/domain/execution_recovery.py`              | Defines the independent Durable Execution, persistent ExecutionLease, RecoveryPoint records, and P0.3A RecoveryPlan/decision/strategy contracts. Each durable execution `runId` is a single-attempt identity; reuse raises the structured `DURABLE_EXECUTION_RUN_ID_CONFLICT` error. It is not part of ApplicationLifecycle or LangGraph state. | Read when changing durable execution/lease observation fields, status mapping, checkpoint identity, recovery decisions, or replay-safety semantics. |
| `Backend/app/persistence/run_store.py`                  | Writes `.xcodeagent/runs/<runId>` artifacts and JSONL run events.                                                                                                                                                                                                                                                                                                                                  | Read when changing run artifact lifecycle, formats, or retention.                                                                                                                                                   |
| `Backend/app/persistence/execution_recovery.py`         | Owns the independent `.xcodeagent/recovery/execution-recovery.sqlite` store, schema v2, short-lived WAL connections, atomic Execution+Lease creation, transactional runId conflict protection, lease renewal/release/expiry CAS operations, historical RecoveryPoint persistence, stable ascending/descending history reads, and checkpoint-based idempotency. It never writes LangGraph checkpoint tables or ApplicationLifecycle. | Read when changing recovery storage paths, lease schema/ownership, deduplication, history ordering, latest-point indexing, or future recovery readers. |
| `Backend/app/persistence/checkpoints.py`                | Owns workspace-scoped LangGraph SQLite checkpointers for the main workflow, including `.xcodeagent/checkpoints/checkpoints.sqlite` path creation, optional global `XCODEAGENT_CHECKPOINT_DB` override, conservative retention cleanup, target-workspace row deletion, and connection shutdown. Full-app deletion identifies rows from checkpoint channel values, metadata, and known lifecycle threads; it removes only matching rows from a shared override DB, while a local DB connection is closed before its project directory is trashed. | Read when changing workflow state persistence, checkpoint retention, recovery/deletion behavior, or `XCODEAGENT_CHECKPOINT_DB` handling. |
| `Backend/app/services/execution_recovery.py`            | Provides fail-open runtime observation for atomic execution+lease start, node start, checkpoint capture, normal/awaiting-user completion, failure, and explicit/non-explicit cancellation. Ordinary recovery-store failures remain fail-open, while the structured durable runId conflict is fail-closed. It stores only compact checkpoint/revision/hash references and logs `recovery.*` events without selecting or replaying recovery strategy. | Read with `protocols/workflow/runtime.py`, `execution_lease_heartbeat.py`, and `execution_recovery_scanner.py` when changing lease ownership, P0.2 observation boundaries, terminal status mapping, or recovery-store failure handling. |
| `Backend/app/services/execution_recovery_selector.py` | P0.3A read-only RecoveryPoint history selector. It folds duplicate observations by `(threadId, checkpointNs, checkpointId)`, preserves completed boundaries, ignores entry/terminal/failure observations, and returns ordered checkpoint candidates without inferring a graph node. | Read with `domain/execution_recovery.py`, `persistence/execution_recovery.py`, and `execution_recovery_coordinator.py` when changing candidate selection or history semantics. |
| `Backend/app/services/execution_recovery_strategy.py` | P0.3A replay-safety contract and deny-by-default resolver. Production leaves `unassessed` checkpoints at `REQUIRES_HANDLER`; explicit policy injection can prove a test node safe and produce `READY_NATIVE` without adding business-node branches to the Coordinator. | Read when adding a Native Replay or domain-specific Recovery Handler policy. |
| `Backend/app/services/execution_recovery_coordinator.py` | P0.3A pure recovery-plan preparation: reads source status, scans and validates historical RecoveryPoints against exact LangGraph checkpoint identity/`snapshot.next`, checks lifecycle/workspace drift, detects interrupts, and returns `RecoveryPlan`. It never changes Execution/Lifecycle, creates leases, or starts Graph; P0.3A has no frontend/API entry. | Read with `execution_recovery_selector.py`, `execution_recovery_strategy.py`, `protocols/workflow/runtime.py`, and `execution_recovery_scanner.py` before implementing P0.3B execution. |
| `Backend/app/services/backend_instance.py`              | Creates and exposes the current process-scoped Backend instance identity used as the persistent ExecutionLease owner; it is initialized from the FastAPI lifespan and is not itself persisted. | Read when changing Backend restart/orphan ownership rules or health diagnostics. |
| `Backend/app/services/execution_lease_heartbeat.py`     | Maintains and stops the per-Execution lease heartbeat task. Heartbeat failures warn and remain fail-open; a rejected renewal ends the task without reviving an interrupted execution. | Read when changing lease TTL/heartbeat timing or runtime cleanup. |
| `Backend/app/services/execution_recovery_scanner.py`   | Performs workspace-scoped lazy recovery scans from lifecycle get or before a new Workflow execution, preserving active local runs and marking stale RUNNING records INTERRUPTED without replaying Graph state. | Read when changing Backend restart detection, local run registry checks, or recovery projection boundaries. |
| `Backend/app/observability/agent_events.py`             | Helpers for append-oriented agent run event payloads.                                                                                                                                                                                                                                                                                                                                              | Read when changing event names or JSONL fields.                                                                                                                                                                     |
| `Backend/app/utils/model_output.py`                     | Shared best-effort JSON object extraction for direct model outputs, including fenced or commentary-wrapped JSON.                                                                                                                                                                                                                                                                                   | Read when changing model JSON parsing used by requirements, product/technical planning, task preparation, or entity-source binding.                                                                                |

## Current Build DAG Contract

The current Build path is `confirmed formal artifacts + template readiness + PageImplementationContract + confirmed Endpoint API designs → api_design_readiness_gate → inspect_workspace → prepare_build_tasks → DAG confirmation → build`. The following boundaries are part of the current contract and should be checked together when changing this path:

| Concern | Current contract | Read first |
| --- | --- | --- |
| Runtime context | `project_plan` is a read-only runtime projection used to assemble the current Build context. It is not a new editable formal artifact, and DAG generation/confirmation must not write it back into TechnicalPlan Markdown or JSON. | `Backend/app/graph/nodes/tasks.py`, `Backend/app/services/build_context_resolver.py`, `Backend/app/workspace/plan_documents.py` |
| DAG prerequisites | `RequirementSpec`, `ProductPlan`, `UiManifest` (confirmed or explicitly skipped), `TechnicalPlan`, template-generation manifest/real-file readiness, current PageImplementationContract, Endpoint semantics, and current confirmed Endpoint API designs must pass before candidate generation. | `Backend/app/graph/nodes/tasks.py`, `Backend/app/services/application_template_generation.py`, `Backend/app/services/api_design.py`, `Backend/app/services/build_context_resolver.py` |
| Generation and validation | The platform owns Unit selection, Worker Pool concurrency, Attempt identity, Local retry routing, Scope Assembly, Global repair selection, and final execution-batch compilation. Each model returns only one Unit Candidate and receives structured feedback for its own retry. Candidate Task-level parallel hints remain part of the current schema, but they do not choose Unit workers or final batches; dependencies and file conflicts are applied by the compiler. Exhausted content attempts fail the Run without writing Pending. | `Backend/app/graph/nodes/task_planning_adapter.py`, `Backend/app/services/unit_generation_scheduler.py`, `unit_candidate_validator.py`, `scope_assembly.py`, `global_repair_orchestrator.py`, `build_task_planner.py` |
| Deliverable contract | `deliverables[].kind` must use the exact `DELIVERABLE_KINDS` allowlist; values outside it are rejected and do not receive a business acceptance projection. Existing `deliverables[].paths` behavior remains unchanged. | `Backend/app/agents/main/task_preparer_prompt.py`, `Backend/app/services/business_acceptance.py` |
| Artifact authority | `.xcodeagent/plans/build-task-plan.pending.json` is the sole awaiting-confirmation PendingPlan；its DraftIdentity freezes `owner_session_id` and `workflow_run_id` together with the Planning identity and execution scope. Generation must not mutate Formal. Exact Confirm atomically promotes it to `.xcodeagent/plans/build-task-plan.json`, which is the only Build authority. At Build Run start Formal is copied to `.xcodeagent/plans/build-runs/` and bound by SHA-256；the Run, Retry, Repair, and Testing use only that copy, while a later Formal digest change terminates the Run. | `Backend/app/workspace/task_documents.py`, `Backend/app/services/build_task_plan_lifecycle.py`, `Backend/app/graph/nodes/task_planning_adapter.py`, `Backend/app/graph/subgraphs/build.py`, `Backend/app/protocols/workflow/request.py` |
| Ownership boundary | Main template initialization owns page placeholders and menus. Auth Build pre-projection owns routes and backend AuthConstants, while the deterministic `frontend:auth-guard` Task exclusively owns `resources.ts`. For real APIs, `frontend:api-client` owns the shared ResponseEntity adapter and business API modules; static modules do not create it. Template branch mismatch fails before DAG generation. Normal Build does not create `database:*` Units or database-owner tasks. | `Backend/app/agents/main/unit_task_prompt.py`, `Backend/app/services/deterministic_unit_candidates.py`, `Backend/app/services/authorization_platform_projection.py`, `Backend/app/graph/subgraphs/build.py` |
| Planning-result actions | The AG-UI `build_task_plan_confirmation` mode is read-only and exposes identity-bound `confirm`、`abandon`、`regenerate`. Confirm promotes the exact Pending and enters Build. Abandon records the lifecycle result and deletes the exact Pending；the refresh projection becomes idle only when the Pending file is absent. Regenerate first deletes the exact old Pending, then returns to `prepare_build_tasks`, allocates a new PlanningRun and writes a new Pending on success；failure never restores the old Pending. Platform Local/Global validation retries remain internal and are not user actions. | `Backend/app/services/build_task_plan_lifecycle.py`, `Backend/app/services/dag_planning_regeneration.py`, `Backend/app/services/application_lifecycle.py`, `Backend/app/protocols/workflow/request.py`, `Backend/app/protocols/workflow/run_control.py`, `Backend/app/protocols/workflow/definition.py`, `Frontend/src/renderer/src/components/AiChatPanel/components/WorkflowRunCard/BuildTaskPlanConfirmation.tsx`, `Frontend/src/renderer/src/components/AiChatPanel/hooks/useWorkflowConversation.ts`, `docs/DAG_TASK_GENERATION_AND_VALIDATION_MININAL_CHANGE_PLAN.md` |
| Planning concurrency, cancel, refresh | The Backend refresh authority is only `.xcodeagent/plans/build-task-plan.pending.json`: when it exists, `planningRefresh` is `source=pending_plan/status=awaiting_confirmation` and reads `planningRunId`、`workflowRunId`、`ownerSessionId` and scope from the Pending DraftIdentity; when it is absent, it is `source=none/status=idle`, regardless of active executions, old `awaiting_user`, resource locks, activeRunId, PlanningRun snapshots, Formal plans, or history. The current renderer lock remains a later migration boundary. | `Backend/app/protocols/workflow/run_control.py`, `Backend/app/services/planning_refresh_recovery.py`, `Backend/app/services/build_task_plan_lifecycle.py`, `Backend/app/workspace/task_documents.py` |

## Frontend

Planning Runtime ownership: `Frontend/src/renderer/src/service/applicationPlanningRuntime.ts` owns independent AG-UI execution, authoritative reconcile, submit/save/revision/retry/stop and temporary streaming subscriptions. Its exclusive transport primitive remains the only application-planning Graph-write `session.sendMessage` owner. `applicationPlanningRecovery.ts` is the dedicated read-only AG-UI control-plane client with bounded timeout/retry; Runtime single-flights those reads and atomically commits checkpoint plus lifecycle through `reconcile_received`. Generic network/SSE/finalization failures, cold renderer restore, stop uncertainty, manual sync, and one browser `online` event can trigger reconcile, but never replay confirm/revise or poll periodically. `activeApplicationPlanning.ts` keeps business `error` separate from `syncError` and blocks mutations while reconciling or uncertain. Stateless interaction, checkpoint identity and bounded-stop helpers live in `applicationPlanningRuntimeHelpers.ts`, while `applicationPlanningProductConversation.ts` owns the pure product-conversation interaction/error domain functions and the ChatComposer path only re-exports them for compatibility. `hooks/useActiveApplicationPlannings.ts` owns the synchronous Canonical State getter/reducer, cold loading, visibility and template orchestration; it holds no component execution handlers. Runtime updates Canonical State before publishing recovered Workflow history. No new storage contract is added.

`AppEntryPage.tsx` mounts `hooks/useApplicationPlanningRuntimes.ts` once to manage Runtime instances by application plus thread, dispose removed/replaced instances, and route Workbench submit/save/revision/retry/stop actions directly. A design-stage formal revision ensures the original planning Runtime synchronously, without a Modal-mount wait. `hooks/useApplicationPlanningWorkbenchBridge.ts` retains `deliverPlanningChunk`, current-thread history buffering, and the existing Workbench revision-continuation bridge; it is not a Planning state owner.

Only the actually visible `ApplicationPagePlanningModal.tsx` is mounted. It consumes current state, temporary streaming content, template loading and business actions; `Welcome/applicationPlanningPresentation.ts` holds its pure progress/page-count projections. `hooks/useApplicationPlanningStreamingContent.ts` subscribes the visible view to Runtime presentation content without storing tokens in Canonical State. WorkbenchPage/LeftPanel pass the `onSavePlanningRequirementSpec` business action to AiChatPanel; Runtime saves and updates current state, while the view only updates its document cache and toast from `RequirementSpecDraftSaveResult`. Focused cases A-M live in `Frontend/tests/applicationPlanningRuntime.test.ts`, with J-M covering shared pre-submit ownership, busy state, internal recovery retry and Canonical-before-submit ordering. `pnpm test:application-planning-runtime` uses the repository's Vite temporary-runner pattern. Consumer integration has not been validated because the user explicitly requested no verification.

The user-visible product brand is **AIStudio**, including login/welcome/workbench labels, accessible names, window/tray titles, default documents and backend messages shown in the UI. Internal `window.xcodeAgent`, package/environment identifiers and `.xcodeagent` storage paths are unchanged.

| Path                                                                                                          | Responsibility                                                                                                                                                                                                                                                                                                  | Read first / common edits                                                                                                                                                              |
| ------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Frontend/src/main/index.ts`                                                                                  | Electron main process: app/window/auth/preview lifecycle, storage and workspace IPC, controlled application deletion, template clone/retry, local sessions, and planning-artifact inspection. `sessions:create` is the only current-contract chat-session creation path: it generates the AG-UI Thread, persists workflow and phase/stage identity, persists an immutable page/API `developmentTarget` for newly created DEVELOPMENT sessions, and allocates DESIGN/PLAN/DEVELOPMENT sequence under the main-process lock. Formal revision creates DESIGN/PLAN sessions only; its development continuation returns to the recorded source DEVELOPMENT session instead of creating or recovering another Thread. `sessions:save` can update content but rejects phase, thread, or development-target identity drift. Current inspection merges ProductPlan pages with TechnicalPlan references and projects API plus entity outlines (including field summaries/design status) for the workbench; page/API design status comes from formal planning artifacts rather than chat-session existence. No historical session migration or title/message target inference is performed. | Read with `Frontend/src/main/stageSessions.ts` and `planningArtifactStatus.ts` when changing Electron lifecycle/IPC, stage-session allocation, template download, planning inspection, endpoint design-document status, app/session storage, preview, or filesystem-backed behavior. |
| `Frontend/src/main/stageSessions.ts`                                                                          | Current three-stage session primitives: product/planning/development mapping, per-Workflow/Stage sequence allocation, and an in-process serialized creation lock. DEVELOPMENT sessions may carry an immutable page/API target; free sessions remain unbound. It does not create sessions for test/review/acceptance or react to top-bar browsing. | Read with main-process `sessions:create`, renderer `service/chatSessions.ts`, and `AiChatPanel/hooks/useChatSessions.ts` when changing StageSession identity, sequence, target binding, or creation idempotency. |
| `Frontend/src/main/planningArtifactStatus.ts`                                                                 | Filesystem-backed Endpoint design status: requires the current JSON and Markdown pair, confirmed JSON state, matching contract/endpoint identity, valid current Mapping including one-sentence business descriptions, and a TechnicalPlan fingerprint equal to the current contract hash; reports pending/confirmed/stale reasons to the workbench outline. | Read with `endpoint_design_documents.py` when changing API-design status or Endpoint document naming/fingerprinting. |
| `Frontend/src/renderer/src/components/AiChatPanel/components/WorkflowRunCard/ApiDesignPanel.tsx` + `ApiFieldMappingTable.tsx` + `ApiFieldMappingEditor.tsx` + `ApiBusinessDescriptionEditor.tsx` + `ApiSourceFieldSelector.tsx` + `apiDesignTableModel.ts` + `useApiDesignDraft.ts` + `apiDesignSerialization.ts` | Dual-theme Endpoint API-design surface and bounded draft controller. The seven-column table renders bindable API leaves and summary states; direct, through-entity, and one-sentence-description modes are edited in a single drawer with local draft isolation. `ApiSourceFieldSelector` submits AG-UI database table/column and external Operation metadata loads, including restoration for existing database mappings. | Read with workflow typings, `WorkflowRunCard/index.tsx`, `useWorkflowConversation.ts`, and backend `graph/nodes/api_design.py` when changing API-design UX or payloads. |
| `Frontend/src/main/filesystem.ts`                                                                            | Small Electron main-process filesystem helpers for idempotent `lstat`/recursive directory removal: an absent path is a successful no-op, while permission, type, symlink, and other filesystem errors remain visible to the caller. | Read when changing safe project deletion or other main-process filesystem operations that need explicit `ENOENT` semantics. |
| `Frontend/src/main/applicationSettings.ts`                                                                    | Versioned application-wide settings persisted atomically at Electron `app.getPath('userData')/settings.json`; currently owns the light/dark appearance theme, validates IPC writes, and broadcasts changes to every open Electron window. | Read when changing the application settings schema, theme persistence/defaults, settings IPC, or cross-window synchronization. |
| `Frontend/src/main/auth.ts`                                                                                   | Environment-scoped desktop authentication state: CMB Device Flow placeholder, current-process `auth.json` `{ access_token }` persistence, main-process memory access, and shared startup/reauthentication/quit cleanup. Cold starts delete any residual file before backend startup; ordinary window close only hides the window.                                                | Read when changing login, token persistence/memory semantics, environment auth paths, cold-start policy, or logout behavior.                                                            |
| `Frontend/src/main/backendService.ts`                                                                         | Packaged Windows/macOS backend service lifecycle: locating the PyInstaller executable, checking the generic built-in-skills resource directory, selecting a local port, health polling, renderer base URL, and process shutdown. Backend Python remains the authority for required skill names and files.        | Read when changing bundled backend startup, packaged resource structure, ports, health checks, or quit cleanup.                                                                         |
| `Frontend/tsconfig.node.json`                                                                                 | TypeScript project for Electron configuration, main-process, and preload sources; loads `electron-vite/node` declarations for resource queries such as `?asset`.                                                                                                       | Keep `src/main/**/*` and `src/preload/**/*` included so `pnpm typecheck` validates desktop runtime code and resolves Electron Vite asset modules.                                        |
| `Frontend/src/preload/index.ts` and `Frontend/src/preload/index.d.ts`                                         | Context-bridge API exposed as `window.xcodeAgent` plus preload typings, including authentication status/login/token access/reauthentication, application-settings load/theme-save/change subscription, session workspace history calls, browser/report actions, and project-preview registration. | Update with every new or changed Electron IPC channel. |
| `Frontend/src/renderer/src/window.d.ts`                                                                       | Renderer-side `window.xcodeAgent` type declarations.                                                                                                                                                                                                                                                            | Update with preload API changes.                                                                                                                                                       |
| `Frontend/src/renderer/src/main.tsx`                                                                          | React renderer entry; mounts the global authentication-failure gate outside the workbench provider so every product page receives the same blocking 401/403 UI.                                                                                                                                                 | Read for app bootstrapping or global authentication-failure UI issues.                                                                                                                 |
| `Frontend/src/renderer/src/login.tsx` and `login.less`                                                        | Dedicated frameless Electron authentication window: custom close control, draggable themed security-workbench layout, Device Flow entry, login loading/error states, and technology-oriented system/terminal presentation.                                                                                       | Read with `Frontend/src/main/index.ts` when changing login interaction, window frame/dimensions, authentication messaging, or light/dark visual treatment.                             |
| `Frontend/src/renderer/src/pages/AppEntryPage.tsx`                                                            | Top-level renderer routing between welcome flow and workbench.                                                                                                                                                                                                                                                  | Read when changing app open/create lifecycle.                                                                                                                                          |
| `Frontend/src/renderer/src/pages/WelcomePage.tsx`                                                             | Thin composition page for the welcome hero and create/open application actions.                                                                                                                                                                                                                                 | Read with `components/Welcome/` when changing the welcome screen layout.                                                                                                               |
| `Frontend/src/renderer/src/components/Welcome/`                                                               | Welcome and new-application planning UI. Creation requires a new/empty independent project directory, writes lifecycle state, keeps the platform database default hidden, conditionally collects one external database mode, encrypts stored credentials, and renders RequirementSpec/ProductPlan/UI/TechnicalPlan confirmation and retry states. | Read the creation form, planning modal, summaries, constants, template service, and credential helpers when changing initialization. |
| `Frontend/src/renderer/src/pages/AppEntryPage.tsx` | Root renderer surface switcher and Planning Runtime owner for welcome, background execution, template readiness, and the single application workbench. It mounts at most one visible planning view and routes business actions directly to the Runtime manager. A design-stage formal revision reuses the application's original planning thread immediately; it does not wait for a Modal handler. Workspace switching preserves preview cleanup and background planning isolation. | Read with `hooks/useApplicationPlanningRuntimes.ts`, `hooks/useApplicationPlanningWorkbenchBridge.ts`, lifecycle/template services, `ApplicationPagePlanningModal.tsx`, and `useWorkflowConversation.ts` when changing application opening, planning-to-workbench handoff, or design-revision routing. |
| `Frontend/src/renderer/src/pages/WorkbenchPage.tsx`                                                           | Workbench shell; reads the complete page outline from RequirementSpec `pages`, hydrates designed status plus menu-tree `pageTree` from ProjectPlan `frontend_pages`, optionally synchronizes workspace `application.json`, starts the current frontend preview server, registers successful preview workspaces with Electron for explicit-exit cleanup, and keeps its runtime origin/error in memory for page-level preview URL composition. All workflow phases remain in this workbench and use StageSession/thread boundaries rather than separate Electron phase windows. | Read when changing workbench page selection, menu hierarchy projection, workspace-backed application synchronization, phase entry, or frontend preview startup/registration state. |
| `Frontend/src/renderer/src/components/AiChatPanel/`                                                           | Main Workflow chat UI split by responsibility. Design owns RequirementSpec/ProductPlan/UiDesign; Planning owns TechnicalPlan, template preparation and controlled revision handoff; Development owns target-bound work, DAG Planning and Build. Page/API work enters `api_design_readiness_gate`, while legacy EntitySourceBinding remains an independent flow. `AiChatPanel.tsx` consumes the canonical `planningState`, resolves the current PendingPlan and DraftIdentity from lifecycle plus session state, keeps non-owner sessions read-only, and renders DAG generation/confirmation in the Stage Output panel. `MessageList` uses history workflows for display while central Planning sync/mutation guards remain authoritative; the same shell preserves phase-owned sessions, test/review/acceptance handoffs, reports, code changes, preview, formal revision controls, and light/dark behavior. | Read `AiChatPanel.tsx`, `hooks/revisionSession.ts`, `components/ApplicationRevisionCard/`, `components/DocPanel/`, `components/PlanExecutionDock/`, `components/MessageList/`, `components/WorkflowRunCard/`, `components/SessionExecutionLockDock.tsx`, `stageOutputState.ts`, and `useWorkflowConversation.ts` when changing lifecycle locking, revision routing, DAG progress/confirmation, sessions, reports, or preview controls. |
| `Frontend/src/renderer/src/components/AiChatPanel/components/WorkflowRunCard/BuildTaskPlanConfirmation.tsx` + `BuildTaskPlanConfirmationContent.tsx` | Read-only structured Build task confirmation card. It merges target and scope on the first layer, renders ProductPlan page acceptance and only actually related TechnicalPlan Endpoints, and summarizes reused/retained tasks. Current-scope task cards default to title-only; opening one reveals its execution instructions, while modification paths and business acceptance remain independently collapsed on a second level. Internal engineering checks, task IDs, Units, dependency edges, edit controls, and full historical task bodies are not shown. It submits identity-bound `confirm`、`abandon` or `regenerate` through AG-UI；Regenerate 明示旧 Pending 先被丢弃且失败不恢复。 | Read with `useWorkflowConversation.ts`, `typings/workflow.ts`, backend `protocols/workflow/request.py`, `protocols/workflow/run_control.py`, and `graph/nodes/tasks.py` when changing task-confirmation UX, target/check projection, or action payloads. |
| `Frontend/src/renderer/src/components/AiChatPanel/components/WorkflowRunCard/ExternalApiDesignPanel.tsx` + `useExternalApiDraft.ts` | Full-width vertical external API EntitySourceBinding designer. It maintains the current multi-operation request/response/mapping draft, validates endpoint assignment and required coverage, and submits the final binding through AG-UI. | Read with `entityDesignSerialization.ts`, `EntityDesignPanels.tsx`, workflow typings, and Backend entity-design services when changing the external API binding contract or UI. |
| `Frontend/src/renderer/src/components/AiChatPanel/components/StageOutputPanel/` + `stageOutputState.ts` | Development-stage right-workspace DAG output. `lifecycle.extensions.planningRefresh` and the current Pending execution outrank stale chat snapshots; generation renders `dag-generation.v1` Unit/local/global state, while confirmation is restored only for the exact owner/session and DraftIdentity. The panel has no Cancel action; Build confirmation uses identity-bound Confirm/Abandon/Regenerate. | Read with `AiChatPanel.tsx`, `SessionExecutionLockDock.tsx`, `WorkflowRunCard/index.tsx`, `BuildTaskPlanConfirmation.tsx`, `DagGenerationProgress.tsx`, and Backend planning-refresh recovery when changing DAG recovery, placement, or confirmation. |
| `Frontend/src/renderer/src/components/AiChatPanel/components/ApplicationOutline/` + `components/DevelopmentArtifactsPanel/` | Development artifact navigation for ProjectPlan pages, API endpoints, and entities. It owns browsing/filtering and in-memory detail selection without changing chat-session ownership, readiness state, or workflow target. | Read with `useDevelopmentArtifactDetail.ts`, `AiChatPanel.tsx`, and `SessionSidebar/` when changing artifact browsing or the right-workspace split layout. |
| `Frontend/src/renderer/src/components/AiChatPanel/components/QuickTaskGuide/`                               | Empty-conversation start surface for page, Endpoint, and entity shortcuts. Endpoint status chooses “设计 API/重新设计” versus development; API design opens the independent `ApiDesignConfigModal`, while page/API development creates a target-bound DEVELOPMENT session before entering the readiness gate. The retained entity shortcut starts the independent legacy EntitySourceBinding flow and is explicitly unrelated to Build readiness. | Read with `AiChatPanel.tsx`, `components/MessageList/`, `hooks/useChatSessions.ts`, `hooks/useWorkflowConversation.ts`, and API-design status projection when changing new-conversation UX or target actions. |
| `Frontend/src/renderer/src/components/AiChatPanel/components/TemporaryChatOverlay/`                        | Temporary full-height chat overlay anchored beside the collapsed left rail. It does not participate in workspace width allocation or enter persisted/AG-UI conversation state; the composer remains disabled until an Agent is selected, and the surface supports light/dark themes. | Read with `AiChatPanel.tsx` and `components/SessionSidebar/` when changing the temporary-chat entry, overlay layout, placeholder conversation, close behavior, or future Agent integration. |
| `Frontend/src/renderer/src/components/SkillsPage/`                                                          | Categorized skill management UI with user/built-in segmented filtering, request-safe manual refresh, read-only built-in cards, per-user-skill enable switches, create/edit/delete flows, and themed ZIP upload. Catalog helpers also filter enabled skills and remove stale chat selections. | Read with `service/userSkills.ts`, `typings/userSkills.ts`, `AiChatPanel`, and `Frontend/package.json` when changing catalog categories, refresh, enablement, editor/import interaction, chat selection cleanup, or light/dark styles. |
| `Frontend/src/renderer/src/components/AgentFilesPage/`                                                     | Environment-level AGENTS.md file page: one fixed `AGENTS.md` list item plus a plain textarea, save state, revision-conflict feedback, and dual-theme styling; it has no tabs, preview, file switches, or additional files. | Read with `service/agentFiles.ts`, `typings/agentFiles.ts`, and `AiChatPanel`/`SessionSidebar` when changing the file-management UI or navigation. |
| `Frontend/src/renderer/src/components/DataSourcesPage/`                                                   | Standalone workspace data-source management UI. It renders database and external-API domain rows on the main page; each external API opens a dedicated centered manager modal with left-side directory/interface navigation and a right-side read-only operation detail view. Request/response JSON uses full-width stacked structure/sample panels; editing previews follow the current sample; saving submits complete standard JSON Schema fragments containing field types and descriptions, and read-only details parse the saved properties/items directly. Removed fields are cleaned on save. Domain, directory, and operation editing use opaque centered modals, duplicate Base URLs warn without blocking, passwords stay masked, and all persistence goes through the independent AG-UI client. Path/Query editors use stable draft row IDs, independent parameter arrays, and explicit type selectors. A separate operation-detail hook reads the selected interface and retries failures without writing sibling summaries into the main catalog. JSON type selection preserves convertible values, updates all matching sample elements, and confirms destructive container changes before committing the draft. Request/response structures remain independent; save merging uses the latest full source plus the explicitly edited operation so summaries cannot overwrite other operation details. | Read with `DataSourceEditorModal.tsx`, `ExternalApiManagerModal.tsx`, `DataSourceDirectoryModal.tsx`, `DataSourceOperationModal.tsx`, `JsonStructureViewer.tsx`, `JsonStructureViewer.less`, `service/dataSources.ts`, and `typings/dataSources.ts` when changing the data-source menu or forms. |
| `Frontend/src/renderer/src/components/DataSourcesPage/DataSourceParameterEditor.tsx` | Shared Path/Query parameter editor; each group edits its own persisted parameter array, location is not stored on individual parameters, and draft row IDs survive edits/deletions, and Path parameters are required scalar values. | Read with `ExternalApiFormParts.tsx`, `DataSourceOperationModal.tsx`, and data-source typings. |
| `Frontend/src/renderer/src/components/DataSourcesPage/jsonStructure.ts` and `jsonFieldTypes.ts` | React-independent live JSON structure inference, safe draft-only paths, type matching, immutable sample conversions, and bounded display projection directly from stored JSON Schema properties/items. | Read with `JsonStructureViewer.tsx` and `tests/jsonFieldTypes.test.ts` when changing structure previews or field-type editing. |
| `Frontend/src/renderer/src/components/DataSourcesPage/jsonSchema.ts` | Reads standard JSON Schema nodes into temporary editor state and builds complete request/response structures on save. Field descriptions and types are serialized only inside properties/items nodes, not as separate API or storage maps. | Read with the operation editor, JSON structure viewer, and `tests/jsonSchema.test.ts`. |
| `Frontend/src/renderer/src/components/DataSourcesPage/dataSourceOperations.ts` | Separate Path/Query array validation, explicit detail-target lookup, and safe merging of candidate directory references with a complete domain and an explicitly edited operation. | Read when changing operation persistence or preventing summary data from replacing full configurations. |
| `Frontend/src/renderer/src/components/DataSourcesPage/useOperationDetails.ts` | Keeps the selected operation detail separate from list summaries. Reloads on catalog/selection changes, ignores stale responses after switching/closing, and exposes local loading/error/retry state. | Read with the data-source page and manager modal when changing lazy detail loading. |
| `Frontend/src/renderer/src/deprecated/components/RequirementPlannerPanel/`                                    | Deprecated, fully commented old planner UI for collecting answers and rendering structured development plans.                                                                                                                                                                                                   | Do not import from main flow.                                                                                                                                                          |
| `Frontend/src/renderer/src/deprecated/components/OrchestrationPanel/`                                         | Deprecated, fully commented old structured orchestration card for complex requirements, task graph summary, and plan confirmation.                                                                                                                                                                              | Do not import from main flow.                                                                                                                                                          |
| `Frontend/src/renderer/src/components/BrowserPreviewPanel/`                                                   | Embedded browser preview panel with in-memory address/history navigation, optional Workflow-driven navigation, and a versioned cross-origin `postMessage` element-inspection bridge. Inspection readiness and selection messages are validated against the active iframe window/origin; valid tag/source/line/column selections are lifted into `AiChatPanel`, displayed as a single replaceable Composer tag, sent through `/conversation/run` `elementContext`, and appended to the ordinary conversation prompt as a `frontend/src/...` target-line implementation hint, while missing source locations do not replace the current selection. Navigation, refresh, session switch, or a successful send clears the scoped selection; closing preview alone preserves it. Page preview URLs are rebuilt on every preview action from the latest runtime frontend origin/port plus the selected ProjectPlan page path; preview URLs and launch errors are never persisted to localStorage. | Read with `AiChatPanel`, `ChatComposer`, `useWorkflowConversation`, `service/agUiAgent.ts`, and `utils/previewUrl.ts` when changing in-app preview, element inspection, page-route composition, or post-launch preview handoff. |
| `Frontend/src/renderer/src/deprecated/components/ProtectedToolPanel/`                                         | Deprecated, fully commented old UI for protected tool approvals.                                                                                                                                                                                                                                                | Do not import from main flow.                                                                                                                                                          |
| `Frontend/src/renderer/src/components/GlobalConfigPanel/`                                                     | Global configuration UI.                                                                                                                                                                                                                                                                                        | Read when changing app-wide settings UI.                                                                                                                                               |
| `Frontend/src/renderer/src/components/EditorPanel/`                                                           | Displays assistant output bridged into editor-facing panels.                                                                                                                                                                                                                                                    | Read when changing editor output presentation.                                                                                                                                         |
| `Frontend/src/renderer/src/components/LeftPanel/` | Left workbench panel composition and business-action forwarding to AiChatPanel. Planning views receive Canonical State for display plus submit/save/retry/stop/revision actions; they receive no Planning event dispatcher. | Read with `WorkbenchPage.tsx` and `AiChatPanel.tsx` when changing panel layout, mounted assistant panels, or Planning action props. |
| `Frontend/src/renderer/src/components/ActivityBar/`, `ResizeHandle/`, `PlaceholderPanel/`, `MarkdownContent/` | Shared UI pieces for navigation, resizing, empty states, and markdown rendering.                                                                                                                                                                                                                                | Read only for UI work touching those widgets.                                                                                                                                          |
| `Frontend/src/renderer/src/service/agUiAgent.ts`                                                              | Shared AG-UI client for `/workflow/run` and `/conversation/run`. It strictly parses `dag-generation.v1`, rejects older revisions of the same PlanningRun, reconciles custom/snapshot/result payloads, preserves the final Workflow snapshot on `RUN_ERROR`, and keeps cancellation on the active endpoint. Stop allows a short natural stream drain, then requires the backend final cancellation state, aborts residual local transport with bounded cleanup, and retains unresolved server identity after timeout so the same thread cannot start concurrently. | Read when changing workflow/conversation inputs, DAG progress, revision ordering, cancellation cleanup, failure handling, or session identity. |
| `Frontend/src/renderer/src/components/AiChatPanel/components/ApplicationRevisionCard/` | Dual-theme application-revision UI: the formal-revision confirmation card shows only the routing `reason` plus confirm/cancel actions; a TechnicalPlan draft card supports save/revise/confirm/discard through lifecycle/hash-bound structured actions. Neither exposes Graph nodes. | Read with `WorkflowRunCard`, `useWorkflowConversation.ts`, application planning service, and revision typings. |
| `Frontend/src/renderer/src/service/authentication.ts` and `components/AuthenticationFailureGate/`            | Shared AG-UI `HttpAgent` factory and `onRunFailed` subscriber for transport-level 401/403 detection, deduplicated authentication-failure notification, error classification, and the light/dark full-window blocking reauthentication modal.                                                                    | Read when changing Python AG-UI authentication-failure handling, subscriber composition, or the reauthentication UI.                                                                  |
| `Frontend/src/renderer/src/service/cloudApi.ts`                                                               | Generic non-streaming cloud Java JSON client; reads the main-process in-memory access token through preload IPC, forces the Bearer header, and routes missing-token/401/403 failures through the shared reauthentication gate.                                                                                   | Read when adding cloud Java calls or changing their token/header/error contract.                                                                                                       |
| `Frontend/src/renderer/src/service/databaseCredentialCrypto.ts`                                               | Fetches the Backend `/health` public database-encryption metadata, validates/imports the RSA key with WebCrypto, encrypts `plantMode.pwd` using RSA-OAEP-SHA256, and returns a copied persistence schema containing the versioned base64url secret envelope. | Read before changing application creation credential persistence. Never log/store plaintext, silently fall back to plaintext, or send it through AG-UI. |
| `Frontend/src/renderer/src/service/userSkills.ts`                                                           | Workflow-independent `@ag-ui/client` wrapper for `/skills/run`. Sends list/get/create/save/delete/import/set-enabled actions, reconciles custom events/snapshots/final results, validates user and built-in catalog shapes, and converts failed AG-UI runs into UI-facing errors. | Read when changing skill action payloads, transport, result reconciliation, catalog schemas, enablement acknowledgement, revision/deletion/import acknowledgement, or frontend error handling. |
| `Frontend/src/renderer/src/service/agentFiles.ts`                                                           | Workflow-independent `@ag-ui/client` wrapper for `/agent-files/run`; sends `get` and revision-protected `save` actions, reconciles the `agent-files` custom event, `agentFiles` snapshots, and final result into the single editable AGENTS.md document. | Read with `services/agent_file_documents.py` and `protocols/agent_files.py` when changing the AGENTS.md product contract. |
| `Frontend/src/renderer/src/service/dataSources.ts`                                                         | Workflow-independent `@ag-ui/client` wrapper for the data-source catalog and API-design metadata endpoints. It reads the source list and, on demand, database tables/columns or an external Operation Schema through dedicated AG-UI actions; API-design metadata never travels through `/workflow/run`. | Read with `services/data_sources.py`, `protocols/data_sources.py`, `routes/data_sources.py`, and `components/DataSourcesPage/` when changing the standalone catalog contract. |
| `Frontend/src/renderer/src/service/codeChanges.ts`                                                          | Workflow-independent `@ag-ui/client` wrapper for `/code-changes/run`; submits confirmed historical change sets and reconciles custom events, state snapshots, and final rollback results. | Read with `AiChatPanel/components/CodeChangeCard`, Backend `protocols/code_changes.py`, and `services/code_change_revert.py` when changing review or rollback behavior. |
| `Frontend/src/renderer/src/service/versionControl.ts` and `components/AiChatPanel/components/VersionCommitReminder/` | Workflow-independent `@ag-ui/client` wrapper and dual-theme revision commit reminder. A successful reminder remains dormant until the Workflow emits a completed `application-revision` event after final acceptance; failed quick changes only offer review and repair guidance. | Read with Backend `protocols/version_control.py`, `services/version_control.py`, `MessageList`, and `docs/APPLICATION_REVISION_WORKFLOW.md` when changing revision commit timing or UI. |
| `Frontend/src/renderer/src/deprecated/service/orchestratorAgent.ts`                                           | Deprecated, fully commented thin re-export service for old development orchestrator AG-UI sessions.                                                                                                                                                                                                             | Do not import from main flow.                                                                                                                                                          |
| `Frontend/src/renderer/src/service/chatSessions.ts`                                                           | Renderer current chat-session contract and persistence client. A DESIGN/PLAN/DEVELOPMENT ChatSession is the StageSession itself and requires `workflowId`, mapped `stage`, global per-stage `sequence`, immutable `entryKey`/Thread, and may carry an immutable page/API `developmentTarget` for newly created DEVELOPMENT sessions; free sessions remain unbound. New sessions go through Electron `sessions:create`, while saves only update an already-created record. Formal-revision context belongs to its DESIGN/PLAN sessions and records the exact source DEVELOPMENT session/thread; TechnicalPlan confirmation returns to that source and appends the prerequisite-updated entry card there. Message normalization preserves explicit revision receipts and the entity-completion `developmentContinuation` action card; the latter carries a one-shot run target and is not session ownership. No historical session migration, title inference, or message-based target backfill is performed. | Read with `AiChatPanel/hooks/revisionSession.ts`, `hooks/sessionRuntime.ts`, `hooks/useChatSessions.ts`, `Frontend/src/main/stageSessions.ts`, main-process session normalization, and `docs/APPLICATION_REVISION_WORKFLOW.md` when changing chat history/session behavior, phase ownership, target binding, formal-revision recovery, or persisted message shape. |
| `Frontend/src/renderer/src/service/applicationStorage.ts` | Application index persistence and Electron-owned trashing. Full deletion delegates the prepare/trash/complete transaction to Electron, then clears renderer recovery caches and removes the index only after the IPC succeeds. Electron verifies the application and workspace identity through the backend handshake; prepare, trash, and completion failures propagate to the renderer. Index-only removal invokes none of these steps. Regression: `pnpm test:project-deletion` and backend deletion/lifecycle protocol tests. | Read with `WelcomeRecentProjects.tsx`, `chatSessions.ts`, `workbenchPhase.ts`, Electron `src/main/index.ts`, and Backend `protocols/application_deletion.py` when changing saved applications, full deletion, or client recovery caches. |
| `Frontend/src/renderer/src/components/ProjectPlanPageTreePreview/`                                            | Shared menu-tree preview component that converts `ProjectPlan.frontend_pages` into a visual hierarchy, including menu `unique_path` and leaf-page descriptions. The ProjectPlan confirmation surface reuses it inside a JSON-driven modular summary; Markdown remains the formal artifact but does not decide which confirmation modules are displayed. | Read before changing ProjectPlan confirmation modules, menu preview rendering, or tree-node conversion helpers. |
| `Frontend/src/renderer/src/service/applicationSettings.ts` and `hooks/useApplicationTheme.tsx`                | Renderer application-theme client and root Provider. It loads Electron `userData/settings.json` once before initial render, keeps theme state shared across welcome/planning/workbench/authentication surfaces, receives cross-window updates, and uses memory-only behavior in non-Electron browser development without localStorage fallback. | Read when changing application-wide appearance settings, initial theme loading, theme persistence, or cross-window theme behavior. |
| `Frontend/src/renderer/src/service/applicationLifecycle.ts`, `templateApi.ts`, `activeApplicationPlanning.ts`, `applicationPlanningWorkflowState.ts`, and `applicationPagePlanning.ts` | Dedicated lifecycle AG-UI client with template readiness orchestration, the canonical per-application Planning Current State, and the creation-planning Graph client. `activeApplicationPlanning.ts` owns the pure event reducer, monotonic lifecycle merge, transport/error-derived display status, and same-thread Workflow filtering; `applicationPlanningWorkflowState.ts` is the service-owned implementation of planning Workflow projection helpers, while the former Welcome component path is compatibility-only; `useActiveApplicationPlannings` is the single writable controller and exposes synchronous ref-backed state reads. Template readiness explicitly begins the template stage before asking Electron for structured clone results, submits them through `prepare_template_generation`, and finishes through the backend manifest gate; TechnicalPlan confirmation and the dedicated failed-template retry share the same per-application idempotent task without restarting the planning Graph. Welcome-page opening, application restart, and workbench entry do not auto-start readiness; an explicit retry may reopen only the failed template stage. Business stages come directly from the projected lifecycle; no old localStorage, application-index, or checkpoint stage migration is supported. | Read when changing creation planning state ownership, reducer events, confirmation, lifecycle AG-UI payloads, template download/readiness, homepage status, or workbench entry gating. |

- Planning current-state projection is intentionally prop-drilled as one `planningState` object from `AppEntryPage` through `WorkbenchPage` and `LeftPanel` to `AiChatPanel`/`MessageList`; do not add a parallel Context or split it back into independently rendered workflow/error/thread props. `ApplicationPlanningRuntime` owns transport execution and runtime guards; `ApplicationPagePlanningModal` only presents Canonical State and forwards business actions. `message.workflow` is history-only; `MessageList/planningMessageWorkflow.ts` selects `planningState.workflow` only for the explicitly identified current Planning card, and the right-side Planning documents also derive phase/loading/structured memory from that canonical workflow.
| `Frontend/src/renderer/src/service/workspaceTools.ts`                                                         | Frontend client helpers for backend workspace tools and approval approve/reject calls.                                                                                                                                                                                                                          | Read when changing workspace tool invocation or approval API usage from the UI.                                                                                                        |
| `Frontend/src/renderer/src/context/WorkbenchContext.tsx`                                                      | Shared workbench state for assistant messages published to editor panels.                                                                                                                                                                                                                                       | Read when changing cross-panel message flow.                                                                                                                                           |
| `Frontend/src/renderer/src/typings/`                                                                          | Shared TypeScript domain types for applications, workbench mode, and plans. `application.ts` includes the saved app metadata plus the app-creation JSON schema emitted by the new-app form, including `DatasourceEnum` and the built-in/external database connection contract. | Update when payload shape changes; never persist both `datasource.db.dbidMode` and `datasource.db.plantMode`. |
| `Frontend/src/renderer/src/constants/`                                                                        | Static workbench constants.                                                                                                                                                                                                                                                                                     | Read when changing mode/navigation constants.                                                                                                                                          |
| `Frontend/src/renderer/src/utils/`                                                                            | Class name helper, layout helpers, preview URL/open helpers.                                                                                                                                                                                                                                                    | Read for shared renderer utilities.                                                                                                                                                    |
| `Frontend/src/renderer/src/styles/global.less` and component `.less` files                                    | Global and component-scoped styling.                                                                                                                                                                                                                                                                            | Keep style changes near the component unless truly global.                                                                                                                             |
| `Frontend/scripts/verify-backend-resource.mjs`                                                                | Generic preflight check used by Windows/macOS Electron package scripts to ensure platform-specific `resources/backend/<platform>` contains the staged PyInstaller backend, `.env`, `_internal`, and `builtin_skills` directory. It intentionally does not duplicate Backend skill manifests.                       | Read when changing frontend desktop packaging or packaged resource structure.                                                                                                           |

## Generated Or External-Like Areas

| Path                                                        | Notes                                                                                                                                                                                                          |
| ----------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Frontend/node_modules/`, `Frontend/out/`, `Frontend/dist/` | Generated/dependency output; do not inspect for normal feature work.                                                                                                                                           |
| `Frontend/resources/backend/`                               | Generated platform backend staging output from `scripts/build-backend-win.ps1` and `scripts/build-backend-mac.sh`; contains PyInstaller support files and copied `.env`. Do not commit or inspect secrets.     |
| `Backend/build/`, `Backend/dist/`                           | Generated PyInstaller build output.                                                                                                                                                                            |
| `Backend/.venv/`, `Backend/.pycache/`                       | Environment/cache output; do not inspect for normal feature work.                                                                                                                                              |
| `var/workspaces/`                                           | Runtime workflow workspace artifacts such as generated specs, plans, JSON `build-task-plan.json` / `repair-task-plan.json`, reports, previews, and other local run output. Default workflow writes resolve here from the repository root. Do not treat as source unless a task explicitly asks to inspect a run artifact. |
| `.pnpm-store/`                                              | Package store; do not inspect.                                                                                                                                                                                 |

## Common Change Routes

| Task                                                   | Start here                                             | Usually also touch                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------ | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Add or change backend API route                        | `Backend/app/main.py`                                  | Runtime/tool module, frontend service caller, validation docs if behavior changes.                                                                                                                                                                                                                                                                                                             |
| Add or change workspace/file/git/terminal tool         | `Backend/app/workspace/workspace.py`                   | `Backend/app/main.py`, `Backend/app/tools/*` when exposed to DeepAgents, affected `Backend/app/agents/*`, frontend tool UI/service if exposed.                                                                                                                                                                                                                                                 |
| Change app-generation workflow phase order/routing     | `docs/WORKFLOW.md`, `Backend/app/graph/workflow.py`    | `Backend/app/graph/state.py`, matching `graph/nodes/*`, `graph/subgraphs/unit_testing.py`, `graph/subgraphs/testing.py`, `graph/subgraphs/acceptance.py`, `protocols/workflow/`, `protocols/workflow_visualization.py`, frontend `workbenchPhase.ts`, `WorkbenchTopBar.tsx`, `MessageList`, `WorkflowRunCard`, `AiChatPanel` acceptance dock, and workflow debug UI when node selection changes. The current workbench order is design → development (Build → unit_test → unit_test_repair) → test (integration/repair/review confirmation) → review (code review → acceptance phase confirmation) → acceptance (launch → acceptance review → finalize), with separate unit-test/test-phase/review/acceptance confirmations and independent development/test/review/acceptance sessions. |
| Change workflow state or persisted artifact references | `Backend/app/graph/state.py`                           | Matching node, `workspace/*_documents.py`, `domain/models.py`, `workflow.md` if design semantics change.                                                                                                                                                                                                                                                                                       |
| Change requirement confirmation in workflow            | `Backend/app/graph/nodes/requirements.py`              | `agents/main/requirements_analyzer.py`, `services/requirement_spec.py`, `services/entity_definitions.py`, `tools/ask_user.py`, and `workspace/spec_documents.py`. Requirement entities are display-only and never choose data sources; later product/technical/entity stages own those decisions. |
| Change product/technical planning or Endpoint API design/readiness | `Backend/app/graph/nodes/product_planning.py`, `Backend/app/graph/nodes/planning.py`, `Backend/app/graph/nodes/api_design.py` | ProductPlan owns visible behavior, UiManifest owns controls/local effects, TechnicalPlan owns API/schema, Endpoint identity, and Contract `entity_ids`, while Endpoint API Design owns each Endpoint's complete self-contained `fieldMappings`, database/external source snapshots, and business descriptions. Legacy EntitySourceBinding remains independent and outside normal Build readiness. |
| Change workflow workspace inspection or code graph scan UI | `Backend/app/graph/nodes/workspace_inspection.py`      | `services/workspace_inspector.py`, `services/code_graph/`, `workspace/workspace_snapshot_documents.py`, `protocols/workflow/`, `protocols/direct_modification.py`, frontend `ProcessSteps/WorkspaceInspectionPanel.tsx`, `processStepHistory.ts`, `Backend/app/graph/state.py`; preserve explicit user `workspaceRoot` boundaries, bounded `workspace_inspection.progress`/AG-UI projections, cache/incremental/full statuses, CRG node/edge distributions, 30-second file-search fallback, and no request-scoped navigation state, host absolute paths, source bodies, MCP server, or standalone graph endpoint. |
| Change workflow task DAG preparation                   | `Backend/app/graph/nodes/task_planning_adapter.py`     | `task_planning_inputs.py`, `agents/main/unit_task_prompt.py`, `agents/main/unit_task_rules.py`, `services/dag_planning_inputs.py`, `dag_planning_orchestrator.py`, `unit_generation_scheduler.py`, `unit_candidate_validator.py`, `scope_assembly.py`, `build_context_resolver.py`, `build_task_planner.py`, `build_task_progress.py`, and `workspace/task_documents.py`; preparation rechecks Endpoint API Designs and freezes PageImplementationContract, TechnicalPlan/API facts, mapping summaries, business descriptions, source snapshots, and authorization slices. |
| Change workflow code generation/build execution        | `Backend/app/graph/subgraphs/build.py`                 | `services/build_scheduler.py`, `services/build_repair_planner.py`, `services/build_tool_activity.py`, `agents/tool_activity_stream.py`, `agents/messages.py`, `agents/code_graph_guidance.py`, `agents/repair_planner/`, `agents/frontend/`, `agents/data_source/`, `agents/database/`, `tools/code_graph_context.py`, `services/database_execution.py`, `services/build_result_coordinator.py`, `workspace/code_changes.py`, `workspace/plan_documents.py`, `workspace/task_documents.py`. Frontend uses graph-first navigation with task-scoped file-search fallback; DataSource uses exact task paths, task-level source Skill routing, targeted contract/design context, and a standalone backend working-directory projection from the inspection-stage WorkspaceSnapshot; Database/RepairPlanner do not receive the graph tool. All code owners read real source before edits. Tool activity consumes root and delegated streams and falls back to root message chunks when values omit messages; code owner reports are verified against actual file changes, while database owner reports are verified by database execution evidence and may pause for approval before SQL writes. |
| Change workflow unit testing or integration quality gate | `Backend/app/graph/subgraphs/unit_testing.py`, `Backend/app/graph/subgraphs/testing.py` | `graph/workflow.py`, `graph/nodes/small_task.py`, `services/small_task.py`, `protocols/workflow/`, `services/integration_test_runner.py`, `services/frontend_performance_runner.py`, `services/frontend_project_launcher.py`, `agents/test_generation/`, `agents/repair_planner/`, `agents/small_task/`, `services/test_validation.py`, `workspace/test_documents.py`, `workspace/task_documents.py`, frontend `AiChatPanel/hooks/useTestReportPanel.ts` and `components/ProcessSteps/`; `unit_testing.py` owns Build Diff capture, TestGenerationAgent, generation validation, frontend/backend unit execution, the unit quality gate and per-check ten-attempt `unit_test_repair`. `testing.py` owns dependency installation, frontend/backend Build checks, temporary backend startup checks, frontend performance, integration quality gate and independent `small_task_repair`; it must not generate or execute unit tests. TestGenerationAgent always receives `unit_test_generation_context.code_diff` derived from the first Build snapshot; generated-test and repair changes merge into the development Diff before the test-phase confirmation. Unit and integration reports, next actions, repair plans and counters remain separate. Runtime progress uses `unit_test.checks` for the development matrix and `integration_test.checks` for the test matrix; the integration matrix has no unit-test rows. Internal integration data remains `test-report.json`; AG-UI exposes only normalized `testReportResult.reportPath` for `test-report.md`, and full Lighthouse HTML opens only through the existing Electron allowlist. |
| Change launch/preview and acceptance after tests pass | `Backend/app/graph/subgraphs/acceptance.py`, `Backend/app/graph/nodes/lifecycle.py` | `graph/workflow.py`, `services/backend_project_launcher.py`, `services/project_launcher.py`, `protocols/workflow/runtime.py`, `protocols/workflow/projection.py`, `protocols/workflow/lifecycle.py`, `protocols/workflow_visualization.py`, frontend `AiChatPanel`, `AcceptanceDecisionDock`, `WorkflowRunCard`, and workflow typings if launch metadata changes. Detect Maven projects from workspace-root `backend/Backend/pom.xml`; preserve backend-before-frontend ordering when detected, frontend-only launch with `launch_result.backend.status=skipped` otherwise, and project structure/backend/frontend/ready progress through AG-UI `summary/state.launchProgress`. The main graph mounts the compiled acceptance subgraph directly and the runtime enables subgraph namespaces so its custom launch events arrive before node completion; nested update/message frames remain private until the parent `acceptance` update. Preserve the `launch_result.backend` / `frontend` / `failed_stage` projection and the failure contract that copies the launch reason into all `preview_url` projections while status remains `failed`. The acceptance subgraph owns `launch_project → acceptance_review`; `page_acceptance` maps explicitly to lifecycle `PAGE_ACCEPTANCE`, the acceptance decision dock is positioned inside the preview pane so it never covers the application outline, and the current UI keeps accepted/finalize backend capability disconnected. |
| Change workflow debug UI                               | `Frontend/src/renderer/src/components/AiChatPanel/components/ChatComposer/index.tsx` | `Frontend/src/renderer/src/typings/workflow.ts`, `Frontend/src/renderer/src/service/agUiAgent.ts`, `Backend/app/protocols/workflow_request.py`, `Backend/app/protocols/workflow_visualization.py`, `Backend/README.md`.                                                                                                                                                  |
| Change AG-UI workflow chat behavior                    | `Frontend/src/renderer/src/service/agUiAgent.ts`       | `Backend/app/protocols/workflow/`, `AiChatPanel`, `ProcessSteps`, `WorkflowRunCard`, `processStepHistory.ts`, `chatSessions.ts`, workflow typings. API development uses `clarification.mode=api_design_required|api_design_confirmation`; independent `endpointDesigns.ts` handles `get/prepare/save`. Legacy EntitySourceBinding keeps its independent submission/terminal result. DAG, inspection, testing, tool activity, failure, and history projections remain shared. |
| Change free-conversation routing or Prompt wrappers   | `Backend/app/graph/direct_modification_workflow.py`   | `agents/direct_modification.py`, `agents/workspace_assistant/`, `agents/small_task/`, `graph/nodes/direct_modification.py`, `protocols/direct_modification.py`, shared testing/lifecycle nodes, frontend `service/agUiAgent.ts`, `conversationMode.ts`, `AiChatPanel.tsx`, `components/AiChatPanel/components/ChatComposer/`, and `useWorkflowConversation.ts`. Ordinary workbench input uses the default `/conversation/run` Coordinator route; the server classifies casual chat, workspace questions, clarification, implementation fixes, and formal revisions from one model JSON. It does not run an additional model to generate Contract Evidence, and the formal confirmation UI exposes only `reason`; deterministic routing still validates formal-artifact safety and output contracts. Keep the design-stage-only change entry on the planning Graph, optional current page/API target context, workspace-only direct writes, `implementation_fix_confirmation` before frontend/backend/fullstack writes, write-only lease acquisition, backend-first fullstack order, precise workspace paths, 4000-character summary, confirmation before formal Workflow, and main Workflow isolation. |
| Change revision version reminders or commits | `Backend/app/services/version_control.py`             | `protocols/version_control.py`, `docs/APPLICATION_REVISION_WORKFLOW.md`, frontend `service/versionControl.ts`, `typings/versionControl.ts`, `AiChatPanel/components/VersionCommitReminder/`, `MessageList`, and `CodeChangeCard`. Preserve final acceptance as the only success-trigger boundary, real Git status as authority, independent repository roots, fingerprint revalidation, rejection of existing staged work, exact selected paths, explicit user confirmation, failed-run review-only behavior, and AG-UI transport. |
| Change SmallTask repair routing or free-chat handoff  | `Backend/app/graph/nodes/small_task.py`                  | `services/small_task.py`, `services/small_task_scope.py`, `agents/small_task/`, `graph/workflow.py`, `graph/state.py`, `protocols/workflow/`, `graph/nodes/direct_modification.py`, and `useWorkflowConversation.ts`; preserve no direct `integration_test -> build`, max concurrency 2 (hard cap 3), exact-path diff attribution, user confirmation before scope/workflow escalation, and return to the explicit `repair_return_node` after local fixes. |
| Change workbench plan-execution mode or input locking  | `Frontend/src/renderer/src/components/AiChatPanel/components/PlanExecutionDock/` | `pages/AppEntryPage.tsx`, `hooks/useApplicationLifecycleStore.ts`, `AiChatPanel.tsx`, `hooks/useSessionRuntimeStore.ts`, `components/SessionSidebar/`, `planExecutionMode.ts`, `useWorkflowConversation.ts`, `service/agUiAgent.ts`, workflow typings, Backend `services/execution_resource_scope.py`, `protocols/workflow/lifecycle.py`, `protocols/workflow/request.py`, `run_control.py`, `runtime.py`, `workspace/run_lease.py`, and application lifecycle service/schema. Page executions persist their ProjectPlan-derived page/API/data-source resource set in `resourceLocks`; endpoint executions carry `buildExecutionScope.type=endpoint`, `targetId`, and `apiContractId`, persist endpoint/API/data-source/page dependency claims under `resourceLocks.endpoints` plus related groups, and prepare only the selected endpoint Unit. Endpoint `prepare_build_tasks` debug resumes reuse the API Contract ID from the snapshot or infer it only when the ProjectPlan match is unique. Failed Build controls use `buildSummary.recovery_available`/`recovery_task_ids` (with retry metadata retained for compatibility) and send `forwardedProps.workflowAction=retry_failed_tasks`; the action first retries transient failures and otherwise executes a ready RepairPlanner task set, while confirmation-required failures keep the confirmation path. If a public failure snapshot omits internal plans, the request adapter restores the persisted Build/Repair JSON plans from the current workspace before dispatch. The current phase treats locks as observation metadata only: overlapping claims and process-local leases do not block new runs, and the latest writer owns each displayed key. Every successful lifecycle write emits `application-lifecycle` before the related Workflow projection; the application-entry store merges events and cold/reconnect calibration by revision and supplies the composer, page outline, API outline, and return-home guard without polling the file. The application-entry session runtime provider and mounted workbench remain alive while the welcome page is visible, so AG-UI streams and in-memory UI state continue without persistence-based recovery. Continue uses the old runId only as a validated same-thread/same-target recovery token while live AG-UI state and the LangGraph checkpoint remain the current-thread Graph source. Only replace the bottom composer visually; preserve all other workbench regions and keep product actions on AG-UI. |
| Change per-message Workflow skill selection            | `Frontend/src/renderer/src/components/AiChatPanel/components/ChatComposer/` | `skillSelection.ts`, `useSessionRuntimeStore.ts`, `useWorkflowConversation.ts`, `service/agUiAgent.ts`, `service/chatSessions.ts`, Electron `src/main/index.ts`, Backend `protocols/workflow/`, `services/user_skill_runtime.py`, and `agents/registry.py`. Preserve AG-UI transport, message snapshots, resume conflict checks, whitelist isolation, and forced prompt loading. |
| Change user-skill catalog, enablement, editor, ZIP import, or runtime injection | `Backend/app/services/user_skills.py` | `services/user_skill_settings.py`, `services/user_skill_documents.py`, `services/user_skill_imports.py`, `services/user_skill_runtime.py`, `services/builtin_skills.py`, `protocols/user_skills.py`, `agents/registry.py`, `agents/workspace_scope.py`, frontend `service/userSkills.ts`, `components/SkillsPage/`, `AiChatPanel`, and user-skill typings. Keep client preflight aligned with authoritative backend checks, fail-closed settings, and immutable per-run Agent snapshots. |
| Recover old requirement planning behavior              | `Backend/app/deprecated/agents_requirement_planner.py` | Port only the needed behavior into workflow nodes or services; do not re-import deprecated files into main flow.                                                                                                                                                                                                                                                                               |
| Recover old orchestration/verification behavior        | `Backend/app/deprecated/graph_orchestrator.py`         | Port only the needed behavior into `graph/workflow.py`, `graph/nodes/`, or `graph/subgraphs/`; do not re-import deprecated files into main flow.                                                                                                                                                                                                                                               |
| Change Development Contract payload                    | `Backend/app/domain/development_contract.py`           | `Frontend/src/renderer/src/typings/developmentContract.ts`; deprecated planner/orchestrator files are reference-only.                                                                                                                                                                                                                                                                          |
| Change local chat history                              | `Frontend/src/main/index.ts`                           | `stageSessions.ts`, preload typings, `sessionMessageNormalization.ts`, `chatSessions.ts`, `AiChatPanel`, `hooks/sessionRuntime.ts`, `hooks/useSessionRuntimeStore.ts`, `hooks/revisionSession.ts`, and `SessionSidebar`. Every current session persists `workflowId` and `workbenchPhase`; DESIGN/PLAN/DEVELOPMENT sessions additionally require mapped `stage`, per-Workflow/Stage `sequence`, immutable `entryKey`, and a distinct AG-UI `threadId`. Newly created page/API DEVELOPMENT sessions also persist an immutable `developmentTarget`; free sessions remain unbound. The application-entry SessionRuntime owns an in-memory single-run registry keyed by workspace/application/phase without splitting frontend/backend: `starting`, `running`, and `stopping` occupy that phase; the owner keeps its execution controls, while other history sessions remain viewable but replace the composer with a read-only notice and disable new-session actions. AG-UI terminal completion/error/confirmed stop releases the phase immediately, so any viewed history session may send the next run; initialization planning's independent AG-UI running snapshot participates in the same UI rule. Selecting a target-bound history session restores its page/API context; target changes create a new session, and no historical session is migrated or inferred from titles/messages. Temporary chat remains an unpersisted overlay without a target. Formal revisions retain the original planning Graph thread only in technical `revisionContext`; DESIGN→PLAN uses a separate visible thread and source receipt, while PLAN→DEVELOPMENT returns to the exact source DEVELOPMENT session/thread, appends a static prerequisite-updated entry card after its existing history, and lets that same thread own the automatic continuation, scan, DAG, and execution output. Recovery matches the current revision identity and fails closed if the original development session is absent; failed design/plan stage startup removes only an unentered phase session with no durable source receipt before returning to the source stage. Ordinary top-bar browsing creates nothing. Environment session directories use normalized absolute `workspaceRoot`; permanent managed-project deletion removes them with renderer caches, index-only removal preserves history, and deleting an unfinished design-stage plan moves its `.xcodeagent` data and session root to the OS trash. |
| Change saved applications/workspace open flow          | `Frontend/src/renderer/src/components/Welcome/`        | `WelcomePage.tsx`, `WelcomeRecentProjects.tsx`, `applicationStorage.ts`, Electron workspace/app storage IPC, `Frontend/src/main/managedWorkspace.ts`, `Frontend/src/renderer/src/typings/application.ts`. The application index is the single welcome-page list and includes new applications immediately, regardless of their current design, planning, or development stage; orphan session workspaces never become implicit applications. Clicking any indexed application opens the workbench, while lifecycle recovery selects the current phase without acting as an entry gate. Adding a local folder still validates a real non-symlink `.xcodeagent` directory and a readable `application.json` before creating the index. Index-only and managed-project deletion use the same UI at every stage, and creation has no unfinished-plan count limit. |
| Change new-app planning or lifecycle recovery | `docs/PRODUCT_UI_TECHNICAL_PLANNING.md`, `docs/APPLICATION_LIFECYCLE.md` | Start with `domain/application_lifecycle.py`, `domain/application_planning_interaction.py`, `services/application_lifecycle.py`, `graph/application_planning_workflow.py`, `graph/application_planning_interrupts.py`, `graph/nodes/requirements.py`, `graph/nodes/product_planning.py`, `graph/nodes/ui_confirmation.py`, `graph/nodes/planning.py`, `protocols/workflow/runtime.py`, `protocols/application_page_planning.py`, frontend `service/applicationPlanningRuntime.ts`, `hooks/useApplicationPlanningRuntimes.ts`, `hooks/useActiveApplicationPlannings.ts`, `AppEntryPage.tsx`, `ApplicationPagePlanningModal.tsx`, `ApplicationPlanningQuestionPanel.tsx`, and workflow typings. RequirementSpec and ProductPlan are one jointly confirmed requirement document: ProductPlan may consume only the validated RequirementSpec draft in that node; both Markdown/JSON pairs are atomically promoted and hash-bound before UI can run. Do not create a requirement-document manifest. Preserve explicit action-driven node decisions, per-thread atomic resume validation, the optional UI skip, stale-card rejection before started projection, formal Markdown synchronization, and server-checkpoint authority. TechnicalPlan confirmation, not UI confirmation, moves initialization to template generation. |
| Change product-stage bottom conversation or artifact rollback | `docs/DESIGN_CONVERSATION_AGENT.md`, `Backend/app/graph/application_planning_revision.py` | `graph/application_planning_workflow.py`, `agents/design_conversation/{models,router,fallback,policy}.py`, `protocols/application_page_planning.py`, shared RequirementSpec/ProductPlan/UiDesign/TechnicalPlan nodes, workflow request/projection/runtime, `service/applicationPlanningRuntime.ts`, `hooks/useApplicationPlanningWorkbenchBridge.ts`, `AppEntryPage.tsx`, `ApplicationPagePlanningModal.tsx`, `AiChatPanel`, `MessageList/productConversationPresentation.ts`, and `ChatComposer/productConversation.ts`. Preserve the phase-first Product Agent boundary before and after `ready_for_workbench`, Coordinator semantic classification plus the three-pair node whitelist, lifecycle-authoritative formal revision targets, explicit non-mutating result projection, review-card identity by artifact/gate/revision, zero mutation for non-whitelisted semantics and completed-product reply rounds, the original planning thread/checkpoint, latest gate/revision CAS, single-writer planning runs, existing-artifact incremental updates, and every downstream formal confirmation gate. |
| Change workbench page/API/entity artifacts, selection, or application development planning | `AiChatPanel/hooks/useDevelopmentArtifactDetail.ts` for artifact browsing; `docs/APPLICATION_DEVELOPMENT_PLANNING.md` for formal development actions | The right-side “开发产物” tab contains a persistent menu and adjacent detail region; API leaf selection keeps `apiContractId + endpointId` and reads the current formal mapping through the independent `/endpoint-designs/run` AG-UI flow, while page/entity browsing retains its existing behavior. Endpoint design actions open `ApiDesignConfigModal`; page/API development enters `api_design_readiness_gate`, which first shows the missing list and only opens the same modal after the user clicks a target, then requires explicit confirmation; entity actions retain the independent legacy binding flow. Read `AiChatPanel`/`DevelopmentArtifactsPanel`/`ApplicationOutline`, `EndpointDesignResult`, `service/endpointDesigns.ts`, workflow hooks/request protocol/API-design node/build-context resolver, and legacy EntitySourceBinding services for formal actions. |
| Change frontend preview                                | `AiChatPanel` preview actions                          | `BrowserPreviewPanel`, `utils/previewUrl.ts`, Electron browser IPC.                                                                                                                                                                                                                                                                                                                            |
| Change shared domain types                             | `Frontend/src/renderer/src/typings/`                   | All service/UI/backend payload producers that consume the shape.                                                                                                                                                                                                                                                                                                                               |

## Validation Routes

### Endpoint API design and readiness

- `Backend/app/services/api_design_schema.py` resolves local references and `allOf` for API-design leaf enumeration, merging nested properties and required constraints. `business_acceptance.py` keeps TechnicalPlan entity semantics separate from Endpoint field mappings, while projecting one-sentence business descriptions to Controller/ApplicationService checks.
- `Frontend/.../WorkflowRunCard/ApiBusinessDescriptionEditor.tsx` edits one concise sentence for either a Request or Response field. `apiDesignTableModel.ts` and `apiDesignSerialization.ts` keep `source_mapping`/`business_description` records, source cleanup, and validation pure; draft-only `unconfigured` rows cannot be confirmed and no executable expression is persisted.
- `Backend/app/graph/nodes/api_design.py` owns the `api_design_readiness_gate` used by page/API development. `Backend/app/services/endpoint_design_detail.py` and `Backend/app/protocols/endpoint_designs.py` provide independent AG-UI `get/prepare/save` actions; save validates the complete mapping, checks `baseRevision`, writes the JSON/Markdown pair and invalidates an old Build DAG. Page confirmation rechecks every displayed Endpoint revision, while Endpoint confirmation rechecks its single revision. The retained EntitySourceBinding path is legacy-only and is not a prerequisite for normal Build.
- `Frontend/src/renderer/src/components/AiChatPanel/components/WorkflowRunCard/ApiDesignConfigModal.tsx` hosts the independent editor while `ApiDesignPanel.tsx` edits the optional Endpoint-level implementation description. `ApiFieldMappingTable.tsx` renders the Request/Response seven-column mapping table and opens `ApiFieldMappingEditor.tsx` for direct, read-only template-entity, or one-sentence-description mapping; `ApiSourceFieldSelector.tsx` owns automatic database/Operation metadata loading and restores table/column metadata for existing mappings; `apiDesignTableModel.ts` keeps row projection and self-contained field-mapping edits pure. Scene entities are copied only from read-only TechnicalPlan templates. `ApplicationOutline` and `planningArtifactStatus.ts` expose `pending`/`confirmed`/`stale` from the current pair, TechnicalPlan fingerprint, and template-copy validity. Saving the configuration never starts development; the gate rechecks the mapping and asks for explicit confirmation before the full chain, while page development continues to report all missing Endpoint designs in one readiness result.
- `Backend/app/protocols/endpoint_designs.py` and `Backend/app/services/endpoint_design_detail.py` expose the independent `/endpoint-designs/run` AG-UI `get/prepare/save` actions. `Frontend/src/renderer/src/service/endpointDesigns.ts` consumes its lifecycle and `EndpointDesignResult` renders the same formal JSON projection in the right-side artifact detail and the gate confirmation card. A missing gate lists every unresolved Endpoint with per-item configuration plus a workflow “确认” action; the action submits the detection request only after the user clicks it. The gate stores the complete confirmed mapping set on the original workflow message; completion, stop, failure, or later mapping edits do not overwrite that historical snapshot.

### Entity binding development continuation

- `Backend/app/services/development_continuation.py` owns the server-recorded original page/API target, binding validation and readiness/token issuance. Request parsing validates without consumption; `services/application_lifecycle.py::start_workbench_execution` atomically consumes the token and replaces the original execution.
- `Backend/app/graph/nodes/planning.py::entity_source_binding` and its continuation protocol remain for the independent old entry only. Normal page/API development never routes through it and never consumes `developmentContinuation`.
- `Frontend/.../EntityInfoPanel` and legacy EntitySourceBinding cards remain usable, but explicitly label that their result does not affect Endpoint API design, development readiness, or Build context.
- Regression: `Backend/tests/test_development_continuation_stream.py` drives real Graph checkpoints and final AG-UI frames for page/API targets with multiple entities; `test_development_continuation.py` covers atomic startup failure and one-time consumption; `Frontend/tests/workflowPreview.test.ts` covers ready and partial-confirmation cards.

| Change type                                   | Expected validation                                                                              |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| Documentation/instruction/index only          | No backend health, Vite check, build, or compile required unless explicitly requested.           |
| Frontend TypeScript/UI/Electron behavior      | `pnpm build` from `Frontend`; if a dev server is running, check the active Vite URL.             |
| Windows Electron package with bundled backend | Run `scripts/build-backend-win.ps1` on Windows first, then `pnpm build:win:dev` from `Frontend`. |
| macOS Electron package with bundled backend   | Run `scripts/build-backend-mac.sh x64` or `arm64` on the matching macOS host, then the matching `pnpm build:mac:<arch>:dev` from `Frontend`. |
| Backend Python behavior                       | Focused `python3 -m py_compile` or narrower test for changed Python files, plus `/health`.       |
| Backend API/agent behavior                    | `/health`; add focused endpoint/manual checks when behavior changes.                             |
| Application-level database credential encryption | From `Backend`, run `.venv/bin/python -m pytest tests/test_database_credentials.py tests/test_entity_design.py tests/test_entity_detail_plan.py` plus focused `py_compile`; from `Frontend`, run `pnpm test:database-crypto`, focused ESLint, and `pnpm build`; then verify `/health.database_encryption` contains only public metadata. |
| User-skill catalog, enablement, runtime, and ZIP import | From `Backend`, run `python3 -m unittest tests.test_user_skill_settings tests.test_user_skills tests.test_user_skill_documents tests.test_user_skill_imports tests.test_user_skill_runtime tests.test_builtin_skills` plus focused `py_compile`; from `Frontend`, run `pnpm test:chat-skills`, `pnpm typecheck`, and `pnpm build`; then check `/health` and the active Vite URL when present. |
