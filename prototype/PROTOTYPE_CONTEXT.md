# 工作台原型领域规则

本文档描述 `prototype/` 演示工程当前采用的工作台状态、对话关系、初始化规划、正式产物审阅、Workflow 边界和测试准备规则。它是改版后原型的统一规则，不作为正式工程已经实现的架构声明。

## 一、本次重构结论

### 数据来源与绑定映射（2026-09-11）

- 左侧窄功能栏「数据来源」打开与任务管理同层级、同宽度的互斥抽屉，交互与任务管理抽屉完全同构：顶部复用同一套 Tab 栏按「数据库 / 外部 API」分页（下划线选中态 + 数量角标，右侧挂新增按钮），列表行只展示名称与接入标签，不做目录 + 详情双栏；数据库行内不展示连接字符串，直连行带“密码已配置/缺少密码”标签（对齐原工程）。
- 编辑弹窗复刻原工程数据库连接表单：连接模式三选一（内置数据库 / DBID / 本地直连），非内置模式填写 数据库地址、端口、Schema、用户名，DBID 模式追加 DBID 字段、直连模式追加密码（留空保持不变），新建内置模式提示无需外部连接信息；编辑已有连接时连接模式锁定不可改（改模式会让已填参数失效），连接明细仍可修改，并就地提示“连接模式在创建后不可更改”；外部 API 弹窗只维护 名称 + 域名/Base URL。表、接口、字段候选由实体开发时按需发现。删除有二次确认，外部 API 提示级联删除的目录与接口规模；直连提供模拟“检测连接”。目录通过 `devagentstudio:prototype:data-sources:v4` 浏览器缓存跨面板同步（结构升级即换键弃旧缓存）。弹窗浮层 Portal 到 body，主题变量经 `global.less` 的 `body:has(...)` 桥接（`.wb-light-tokens`），否则 `--wb-*` 解析不到会渲染出透明弹框。
- 实体的实现结构升级为“计划意向 → 开发绑定”两段：计划阶段在技术规划方案“实体”子 Tab 中为每个操作记录实现类型与来源级意向（不选表/接口、不做字段映射）；开发阶段在“开发产物 → 实体 → 方法”中完成表级/接口级绑定，保存后 AI 自动推导字段映射（能确定的自动填充，疑似歧义的字段置为待确认），用户逐个确认歧义字段后“确认绑定”。意向阶段没有确认门，绑定确认以“全部字段已匹配”为前置条件。开发产物目录不设文件夹级：实体直接罗列（BlockOutlined 图标），展开下一级即方法清单（统一术语「方法」，内置方法用锁形图标表达平台锁定模板、需求自定义用函数 ƒ 图标；点实体行 = 选中并切换方法清单展开态；新增入口收在实体行尾悬停显现的省略号下拉菜单里——行内兄弟节点而非浮层，悬停时替换状态圆点；删除方法在工作台方法头部且仅自定义可删）；右侧由目录选择驱动，两页各司其职，且**面板只读呈现设计定稿、不在面板内编辑**：**实体页（字段维护）**头部是只读数据来源卡——展示已确认的类别标签（数据库/外部 API/混合）与库表绑定行，数据库类别附实体字段↔表字段对应表（同义自动对应、疑似与无关标待确认），并注明内置增删查改方法由平台按表结构模板生成；底部注明“绑定在实体开发工作流中确认，需要调整时在对话中重新发起”。**方法页（方法定义）**呈现出入参契约（操作输入/返回字段/页面使用以 chip 结构化展示）+ 数据实现与字段绑定工作台（ImplementationPanel：实现类型→绑定目标→AI 自动匹配→逐字段确认→确认绑定门）。数据来源类别模型：纯 DB 单表走模板内置方法，纯外部 API 按业务自定义方法逐个绑定固定契约（出参翻译 + 入参适配），混合/多表联查走高级自定义（SQL/Java 编排）；对象来源的“对话交互卡片式调整工作流”与入参槽位结构化适配为下一步。
- 演示案例配套：需求说明书中的实体新增 `used_by_pages` 页面关联，设计阶段即可看到“页面通过 实体.操作() 使用业务能力”；技术规划方案逐个展示实体，并把“业务字段 / 平台内置操作 / 需求自定义操作”分区。开发阶段的产物、计数和发起器只包含页面与实体，不把内部 Endpoint 当作用户产物。实体演示缓存升级为 `business-objects:v7`（结构变化后旧缓存自动重建）。
- **2026-09-12 术语统一：** 组长反馈“业务对象”概念不合适——外部 API 仍需映射绑定，绕不过去；用户投影全部改称「实体」，技术规划方案二级页签调整为「架构 / 实体 / API 契约 / 页面 / 权限」，实体操作面板中的「页面如何使用」小节同步改称「页面」。代码内部组件命名（BusinessObjects/*）、事件名与缓存键前缀保持不变；缓存键版本升到 v7，让旧演示缓存按新文案重建。
- **2026-09-12 文案口径：** 计划阶段进入下一阶段的门禁按钮写阶段名（「进入计划阶段」/「进入下一阶段」），计划产物文档统一称「技术规划方案」——确认卡（确认技术规划方案 / 确认技术规划方案并生成模板）、右侧面板标题、应用文件条目、Markdown 文档标题、生成提示与收尾句全部对齐；「技术规划工作流」与启动语「开始技术规划」保留活动口径。
- 演示方法规模收敛：回检单只保留 5 个平台内置方法 + 2 个需求自定义方法（查询我的回检走数据库绑定、查询审核人信息走外部 API 绑定），需求说明书 business_operations 同步裁剪。
- **2026-09-12 实体开发工作流走通：** 此前实体发起后只挂一条静态轨迹（提示去右侧绑定但右侧只读），旅程走不下去。现在实体与页面/接口同为真实可续跑的工作流剧本（`replayEntityWorkbench`；DAG 新增 development/entity 段：读取实体结构 → 绑定操作的数据实现 → 生成数据适配逻辑，另接通用「确认代码变更」，共 4 步）。交互遵循「过程互动在对话区、右侧只静态呈现」：发起后在对话区待输入节点上内嵌「确认数据实现绑定」卡（`EntityBindingCard`，逐操作列出 内置/自定义、实现类型、绑定去向、字段规模），单按钮「确认绑定」一次性把全部意向落实为结构化绑定与字段映射（`withConfirmedBindings`：已有绑定保留、数据库类别回落实体级表绑定、外部服务按意向来源名在目录中落位；字段映射 AI 自动匹配 + 公共前缀/后缀推荐回填，确认后全部就绪）并写回 BusinessObjects 缓存——产物树、发起引导、实体工作台随即静态刷新为已绑定/已完成，无需任何右侧编辑。确认后当场分帧生成 `backend/entity-adapters/<id>-entity-adapter.java` 数据适配层（数据库绑定产出 SQL、外部服务产出契约调用与出参翻译），右侧源码区逐帧跟随；接受 Diff 后工作流收口，Agent 只留一句收尾。同步单通道执行、不进任务池。
- **2026-09-14 实体状态按版本隔离 + 状态口径对齐原工程：** 实体绑定缓存原先是应用级（`business-objects:v7`），上一迭代确认的绑定会泄漏进新迭代——一进开发阶段实体就已是完成态。现缓存键升为 `business-objects:v8` 并加入版本 ID：每个迭代从需求意向重新开始，已生成版本的只读回看保留该版本自己的绑定结果；读写双方（开发剧本 `options.application.currentVersionId`、AiChatPanel `versionViewKey`、产物目录/实体工作台 `currentVersionId`）共用同一把键。同时把产物实施状态徽标文案从原型自造的「待实施/实施中/已交付」对齐真实工程与用户记忆中的「未开始/进行中/已完成」（真实工程 `developmentStatusText` 用 未开发/开发中/已初次完成、任务用 待开发/进行中/已完成，全库无「实施」口径）；受影响面：发起引导卡、输入框产物弹窗、禁用原因与轨迹文案；内部状态机不变（not-started/in-progress/completed），产物目录树仍用状态圆点。
- **2026-09-14 工作流消息纯度（长期纪律，无例外）：** 同一条工作流消息里不允许出现任何纯文字正文——启动、生成中、暂停、完成全部如此。口径与页面工作流对齐：**启动引导由交互卡自身承载**（模板卡/绑定卡的 message 字段），剧本不发 `onContent`；**操作指引一律写进载体**——节点 step.detail、确认卡内文案、文件改动授权条，而不是消息正文；**完成态与暂停态都不追加收尾/提示文本**。页面与实体剧本曾各留一句「代码已生成，请在右侧源码区确认 Diff 并接受」的暂停态指引，已删除（指引在 `confirm_changes` 步骤 detail 与授权条中同样存在）；验收剧本「无待验收任务」的无效入口答复改为纯文本回复、不挂工作流卡。新增剧本一律照此办理，`replay*` 内出现 `onContent` 即视为违规（唯一例外：不挂工作流卡的纯答复文本）。
- **2026-09-14 开发完成门禁改按用户产物判定 + 移除调试/Trace：** ① 开发→测试准入门（`allDevelopmentModulesComplete`）原先要求「页面 + 全部 API 契约端点」完成；去接口化之后端点产物永远无法交付，门禁和「开发阶段的产物已全部完成」弹框就再也不弹。现改为只判定**用户产物**：全部页面完成 + 全部实体完成（绑定确认即视为实体交付）；API 契约只是规划产物，不参与开发完成判定。② 移除继承自原工程的「Workflow 调试」（bug 图标，从指定节点续跑/分层 DAG 范围）与「Trace 日志」（LangSmith 观测事件面板）两个输入区工具及其全链路逻辑（`WorkflowDebugOptions` 类型、`workflowDebug` 在 options/forwardedProps/直接修改模式中的穿透、`WorkflowTraceLog` 组件与配套样式）；`buildExecutionScope` 因接口续跑仍在使用而保留。
- **2026-09-14 只读版本视图不再泄漏阶段门禁弹框（旅程验证修复）：** `pendingGateWorkflow` 原先只看 `summary.status === 'requires_user_input'` + clarification.mode，不检查 clarification 自身的 status——用户提交确认后历史消息里的门禁卡仍带着 `requires_user_input` 的 summary 与 `submitted` 的 clarification，切到只读历史版本时跨会话扫描把它当成待处理门禁，自动弹出「设计阶段完成/进入计划阶段」弹框（预置应用不受影响，其静态会话无挂起门禁；用户应用发布后回看必现）。现对齐 `sessionRunBlocksConversationCreation` 的既有纪律：只认 `clarification.status === 'requires_user_input'` 的卡；另为规划/开发两个准入门的自动弹框 effect 补上 `versionReadOnly` 守卫（测试/审查门本就有）。
- **2026-09-14 回退/迭代后的规划产物检查改用当前工作版本号：** `WorkbenchPage.syncWorkspaceFiles` 原先用冻结的 `application` prop 里的 `currentVersionId` 调 `inspectPlanningArtifacts`——回退或发起新迭代后 prop 仍是旧版本号，预置应用的完成态判定（`app-pms-new` + `v1-3` → 已设计基线）会在窗口聚焦/规划刷新时把 v1.3 的"已设计"页面泄漏进新迭代。现改用 `workspaceApplication.currentVersionId` 并纳入 effect 依赖，新迭代一律取待开发基线。
- **2026-09-14 版本域清理（不改变旅程）：** ① 移除 `versionPreviewOnly` 死 prop（WorkbenchPage → LeftPanel → AiChatPanel 全链路无消费分支，连同两处描述不存在行为的过期注释）；② `createRollbackVersion` 与 `createIterationVersion` API 一致化——lifecycle 由调用方显式传入（原实现复制历史版本 lifecycle 后在唯一调用点被立即覆盖，具误导性）；③ `createInitializationPlanningRecord` 的版本 lifecycle 读取补可选链；④ 欢迎页 `saveApplication` 工作区去重改为规范化比较（统一分隔符 + 忽略大小写），与 mock 层「目录已被应用占用」拦截同一口径，防大小写差异绕过去重。
- **2026-09-14（第二批）全旅程复验 + 剧本/面板代码精简：** IAB 真实点击复验「新建应用」与「版本回退」两条旅程全程走通，首批两处硬伤修复无回归。精简重构（不改变任何交互）：① `workbenchShared` 新增 `step`/`stepRecord` 步骤构造器，替换两个剧本 45 处 8 行 `ProcessStepRecord` 字面量；② 新增 `streamCodeFrames` 统一页面/接口/实体/审查报告 4 处分帧生成循环（含 `finishedSources` 累积与 `ChangeSource` 类型收编）；③ AiChatPanel 顶部 421 行纯函数独立为 `panelHelpers.ts` 可测模块；④ `useChatSessions` 的 5 个同构阶段默认会话函数合并为 `ensureKindSession(kind, titles)`、页面/接口会话查找合并为 `openExistingArtifactSession`；⑤ `inferEndpointContextFromMessages` 消除 hook/service 双实现；⑥ WorkflowRunCard 14 处禁用表达式收拢为 `actionDisabled`。净减约 750 行重复逻辑（源码总量 -326 行）。
- **2026-09-14（第三批）顶部比值显示口径 + 预置 v1.3 完成态基线：** ① 顶部「开发产物 N/M」与测试用例数量芯片的显示口径改为技术规划方案确认后（`planCountersVisible` 白名单：generating_application_template_files / awaiting_development_entry / ready_for_workbench），原先误传「测试准备已启用」口径（UI 设计阶段起就显示 0/3），违反「确认前两个数字均不出现」规则；后台用例准备任务的启动时机不变，只约束可见性。② 预置应用 v1.3 补齐完成态基线，打开即呈现与真实走完旅程一致的终态：**开发产物 3/3 + 测试用例 6/6**（此前 2/3 与 0/0，与 lifecycle 全 passed 矛盾）。实体侧：`BusinessObjects/store` 对预置完成版本在缓存未命中时按「确认绑定」同一权威路径（`withConfirmedBindings` + 数据来源目录）落完成基线并写回缓存；用例侧：`backgroundTasks` 新增 `preloadCompletedTestCaseTasks`，preload 启动时为 `app-pms-new-v1-3` 预置 6 条就绪终态的用例任务（照抄新建应用剧本的用例蓝图，幂等跳过已有任务，显式触发 store 惰性恢复防止覆盖上一会话流水）。③ 顺带修掉一个被它掩盖的定位漂移：`getApplicationLifecycle` 的 get 请求携带 `versionId`（currentVersionId），mock 侧对预置完成版本恒返回发布完成态 lifecycle——此前同应用其它版本的运行实验记录（如 v1.4 的 analyzing_requirement）会劫持 v1.3 锁定视图的阶段定位，进而连带影响比值口径。
- **2026-09-14（第四批）重复块数据化清剿：** 用滑窗哈希脚本对全工程做重复块检测，按数据清剿复制粘贴：① `SettingsPage` 四个环境变量下拉（Form.Item + Select + 「没有找到？去环境变量」脚注）收敛为 `envSelectField` 同构封装；② `ProcessSteps` 用例授权/产物验收/执行方式选择/实体绑定四个完全相同的内嵌 `WorkflowRunCard` 分支合并为单一 `awaitingEmbeddedAction` 分支；③ `useWorkflowConversation` 六处 `persistSession` 身份字段展开收敛为 `persistSessionFor`；④ `agUiAgent` 合并 `readWorkflowFromState`/`readResultWorkflow` 双胞胎、收敛四个无外部消费者的导出；⑤ mock 剧本页面/接口同步执行段（构建计划 → 分帧生成 → 挂确认节点）收敛为共享编排 `runForegroundBuild`（页面/接口各自提供 exec 域，逐行核对行为等价）。本轮净精简约 200 行。样式层（19684 行，占源码四成）经三重审计确认无可删项：零孤儿文件（4 个未被 ts 直接 import 的 less 均经 less `@import` 链生效）、零死规则（唯一命中 `styles/global.less` 的 `.wb-light-tokens()` 为浅色主题桥接 mixin，误报）、跨文件重复仅 15 组 2-3 处 ×6-12 行属性的小片段（合计约 200 行，均为同类 UI 元素带微差的平行声明，如 SessionSidebar 三种行条目的图标按钮——合并需引入 mixin 参数化，可读性与视觉回归代价高于收益，不建议动）。两套后台任务系统（async/tide）亦确认非代码重复：`backgroundTaskEngine` 单引擎带两份节奏常量表（异步 1.2s/5s、潮汐 6s/26s，潮汐刻意更长以呈现"开发完成而用例仍在排队"的演示效果），`BackgroundTaskDrawer` 单组件渲染双 Tab，差异仅文案与算力口径。已知待办（有意未做，风险/收益比不划算）：WorkflowRunCard 九处卡壳 JSX 的壳组件化（约 -100，触面大）、`sendWorkflowMessage` 主通路拆分（约 -150，中高风险）、`RequirementAuthorizationEditor` 受控页面/操作双 section 合并（约 -37）。**待复核问题：刷新重开 iterating 迭代（record 已到 ready_for_workbench）时，阶段条定位回「设计阶段」而非开发阶段，持久化事实正确；早前同类刷新场景（released 版本）定位正常，疑似既有恢复定位口径问题，非本批改动路径。**

- **2026-09-14（第五批）存量应用 v1.3 改为"验收完成待发布"的当前迭代：** 演示剧本原先把 v1.0-v1.3 四个版本全部置为 released，缺少"已生成历史 + 最新迭代并存"的混合状态。现改为：**v1.0-v1.2 保持 released 并补齐 `gitRef`（码云提交+Tag 模拟值）与 `artifactSummary`（deployableScript），与"生成版本"流程真实写入的字段同构；v1.3 改为 `iterating` 当前迭代**——lifecycle 停在全部阶段通过的验收完成态（`makeCompleteLifecycle`），releasedAt/description/gitRef/snapshot 留待生成版本时才写入。打开存量应用即定位在验收阶段：六个阶段全部可点击，静态回看任一阶段历史会话与产物样貌；「生成新版本」按钮立即可点——"审查通过自动弹出生成版本弹框"仅对本会话内推进到可生成的旅程生效，进入时即已可生成的迭代（预置快照）视为已消费、不抢弹，避免开门即弹打断阶段回看。点击走 打包→提交码云→打 Tag 把 v1.3 落为最新已生成版本——版本下拉随即呈现"最新版本 vs 已生成版本"的对比，还可继续发起新迭代演示完整版本链。配套口径：会话镜像保留全阶段历史（含测试 6/6 通过、审查完成可生成版本）；实体完成基线保留（开发产物 3/3，缓存键 v9）；用例预载 6/6 就绪（`preloadCompletedTestCaseTasks` 更名 `preloadPresetTestCaseTasks`，`readyCount` 控制就绪条数；任务流水键 v2→v3）；lifecycle `get` 的静态基线特判移到"运行期注册 lifecycle"之后——注册态优先（同会话推进过的旅程不因重进工作台丢失），静态基线兜底冷启动并继续防同应用其它版本遗留记录劫持阶段定位。
- **2026-09-14（第六批）全阶段回看的布局口径与门禁静默：** v1.3 验收完成态要支撑"六阶段任一切换、静态回看"，走查发现三类破坏回看口径的问题并修复：① **分阶段右侧布局默认值**——阶段切换 effect 中审查分支漏设布局，验收带入的全宽残留到审查；现审查补 `setRightPanelLayout('split')`，形成"验收=全宽预览、其余五阶段=分栏"的默认口径，验收 entry effect 离开时清记账、重进即恢复全宽。② **门禁弹框按旅程位置静默**——"开发完成进入测试""测试通过进入审查"两个门禁的自动弹出只看阶段与产物状态，不看旅程是否已越过该门：回看开发阶段时弹"进入测试"、回看测试阶段时弹"进入审查"；现两个 effect 补 `reachedPhase` 守卫（已到达 testing/review 及之后不再自动弹），顶部手动再唤起路径不受影响。③ **审查工作流回看重放**——`autoStartReview` 在"处于审查阶段+测试已通过"即自动发送"开始代码审查"，对审查历史已完成的 v1.3 会在回看时重放一遍审查工作流并挂出待确认的报告 Diff；现以"审查报告已随审查会话落盘（`docs/code-review.md`）"为审查历史存在信号，存在即不自动重放（`!loadingSessions` 一并守卫避免会话未加载竞态），真实旅程首次进入审查仍自动开启。另修：实体缓存键 v9→v10——上一轮"开发中"方案曾把未绑定实体写入 v9 缓存，恢复完成基线后缓存命中导致开发产物回退 2/3，换键强制重落确认绑定基线。走查基线（浏览器实测）：验收入口=全宽预览+验收已通过+无自动弹框；设计/计划=split+已确认产物+阶段会话；开发=split+两条工作流轨迹+3/3；测试=split+6 条用例全通过详情；审查=split+单条历史审查工作流+报告只读。
- **2026-09-14（第七批）预置历史改为"剧本无头回放"生成，废除手搓静态会话：** 手搓的 `chat-sessions.ts` 违反两条铁律——工作流消息混排文字正文（违反"工作流消息纯度"）与节点步骤自编（不来自真实 DAG）。现改为**数据回放**：`mock/scripts/historyReplay.ts` 复用交互演示的同一组剧本（replayDesignPhase/replayWorkbench/replayApplicationTesting/replayCodeReview），按版本终态无头快进驱动——门禁用预置应答逐个原地落定（应答键值与 WorkflowRunCard 各卡 submitClarification 逐字对齐；澄清向导读题目 presetAnswer），采集真实轨迹组装成静态历史。配套：① `replayClock.ts` 统一快进时钟，workbenchShared.delay 与 planningDelay 快进时立即兑现；② 生成结束 `resetWorkbenchLifecycle()` 复位注册态，冷启动校准仍走静态基线；③ mockHttpAgent 的 fixtures 播种改惰性（循环依赖 TDZ：chat-sessions→historyReplay→mockHttpAgent→fixtures）；④ 会话 list/read 改异步等待生成；⑤ 开发阶段回放四条链：介绍页/我的回检页/接口/实体，文件 Diff 接受门禁沉淀真实文件快照进会话（应用文件树与产物状态由同一事实推导）；⑥ 顺带修剧本真缺陷——独立接口工作流"接受 Diff 续跑"被"同步重建"分支抢先拦截（页面流程同序判断在前），调序后接口同步链路才可走完。最终形态（浏览器实测）：设计=澄清向导卡(预置答案只读)→需求确认卡→UI 逐页模板确认卡→一句收尾，一轮一条用户消息一条卡片；计划=技术规划确认卡→模板生成收尾→开发准入确认；开发=四条工作流轨迹 9/9、9/9、4/4、4/4 全完成；测试=非功+六条用例各自独立完成卡；审查=单条完成轨迹+报告落盘；全程无混排文字、无 pause 残留。
- **2026-09-15（第八批）新建应用初始化必填码云仓库地址：** 版本管理本质是 Git 管理，生成新版本的"提交码云/打 Tag"步骤要有仓库落点，故新建应用时即绑定远程仓库。**输入框位置放在「项目位置」分区、紧贴项目目录下方**——本地目录与远程仓库成对回答"代码放在哪"，二者都是创建时唯一确定的代码载体；不放进「基础信息」（那里只管命名/图标/终端）也不单独开分区（单字段不值得）。字段链路：`ApplicationSchemaConfig.gitRepoUrl`（带中文注释，经 schema 持久化）→ `ApplicationDraft.gitRepoUrl` → `buildApplicationSchema` 裁剪落库 → 演示预填与存量应用 `makeSchema` 同补该字段（v1.0-v1.2 已有 gitRef，指向同一仓库地址）。**校验刻意宽松：只有必填（含纯空白拦截），无任何 URL 格式约束**——行内码云地址规则未定，原型不替用户发明格式门槛；extra 文案只解释用途不规定形态。浏览器实测：清空提交被"请输入码云仓库地址"拦截且弹框保持；键入任意非 URL 文本错误即消、可正常创建。
- **2026-09-15（第九批）新建弹框瘦身，数据源与埋点全部滞后配置：** 用户明确口径——**数据源滞后到开发阶段再配置（左侧「数据来源」菜单已承载），设计/计划阶段只面向 API 契约，不涉及真实数据来源**；埋点同理不属于创建时的骨架决策。据此新建弹框删除「数据源」「页面埋点」「接口埋点」三个分区，仅留 项目位置（目录+码云地址）/ 基础信息 / 导航模式 / 菜单 / 主题 / 认证 六区。**只删表单 UI，不动数据模型**：`ApplicationDraft`/`ApplicationSchemaConfig` 的 `datasource/track/apiTrack/environment/database` 字段保留（`planning/seed.ts` 读 `datasource.type` 投影技术规划架构文案，'None' 分支即"前端本地静态演示数据；无需数据库连接"，与滞后口径自洽），新建应用落默认值。同步清理死代码：`datasourceTypeOptions` 常量、表单内三个"开发中"提示 effect、trackMethod 过滤 state（`trackMethodOptions` 保留——SettingsPage 仍在用）、`datasource-*` 样式块、无人引用的 `TabHintAutoComplete` 组件、演示预填中的 datasource 配置块。浏览器实测：弹框六区渲染正常、预填完好，完整创建链路直达工作台设计阶段。
- **2026-09-15（第十批）两旅程复验修复 + 版本派生收敛：** 浏览器真实点击复验「新建应用」「版本回退」两条旅程，修复三处问题并做两处收敛重构（交互不变）。**硬伤 ①（新建必崩）：** 第九批删除埋点分区后 `validateFields()` 不再返回 `track`/`apiTrack`（datasource/environment/database 同理），`CreateApplicationAction` 读 `schema.track.enable` 崩溃——任何新建应用提交都报"Cannot read properties of undefined"。修复：校验结果用 `initialApplicationDraft` 兜底合并，无控件字段回落默认值（与 OpenWorkspaceAction 既有口径一致）。**硬伤 ②（版本 id 复用劫持，即第四批"待复核问题"的根因）：** 回退/迭代派生的版本 id（如 `app-pms-new-v1-4`）在重载后复用，实体缓存已清理但**规划记录未清理**——上一轮同名版本的中间态（analyzing/ready_for_workbench）劫持新迭代：继承文案丢失、阶段定位错乱。修复：`initializationPlanning` 新增 `clearInitializationPlanningRecord`（按应用+版本精确清除），回退与发起新迭代在 `resetBusinessObjectsCache` 旁同步清理。**硬伤 ③（空目录无字段反馈）：** `projectPath` 嵌在 noStyle 复合控件里，antd 错误冒泡不生效——清空目录提交只见笼统"创建应用失败"toast，字段级提示不渲染（Playwright 原生 fill 复现，非脚本伪影）。修复：创建 catch 里识别 antd 校验拒绝对象，把首个字段错误消息透出到 toast（对全部静默字段生效）。重构：① `WorkbenchPage` 回退/迭代两个近重复处理器收敛为 `makeResetLifecycle` + `installDerivedVersion`（持久化版本链、切指针、重置规划产物、清版本域缓存一体）；② `WorkbenchVersionModals` 迭代/回退双胞胎弹框合并为 `DerivedVersionModal` 共用壳（回退弹框 meta 文案随之统一为"已生成版本保持锁定"）。复验：清洁缓存新建全链路（空值拦截→创建→设计阶段向导）、v1.3 发布→新迭代（继承文案回归）、回退 v1.0→v1.5 全通；typecheck + vite build 通过。
- **2026-09-15（第十一批）预置多版本完成态基线 + 回退继承真实化 + 只读不写：** 落实"同一应用并存多版本多状态是演示刚需"的口径——版本间的状态差异由 版本状态（released/iterating）、阶段定位、描述与 gitRef 表达，而不是靠矛盾的完成度数字。① **完成态版本集合数据化（`presetCompletedVersionIds`）：** mock-data 按 lifecycle（测试+验收全 passed）推导完成版本集合（v1.0-v1.3），实体绑定基线（BusinessObjects/store，替换硬编码 v1-3 常量）、用例队列播种（preload 为每个完成版本预置 6/6 已就绪任务）、规划产物检查（inspect 按集合判定是否还原已设计终态）三方共用同一判定；修复 v1.0-v1.2 只读回看"验收已通过但 2/3、0/0"的自相矛盾，现均呈现 3/3 + 6/6 + 抽屉 6 条已完成用例任务。② **回退继承真实化：** preload 为每个完成版本播种确认态规划记录（版本链正序播种，后版本以前版本记录为继承来源），"基于此版本迭代"经 `createInitializationPlanningRecord` 读到真实内容来源（v1.3 记录随后被历史回放覆盖为带正式文档的版本）；③ **只读不写：** `useInitializationPlanningRecord` 增加 readOnly 选项（AiChatPanel 传 `versionReadOnly`），已生成版本回看时规划记录缺失用内存态呈现、不写 localStorage；④ **inspect 跟随查看版本：** `WorkbenchPage.syncWorkspaceFiles` 的规划产物检查改用 `viewingVersionId`（effect 依赖同步）——active 是新迭代时切看历史完成版本也呈现已设计终态、切回待开发迭代还原基线；⑤ **码云仓库闭环展示：** 发布弹框在确认态与生成进度顶部显示仓库地址行（`workbench-generate-repo`，等宽字体+任意折行），成功通知文案带上"已提交至 {仓库}"；打开工作目录派生的应用仓库回落演示默认值（initialApplicationDraft 含预填），已加注释显性化。**顺带修复：** `resetBusinessObjectsCache` 仍过滤 `v8:` 前缀而缓存键第十批已升 v10——回退/迭代的实体缓存复位实为空操作（跨会话版本 id 复用时旧绑定泄漏进新迭代）；提取 `STORAGE_PREFIX` 共享常量后修复。浏览器复验：冷启动播种（4 版本×6 用例+4 规划记录）、v1.0 只读 3/3+6/6+阶段禁用+无新增规划键、回退 v1.0→v1.4 继承文案与向导正常、发布弹框/生成中/通知三处仓库展示、新建应用回归全通；typecheck + vite build 通过。
- **2026-09-15（第十二批）概念切换：实体 → 应用API（设计/计划/开发三阶段贯通）：** 组长定调新概念模型——**用户产物 = 应用页面 + 应用API**：应用API 是应用内部的 API 资源（资源名 + 一组方法，每个方法有约定好的出入参契约），页面的开发工作流到"调用应用内API"为止；应用API 的开发就是把它每个方法的契约参数**直接映射**到数据来源（数据库表 / 外部API接口），不再经过实体、业务对象等中间抽象层；两类数据来源的接入配置仍由左侧「数据来源」菜单承载（第九批口径不变）。① **数据层：** `components/BusinessObjects/` 目录更名 `AppApis/`，`BusinessObject→AppApi`、`BusinessOperation→ApiOperation`、`business-object:→app-api:`（产物身份/资源键/detailTargetType 全链）、`entity_binding→api_binding`（澄清模式与答案键）、缓存键 `business-objects:v10→app-apis:v1`（跨概念换键弃旧缓存，版本 id 仍参与键名）；`bo-*` less 类名保留为纯样式钩子，但两个样式文件按存活类重写，删除旧可编辑面板（BusinessObjectsPanel/OperationDetails/BusinessObjectEditor/EmbeddedBusinessObjectPlan，2026-09-11 改版后已无消费者）遗留的死规则。② **设计阶段：** 需求说明书「实体」分栏改「应用API」（spec JSON `entities→apis`、`business_operations→operations`）：每个API描述用途、读写的业务数据、需要支持的方法与调用页面；页面通过「{API名}.方法()」使用业务能力；RequirementSpecEditor 编辑态分栏与就绪检查（requirementQuality 提示词与校验）同步。③ **计划阶段：** 技术规划方案页签 5→4——「实体」与「API 契约」合并为「应用API」页签，以接口契约为核心：每个应用API一张契约卡（资源名+basePath+业务数据字段+接口表+Inspector：鉴权/参数/请求/响应/错误码/数据实现意向），投影器更名为 AppApiContractsProjection（附 description/fields）；「页面」页签的绑定行改为 页面→应用API.方法()→接口；technical-plan.json 的 `entities`+`api_contracts` 合并为 `apis`（含 base_path），buildTechnicalPlanDoc/documents.ts 标签同步。④ **开发阶段：** 产物树「实体」分组改「应用API」（ApiOutlined 图标，`artifact-business-object→artifact-app-api` 样式类），方法清单展开形态不变；发起引导卡与输入区「产物」面板分组同步（hint 文案改"N 个字段 · N 个方法"）；右侧工作台更名 AppApiDevelopmentPanel：资源视图=数据来源卡（资源字段↔表字段）+资源数据字段表，方法视图=出入参契约+「数据绑定与字段映射」工作台；工作流剧本 `replayEntityWorkbench→replayAppApiWorkbench`，DAG development 段 `entity→app-api`（读取 API 契约→确认数据绑定→生成数据适配逻辑），适配层产物改 `backend/api-adapters/{id}-api-adapter.java`（`{X}ApiAdapter`、包 `api.adapter`）；工作流标题「开发应用API「回检单」」、确认卡改「确认数据绑定」（逐方法行示绑定去向与字段规模）；开发→测试准入门仍按"全部页面完成+全部应用API完成绑定确认"判定（与第九批口径自洽）。⑤ **历史回放与预置基线：** historyReplay 的回检单链改「开始开发应用API：回检单」，api_binding 预置应答同步；预置完成版本的绑定基线走同一 withConfirmedBindings 权威路径（换键后冷启动自动重落），产物计数保持 3/3（2 页面+1 应用API）不变。⑥ **顺手清理：** 删除 `workbenchDomain.entityArtifactId` 与 `DevelopmentPlanningEntity` 占位类型及其全链（inspectPlanningArtifacts 的 entities 空数组字段、window.d.ts/preload/WorkbenchPage/LeftPanel/AiChatPanel 的 props 透传）——旧实体概念的最后一根残留管道；`ApplicationSchemaConfig.datasource.entities` 为真实工程对齐的 schema 形状，保持不动。验证：`pnpm typecheck` + `vite build --config vite.prototype.config.ts` 通过；ESLint 对触碰文件的报错均为存量风格规则（no-explicit-any/缺返回类型），无新增。**风险提示：** 产物身份前缀变更（`business-object:→app-api:`）后，浏览器里旧会话/旧后台任务流水对该产物的状态推导会失配，属预期——「清理演示数据」或换版本键即回到新概念基线。
- **2026-09-15（第十三批）产物术语统一：页面 → 应用页面：** 与「应用API」配对，用户产物口径统一为「应用页面 + 应用API」。改动范围是**产物指代**：需求说明书概览计数（「N 个页面」→「N 个应用页面」）、审阅/编辑分栏「页面与操作」→「应用页面与操作」、技术规划方案「页面」页签与「应用页面绑定的业务能力」、开发产物树分组与发起引导卡/输入区产物面板的「页面」类别、产物门禁与禁用原因（「该页面已完成」等）、开发准入门（「想先从…哪个应用页面开始？」）、工作流 DAG 节点（选择应用页面执行方式 / 梳理应用页面范围 / 选择应用页面模板）、后台任务标题（应用页面「我的回检」代码实现）、需求校验与生成提示词（应用页面清单/应用页面需包含…）、文档构建器章节标题（应用页面清单/应用页面技术绑定/应用页面与产品行为）以及 mock 数据（project-plan.json 摘要与执行顺序、page-designs 行为文案）。**刻意保留「页面」的技术性复合词**：页面预览、页面状态、页面目标、页面验收标准、页面行为、页面跳转、受控页面、目标页面、页面名称/路由等属性字段——它们描述应用页面的组成或属性，展开成「应用页面X」只会更长不含更多信息。spec/plan JSON 的 `pages` 键为内部结构、概念无误，维持不动（`entities→apis` 是概念错误才改，本次仅术语投影）。验证：`pnpm typecheck` + `vite build --config vite.prototype.config.ts` 通过。
- **2026-09-15（第十四批）需求文档升级为应用API契约（借鉴原工程 api_contracts）：** 组长定调——需求文档应当就是 API 契约：设计阶段即确定应用内部接口面，计划阶段聚焦应用页面与应用API的实现规划。① **契约结构前置（借鉴原工程 TechnicalPlan.api_contracts 的 base_path + endpoints 形态）：** spec JSON 的 `apis[]` 从「叙述性方法描述」升级为契约结构——`base_path` + `endpoints[]`（稳定 id、name、method、path 完整路径、summary 业务说明、params 参数说明、response 返回字段）；`operations` 字符串形态废止。演示契约：回检单 API（/api/rechecks）声明 GET /api/rechecks/my（查询我的回检）与 GET /api/rechecks/{id}/reviewer（查询审核人信息）。② **设计阶段「应用API」分栏 = 契约视图：** RequirementApis 重写为契约卡（资源名 + base_path + 业务数据字段 + 接口表：方法徽标/完整路径/名称/调用页面，点行展开参数与返回字段详情），页签顺序提到「概览」之后；导语明确「应用页面不直接连接数据库或外部服务，只调用这些应用内部接口」。③ **编辑态契约编辑器：** ApisEditor 重写为 名称/基础路径/用途/业务数据/接口契约（逐接口：名称 + 方法下拉(4 动词)+路径 + 业务说明 + 参数 + 返回字段 tags(候选来自业务数据) + 增删）/调用页面。④ **下游消费真实契约：** ApiOperation 增加 method/path/params、AppApi 增加 basePath；createAppApis 改为 endpoints 驱动（页面 ID 经 spec.pages 翻译为页面名）；计划阶段投影 apiContractsFromObjects 优先沿用契约的 method/path/params（平台内置方法仍按模板规则补全路径与形态），基于操作名的推测只作兜底。⑤ **计划阶段口径：** 「应用API」页签 = 契约继承 + 平台内置补全 + 数据实现意向（页脚文案与技术规划 Markdown 同步：每个操作渲染 `method path` + 数据意向）；technical-plan.json 的 apis.operations 补齐 method/path 并对齐需求契约（删除规格中不存在的 提交回检/审核回检）；requirementQuality 生成提示词与就绪检查改为契约校验（API 需 base_path，接口需 method/path/返回字段）。⑥ **绑定确认卡增强：** ApiBindingPlanRow/ApiBindingCard 逐行携带契约标签（method + path），绑定确认时先看契约再看去向。验证：`pnpm typecheck` + `vite build --config vite.prototype.config.ts` 通过。
- **2026-09-15（第十五批）契约完整化 + 演示门禁误杀修复：** ① **设计阶段契约升级为完整接口面（直接对齐原工程 API 契约形态）：** spec 契约从「2 个业务接口」扩为全量 7 接口（平台内置 分页查询/查询回检详情/新增/更新/删除 + 业务自定义 查询我的回检/查询审核人信息），每条接口带 id/name/operation_type/method/path/summary/params/request/response/errors/used_by_pages；需求说明书「应用API」分栏的接口详情与原工程契约 Inspector 同构（鉴权/参数/请求/响应/错误码/响应字段），编辑态接口条目同步补 请求体 与 错误码（每行一条）字段。createAppApis 按契约 operation_type 识别内置接口，并按 分页/详情/新增/更新/删除 五类角色去重合成（契约已声明的角色不再重复合成）；计划投影透传契约的 params/request/errors（缺失才走模板兜底）。② **修复演示门禁误杀（上一批引入）：** requirementReadinessIssues 对 endpoint.response（字符串数组）误用 recordsOf（对象数组口径），导致预置契约被判「接口 1、接口 2 不完整」、需求确认永远放行不了——改用字符串数组口径后演示旅程放行；就绪标签同时带上接口名（如「回检单 · 查询我的回检」）。③ **口径修正：** 契约在设计阶段定稿完整接口面，计划阶段不再「补全平台内置接口」，只确定每个接口的数据实现意向（技术规划方案页脚、planning_scope 节点文案同步）。验证：`pnpm typecheck` + `vite build` 通过，apis 段就绪检查对预置契约模拟放行。
- **2026-09-15（第十六批）需求文档「API契约」回归原工程紧凑形态：** 组长纠偏——上一批把接口的技术详细设计（鉴权/参数/请求/响应/错误码）塞进了需求文档，层级错了。现按原工程 api_contracts 的紧凑形态回归：① **契约数据瘦身：** spec 的 endpoints 每条只留 id/name/operation_type/method/path/summary（方法、路径、用途说明），删除 params/request/response/errors 与 endpoint 级 used_by_pages；7 条接口全量保留（5 内置 + 2 自定义）——契约约定接口面，不是接口详设。② **审阅态「API契约」分栏（页签同步改名）：** 每个应用API一张契约卡（名称 + base_path + 接口表：方法/路径/说明/页面），去掉点行展开的 Inspector；保留业务数据 chips 与「哪些应用页面会调用」两块需求级事实；页脚明确「技术详细设计与数据实现方式由后续阶段按接口逐个细化」。③ **编辑态接口条目瘦身为 名称/方法+路径/业务说明**，移除 参数/请求体/错误码/返回字段 编辑项。④ **分层归位：** 参数、响应结构、错误码属于技术详细设计——计划阶段「应用API」页签按模板合成这些细节（describeEndpoint 恢复合成口径）叠加数据实现意向，开发阶段做绑定映射；model 的 ApiOperation 只保留 method/path 两个契约字段，params/request/errors 类型删除。⑤ requirementQuality 提示词与就绪检查同步瘦身（接口需 id/method/path/summary）。验证：`pnpm typecheck` + `vite build` 通过；预置契约门禁放行。
- **2026-09-15（第十七批）「API契约」分栏回归原工程契约工作台交互 + 删除解释文案：** ① **交互形态对齐原工程 DocPanel 契约工作台**（Frontend TechnicalPlanDocSections 的 ContractSection）：左右两栏——左列「Base_Path」契约列表（点选切换，行内展示 base_path + 应用API名 + 调用页面标签），右块「Endpoints」接口表（方法徽标/路径/说明三列，静态只读）；去掉上一批复用计划阶段样式的单卡堆叠、业务数据 chips、页面列与点行 Inspector——需求阶段只呈现接口面。② **删除全部解释文案**（审阅态导语与页脚说明、编辑态工具栏提示）：需求文档只承载文档内容本身，不混入「什么是契约/哪个阶段做什么」的元说明；新增 requirement-api-* 样式（--wb-* 主题令牌，窄宽度单列折行）。③ 本批未动数据契约与编辑器字段结构（接口条目仍为 名称/方法+路径/业务说明）。验证：`pnpm typecheck` + `vite build` 通过。
- **2026-09-15（第十八批）「API契约」分栏改文档排版：** 工作台两栏（Base_Path/Endpoints 结构标签 + 点选交互）是工具界面形态，与需求文档的文档阅读定位不符。现与概览/业务流程等分栏同一套文档 idiom：根节点 planning-review-document + planning-review-collection，每个应用API一节——Title4 名称 → 用途段落 →「基础路径：`/api/rechecks`」→ h5「接口清单」（ul 清单：接口名 + `GET /api/rechecks/my` code + 用途段落）→ h5「调用页面」（页面 Tag）。上一批的工作台样式整体移除，仅保留 requirement-api-endpoint-list 文档清单样式；数据契约（endpoints：name/method/path/summary）与编辑器字段不变。验证：`pnpm typecheck` + `vite build` 通过。
- **2026-09-15（第十九批）演示接口面收敛为 3 条 + 契约即接口面：** 考虑演示剧本节奏，需求契约从 7 条接口收敛为 3 条——分页查询（builtin，GET /api/rechecks，平台模板能力示例）、查询我的回检（custom，GET /api/rechecks/my，走数据库绑定）、查询审核人信息（custom，GET /api/rechecks/{id}/reviewer，走外部API绑定）；开发产物保持 3 个（介绍页/我的回检页/回检单应用API），绑定确认卡从 7 行降到 3 行。① **同步计划阶段：** technical-plan 的 apis.operations 对齐 3 条（数据意向：武汉回检数据库/用户中心）。② **契约即接口面：** createAppApis 删除平台内置方法合成（makeBuiltinOperations/BUILTIN_DESCRIPTIONS 与五类角色去重逻辑移除），方法完全来自需求契约的 endpoints，operation_type 由契约声明；适配器命名映射与 bod 数据来源卡文案同步（去掉增删查改表述）。③ **后两阶段核对：** 历史回放四条开发链与 endpoint-designs（GET /api/rechecks/my）、审查报告（接口：GET /api/rechecks/my）、测试用例（业务级）均不引用被删接口，无需改动。验证：`pnpm typecheck` + `vite build` 通过。
- **2026-09-15（第二十批）接口即产物——API契约扁平化，废除「资源 → 方法」两级分组：** 组长定调：都已经是 API 了，不应再有「回检单这样一个实体下面挂着清单」的两级概念，API 契约就是一张扁平的接口清单。① **spec 契约扁平化：** `apis` 直接就是接口列表（id/name/method/path/summary/response/used_by_pages），资源级包装（base_path/fields/description/operation_type 分组）全部移除；演示契约 2 条业务接口——查询我的回检（GET /api/rechecks/my，走数据库绑定）与 查询审核人信息（GET /api/rechecks/{id}/reviewer，走外部API绑定），不再保留内置分页查询示例。② **模型扁平化（v5，缓存键升 app-apis:v2）：** `AppApi` 即单条接口（method/path/response/pages + 自身 Implementation），删除 ApiOperation/operations 分组、资源字段表与内置方法合成（makeBuiltinOperations/名字特判全移除）；数据实现意向由技术规划按接口 id 合入（createAppApis 接收可选 technicalPlan，数据实现类型按意向命中目录归类）。③ **计划阶段：** 「应用API」页签与权限页签消费扁平接口投影（apiEndpointsFromObjects）；页面绑定卡「页面 → 调用的接口 → 契约」直接按接口关联；technical-plan 的 apis 同步扁平（含 data_intent）。④ **开发阶段：** 产物树「应用API」分组下直接平铺接口行（无展开层级），点行打开单接口工作台（契约头 + 绑定映射面板）；绑定确认卡单行展示「接口名 + 契约标签 + 绑定去向」；适配层生成器按接口输出单类适配（queryMyRechecks 等驼峰方法名）。开发产物 = 2 应用页面 + 2 应用API = 4 个。⑤ **演示剧本：** 历史回放的应用API链拆为两条（查询我的回检 / 查询审核人信息），开发阶段共五条链；编辑器接口条目瘦身为 名称/方法与路径/用途/调用页面；requirementQuality 提示词与就绪检查按扁平接口校验（预置契约门禁放行）。验证：`pnpm typecheck` + `vite build` 通过。
- **2026-09-15（第二十一批）计划阶段「应用API」页签静态文档化，按原工程文档分层定稿内容口径：** 对照 docs/PRODUCT_UI_TECHNICAL_PLANNING.md 确认分层——需求文档（产品视角）只声明接口面（哪些接口、用途、调用页面）；技术规划（开发视角）负责「API、数据、权限和工程如何实现」，即参数/响应 Schema/错误码等技术细节与数据实现意向。① **计划阶段「应用API」页签重写为静态接口文档：** 删除「接口表点行 → 下方 Inspector 切换」的动态交互，每个接口一节随文档全部铺开——Title4 接口名 → 用途段落 → 契约行（方法徽标 + `path` code + 调用页面）→ 鉴权/参数/请求/响应/错误码 dl 网格 → h5 数据实现（意向 · 来源）→ h5 响应字段 Tags；与需求文档同一套 planning-review-collection 文档排版。② **删除解释文案与遗留动态样式：** 架构页签页脚、页面绑定卡页脚、应用API页签页脚三处元说明删除；planning-api-endpoint-table/endpoint/inspector 等点击表与 Inspector 样式整体移除（新增 contract-line/detail-grid/output-fields），窄屏媒体查询只折叠架构网格。③ **扁平化遗留修补：** 需求文档「API契约」分栏删除「基础路径：待补充」行（扁平接口已展示完整路径，base_path 随资源级包装在批二十废除，标签表同步清理）；buildTechnicalPlanDoc 的应用API段从旧资源级结构（base_path/fields/operations）修正为扁平接口行（`method path` + 名称 + 数据意向 + 用途）。验证：`pnpm typecheck` + `vite build` 通过。
- **2026-09-15（第二十二批）需求文档「API契约」分栏按扁平接口重写（修复整栏空白）：** 批二十把 spec.apis 扁平为接口列表后，RequirementApis 仍按旧两级结构读 `api.endpoints`/`api.description`，导致需求审阅的 API契约 分栏只剩接口名与空清单（组长截图确认）。现按扁平契约重写：每个接口一节——Title4 接口名 → 用途段落（summary）→ `GET /path` 契约 code 行 → h5 返回字段 Tags → h5 调用页面 Tags；空值给「待补充」次级文案。需求级契约保持基础接口知识（方法、路径、用途、返回字段、调用页面），鉴权/参数/请求体/错误码仍在计划阶段应用API文档。同步删除废弃的 requirement-api-endpoint-list 样式。验证：`pnpm typecheck` + `vite build` 通过。
- **2026-09-15（第二十三批）「API契约」分栏排版对齐其它分栏：** 组长反馈排版与行间距不齐。现完全套用「应用页面与操作」分栏的卡片 idiom：`planning-review-item-title` 标题行（接口名 Text strong + `GET /path` Text code 同行）→ 用途 Paragraph（collection 统一 `margin: 6px 0 0` 节奏）→ h5「返回字段」Tag 组 → h5「调用页面」Tag 组；去掉上一版的大号 Title4 与孤立的 code 段落行，空值统一「待补充」次级段落。验证：`pnpm typecheck` + `vite build` 通过。
- **2026-09-15（第二十四批）技术规划方案整体回归平铺文档排版（去紫色强调框）：** 组长反馈架构与应用页面两页签的 accent 紫框卡片突兀，应平铺直叙。① **架构页签：** 技术架构从三卡片网格改为「前端：/后端：/数据：」三行加粗标签段落；数据架构·接入来源从 accent 底色卡片网格改为文档清单（来源名加粗 + 模式·数量 + 表/接口括注）。② **应用页面页签：** 调用接口块从 accent 底色块改为普通 ul 清单行（`查询我的回检()` + `GET /api/rechecks/my` code）；静态页面给次级说明段落。③ **应用API页签：** 接口卡片头对齐需求文档标题行 idiom（item-title 行：接口名 + `method path` code + 调用页面右置灰字），技术细节保留 dl 网格与 h5 分节。④ **样式清理：** 删除 planning-architecture-grid/planning-data-architecture/planning-api-contract-line/planning-api-method 徽标/planning-page-usages/planning-page-usage 及窄屏媒体查询；技术规划专属样式只剩接口细节网格、响应字段 Tag 组、页面标题行与权限角色卡。验证：`pnpm typecheck` + `vite build` 通过。
- **2026-09-15（第二十五批）需求规格说明书回归朴素文档排版：** 组长反馈需求说明书杂七杂八的颜色、满屏矩形框、大小标题层级太多。六个分栏统一为平铺文档 idiom：每个分栏只用一级 h5 标题（应用页面分栏以页面名+路径 code 为小节标题），内容一律用「标签：内容」文档行与有序/无序清单表达。① 概览：删除 accent 紫底 hero 卡与三个指标方块，应用定位改为标题+名称+描述+一行灰色统计（2 个角色 · 2 个应用页面 · 2 个应用API）；业务参与者/功能模块从卡片+Tag 芯片改为清单行（名称：描述；职责：…；优先级并入括号）。② API契约：接口从带边框卡片+Tag 组改为 h5 小节（名称 + `method path` code）+ 用途段落 +「返回字段：」「调用页面：」两行文档行。③ 应用页面与操作：删除页面卡片与四组卡内 h5，业务信息/页面状态折叠为单行文档行，用户操作为清单行（行为类型以括号文字呈现，不再用 Tag）。④ 业务流程/权限需求/验收标准同步去卡片化。样式删除 planning-review-hero 与 planning-review-metrics；就绪检查 Alert 与面板头部「已确认」状态签保留（工作流信号，非文档排版）。验证：`pnpm typecheck` + `vite build` 通过。
- **2026-09-15（第二十六批）修复「确认进入开发阶段不切换」：** 组长反馈计划阶段确认开发准入门后只回了一条消息、界面不切换。排查确认领域层正常（规划记录已推进 ready_for_workbench rev12、任务类型已登记），根因在 lifecycle revision 计数器：`mock/scripts/revision.ts` 的模块级内存计数器被 Vite HMR 重新求值后从 5 万+回退到 50000，而长开页面内存 store 仍持有更大的 revision，`latestApplicationLifecycle` 的「revision 必须更大」守卫据此拒收确认事件——持久化推进、实时投影被丢，整页刷新后才由冷启动恢复。**修复：** revision 高水位持久化到 localStorage（`devagentstudio:prototype:lifecycle-revision-hwm`），`nextLifecycleRevision` 每次取「内存计数与持久化高水位的 max+1」并回写，模块重载后不再回退；存储不可用时退化为纯内存计数。**验证：** 浏览器全旅程复现两遍——修复前确认后确实只在领域层推进；修复后预置高水位 60000 并整页重载（模块重新求值），重新走完新建应用旅程，确认开发准入后阶段条立即切到开发阶段，高水位随事件增长到 60032。`pnpm typecheck` + `vite build` 通过；用户页面 localStorage 已在验证后按备份完整恢复。
- **2026-09-15（第二十七批）删除需求概览的下游就绪检查提示条：** 组长反馈就绪检查 Alert 看不出作用、直接删掉。RequirementReview 概览分栏移除该 Alert（含未确认态的警告形态），连带删除 `confirmed` 参数（唯一调用方同步更新）；`requirementReadinessIssues` 保留供确认门禁 `assertRequirementReady` 使用——完整性校验仍在确认动作里执行，只是不再在文档里常驻展示。验证：`pnpm typecheck` + `vite build` 通过。
- **2026-09-15（第二十八批）数据来源粒度细化 + 应用API产物树分组：** 组长反馈开发阶段数据来源不能只到库/Base URL 级（要能看库表与接口出入参），且应用API目录要像页面一样按业务模块分组、第一级不带图标。① **数据来源抽屉：** catalog 外部接口补充入参 `requestParams`（`ExternalApiParam`，含 required），缓存键升 v5 弃旧数据，`BindableTarget` 一并透出入参；抽屉行改为可下钻结构树——数据库行展开表清单、表行展开字段清单，外部 API 行展开目录→接口（方法徽标 + 路径 + 入/出参数量）、接口行展开入参（必填标记）与出参清单；行点击改为展开/收起（编辑收进行内编辑按钮），连接行补「N 张表 / N 个接口」计数，列表上方加一句目录来源说明；抽屉头描述同步改为「管理数据库连接、库表结构与外部 API 接口出入参」。② **产物树：** 新增 `AppApis/grouping.ts`，应用API 经调用页面映射到页面菜单（pageTree）的业务模块分组，未匹配的落「未分组」；应用API 分区头去掉 ApiOutlined 图标（与应用页面一致第一级只有折叠箭头）、计数改为 `完成/总数`，分组行复用页面菜单的 branch 样式（folder + n/m）且默认全部展开（记录折叠集合语义）。③ **应用API开发面板：** 阶段标题改为「定位数据来源 · 字段绑定与参数映射」，说明明确“页面与后端的交互编排集中在应用页面产物开发中”；契约头「应用页面使用」改名「调用页面」；新增参数映射区（`contractPathParams` 提取契约路径占位参数 + `externalParamMappings` 按名称对齐外部接口入参，未命中回退首个入参、无定义时按名称透传），字段绑定标题带匹配进度。④ **顺带修复存量样式断层：** ImplementationPanel 此前用裸 `bo-*` 类名而 LESS 全是 `xa-bo-*` 前缀选择器，整套绑定工作台样式从未命中（含本应隐藏的重复标题）；现全部类名经 `cx()` 包装，内层标题改为「数据来源」+状态徽标可见，仅隐藏与外层重复的说明行。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查产物树分组（应用API 0/2 → 需求回检 0/2 → 两接口，第一级无图标）、抽屉下钻（库→recheck 表→5 字段；用户中心→查询用户信息→入参 id 必填/出参 2）、面板确认态（字段绑定 2/2 + 参数映射 id←id（员工工号）必填）。**状态事故与恢复：** 验证中临时驱动了一次面板绑定，其写入的 `app-apis:v2` 版本键已精确删除；期间一次模糊匹配误删了旅程应用的 `initialization-planning` 记录，已从同剧本兄弟应用（o69nr1）复制记录并改写 applicationId/versionId/workspaceRoot 后原键恢复，冷启动验证直接落在开发阶段、目录与绑定状态正常。
- **2026-09-15（第二十九批）数据来源回归“表/接口”两个平铺清单：** 组长复核后两点调整：数据库不要“连接→表”两级树（多数情况就一个库，主体应是表，连接是引入步骤）；外部 API 不要公共 URL 站点层级，直接平铺接口，重点是把参数维护进来（类似 Postman 的 Headers/Query/Response 与入参出参）。① **模型（catalog v6）：** `DatabaseTable` 增加 `imported` 标记——只有已引入的表进入清单和 `flattenTargets` 绑定候选；`ExternalApiSource` 扁平为单接口（name/description/method/url/headers/queryParams/requestParams/responseFields），删除站点与目录层级及 `externalOperationCount`；新增 `importedTables`（已引入表视图）与 `externalApiPath/externalApiSignature`（`decodeURI` 还原 `{id}` 占位，避免 `%7B%7D` 编码泄漏到界面与绑定键）；缓存键升 v6。② **抽屉：** 数据库页签改名「数据表」，平铺已引入表（表名+说明+所属连接灰字+字段数，行点击展开字段，行内「移除引入」带确认），主按钮「引入数据表」打开分步弹窗（第一步选数据库连接、可就地新建连接；第二步勾选连接下的表，已引入的置灰标注），连接管理收进「管理数据库连接」弹窗（列表 + 检测/编辑/删除 + 新建，编辑表单复用原连接字段并锁定连接模式）；外部 API 页签平铺接口行（方法徽标+名称+URL+参数/出参计数，行点击展开 Headers/Query/入参（必填）/出参 四组只读明细），主按钮「新增接口」打开 Postman 风格编辑弹窗（方法+URL、四组动态行维护 Headers[名称/取值]、Query/入参[名称/说明/必填]、出参[名称/说明]）；`DataSourceEditorModal` 拆分为 `DatabaseConnectionModal`/`ExternalApiEditorModal`/`DatabaseImportModal`/`ConnectionManagerModal` 四个组件。③ **联动适配：** 技术规划「数据架构·接入来源」数据库行改为“已引入 N 张表（表名）”、外部 API 行改为“接口名 + `method path` 契约签名”；`mock-data/pms-new/technical-plan.json` 的 `data_intent` 从来源名「用户中心」改为接口名「查询用户信息」（接口即来源，`kindForIntents`/`resolveInterfaceBindings` 均按来源名匹配，自然兼容）。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查——数据表页签 5 张已引入表平铺、引入弹窗两步（连接列表含“已引入 n/m”、第二步已引入表置灰）、连接管理弹窗、外部 API 页签两条接口平铺且明细四组齐全、Postman 编辑弹窗结构完整、绑定目标卡显示 `GET /users/{id}`/`POST /messages`、参数映射 id←id（路径参数·员工工号）必填。**状态说明：** 探明「进入开发阶段时剧本回放会按当时目录重建 `app-apis:v2` 缓存」，故演示旅程应用的缓存里残留了旧意向「用户中心」；已把该应用规划记录（technicalPlan artifacts + 文档）中的 4 处「用户中心」替换为「查询用户信息」，并把「查询审核人信息」的缓存恢复为仅意向状态（bindings/mappings 清空、意向=查询用户信息），复核面板回到「意向 · 查询用户信息 待绑定」，「查询我的回检」的已绑定/5/5 匹配完成态不受影响。
- **2026-09-15（第三十批）数据来源抽屉去掉行内展开，Tab 变为“外部 API + 每库一页”：** 组长复核三点：行内展开收起不要（420px 抽屉放不下详情，详情放弹框）；Tab 要是活的——第一个固定「外部 API」，其后一个数据库连接一个 Tab；表一行一个可看详情，重点打磨“引入表”的分步设计。① **Tab 结构：** Tab 栏改为 `外部 API | <连接A> | <连接B> | ＋`——首 Tab 固定外部 API（平铺接口），其后每个数据库连接一个 Tab（连接名 + 已引入数角标），末尾「＋」= 添加数据库（打开引入第一步）；Tab 栏允许横向滚动、名称截断带 title；右侧行动按钮跟随 Tab：外部 API 为「新增接口」、数据库 Tab 为「引入数据表」（带 sourceId 直达该连接的选表步骤）。② **行与详情：** 行不再展开/收起——外部 API 行点击打开接口详情弹框（方法+`GET /users/{id}` 签名、说明、Headers/Query/入参/出参四组只读清单，底部「编辑/删除」），数据表行点击打开表详情弹框（所属连接 + 字段清单，底部「移除引入」）；`FieldList`/`ParamHead` 抽为共享组件供抽屉与弹框复用，新增 `ExternalApiDetailModal`/`DatabaseTableDetailModal`。③ **引入流程打磨：** 「＋」进入时第一步选连接（可就地新建连接，保存后自动选中并落到该连接的选表步骤，随后切出该数据库 Tab）；数据库 Tab 内的引入按钮携带连接 id 直达第二步。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查 Tab 结构与计数、接口/表两个详情弹框、库 Tab 引入直达第二步、「＋」落在第一步。
- **2026-09-15（第三十一批）数据来源 Tab 交互修整：** 组长反馈 Tab 上不要图标、库名太长、下划选中线切换时抖动。① 数据库 Tab 去掉 DatabaseOutlined 图标；② 库名用 `conversation-tab-label` 包裹并截断到 64px（约 5 字 + 省略号，title 提示全名）；③ 抖动根因有二——选中态 `font-weight: 600` 让当前 Tab 变宽、兄弟 Tab 位移，且 Tab 是可收缩 flex 项、空间不足时被压缩再分配。修复：conversation-tab 统一 600 字重（选中只靠颜色与下划线区分，共享样式、任务管理等 Tab 同步受益）；`ds-tabbar` 内 Tab 设 `flex: none` 恒定内容宽，超宽靠容器横向滚动消化。实测切换前后每个 Tab 宽度/间距逐像素一致，下划线为对称内缩的内容宽、无过渡位移。`pnpm typecheck` + `vite build` 通过。
- **2026-09-15（第三十二批）数据来源条目瘦身 + 层级递进维护页：** 组长反馈行卡片太 Low（堆了 URL/参数数/出参数/字段数）、详情弹框装不下庞大的字段参数维护，设想是层级递进的抽屉、右侧扩展出页面。① **条目瘦身：** 列表行改为与任务管理抽屉同款的单行条目（图标/方法徽标 + 名称 + 右对齐灰字说明），删除 URL、参数数、出参数、字段数等详情里可见的信息，只标识“是什么”；行内操作按钮全部移除。② **层级递进：** 行点击进入二级维护页（Tab 栏与列表被替换），抽屉经 `:has(.ds-detail-page)` 自动加宽到 680px（width 过渡 0.2s），返回时收回 420px；头部为「← 返回 + 标题 + 契约签名」通栏，底部为动作条。接口维护页把 Postman 式表单从弹框搬进抽屉（名称/说明/方法+URL/Headers/Query/入参/出参动态行 + 保存/删除/取消），「新增接口」进入同一页面的空白态；数据表维护页只读呈现所属连接与字段清单，动作仅「移除引入」（表结构由连接发现）。`ExternalApiDetailModal`/`DatabaseTableDetailModal`/`ExternalApiEditorModal` 三个弹框组件删除，新增 `ExternalApiDetailPage`/`DatabaseTableDetailPage`；`FieldList`/`ParamHead` 继续共享。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查——列表条目单行化、接口维护页（680 宽、表单预填、保存后回列表并收窄）、表维护页（只读字段 + 移除引入）、新增态空白表单与取消返回。
- **2026-09-16（第三十三批）数据来源交互对齐既有设计 + 右侧衔接第二抽屉：** 组长三点反馈：Tab 不要统计数字（没说的需求别瞎发挥）；单行卡片不要自造，用任务管理抽屉的对话卡片同一套设计；最大问题是详情联动——列表抽屉不该动，应在其右侧再扩展一个抽屉衔接详情维护。① Tab 删除 `<em>` 数量角标（外部 API 与各库 Tab 均只剩名称）。② 列表条目改用 `conversation-item` 对话卡片类（同款栅格/悬停/圆角），数据来源页仅把首列放宽为 `auto` 以容纳 GET 方法徽标，标题复用 `conversation-item-title`、说明复用 `conversation-item-meta`，自造的 `ds-item` 样式删除。③ 详情联动重构：列表抽屉恒定 420px 不再变化；AuxiliaryDrawer 持有 `DataSourcesDetailTarget` 定位（api/api-new/table），在列表抽屉右侧渲染兄弟节点 `.data-sources-detail` 抽屉（left:420、宽 680、`max-width: calc(100% - 420px)`、左缘 1px 分隔线、`auxiliary-drawer-detail-in` 动画从右缘淡入滑出 0.22s）；维护页组件改为内部 `useDataSources()` 自治读写（保存/删除/移除引入即时生效并返回列表），`ExternalApiDetailPage`/`DatabaseTableDetailPage` 的 props 收敛为 `target + onBack`。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查——Tab 无角标、条目为对话卡片、点击行后列表抽屉 (60,420) 不动且详情抽屉 (480,680) 紧贴其右缘、接口保存原值回写后详情层关闭、表详情只读字段与移除引入正常、返回/取消均正确收回详情层。
- **2026-09-16（第三十四批）双层抽屉接缝、英文库标识与按钮层级归位：** 组长对双层抽屉方向表示认可并提出三处细节。① **图层与接缝：** 两抽屉贴边处出现双线（列表 border-right + 详情 border-left 叠加）；详情层改为 `border-left: 0`、外缘自持 `border-right` 与投影——列表右框线成为内部分隔，外轮廓一根线一个投影，视觉上同一图层（实测 listRight 与 detailLeft 均为 480 严丝合缝）。② **库标识英文化：** 默认连接名改为数据库风格的 `RECHECK_DB` / `PROJECT_DB`（id 不变、bindingKey 不受影响），目录缓存键升 v7 弃旧数据；`technical-plan.json` 的 data_intent、模型注释、连接表单占位符同步，并在浏览器里对 localStorage 全键做了一次「武汉回检数据库→RECHECK_DB、本项目数据库→PROJECT_DB」的值替换（26 个键：规划记录、文档与 app-apis 缓存），绑定面板随之为「RECHECK_DB · recheck」。③ **按钮层级归位：** Tab 栏语义只保留“增删数据库”——右端 conversation-create-btn 槽位撤掉，＋添加数据库留 Tab 尾，库的管理/删除收在「管理数据库连接」；内容级操作移入说明行右侧操作组（外部 API Tab：新增接口；数据库 Tab：管理数据库连接 + 引入数据表主按钮），对象级操作在详情层（删除接口/移除引入），三层逻辑闭环。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查 Tab 英文全名、库 Tab 操作组、接缝边框数据、绑定面板英文名联动。
- **2026-09-16（第三十五批）双层抽屉同平面化 + 库名全称：** 组长复核：Tab 显示数据库全名；两层抽屉仍不在一个平面——第一层右缘发灰而第二层没有，要求用视觉核查衔接。排查：两层抽屉背景同为 `var(--wb-canvas)` 纯白，“发灰”的真凶是**详情层阴影的左半影**——`12px 0 30px` 的模糊半径向左溢出约 18px，叠印在列表抽屉右缘上形成灰色渐变（DOM 采样确认两层 computed 背景色一致后锁定阴影）。修复：详情层 `box-shadow: none`，两层面板纯靠 1px 框线分隔、同平面无缝衔接；同时去掉库名 64px 截断，Tab 显示完整全名（RECHECK_DB / PROJECT_DB 实测 scrollWidth ≤ clientWidth 未截断），超宽场景仍由 Tab 条横向滚动兜底。浏览器截图接口在本环境持续超时，视觉复核以 DOM 计算样式为准（详情层阴影 none、两层背景同白、label 无溢出）。`pnpm typecheck` + `vite build` 通过。
- **2026-09-16（第三十六批）双层悬浮投影回归 + “引入”文案统一为“添加/移除”：** 组长反馈：两层抽屉要给人盖在下面页面之上的悬浮感（切到别的单层抽屉对比明显）；“引入”不好理解，改“添加/移除”并全文统一。① **悬浮投影：** 详情层阴影恢复但改为 `32px 0 30px -16px`——负 spread 抵消模糊的左半影（阴影左界在元素内 1px，不再叠印列表抽屉），投影只向右侧主区伸展，组合面板恢复“盖在页面之上”的层次，且不复发上一批的发灰问题。② **文案统一：** 全量替换“引入”→“添加”、“移除引入”→“移除”（先替换后者再全局替换前者避免“移除添加”）：弹窗标题「添加数据表」、确认按钮「添加 N 张表」、第一步说明“已添加 n/m 张表”、“已添加”角标、抽屉头描述与各处注释同步；`rg 引入` 零残留。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器复核详情层 computed 阴影为 `32px 0 30px -16px`、Tab 全名无截断、库 Tab 操作组为「管理数据库连接 / 添加数据表」。
- **2026-09-16（第三十七批）两层抽屉关闭方式统一：** 组长指出第一层有关闭按钮、第二层只有「← 返回」，关闭方式不一致。详情层改为与列表抽屉同构的 header：同款 badge 图标 + 左侧标题（接口名 + 契约签名 / 表名 + 说明，`describeDataSourcesDetail` 从共享目录解析）+ 右上角同款 X 按钮（aria-label「关闭详情」，仅收起详情层回到列表）；第一层的 X 仍是关闭整个数据来源抽屉（两层一起收）。维护页组件移除各自的 `ds-detail-header`（`ExternalApiDetailPage`/`DatabaseTableDetailPage` 的返回箭头删除，底部「取消/返回」保留），标题文案上移到抽屉 header（`useDataSources` 解析，数据变化实时跟随）；`ds-detail-header` 样式删除、`ds-detail-sub` 改为独立规则。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查详情层 header 结构（badge + 标题 + 关闭详情）、「关闭详情」点击后详情层收回且列表抽屉保持 420px。
- **2026-09-16（第三十八批）「新建数据库」与「添加数据表」功能拆分：** 组长指出「＋」的新建语义只应针对数据库，单个数据库 Tab 内才是对当前连接的编辑，要区分功能。① Tab 尾「＋」改为直接打开「新建数据库连接」表单（不再进入“选连接→选表”的分步流程），保存后新 Tab 出现并自动选中；② 「添加数据表」弹窗（DatabaseImportModal）去掉第一步选连接，收敛为单步：固定作用于当前 Tab 的连接（props 收敛为 sourceId + sources），标题「添加数据表 · <连接名>」，勾选连接下的表（已添加的置灰标注）。功能层级随之闭环：Tab 层「＋」=新建连接；库 Tab 内容层「添加数据表 / 管理数据库连接」=对当前连接的编辑；对象层详情页「移除」=退出清单。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查——库 Tab「添加数据表」直达「添加数据表 · RECHECK_DB」勾选列表、「＋」直达「新建数据库连接」表单。
- **2026-09-16（第三十九批）连接管理收敛为当前库的「连接设置」：** 组长指出“管理数据库连接”打开全局连接列表有问题——只能对当前数据库管理；并要求按数据库常识分析连接后哪些字段可改。① **管理归位当前库：** 删除全局连接列表弹窗 `ConnectionManagerModal`，库 Tab 说明行的按钮改为「连接设置」，直接打开当前连接的表单（预填当前连接信息）。② **可改性按常识区分（DatabaseConnectionModal）：** 连接模式决定参数结构、创建后锁定（radio disabled + 说明“连接模式创建后不可更改；地址、端口、账号与密码可随时修改，改后请重新检测连接”）；地址/端口/Schema/用户名/DBID/密码为运维常态修改项（迁移、换端口、轮换凭据），随时可改；表结构由连接发现，不在表单维护（保存时原样保留）；名称为显示标识可改。③ **弹窗 footer 自定义：** 编辑态左侧提供「检测连接」（假动作自检提示）与「删除连接」（danger，二次确认后级联移除连接与已添加表、Tab 回外部 API），右侧取消/保存；新建态只有取消/保存。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查——RECHECK_DB Tab「连接设置」预填当前连接、模式锁定与可改说明、footer 动作组（检测连接 | 删除连接 | 取消 | 保存）齐全。
- **2026-09-16（第四十批）连接目录补齐“已发现、未添加”的表：** 组长指出添加数据表弹窗里“发现的表”与已添加完全一致（全部置灰），不符合真实数据库常识——可发现的表应远大于已添加。默认目录扩充：RECHECK_DB 在 recheck/recheck_audit/user 之外补 sys_dict、operation_log、department、notice、approval_flow 五张未添加表；PROJECT_DB 补 sys_config、page_draft 两张；未添加表均带字段结构（添加后即可作绑定候选）。目录缓存键升 v8 弃旧数据。flattenTargets 仍按 imported 过滤，未添加表不进清单与绑定候选；弹窗内已添加置灰、未添加可勾选（实测 RECHECK_DB 弹窗 3 张置灰 + 5 张可选，数据表 Tab 仍只显示已添加 3 张）。`pnpm typecheck` + `vite build` 通过。
- **2026-09-16（第四十一批）连接的全量表扩到真实量级：** 组长强调真实业务库的可发现表是几十张，上一批只补 5+2 张、与已添加同量级仍不符合现实。改为批量构造：catalog 新增 `discoveredTable(name, comment, columns)` 辅助函数，RECHECK_DB 未添加表扩到 35 张（权限/角色/菜单/组织/岗位/客户/合同/发票/支付流水/附件/消息/短信/邮件/流程实例/待办任务/报表模板/快照/指标/接口与登录及审计日志/定时任务/外部数据源配置/导出任务/项目/需求/问题/反馈等），全量 38 张、已添加 3 张；PROJECT_DB 未添加扩到 12 张（系统配置/页面草稿/应用日志/会话记录/键值缓存/标签/上传文件/页面模板/多语言文案/版本信息/用户偏好/示例数据），全量 14 张、已添加 2 张。目录缓存键升 v9。数量经代码统计核验（imported 3/35、2/12），未添加表均带字段结构、仍被 flattenTargets 的 imported 过滤挡在清单与绑定候选之外。`pnpm typecheck` + `vite build` 通过。
- **2026-09-16（第四十二批）全量表按字母序排列、已添加表穿插其中：** 组长指出已添加的表不应都排在最前——全量表有自己的排序，已添加的表应插在其中才真实。目录 tables 数组按表名字母序整体重排（RECHECK_DB：api_access_log → workflow_task，38 张，recheck/recheck_audit/user 穿插在第 24/25/34 位；PROJECT_DB：app_log → version_info，14 张，recheck/recheck_comment 穿插其中）；helper `discoveredTable` 增加 imported 参数（默认 false），已添加表收敛为单行调用，不再有对象块与单行两种形态。重排过程引入的三处生成错误（三层列数组、对象块转换错格式、控制字符占位）已全部修复并经 typecheck/build 验证；浏览器实测 RECHECK_DB 添加弹窗 total 38、已添加行位于 24/25/34，穿插效果符合预期。
- **2026-09-16（第四十三批）概念纠偏：「内置数据库」改「模拟数据库」：** 组长指出概念错误——“内置数据库”是低代码平台的概念（平台自带运行时，生产也用它承载数据）；DevAgent Studio 的最终产物是代码与脚本，生产部署由用户使用行内资源完成，平台只承担开发任务，因此开发环境这个免配置库只能是「模拟数据库」。`DATABASE_MODE_LABEL.builtin` 改为「模拟数据库」（枚举值 builtin 不变，PROJECT_DB 显示自动跟随），模式注释补充概念边界说明（为何不是低代码的内置数据库），连接设置弹窗的提示与组件注释同步（SkillsPage 等处的“内置技能”是无关概念，不动）。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器复核 PROJECT_DB hint「模拟数据库 · 已添加 2 张表」与连接设置 radio（模拟数据库/DBID/本地直连，模拟数据库选中且锁定）。
- **2026-09-16（第四十四批）模拟数据库语义闭环：** 组长强调“都模拟了还什么地址端口”——模拟数据库只是开发环境跑通应用的手段，甚至可以不是关系型数据库，只要最终生成的脚本能在行内 MySQL 跑起来（为模拟可靠才用关系型）。闭环检查与修改：① 连接设置提示按模式分支——编辑/新建模拟数据库显示“仅用于开发环境跑通应用，无需任何连接配置；应用最终生成的数据库脚本将部署到行内 MySQL 运行”，本地直连/DBID 才显示“地址、端口、账号与密码可随时修改”；② 「检测连接」按钮对模拟数据库隐藏（本地模拟存储无真实连接可测），本地直连保留；③ 添加数据表弹窗的空态文案按模式区分（模拟库“该模拟库暂无数据表”，外部连接才提示先做连接检测）；④ 数据表详情页“表结构由连接发现”通用化为“由系统发现”；组件注释同步。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查——PROJECT_DB（模拟数据库）连接设置显示模拟语义提示且无检测按钮、RECHECK_DB（本地直连）保留检测按钮与原提示，两者均未误伤。
- **2026-09-16（第四十五批）应用API绑定分流：表套增删查改模板、外部API做参数适配：** 组长定调开发阶段应用API工作流的核心——解决契约出入参与数据来源的关系：外部 API 简单，参数适配为主、可选函数表达式做简单加工；数据表则按接口功能套用**固定的增删查改模板**，配置难度要显著低于接口间适配。① **契约补入参：** requirement-spec 两个 API 增加 `request`（查询我的回检：处理状态·可选；查询审核人信息：id·回检单号·路径参数），`AppApi.request` 契约面、右侧契约头新增「入参/出参」两栏 chips。② **表模板：** model 新增 `TableOpKind`（查询/新增/修改/删除，`deriveTableOp` 按方法+动词自动判定）、`buildTableTemplate` 把契约入参按列注释匹配填进槽位（「我的」类接口追加当前登录用户固定条件、查询模板按出参时间列建议倒序），条件/写入行落列与操作符可下拉调整，返回字段映射归入「返回字段」槽位。③ **参数适配：** `externalAdaptations` 渲染期推导入参/出参适配行（入参按名称对齐、缺定义按名称透传；出参随字段映射状态），行尾函数表达式输入（`implementation.expressions` 持久化，留空即直接透传），语义不一致时 AI 推荐表达式（演示启发：回检单号→员工工号 推荐为 `LOOKUP(recheck_audit, recheck_id, reviewer_id)`）。④ **闭环：** `withConfirmedBindings`/面板保存路径共用 `bindingDetailForImplementation` + 表达式推荐；确认卡行增加绑定方式标注（查询模板/参数适配）；`buildAppApiAdapterSource` 按模板生成 SQL（`SELECT … WHERE created_by = :currentUser AND status = :status ORDER BY created_at DESC`）、外部绑定按适配行注释（含表达式）。应用API缓存键升 v3 弃旧结构；运行中 v1.3 的持久化规划记录里契约无 request（工作台读 `initializationPlanning.artifacts.requirementSpec` 而非场景 JSON），已对该记录做补写并清理本会话 v3 缓存让其重推。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查——表模板视图（SELECT 徽标、查询条件 2 项含登录态固定行、返回字段 5/5、自动排序）、外部适配视图（入参 id→id 必填+LOOKUP 表达式、出参 2/2）、更换绑定→生成绑定方案→确认绑定全链路、操作符编辑解确认后可重确认、生成器输出模板 SQL 与适配注释均正确。
- **2026-09-16（第四十六批）应用API工作流拆步：选类型 → 选来源（空则引导配置）→ 配置面板映射绑定：** 组长批评“点击确认绑定就绑定了”——绑定是一段有很多映射关系要处理的旅程，不是一锤子确认。按组长口述把应用API工作流拆为六节点：读取 API 契约 → **选择数据来源类型** → **选择数据来源**（检查目录可用来源：没有则引导去「数据来源」菜单配置，有则选择绑定哪一个）→ **配置映射绑定**（右侧配置面板解决两种映射：表按增删查改模板、外部API做出入参适配，不需 AI 写代码）→ 生成数据适配逻辑 → 确认代码变更。① **workflowGraphs：** app-api 段新增 `api_select_source_type`、`api_select_source` 节点，`api_confirm_binding` 改题「配置映射绑定」。② **新交互卡（ApiSourceStepCards）：** 类型选择卡（数据表/外部API 两选项，技术规划意向命中的带「建议」角标，点击即提交）；来源选择卡（列出该类型全部可绑定目标：名称+所属连接+出参字段数）；来源缺失引导卡（说明文案 + 「打开数据来源」按钮经 `devagentstudio:prototype:open-data-sources` 事件让工作台展开数据来源抽屉 + 「我已配置，重新检测」重新走可用性检查）。③ **剧本多阶段分支（replayAppApiWorkbench）：** 按当轮答案逐段推进——类型答案写回实现类型（换类型清空旧绑定）；无可用来源挂缺失引导卡；来源选定经 `withSelectedSource` 写入绑定 + 模板槽位 + AI 初步字段映射 + 推荐表达式，停在「配置映射绑定」；映射确认后生成适配。④ **映射绑定收口双通道：** 对话卡「完成映射绑定」与右侧面板「确认绑定」等效——面板确认后派发 `devagentstudio:prototype:api-mapping-confirmed` 事件（携带 objectId），WorkflowRunCard 监听并提交同一确认，工作流自动继续。⑤ **配套：** `withConfirmedBindings` 保留人工已选字段映射（确认只收口不覆盖）；v1.3 回放按新链自动走完（建议项缺失时回退接口已确立的实现类型，修复了外部API被误导向数据表的问题）；`buildClarificationContinuationMessage` 补三张新卡的续跑文案（缺失分支曾因无文案被静默拒绝提交，已修复）。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查——v1.3 回放新六节点链且两个 API 绑定方向正确（表→RECHECK_DB·recheck 查询模板 2 条件 5/5、外部→查询用户信息 LOOKUP 表达式）；测试应用交互式全流程 6/6：类型卡（建议角标）→来源卡（5 张表/2 个外部接口按类型分列）→映射卡→面板人工补字段后点「确认绑定」工作流自动推进→接受 Diff 收口，对话卡收口路径同样验证；空目录分支：缺失引导卡出现、「打开数据来源」成功展开抽屉、配置后「重新检测」进入来源选择卡。
- **2026-09-17（第四十七批）来源选择级联化 + 映射全面可配 + 去建议：** 组长三条反馈：① 不要给「建议」——用什么数据源是用户自己的判断，数据来源的信息只有用户知道；② 来源选择不要大卡片全部陈列（几十张表没法呈现），改下拉级联、一个表单项解决；③ 绑定映射必须处理应用API出入参和模型字段的关系且可选择，之前太粗糙。① **去建议：** 类型选择卡删除「建议」角标与 suggestion 透传（卡面零倾向）；payload 保留 derived kind 仅供历史回放预置应答取值（注释注明卡面不展示）。② **级联选择：** 来源选择卡重做为单个表单项——数据表用 antd Cascader 两级级联（连接 → 已添加表，几十张表滚动定位），外部API用单层 Select（可搜索），选到叶子即提交；payload 改为 `databases（connection+tables）/ externals` 分组结构，回放预置应答按分组取首个。③ **映射全面可配：** Implementation 增 `requestParamMap`（契约入参 → 外部入参的登记表，缓存键升 v4）；外部适配行两侧全改为 Select——入参适配「契约入参 → 外部入参下拉 + 表达式」，出参适配「外部出参下拉 ← 契约出参 + 表达式」，已匹配也保持可换选；表绑定模板把未匹配到列的契约入参也进槽位（落列留空待选，生成器对空列/空写入列做过滤）；映射候选下拉展示改「字段（说明）」简称（去掉来源前缀噪音），存储仍用完整来源标签保证适配代码解析不变；只读态同步简称。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查——类型卡无建议角标；来源选择为级联表单项（RECHECK_DB → recheck（需求回检记录）两级选完即推进）；表映射面板条件/返回字段全下拉且显示简称，人工补选后收口 6/6；外部适配面板入参/出参均为下拉（id→id（路径参数·员工工号）、name（姓名）←姓名、department（所属部门）←所属部门）+ 表达式预填；v1.3 回放 v4 缓存下方向正确（表→RECHECK_DB·recheck 5/5，外部→查询用户信息 LOOKUP 预填）。
- **2026-09-17（第四十八批）映射绑定整体回到工作流节点卡，右侧改为 POSTMAN 式终态调试视图：** 组长指出两点：① 绑定过程在右侧面板可随时调整数据来源，与工作流里已确认的历史步骤冲突（来源在工作流里已经选过）；② 右侧只放终态信息——应用API如何使用，类似 POSTMAN 调接口，用于测试验证，与页面预览对应，都是验收视图。① **映射配置整体进对话卡（新组件 ApiMappingCard）：** 「配置映射绑定」节点卡承载完整映射——数据库绑定：模板头（SELECT 徽标 + 来源）+ 查询/定位条件行（契约入参 → 落列下拉 + 操作符下拉）+ 写入字段行 + 返回字段映射行（出参 → 来源列下拉，含待选高亮）+ 自动排序说明；外部服务绑定：入参适配行（契约入参 → 外部入参下拉 + 函数表达式输入）+ 出参适配行（外部出参下拉 ← 契约出参 + 表达式）。卡持本地草稿态，点「完成映射绑定」把整份草稿（conditions/setters/mappings/expressions/requestParamMap/orderBy）作为 `api_binding` 答案提交；剧本分支2经 `withAppliedBindingDraft` 写回并确认（`withConfirmedBindings` 对已带模板槽位的对象保留草稿不再重推导，人工已选映射不覆盖）；来源选定分支（分支3）把 `bindingDraftFrom` 草稿与来源字段元数据（columns/requestParams/inputParams）嵌入 clarification payload，回放预置应答原样回显草稿。右侧「确认绑定」按钮与面板编辑能力（类型四宫格、目标卡、更换绑定）全部移除，`devagentstudio:prototype:api-mapping-confirmed` 事件链删除；ApiBindingCard、ImplementationPanel、apiBindingPlan 及配套 bo-* 样式删除。② **右侧终态面板（新组件 ApiTerminalPanel）：** 未确认时显示「数据绑定进行中」占位；确认后显示——数据来源摘要卡（已交付徽标 + 模板 chip + 来源与映射口径说明）与接口调试视图：方法徽标 + 路径 + 「发送」按钮、入参试填表单（契约入参逐项输入框）、模拟响应（200 OK · 12 ms 静态演示）逐出参给出示例值并标注来源（← 来源列/外部出参）。与应用页面预览同定位：绑定结果的使用与验收视图。映射标签解析兼容草稿简写「name（说明）」（externalNameFromMapping/columnNameFromMapping 双格式）。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器走查——v1.3 回放整链（卡渲染 → 草稿回显 → 写回确认 → 适配生成）后 v4 缓存正确（表 2 条件 5/5、外部 LOOKUP 预填）；查询我的回检终态面板：契约头 + 已交付摘要（数据库 · RECHECK_DB · recheck · 查询）+ 调试视图（GET /api/rechecks/my + 处理状态试填 + 发送后 200 OK 响应逐出参示例值带来源标注）；审核人信息终态面板：适配 chip + id 必填试填 + name/department 来源标注；右侧已无任何绑定编辑入口；一次性验证用应用已删除，用户自建应用未受影响（误触删除弹窗已当场取消）。
- **2026-09-17（第四十九批）映射绑定卡等式化重排 + SQL 预览闭环 + 去噪：** 组长看不懂这张关键表单：① 第一列带标签的（回检单号/关联项目/处理状态）是 API 参数还是别的——条件段与返回字段段左列语义混杂（入参/出参/登录态混排）；② 等于/包含如何作用生效、表字段和参数的关系没闭环；③ 先删数量与提示文案，把核心业务逻辑设计明白。① **条件行改读作 WHERE 等式：** 行序改为「表字段下拉 + 操作符下拉 + 取值来源标签」——取值来源带「登录态/入参」角标（如 `created_by（申请人工号）等于 [登录态·当前登录用户]`、`status（处理状态）等于 [入参·处理状态]`），表字段与参数值的落库关系一眼可读；当前登录用户不再是伪装成参数的裸行。② **返回字段行补方向箭头：** `回检单号 ← 来源列下拉`，出参到表列的取数方向显式化。③ **SQL 预览段（闭环核心）：** 新增 `buildSqlPreview` 由当前草稿实时拼模板语句（查询：SELECT 列列表（未映射出参渲染 `/* xx 未映射 */` 占位）+ FROM + WHERE `列 =/LIKE :参数` + ORDER BY；删除/新增/修改各成句），条件行落列、操作符、返回字段映射的任何改动即时反映，卡上配置与落库效果同屏对应。④ **去噪：** 卡首引导段、模板说明小字、条件/返回字段/写入字段的「N 项/已匹配 N/M」计数、行尾列注释 small、自动排序说明段全部删除（排序进 SQL 预览 ORDER BY 行）；组件 `message` prop 删除（WorkflowRunCard 不再传）。**验证：** `pnpm typecheck` + `vite build` 通过；真实组件页面内挂载走查——头 `查询 | PROJECT_DB · recheck`、条件行等式语序、返回字段 `←` 行、SQL 预览五段结构正确且无计数/提示残留；交互联动实测：处理状态操作符切「包含」→ 预览即时变 `status LIKE :处理状态`，回检单号补选 id（回检编号）→ 占位注释消失且行高亮解除。v1.3 用户数据全程未动（以备份逐字节校验还原）；因产物一版只能发起一次工作流（历史轨迹使行常驻进行中），绑定卡交互验证采用页面内挂载真实组件的方式完成。
- **2026-09-17（第五十批）条件操作符补全 + 绑定卡行布局网格对齐：** 组长指出两点：① 做入参匹配关系就要做完整，等于/包含不够；② 排版要纵向对齐、顺序统一（字段放前面就统一放前面，查询条件与返回字段一致），整体清爽。① **操作符全集（13 个）：** model 新增 `ConditionOperator` 联合类型与 `CONDITION_OPERATORS`（等于/不等于/大于/大于等于/小于/小于等于/包含/不包含/开头是/结尾是/属于/为空/不为空；「介于」需双取值、单值槽位行模型放不下，暂不提供）、`conditionSqlFragment`（操作符 → SQL 片段，卡片预览与 `buildAppApiAdapterSource` Diff 生成器共用同一实现，预览与最终代码完全一致；模糊类用 `LIKE CONCAT('%', :x, '%')` 语义）、`operatorNeedsValue`（为空/不为空不取值，行内隐藏取值来源标签）。卡片操作符下拉接全量选项。② **行序统一 + 网格对齐：** 全部段落统一「来源字段在前、契约参数在后」——返回字段行改为「来源列下拉 → 契约出参 chip」（原先出参在前），写入字段行同样「写入列下拉 → 入参 chip」，外部适配行改「外部字段下拉 → 契约参数 chip → 表达式」（去掉 →/← 箭头，方向语义由段标题承载）；行布局从 flex 改为共享三列网格 `minmax(150px,1fr) | 96px | minmax(110px,1fr)`，各段第一列下拉与第二列元素纵向严格对齐；行本身去边框去底色（清爽），未映射行仅保留浅底高亮；「登录态」角标措辞改「登录用户」。**验证：** `pnpm typecheck` + `vite build` 通过；真实组件挂载走查——条件行与返回字段行第一列 x/w 完全一致（34/157）、第二列起点一致（199），行序统一；交互实测「为空」→ 取值标签隐藏且 SQL 变 `IS NULL`；13 个操作符 SQL 映射逐一核对正确。
- **2026-09-17（第五十一批）契约出入参英文标识 + 绑定卡参数前置与箭头对齐：** 组长两点：① 行序应是参数在前、最后选字段才符合直觉；字段侧除入参也要标识出参——出参的英文名有吗？前面的 API 契约和设计要配套做出来。② 排版：返回字段做个映射箭头，出入参对齐、后面的字段选择也对齐，视觉清爽。① **契约结构升级：** `ContractParam` 增 `code`，新增 `ContractOutput {code,name}`、`AppApi.response` 改为出参对象数组（缓存内 AppApi 即时重解析，无持久化格式迁移）；`contractRequestParams` 合成的路径参数自带 code；新增 `contractFieldLabel`（有标识读作 code（中文名））。契约 mock JSON 两 API 的出入参全部补英文标识（status；recheck_id/project_name/status/created_by/created_at；id；userName/department）。createAppApis 对历史记录里的纯中文名出参按无标识归一（用户自建版本的存量契约不受影响，展示回退中文名）。② **配套展示：** 需求文档 API 契约分栏（RequirementApis）新增「入参：」行、返回字段改英文标识（中文名）并列；需求文档 Markdown 渲染器（planningMarkdown）对 code+name 契约条目单行渲染（标识属契约身份，不作编辑字段）；技术规划契约投影 outputs 与响应模板改用英文标识；开发面板契约 chips、调试视图入参试填与响应逐行均改 code（中文名）；适配代码注释的契约侧同步。③ **绑定卡参数前置 + 对齐：** 行序统一改回「契约参数在前、来源字段在后」——条件行 = 参数 chip（角标 登录用户/入参）+ 操作符下拉 + 落列下拉；返回字段 = 出参 chip + ← 映射箭头 + 来源列下拉；写入字段同理 →；外部适配行 = 契约参数 chip + 箭头 + 外部字段下拉 + 表达式（row-with-expr 修饰去关系符列）。**列轨修复：** 行内独立 grid 按内容算轨会随参数宽度漂移甚至重叠——参数列改定宽 172px、关系符列 84px（外部行 44px 并入表达式列）、字段列 1fr，卡长溢出交由 chip 省略号 + title。**验证：** `pnpm typecheck` + `vite build` 通过；真实组件挂载走查——全部 7 行参数列 x=34、字段列 x=306/w=154 完全一致，参数英文标识、映射箭头、SQL 预览联动正常；工作流澄清载荷新增 outputs 与 inputParams.code（workbench 分支3 → WorkflowRunCard → 卡片）。
- **2026-09-17（第五十二批）应用API工作流去掉「确认代码变更」门禁：生成即交付收口：** 组长定调：适配逻辑完全是配置出来的，不经过 AI，没什么可审核的，下一个节点没必要走 Diff。① **六节点改五节点：** `replayAppApiWorkbench` 删除 `confirm_changes` 节点与 stepTotal +1；映射绑定确认分支（分支2）生成适配代码后不再挂 `file_acceptance` 门禁——流式生成保留（对话里仍可见生成过程与最终代码），随后直接 `onAcceptFiles` 交付 + emit `build/completed`（消息「应用API开发已完成」）。`ReplayCallbacks`/`SendWorkflowMessageOptions` 新增 `onAcceptFiles`；实时链路在 useWorkflowConversation 接 `recordAcceptedFile`（等价用户接受 Diff 的文件沉淀）；历史回放 sink 同步把自动交付文件写进 savedFiles，重建轨迹不丢应用文件。分支1 保留为历史轨迹遗留门禁的收口通道（只补完成态、不触碰绑定状态，防止旧应答重放覆盖人工映射）。② **存量缓存读取边界修复（关键 bug）：** 契约升级后 `readObjects` 直接返回旧 v4 缓存，纯中文名出参以字符串流入新代码导致 `output.name` undefined 崩溃——读取时统一归一（response 字符串 → `{code:'',name}`，request 补 `code:''`），用户自建版本的存量记录无损兼容。**验证：** `pnpm typecheck` + `vite build` 通过；页面内直调剧本实测：查询我的回检提交映射草稿后 5/5 全 completed、无 clarification、`onAcceptFiles` 交付 `backend/api-adapters/query_my_rechecks-api-adapter.java`，存储 confirmed=true 且人工映射（回检单号→id）与审核人 LOOKUP 表达式原样保留。说明：v1.3 为已验收只读版本，产物选择器按设计禁用新实施，故端到端 UI 旅程以页面内直调剧本方式验证（与实时链路共用同一剧本模块与回调形状）。
- **2026-09-17（第五十三批）应用API去掉整套代码生成 Diff 交互：** 组长定调：代码 Diff 过程整个去掉——不需要这套代码生成交互、不用审查、右侧面板不要联动切代码视图，视觉重心保持在产物视图，看做好的 API 长什么样。分支2（映射绑定确认）删除 `generate_code` 阶段、`streamCodeFrames` 渐进流式与全部 `codeChanges` 载荷（流式过程与最终载荷都不再携带），改为：绑定确认 → 适配节点 running → 短延时 → `onAcceptFiles` 静默交付适配文件 → 全节点完成 → `build/completed` 单次收口。右侧面板的 Diff 联动由 codeChanges 驱动，载荷不再携带即不再联动；页面/接口等其它产物的 Diff 流程不动。**验证：** `pnpm typecheck` + `vite build` 通过；页面内直调剧本实测：全流程 workflow 仅 1 次 emit（completed 收口）、codeChanges 出现 0 次、无 clarification、适配文件照常经 `onAcceptFiles` 交付、5/5 节点完成。
- **2026-09-17（第五十四批）应用API全程不联动右侧面板，视觉重心落在产物视图：** 组长指出工作流完成后右侧还在打开应用文件面板看空页面——Diff 都没了就不要再切 Tab。两处修复：① 自动跟随 effect 新增应用API分支——`developmentWorkflowArtifactId` 以 `app-api:` 开头的 build 工作流全程不切源码区（原「build 完成且无审查目标收口到源码区」的兜底不再命中应用API）；完成收口时若面板仍停在源码区（历史遗留联动残留，如混合会话里页面 Diff 先行），拉回「开发产物」视图，其余情况保持不动。② 开发阶段入口从「先展示应用文件源码区」改为直接打开「开发产物」——视觉重心是产物视图（看做好的 API/页面长什么样），页面/接口编码 Diff 出现时仍由 Diff 分支临时接管源码区。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器实测 v1.3 重载 + 阶段进入 + 历史轨迹重放完成后，右侧面板落在「开发产物」不再停留源码区/空文件页，绑定数据（confirmed + 人工映射）原样保留。
- **2026-09-17（第五十五批）映射绑定拆出右侧「字段映射」独立 Tab：外部API按请求部位分组 + fx 表达式 + 请求预览：** 组长定调对话卡塞不下也不该塞完整绑定编辑：①旅程顺序为先想清楚步骤、再页面结构、最后演示数据；②「直接透传」裸输入框没意义，改 fx 按钮浮层（透传/当前登录用户/自定义表达式，三态可视）；③入参适配按外部请求部位分组（路径参数/查询参数/请求体，GET 无请求体组），出参适配取响应 data 层；④请求头不参与映射（平台统一注入，卡上一行灰字）；⑤两个方法各自描述一段调用，不要求一致、卡头并列展示，语义匹配交给用户判断；⑥全部编辑收口到右侧「字段映射」独立 Tab。**实现：** ①目录模型 `ExternalApiParam` 增 `location: path|query|body`，`queryParams` 并入 `requestParams`（缓存键升 v11 弃旧数据），种子同步改造并新增「回检中心查询 GET /api/rechecks」（query: status/page，出参 recheck_id/project_name/status/created_by/created_at 与契约出参对齐，申请人走 fx 演示 `LOOKUP(user, id, name)`）；`ExternalApiDetailPage` 入参行增部位下拉、Query/入参两个区块合并；`BindableTarget` 增 `method`。②`AdaptationRow` 增 `location`；`externalAdaptations` 取消「唯一入参硬凑」兜底——对不上就留空待选（语义用户判断）；`recommendAdaptationExpressions` 补出参启发（申请人 ← 申请人工号 → LOOKUP 译名）。③新组件 `FieldMappingPanel`：数据库形态沿用模板槽位 + SQL 预览；外部形态入参适配按部位分组、fx 浮层三态取值、出参 data 层剥壳说明、底部请求预览实时拼 method+URL+query+body。④`ApiMappingCard` 摘要卡 + 「配置字段映射」入口。⑤AiChatPanel 接线：RightPanelState/WorkspaceTabKey 增 `field-mapping`，草稿正本在面板层，确认经 `handleSubmitWorkflowClarification` 提交后收口回「开发产物」；澄清载荷补 `appMethod/appPath/method`。⑥`buildAppApiAdapterSource` 外部分支按部位装配请求（`[路径]/[查询]/[请求体]` 注释 + request.put 行）。**验证：** `pnpm typecheck` + `vite build` 通过；真实组件页面内挂载走查——外部分组表单、请求预览实时联动、fx 三态写入正确键、行网格对齐；修复 cx 前缀导致修饰类不生效的问题（LESS 改 `&.@{class-prefix}-pending/-row-external/-active`）。
- **2026-09-17（第五十六批）字段映射面板对齐既有设计语言：自动打开 + API 目录双栏 + 节点卡瘦身：** 组长三点批评：①进行字段映射应自动打开右侧面板，面板以应用的API为目录（借鉴其它 Tab）；节点步骤不放卡片，最简文本提示 + 最重要的确认/授权操作即可；②字段映射 Tab 应排在「应用文件」之后，图标不对要体现映射；③排版要参考已有 Tab 的成熟样式——去头部紫色卡片、下拉框用系统成熟交互、对齐间距分栏要处理。**实现：** ①`FieldMappingPanel` 重构为「开发产物」同款双栏：左 220px 应用API目录（方法小徽标 + 名称 + 绑定来源摘要 + 状态点 进行中紫/已确认绿/未开始灰，点击切换，当前绑定对象默认选中），右侧内容区对齐 api-terminal 语言——契约头（方法徽标 + 等宽路径 + 「对象 → 来源」摘要行 + 分隔线）+ 分节卡片（边框圆角浅底），空态给出「开发产物」指引；行网格全段共用（参数列 minmax(210,250) | 关系符 | 字段列 | fx 116px），下拉恢复系统默认尺寸。②工作流节点卡删除整套摘要卡（ApiMappingCard 组件删除）：改为一行文本提示（对象与来源、指引右侧面板）+「打开字段映射」「完成映射绑定」两按钮——确认动作收口在节点卡，面板只承载配置。③绑定澄清出现即自动打开字段映射面板并初始化草稿（effect 守卫：草稿就绪后不再抢占，用户切走不拽回）；「打开字段映射」走同一 control。④Tab 排序移到「应用文件」之后、图标换 NodeIndexOutlined；Tab 常开可浏览（无进行中绑定时内容区显示目录 + 只读说明，修复原先点击无响应的缺口）。⑤线程化 `fieldMappingControl {ready, open, confirm}`（AiChatPanel → MessageList/ProcessSteps → WorkflowRunCard）。**验证：** `pnpm typecheck` + `vite build` 通过；真实应用实测（v1.0）——Tab 顺序 开发产物→应用文件→字段映射、node-index 图标、点 Tab 渲染 220px 目录（两条API带绑定摘要）+ 空态；组件挂载实测——节点卡文本提示 + 两按钮（open/confirm 回调连通、打开按钮真实切面板）、数据库编辑区（查询条件/返回字段/SQL 预览卡片、参数列 x=287 全行一致、未落列条件显示 `? = :?` 占位）、外部编辑区（查询参数分组、fx 列 x=1213 全行一致、LOOKUP 表达式高亮、请求预览 `status={status}&page={?}`）；用户两个版本的绑定数据全程只读未动。
- **2026-09-17（第五十七批）字段映射动作栏与文案对齐 + 草稿可保存：** 组长要求面板也要有「保存」和「保存并确认触发下一步」，节点卡主要就是确认进入下一步，两处文案配套对齐（文案为组长随手示例）。①目录行再简化：去掉状态圆点与方法徽标，每行只显示 API 名称（方法/来源由右侧内容区承载，不重复展示）。②面板内容区底部新增动作栏：`保存`（写回应用API状态但不提交——新增 `withSavedBindingDraft`，工作流仍停在待确认，重开面板读到的就是这份草稿）与 `保存并确认`（主按钮，提交草稿进入下一步）；面板初始草稿改为优先读应用API状态里的已存草稿（`bindingDraftFrom`，保存结果可跨刷新），缺位退回澄清载荷。③节点卡主按钮文案对齐为「保存并确认」，提示语同步为「保存并确认后将生成数据适配逻辑」。**验证：** `pnpm typecheck` + `vite build` 通过；真实应用实测目录行仅名称（圆点 0）；组件挂载实测保存/确认回调连通、节点卡两按钮文案与主按钮回调正确。后按组长反馈再调：①动作按钮从底部动作栏上移到接口标题行右侧（与接口调试视图的发送按钮同位，底部栏删除）；②目录行加 ApiOutlined 图标（沿用开发产物目录的 API 行图标，选中/当前行高亮为主题色）。③目录选中态提升到面板层（selectedApiId/onSelectApi）：「打开字段映射」与自动打开都把目录定位到当前绑定的那个应用API——用户手动看过其它接口后回到节点卡点按钮，目录与编辑器仍精确定位回绑定对象，不再停留在上次浏览的条目。后按组长反馈再调一轮：①卡片标题去掉补充语（只留「入参适配」「出参适配」），删除请求头平台注入说明行——这些是沟通上下文不是界面文案；②目录收窄到 180px，行网格列宽整体收紧（参数列 min 150 / fx 92 / 间距 8），editor/card/group 的 grid 单列轨道钳制为 `minmax(0, 1fr)`——请求预览等长内容此前会把轨道撑出横向滚动，钳制后在内容区 596px 与更窄 496px 下均无横向滚动。真实应用实测：标题行右侧渲染两按钮、底部无残留、目录行带图标。用户绑定数据只读未动。
- **2026-09-17（第五十八批）函数表达式编辑弹框（fx 升级为真编辑器）：** 组长要求找网上现成的函数表达式编辑器做进来，做成专门维护表达式的弹框——API 适配数据的加工关键就在这里。引入 `react-simple-code-editor` + `prismjs`（ mature 轻量组合），新建 `ExpressionEditorModal`：①左侧函数/变量目录——LOOKUP（查表翻译）、DEFAULT（空值兜底）、CONCAT、IF、UPPER 五个函数带签名与说明，点击把示例插入光标处；变量给「当前登录用户 :currentUser」与入参侧契约入参 `:code`（出参侧说明表达式输入是外部出参原始值）。②右侧 Prism 自定义语法 `fxexpr` 高亮（函数/变量/字符串/数字分色，配色走 --wb-* 主题变量明暗自适应）。③底部实时校验：括号配平 + 未知函数名提醒，校验不过时确定按钮禁用；「清空（透传）」一键回到透传态。④fx 按钮点击改为打开弹框（原 Popover 移除），确定后写回草稿 expressions 并高亮摘要。**验证：** `pnpm typecheck` + `vite build` 通过；页面内挂载端到端实测——fx 点击开弹框、目录 7 项、点 LOOKUP 插入 `LOOKUP(user, id, name)` 且函数/变量 token 高亮、`FOO(:status` 触发「括号不匹配」校验且确定禁用、合法表达式确定可用。排障记录：fx 按钮曾漏挂 onClick（重构 Popover→Modal 时丢失）；antd v4 Modal 需用 `visible` 而非 `open`；vite 依赖重优化会换 deps 哈希，挂载测试需动态发现 react 地址。后按组长微调：函数/变量目录加浅底圆角做视觉分区，滚动条隐藏（scrollbar-width:none + ::-webkit-scrollbar display:none，仍可滚动）。
- **2026-09-17（第五十九批）对话区滚动条弱化 + 文本选择修复：** 组长指出对话区旁边的纵向滚动条太显眼（要让用户知道位置但不能这么明显），且文本选中很困难、怀疑图层不合理。排查结论：elementFromPoint 证实无遮挡图层，问题出在两处交互规则——①流程步骤用 `<details>/<summary>`，点击标题即折叠，拖选起始在标题上时内容瞬间消失选区丢失；②步骤标题行 `user-select: none` 让从标题开始的拖选无法起步。修复：①摘要行 onClick 检测 `window.getSelection()` 非空时 preventDefault，拖选不再触发折叠（普通点击仍可展开/收起）；②移除步骤标题行的 user-select:none 恢复文本可选；③对话消息列表滚动条细化：webkit 6px 圆角、轨道透明、thumb 用 --wb-control 低对比常驻、悬停加深（Firefox 走 scrollbar-color），滚动位置提示保留但不抢注意力。**验证：** `pnpm typecheck` + `vite build` 通过；实测 `.ai-message-list` scrollbar-color 已是低对比色，带选区点击摘要不折叠。
- **2026-09-17（第六十批）开发阶段补齐富加载页：** 组长指出设计/计划阶段都有加载页面而开发阶段缺，要求检查补齐。排查：设计/计划的生成态用 `RichLoading`（渐变轨道圆 + 标题 + shimmer 进度条），开发阶段右侧产物区只有静态占位文字。补齐两处：①应用API适配生成中——AiChatPanel 由当前工作流状态推导 `appApiGenerating`（app-api 目标 build+running）传入 DevelopmentArtifactsPanel → AppApiDevelopmentPanel → ApiTerminalPanel，未确认占位升级为「正在生成数据适配逻辑」富加载页（保存并确认后的生成窗口期可见）；②页面后台实现中——产物内容区对 `implementing/impl-queued` 状态从纯文字提示升级为「正在实现「页面名」」富加载页（与页面预览加载同一视觉语言）。**验证：** `pnpm typecheck` + `vite build` 通过；挂载实测两处富加载页标题与提示文案正确渲染。
- **2026-09-17（第六十一批）应用API产物预览重构为 POSTMAN 语义两段布局：** 组长指出该页也有点选不中的问题，且信息要按逻辑语义整理——借鉴 POSTMAN：①请求验证，②已实现的基本接口定义；展示要配套工作流状态；框太多、一层层灰底信息看不清。重构 `AppApiDevelopmentPanel`（吸收并删除 `ApiTerminalPanel`）：①头部=图标+名称+工作流状态徽标（已交付绿 / 生成中主题色 / 绑定进行中灰，与生命周期对齐；生成中整页富加载不变）；②第 1 块「接口调试」= 方法+路径+发送、入参试填、模拟响应（原 URL 栏与契约 chips 里的路径重复展示一并去掉）；③第 2 块「接口定义」= 请求/用途/调用页面/入参/出参/数据来源 六行平铺定义列表（label 列 64px 对齐，行间细分隔线），原来的契约三格芯片盒、出入参芯片盒、来源摘要卡全部取消；④面板显式 `user-select: text` 防继承禁选。**验证：** `pnpm typecheck` + `vite build` 通过；一次性 verify 版本键挂载实测——状态徽标「已交付」、两块标题顺序正确、六个定义行齐全、旧芯片盒 0、定义文本可选（临时缓存键用后即删，用户数据未动）。
- **2026-09-17（第六十二批）字段映射已完成态常驻（只读）+ 文案改「已完成」：** 组长指出工作流走完、映射做好后右侧字段映射内容就空了——映射配置不能是一次性展示；另把「已交付」改「已完成」。①model 新增 `confirmedBindingView(object, sources)`：从已确认实现提取与澄清载荷同形的映射视图（契约上下文 + bindingDraftFrom 草稿）。②AiChatPanel `fieldMappingView` 状态机：选中目录项为活动绑定 → 可编辑草稿（澄清载荷）；已确认绑定 → 只读常驻（confirmedBindingView，映射配置完成后不消失）；未开始 → 空态。视图推导改用 `readAppApisSnapshot` 新鲜读取，规避工作流确认写入后内存 appApis 未同步导致的假空。③FieldMappingPanel 增 `readOnly`：只读态全部控件禁用、隐藏保存/保存并确认动作；空态文案区分「尚未开始」与「进行中」。④文案：产物头部徽标与注释统一改「已完成」。**验证：** `pnpm typecheck` + `vite build` 通过；实测用户已完成的应用——字段映射面板点「查询我的回检」渲染只读映射（查询条件/返回字段/SQL 预览与其确认结果一致，9/9 控件禁用、动作区隐藏）；fiber 取证确认此前空视图源于内存 appApis 未同步，新鲜读取已修复。
- **2026-09-18（第六十三批）新建/回退双旅程复验修复 + 旅程域代码精简：** 全程真实点击复验「新建应用」（弹框预填→空值拦截→设计向导 4 项→需求确认→逐页模板→阶段门→技术规划确认→开发准入门→产物发起卡→应用API五节点工作流至 5/5 交付）与「版本回退」（v1.3 只读定位→切 v1.0→基于此版本迭代→v1.4 继承），修复两处问题并做四处无行为变化的精简。**硬伤①（回退继承空契约）：** preload 为预置完成版本播种规划记录时的幂等跳过（`readInitializationPlanningRecord` 存在即跳过）会让持久化的旧契约记录（应用API契约加入前、`requirementSpec.apis` 为空的 9 月 15 日形态）永远滞留——历史版本只读回看缺应用API完成基线（开发 2/2 而非 4/4），更严重的是「基于此版本迭代」经 `snapshot.planning || 父版本 snapshot || localStorage 记录` 的继承链落到旧记录，新迭代的规格没有 apis、应用API产物整体消失。修复：released 预置版本是只读静态基线、无用户写入，改为每次加载按当前种子重播（iterating 的 v1.3 保持跳过，其记录由历史回放覆盖）；应用API缓存键含 apiIds 段，重播后键名变化自然弃旧空缓存。**硬伤②（预览通知卡死/叠加）：** mock 的 `/projects/launch` 即时 resolve，antd v4 静态通知实例在 React 18 并发调度下异步挂载，同 tick 的 `notification.close(key)` 是空操作——「项目正在启动中」（duration null）永驻屏幕并与成功通知叠加。修复：结果通知（成功/失败）复用加载通知的 key 原位顶替；取消/异常分支用同 key 同内容 + duration 0.01 的顶替实现确定性消失。**精简（不改任何交互）：** ① `applicationVersions` 删除死导出 `releasedVersions`/`iteratingVersion`，`createApplicationVersionId`/`formatVersionLabel`/`bumpVersionLabel` 收为内部函数；② `initializationPlanning` 删除无引用的 `clearInitializationPlanningRecords`（演示悬浮球按前缀自清理）；③ camel/snake 双键回退链收敛为 `workflowStateField` 共享助手（utils 与 panelHelpers 两处消费同一实现）；④ WorkbenchPage：四个阶段准入门的 available/request 状态对封装为 `useEntryGate`、双任务池抽屉映射收敛为 `itemsFor(pool)`、测试用例启动/比值显示两个阶段字面量集合提为模块常量（`TEST_PREPARATION_STAGES`/`PLAN_COUNTERS_STAGES`）、两处应用索引持久化合并收敛为 `persistApplicationToIndex`；⑤ 删除 `scripts/tmp_repair_tables.py`（第四十二批自述“用后即删”的一次性修复脚本）。**遗留观察（未改）：** 应用API「保存并确认」不强制全部字段映射完成（未映射出参可带 `/* 未映射 */` 空洞确认交付，实体时代曾有“全部字段已匹配”前置）；预置应用的发布/迭代是会话级演示（`预置应用恒以静态数据为准`，刷新回到 v1.3 iterating 基线）。**验证：** `pnpm typecheck` + `vite build --config vite.prototype.config.ts` 通过；ESLint 对触碰文件与 HEAD 比对无新增报错；浏览器复验——清洁缓存下回退 v1.0→v1.4 继承 `query_my_rechecks,query_recheck_reviewer` 契约、v1.0 只读回看 4/4、发布 v1.3（三步进度/仓库通知/锁定态）→ 发起新迭代 v1.4 全通；预览通知单条自动消失；空目录提交被「请选择项目创建位置」拦截且弹框保持；复验后演示数据已清理复位。

- **2026-09-18（第六十四批）外部API按域拆分独立抽屉：** 评审结论——后端认为外部API要按域名分组（大部分API同在一个域名下），与数据表混排进一个「数据来源」抽屉区分不开。①目录模型：catalog 新增 `ExternalApiDomain`（type external_domain，name/baseUrl/description），`ExternalApiSource` 挂 `domainId`，缓存键升 v12（旧目录按约定丢弃）；种子改为「门户平台（查询用户信息+发送消息通知，URL 归入 portal 域主机）+ 回检中心（回检中心查询）」两域，接口 id 与 method+path 签名不变，既有绑定引用不受影响；`flattenTargets` 显式跳过域条目，`useDataSourceIndex` 增 `externalDomains`。②抽屉拆分：`DataSourcesPage` 只留数据库连接 Tab（连接被删自动回落第一个连接，无连接时空态引导新建）；新建 `ExternalApisPage`——一个域一个 Tab、末尾「＋」新增域，域内提示行显示域名地址+接口数，提供「域设置」「新增接口」，新增接口预填域地址前缀；`ExternalApiDomainModal` 与数据库连接弹窗同构（新增/编辑/删除域，删除级联移除域下接口并二次确认）。③AuxiliaryDrawer 模式拆为 `data-sources`（数据源）与 `external-apis`（外部API）两个抽屉，详情层按模式路由（数据源只出表详情、外部API只出接口详情，切抽屉后的残留目标不渲染），外部API抽屉徽标新增 api-filled.svg。④左侧栏 PhaseNavigation 拆出「数据源」「外部API」两个入口（Database/Api 图标），WorkbenchPage/LeftPanel/AiChatPanel 透传两组开关；来源缺失引导卡按缺失类型打开对应抽屉并分域文案（「在对应域下登记接口」）；两抽屉头文案同构对齐（连接↔域、表↔接口、字段↔出入参）：「已接入的数据库连接与数据表，按字段粒度绑定」/「已登记的接口域与外部接口，按出入参粒度绑定」。⑤必填红星与配套校验：`ds-editor-required` 样式类（`--wb-danger` 危险色，随主题明暗）标在数据库连接弹窗（名称恒必填；直连模式加标地址/端口/Schema/用户名/密码，DBID 模式加标 DBID，直连参数在 DBID 模式仅展示不标星）、域弹窗（域名称/域名地址）与接口维护页（接口名称/请求行）；连接弹窗校验补齐直连四项与 DBID 必填，错误就地提示不关弹窗。⑥演示数据源连接名改中文（用户自建名称用中文更合适）：RECHECK_DB→回检业务库、PROJECT_DB→工程模拟库，目录缓存键升 v13；模型层新增 `withLiveSourceNames` 接入应用API读取边界——已确认绑定的来源名快照按 sourceId 对齐目录当前名，同步改写字段映射标签前缀与计划意向名，保证目录改名后字段映射只读视图、产物页「数据来源」行、适配摘要不出现新旧名混杂。实测重载后三处（抽屉 Tab、只读映射「回检业务库 · recheck」、产物数据来源行）名称一致且无旧英文名残留。**验证：** `pnpm typecheck` + `vite build` 通过；实测——左侧栏双入口激活态正确，外部API抽屉域 Tab/提示行/接口清单/域设置弹窗（含删除域）齐全，接口详情层签名 GET /users/{id} 与 GET /api/rechecks 正常，新增接口 URL 预填 https://portal.wuhan-branch.example/，数据源抽屉仅剩 RECHECK_DB/PROJECT_DB 连接 Tab。

- **2026-09-18（第六十五批）五个抽屉菜单交互统一：** 组长指出左侧五个菜单（任务管理/异步/潮汐/数据源/外部API）多次点击的抽屉行为不一致——有的直接弹出、有的直接切换显示、动画不同、还分首次和再次。根因：两套抽屉机制不同——后台任务抽屉常挂载走 transform 过渡（但 visibility 无延迟，收起实际瞬间消失）；辅助抽屉条件挂载只在展开瞬间播一次挂载动画，收起直接卸载无动画，已展开时切换模式（数据源↔外部API↔任务管理）纯内容替换零动画。统一为同一机制：①AuxiliaryDrawer 改常挂载（WorkbenchPage 无条件渲染，mode 传 null 即收起），展示模式与详情层内容各带 300ms 滞后清空——收起滑出期间原内容保留不露空抽屉；根节点 key 跟随展示模式，切换模式时整节重挂载重放入场动画，与首次展开同一条滑入轨迹；②开合统一为 transform 过渡 + visibility 延迟隐藏（收起动画可见），辅助抽屉/详情层/后台任务抽屉全部 0.26s 同曲线；③详情层（数据表/接口维护页）同样改过渡开合。实测：任务管理展开→切数据源（重放入场）→再点收起（transform 中间态 -46px 且可见，结束后隐藏清空）、异步收起中间态 -42px 可见、潮汐展开、详情层收起逐帧 opacity 1→0.19→0.03→0→卸载，五个菜单开/关/切换行为一致。**红星位置修正：** 栅格 label（display:grid）里星号 em 与文字是两个栅格子项，红星被竖排到字段文字上方——把「星号+文字」包进同一个行内 span（单个栅格项）回到文字前方同一行；连接弹窗条件星号（直连显隐的地址/端口/Schema/用户名）一并修入 span 内部条件渲染，接口详情页请求行星号本在 flex 行内不受影响。实测全部必填字段星号同行（label 高度 57px 与选填字段一致，此前 82px）。

- **2026-09-18（第六十六批）五抽屉样式量化审计统一 + 模拟库改名：** 组长指出抽屉交互要统一、Tab 栏标题高度和字体大小感觉不一样，且「工程模拟库」演示名不好（与“模拟数据库”模式语义重复、不贴真实场景）。量化审计（逐抽屉读取计算样式）找出三处真实差异并统一：①后台任务抽屉 Tab 未继承字体族且仅选中态 600 字重（未选中 400，读起来像字号不同）——补 `font: inherit; font-weight: 600` 并去掉 active 冗余字重，与 `.conversation-tab` 完全同款；②外部API抽屉 body 内边距漏配（回落 16px、Tab 条负边距 -18/-16，贴头位置与数据源差 2px）——把 14px padding/-14px 通栏负边距规则扩展到 `.xa-external-apis`；③核对徽标圆角（两侧均 9px，本就一致未动）。统一后五抽屉（任务管理/异步/潮汐/数据源/外部API）同口径实测：头部 64px、Tab 条 38px 贴头间距 0、Tab 12px/600/37px/padding 9px 16px、同一字体族。模拟库更名「回检系统库」（应用自身存储，业务语义真实），目录缓存键升 v14。**验证：** `pnpm typecheck` + `vite build` 通过；页面逐抽屉实测上述指标全部一致，数据源 Tab 显示 回检业务库/回检系统库。

- **2026-09-18（第六十七批）二级抽屉运动统一：** 组长指出二级抽屉（数据表/接口详情层）开着时切换一级抽屉，第二级固定原地、第一级滑动，中间割裂。根因：详情层收起是原地淡出+左向 18px 位移，与一级抽屉的水平滑动不同轴。修正为同一水平轴运动：①详情层开合方向改为「从列表右缘滑入 / 向右收回并淡出」（translateX ±48px + opacity，0.26s 同曲线），任何切换或收起场景下两层沿水平轴各自运动（一级左滑、二级右收）；②详情层渲染门控不再依赖当前模式，只看 300ms 滞后的 detailRender——跨模式切换（如外部API+接口详情 → 任务管理）时详情层完整播完收回动画后才卸载，不再瞬间消失；③清理重复的旧 keyframes 定义。实测逐帧采样：切数据源时一级 -213→0 滑入、二级 +24/0.51→+48/0 右收；整体收起时一级 0→-420 左滑、二级 0→+48 右收；跨模式切换二级 +32→+48 收回不再闪没。

- **2026-09-18（第六十八批）添加数据表弹窗加高：** 组长指出弹窗太矮、可添加的表看不全不好操作。`.xa-ds-import-tables` 列表区 max-height 260px→`clamp(320px, 56vh, 620px)`（随视口自适应），弹窗 body 由 340px 增至 600px，38 张表可视行数约 6 行→10+ 行。实测确认后未保存任何勾选。

- **2026-09-18（第六十九批）一级抽屉切换模式不再重放滑入动画：** 组长指出二级抽屉开着时切换一级，第一级重放滑入动画会把中间腾空。修正第六十五批「切换重放入场」的规则：滑入/滑出动画只发生在「关闭↔展开」的开关切换（transform 过渡）；已展开时切换模式原地替换内容、零动画——根节点去掉 key 重挂载、`.open` 移除入场 animation（删除不再使用的 keyframes）。最终行为口径：关闭→展开=滑入；展开→关闭=滑出（visibility 延迟隐藏）；展开中切换=一级抽屉体纹丝不动、仅内容替换，二级详情层向右收回。实测逐帧：二级开着切数据源一级 tx 恒 0、二级 +37/0.22→+48/hidden；收起 -376→-420 滑出可见；展开 -420→-94→-9→0 滑入正常。

- **2026-09-18（第七十批）二级抽屉只随显式点击开合：** 组长定稿简化规则——有二级抽屉时切换一级，二级不再自动恢复展开，切换过程只有一级抽屉自己的动画。实现：AuxiliaryDrawer 增加「mode 变化即清空 dataSourcesDetail」effect，一级切换/收起即清除二级选中（detailRender 滞后 300ms 仍保证收回动画完整）；切回列表抽屉只显示清单，行点击才再次打开详情。实测：外部API行点击开详情 ✓ → 切任务管理详情收回且卸载 ✓ → 切回外部API仅列表、无详情（detailRendered=false）✓。

- **2026-09-19（第七十一批）删除清单上方说明文字：** 组长指出数据源/外部API清单上方两行说明（「本地直连 · 已添加 N 张表…」「域名 · 已登记 N 个接口…」）无意义且复杂，整体删除。DataSourcesPage/ExternalApisPage 去掉非空态的 ds-hint-text（含 DATABASE_MODE_LABEL 计数文案），保留右侧操作按钮；ds-hint-actions 增加 margin-left:auto 维持右对齐，空态引导文字分支不动。实测：两抽屉 hintTextCount=0、连接设置/添加数据表、域设置/新增接口 右缘贴内容区右缘 ✓。

- **2026-09-19（第七十一批补）操作行补回静态短文案：** 组长反馈操作行只剩按钮太空，要求补静态文案且必须展示全不被省略。数据源侧「数据表按字段粒度参与应用API绑定。」、外部API侧「接口按出入参粒度参与应用API适配。」——保持库↔域、表↔API、字段↔出入参的维度对齐与绑定/适配词汇；文案 16 字静态不随 Tab 变化。实测：两处 scrollWidth=clientWidth（219/247px）无省略号 ✓。

- **2026-09-19（第七十二批）外部映射改双列连线画布 + 必填门禁：** 评审指出旧版逐行下拉会漏选外部必填入参（外部入参不可见→确认后调用缺参数藏 bug）。按「配置映射关系、重点在连线」重构外部服务映射：新增 WireCanvas（双列等高节点 + SVG 贝塞尔连线走廊，端点按行号推算、resize 跟随）与 ExternalMappingCards（入参适配/出参适配/请求预览三卡），应用出入参在左、外部出入参在右全部平铺（部位/必填/说明挂节点上）；交互=点源侧节点→点目标侧节点连线、点连线断开、点空白取消。数据模型反转：`requestParamMap`（契约→外部）改为 `requestFeeders`（外部入参→契约入参，空串=固定值来源，缺 key=未连接，显式登记是唯一事实、不再按名隐式推导），表达式新增 `ext:` 前缀存固定值常量；适配代码生成器支持固定值行 `request.put("x", "Y")`。必填防漏：未连接的必填外部入参节点红显 + 卡片警示条 + 状态徽标「必填未连接 N」+「保存并确认」禁用（tooltip 列名单）；左列新增「固定值」源节点补齐没有契约入参对应的必填外部参数。出参连线虚线=AI 建议（autoMatch），实线=人工；确认后只读常驻。目录存储：catalog v15（回检中心域新增「回检单详情」GET /api/rechecks/{recheck_id}，recheck_id/with_reviewer 双必填、响应含 reviewer_name/reviewer_dept，放外部接口首位使演示来源选择默认命中，语义与「查询审核人信息」完全闭环），app-apis v5。演示路径（v1.3 基线=旅程走完，勿在回放里留待办）：清理演示数据 → 生成新版本发布 v1.3 → 发起新迭代 v1.4 → 走完设计/计划确认 → 开发阶段点「查询审核人信息」→ 选外部API → 下拉选「回检单详情」→ 映射卡「打开字段映射」。实测（v1.4 实时流程）：初始 danger×2/警告/确认禁用 ✓；id→recheck_id 手动连线（pending 态+stub+实线）✓；固定值→with_reviewer 弹框填 Y（绿实线+固定值徽标）✓；预览 `GET /api/rechecks/{id}?with_reviewer='Y'` ✓；点连线断开即恢复警告与门禁 ✓；确认后 5/5、feeders/expressions/自动出参映射落库正确、只读画布保留连线 ✓。注意：浏览器截图接口在本环境持续超时，视觉核验以 DOM 几何量测代替。

- **2026-09-19（第七十三批）连线画布排版六点优化：** ①顶部契约头改两行明确对应关系——上行「应用API + 方法徽标 + 路径 + 接口名（动作按钮跟行尾）」，下行「外部API/数据表 + 方法徽标 + 路径 + 来源名」，替代原单行省略摘要；②参数行去矩形框，纯文本「名称（中文注释）」拉通排，行 hover 提供可点感，列头「应用API入参/外部API入参」即两侧身份（外部列头不再重复来源名）；③移除「固定值」源节点——两侧没有对应关系就不显示连线，不设常量补位（requestFeeders 空值/缺 key 一律未连接，AdaptationRow.fixed、ext: 表达式、适配代码固定值行全部移除）；④连线操作指引合并为编辑区顶部单条提示文案（dashed 提示条「连线操作…」），不再每张卡重复；⑤两侧展示统一为 `name（中文注释）`，必填/可选显式标明（必填红色、可选灰色；结合实际判断：外部API列出的参数并非全部必填，如 with_reviewer 改为可选，catalog 升 v16）；⑥fx 按加工对象归位——入参加工的是应用侧取值（fx 挂应用入参节点），出参加工的是外部原始值（fx 移到外部出参节点）。实测（v1.3 只读态）：双行契约头 ✓、节点无边框 ✓、两侧括号注释格式一致 ✓、必填/可选标识 ✓、固定值节点消失 ✓、出参 fx 挂外部节点（reviewer_name/reviewer_dept）且 AI 虚线保留 ✓。

- **2026-09-19（第七十四批）四点反排与「点不动」澄清：** ①主标题回到应用API（方法徽标+路径+接口名+动作按钮单行），外部API/数据表降为次行「数据来源」标记行（来源名 + 目标签名，弱化呈现）；②「去框」指入参适配/出参适配大卡片——两张卡合并为单一画布：WireCanvas 行增加 section（分组标题，24px）/spacer（10px）两类静态行，端点纵坐标按行类型高度累计推算，左列「应用API·入参/出参」、右列「外部API·入参/出参」四分区，参数标签框保留（32px，必填红星 `*` 在名称前同一 span 内、可选不标）；③「点不动」定位为已确认只读视图无任何提示被误读为失效——只读态顶部换明确横幅「只读 · 该映射绑定已确认；如需调整取值关系，请发起新迭代后重新配置」，交互代码经实时流程复测无缺陷；④可选参数不再显示「可选」字样，仅必填加红星。节点 id 加分区前缀 `in:l:`/`in:r:`/`out:l:`/`out:r:` 防应用出入参同名冲突。实测（v1.4 实时流程重走）：单画布四分区 ✓、主标题应用API+数据来源行 ✓、红星仅必填 ✓、id→recheck_id 连线成功（pending 态/手动连线/danger 清除/预览补齐/确认解禁）✓、fx 弹框打开（配置函数表达式 · id（id））✓。

- **2026-09-19（第七十五批）映射画布改 2×2 对齐矩阵：** 组长要求四区块按「列标题（应用API/外部API）× 行标题（入参/出参）」对齐成表格。WireCanvas 重构为区域矩阵模型：props 改为 `columnTitles + regions[{rowTitle, leftRows, rightRows}]`；网格四列（36px 行标题列 | 应用API 1fr | 96px 走廊 | 外部API 1fr），首行为列标题行（26px，居中），每个分区一个行标题单元格（grid-row span 全分区、垂直居中）；分区高度取两列行数较大值，短侧自动留空，行距 6px 由 grid rowGap 承担——**行距必须与端点纵坐标累计公式一致**（rowTops 同步 +NODE_GAP），否则连线与节点错位（首版即因缺 rowGap 错位 6px/行，已修复并量测端点=节点中心）。ExternalMapping 传两个分区：入参（应用入参|外部入参）、出参（应用出参|外部出参）。实测（v1.4 实时流程再次重走）：列标题与两列中心对齐（宽 235/235）✓、行标题入参 70px/出参 222px 跨分区 ✓、id 行与 recheck_id 行同 y（48/48）✓、手动连线 253,48→349,48 水平直线 ✓、出参 AI 虚线端点命中节点中心（258→258、296→182 等）✓、连线后必填已就绪/确认解禁 ✓。

- **2026-09-19（第七十六批）画布细节三修：** ①列标题（应用API/外部API）与行标题（入参/出参）统一样式——同为 11px 灰色（text-tertiary）常规字重；②行标题改为顶部对齐（flex-start + padding-top 8px，与分区第一行参数对齐），分区下方留空时标题不再坠中；③连线端点几何修正——行标题列 36px 使两参数列整体右移，走廊中心不在画布正中，原 `width/2±48` 端点导致线头够不到左列、箭头压上右列；改为按真实列边界取端点（leftEdge=36+列宽、rightEdge=左缘+96px），箭头 marker refX 6→8（尖端与路径终点重合不压区块），待连接引线目标同步改走廊中线。实测：线头 x=271 与左参数块右缘 271 完全贴合、线尾 x=367 与右参数块左缘 367 贴合 ✓、列/行标题同色同字号 ✓、行标题顶对齐 ✓。

- **2026-09-19（第七十七批）已确认绑定改为可调整态：** 组长再次点开字段映射遇到的是 v1.3 已确认只读视图（「箭头、fx 都不能操作」），只读横幅仍被当作失效。定稿：**确认后的绑定可直接调整**——FieldMappingEditor 的锁定只保留提交瞬间（locked=submitting），readOnly 仅约束动作区（已确认时按钮为「保存更新」、无「保存并确认」）；ExternalMappingCards 增加 confirmed 标记，指引条文案切换为「已确认绑定 · 可直接调整连线与 fx；调整后点『保存更新』更新配置」。AiChatPanel 两处配套修复：①确认态视图草稿按选中对象取面板编辑草稿（fieldMappingDraft && fieldMappingSelectedId === selectedId），否则用实现里的已存配置；②handleFieldMappingSave 目标对象改用 fieldMappingSelectedId（原用 activeBindingObjectId，确认态下为空导致保存直接 return）；移除「无工作流即清空面板草稿」的收口 effect（其本意是工作流被外部应答后退出编辑会话，与确认态可调整冲突；工作流内确认路径已在 handleFieldMappingConfirm 里显式收口）。实测（v1.3 确认视图）：无 off 节点、fx 可点、连线 id→recheck_id、fx 配置 LOOKUP(user, id, name)、保存更新后面板保持且存储 feeders/expressions 正确落库 ✓。

- **2026-09-19（第七十八批）自查清理与 1→N 表达式共享修复：** 全量自查映射画布相关改动。修真 bug：一个契约入参可同时喂多个外部入参（1→N），旧逻辑断开/重连其中一条会无条件删除 `in:契约参数` 表达式，连带弄丢仍被其它连线引用的加工逻辑——connectContract 与 handleWireClick 现都先检查该契约入参是否仍被其它外部入参引用，仅无引用时才清理。清理死 CSS：旧版行内 fx 钮（field-mapping-fx 及 pop/quick/hint 系列）、部位分组（field-mapping-group 系列）、row-external、card-head 共约 170 行（组件在七十二/七十三批重构后已无引用）。实测（v1.3 确认可调整视图）：连 id→recheck_id + id→with_reviewer 成 1→2、id 配 DEFAULT(:id,'0')、断开 recheck_id 线后 in:id 表达式与 fx 摘要保留 ✓、typecheck/build ✓。另：git 状态里 `scripts/tmp_repair_tables.py` 显示被删（非本次改动所为，已向组长报告待确认）。

- **2026-09-21（第七十九批）品牌定名 DevAgent Studio，全量替换文案与内部标识符：** 项目再次更名且命名趋于稳定，按 XCodeAgent→AIStudio 同口径整体替换。①文案：页面标题、BrandLogo 文字与 aria-label、工作台入场 kicker「DEVAGENT STUDIO WORKSPACE」、欢迎页操作区 aria-label、新建应用目录提示与目录占用报错、最近项目删除提示、GlobalConfigPanel 应用名称/标识默认值（DevAgent Studio / devagent-studio）、生成产物源码头注释「由 DevAgent Studio 生成」；②标识符（映射规则：`aiStudio`→`devAgentStudio`、`aistudio`→`devagentstudio`）：`window.aiStudio` 桥接整体更名 `window.devAgentStudio`（window.d.ts 类型、mock/preload 注入与全部消费方），存储键/事件/通道前缀更名 `devagentstudio:*`（data-sources v16、app-apis v5、async/tide-tasks v3、initialization-planning、user-applications、lifecycle-revision-hwm、applications、sessions、element-inspector、open-data-sources 事件、show-helper 位置键与 prototypeReset 清理前缀同步），`__aiStudio*__` 运行时全局同步，`.aistudio` 内部目录与 SkillsPage 路径提示改 `.devagentstudio` / `.devagentstudio_dev`，生成 Java 产物 `com.aistudio`→`com.devagentstudio`（groupId/包声明/导入；全库无 com/aistudio 文件路径耦合）。换键即弃旧缓存（既有模式，不做迁移）：旧 `aistudio:*` localStorage 键留作孤儿，最近项目按预置优先回退为 v1.3 存量应用一条。**验证：** `pnpm typecheck` 通过；浏览器全旅程——window.devAgentStudio 注入生效且 window.aiStudio 不存在、新键正常写入、v1.3 打开（入场 kicker/顶栏 Logo/阶段条/会话/版本徽标）、数据来源抽屉目录（回检业务库/回检系统库 + recheck/recheck_audit/user 表清单）、技能页 `.devagentstudio_dev/skills` 路径提示、rechecks-controller.java 产物 `package com.devagentstudio.rechecks.controller` 与「由 DevAgent Studio 生成」头注释、欢迎页与工作台可见文本及 aria-label 无 AIStudio 残留 ✓。

- **2026-09-22（第八十批）字段映射侧面板直连绑定 + 草稿自动保存恢复：** 组长转达评审建议——绑定数据目前只能等工作流推进到映射绑定节点才能发起，交互不灵活；且绑定到一半的草稿要能跨对话恢复。①直连入口：开发产物详情对未确认绑定的应用API把「数据绑定进行中」占位换成动作卡——「数据绑定待完成」说明 + 主按钮（已选定来源为「继续字段映射」、未选定为「绑定数据来源」；来源已从目录移除时仅保留警示不给按钮；只读版本不传回调不显示）。②直连模式：fieldMappingView 新增两分支——已选定来源未确认复用 confirmedBindingView 同源派生（readOnly=confirmed，确认按钮文案「确认绑定」）；未选定来源呈现「选择数据来源」选择卡（数据表+外部接口带类型前缀平铺，一步完成「类型+来源」选择），选定即走与工作流「选来源」同一落位（withSelectedSource：模板槽位+AI初步映射+推荐表达式）。直连「确认绑定」走工作流确认同一权威路径（withAppliedBindingDraft 定稿 + buildAppApiAdapterSource 适配文件落库；当前对话尚无会话记录时跳过文件交付，产物完成态由 confirmed 驱动不受影响），确认后右面板切开发产物并定位该接口调试视图。③草稿自动保存：面板编辑防抖 600ms 写回应用API存储（仅未确认对象；已确认绑定只有显式「保存更新」才覆盖），写入以存储快照为基底防闭包旧数组覆盖其它面板写入，显式保存取消挂起定时器。恢复路径=既有 bindingDraftFrom 读存储（按应用+版本键、跨对话共享），重开对话后从开发产物或字段映射目录进入即取到草稿。④草稿归属修复：目录切换/入口打开统一经 claimFieldMappingDraftFor——草稿正本始终属于当前选中对象，切对象即按存储草稿重建底稿（修掉旧版确认态判据 fieldMappingSelectedId===selectedId 恒真导致的跨对象误带草稿隐患）；目录行加绑定进度点（灰=未开始/紫=绑定中/绿=已确认）；无活动绑定打开字段映射 Tab 自动定位第一个API。**对通用交互原则的修订**：右侧面板「只承载静态与终态」原则自本批起对字段映射工作台开例外——数据绑定允许从右侧直接发起、编辑与确认（组长明确要求，见本批）。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器实测（v1.4 实时迭代）：v1.3 确认视图无回归（保存更新 + 目录状态点）✓；产物详情「绑定数据来源」→ 来源选择卡选「回检单详情」→ 画布/必填门禁/「确认绑定」按钮 ✓；连 id→recheck_id、id→with_reviewer（1→2）不点保存约 600ms 后存储 feeders 落盘 ✓；fx 配 DEFAULT(:id,'0') 落盘 ✓；切目录走/回草稿从存储恢复（来源、连线、必填已就绪、fx 芯片）✓；直连确认 confirmed=true + 产物 0/4→2/4 + 调试视图定位 ✓；数据库API（查询我的回检→recheck 表）同流程 ✓；用户自有 v1.0 应用存储无污染 ✓。注意：预置应用的发布/迭代状态是会话级演示（刷新回 v1.3 基线，第六十三批遗留观察），迭代内草稿的跨刷新恢复随迭代本身不持久化而失效，跨对话恢复不受影响。

- **2026-09-22（第八十一批）直连绑定两处回归修复：** 组长实测反馈：①绑定节点出现时右侧不再自动切到字段映射，仍停在开发产物；②开发产物占位卡上的「继续字段映射」按钮黑字看不清。**根因①：** 第七十六批起的自动打开闩挂在「面板草稿是否存在」上（fieldMappingDraft 非空即永久不再接管）——草稿一旦经由任何路径产生（直连入口点过一次、字段映射 Tab 切过一次、HMR 状态残留），后续工作流到达映射绑定节点时就不再自动打开。改为**按工作流实例（runId）闩**：同一 runId 只自动打开一次（用户切走 Tab 不被拽回的既有语义保留），新的绑定工作流（新 runId）到来时再次接管；接管时草稿正属于当前绑定对象则原样保留编辑现场，否则按存储重建底稿；自动打开同时把收起的面板恢复分栏。**根因②：** `.api-terminal-placeholder` 的后代选择器 `span` 把卡片内 antd 按钮的标签 span 一起染成次级灰 11px（紫色主按钮上是深灰小字，不可读）；改为直接子代 `> strong` / `> span`，按钮文字回归白字 14px。**验证：** `pnpm typecheck` + `vite build` 通过；实测（v1.4 实时迭代重走）：先制造非空面板草稿（切一次字段映射 Tab）→ 切回开发产物 → 发起「查询我的回检」工作流 → 答类型「数据表」、级联选 recheck 表 → 绑定卡出现时面板自动切到字段映射 ✓（旧逻辑在此场景必然不切）；面板内「保存并确认」→ 工作流推进到完成、confirmed 落库、面板按既有行为切回开发产物 ✓；占位卡按钮 span 白字 14px ✓。

- **2026-09-22（第八十二批）工作流重跑的已配置检查：** 组长补充——侧面板直连配置过类型/来源后，左侧工作流新对话重新执行同一条链路时要有检查：已配置的节点只展示提示、用户可再调整、填充值沿用已有配置。①剧本（workbench.ts replayAppApiWorkbench）：「配置映射绑定」澄清载荷构造抽为 bindingClarificationFor 共享助手（来源选定续跑与已配置检查两条路径同形，草稿一律 bindingDraftFrom 从存储提取）；在续跑分支与全新启动之间插入 4.5 已配置检查——bindings 已存在（非 local）时跳过类型/来源发问，轨迹直接展示「检测到已配置数据来源类型：外部API。」「检测到已选定数据来源：…」完成态步骤并落到映射绑定待确认卡（卡片预填存储里的模板槽位/连线/表达式）；已确认完成的接口重跑整链直接给完成态，不重复发问也不重复交付适配文件。②发起门禁放行（AiChatPanel composerMentionItems）：resolveArtifactOccupancy 原本把 app-api 的「进行中」一刀切禁止重跑——而直连选完来源后产物正是进行中，重跑永远不可达；改为绑定进行中（已选来源未确认）允许再次发起，已确认完成仍禁止。**验证：** `pnpm typecheck` + `vite build` 通过；实测（v1.4 重走：直连选回检单详情+连一条线不确认 → 启动器重新发起该API）——工作流轨迹 读取契约✓ → 类型节点「检测到已配置数据来源类型：外部API。」→ 来源节点「检测到已选定数据来源：回检单详情 · GET /api/rechecks/{recheck_id}。」→ 映射绑定待确认（面板自动切字段映射且预填连线，存储 feeders 保留）→ 面板「保存并确认」→ 适配生成完成、confirmed 落库、产物 1/4 ✓。注意：绑定卡文案由卡片按 clarification 字段固定生成（「请在右侧…完成映射绑定」），「检测到…已预填」的说明由类型/来源节点的步骤提示承载。

- **2026-09-22（第八十三批）直连来源选择改三级树：** 组长指出直连来源选择卡把数据源与外部API清单混在一个扁平下拉里，工作流节点是拆成两步的，真实场景来源一多肯定不行——要么面板也拆两步，要么用树结构分级展开。定稿树结构：TreeSelect 三级「类型（数据表/外部接口）→ 连接/域 → 表/接口」，一个控件完成、层级即目录抽屉结构（数据库按连接分组、外部接口按域分组），叶子 value 仍为 bindingKey，onSelectSource/withSelectedSource 链路不变；选类型与选来源的语义由叶子所在分支承载，不与工作流的两步卡强求同形。配套：showSearch + treeNodeFilterProp 支持按名称搜索（过滤保留父级链）、treeDefaultExpandAll 展开全树、dropdownMatchSelectWidth=false 让层级缩进不挤压；提示文案改为「按『类型 → 连接/域 → 数据表/接口』逐级选择」。类型定义 MappingSourceOption（扁平）废弃，改为 MappingSourceTreeNode（title/value/selectable/children），面板层保持纯展示、树由 AiChatPanel 从 useDataSourceIndex 组装。**验证：** `pnpm typecheck` + `vite build` 通过；实测（v1.4 迭代）——树完整呈现 数据表（回检业务库：recheck/recheck_audit/user；回检系统库：recheck/recheck_comment）与 外部接口（回检中心：回检单详情/回检中心查询；门户平台：查询用户信息/发送消息通知）两级分组 ✓；选「回检业务库 → recheck（需求回检记录）」叶落定后模板编辑器与确认动作正常 ✓；搜索「回检单」仅剩 回检系统库 recheck（回检单）与 回检中心 回检单详情 两条命中（父级链保留）✓。两个连接各有一张 recheck 表的同名场景由层级天然消歧。

- **2026-09-22（第八十四批）来源树下拉视觉打磨：** 组长反馈树形菜单功能都对、就是不够美观。①分层排印（title 改富样式节点，搜索过滤改走新增的 filterTitle 纯文本字段 + treeNodeFilterProp）：类型分组行=图标（DatabaseOutlined/ApiOutlined，主题紫）+12px/600 次级色；连接/域行=12.5px/500 正文色；叶子行=名称 12.5px/500 +（说明）11.5px 弱色非斜体。②行观感：行高压缩（content-wrapper 24px、圆角 6px、120ms 过渡）、缩进单位 24px→14px、展开箭头弱色、悬停底 surface-hover、选中叶 accent-soft+accent 文字、分组行不可选中（悬停无底色默认光标）。③样式统一嵌套 .ant-select-dropdown 提升优先级压过 antd 默认行样式（首版单类选择器被 antd 同名规则以优先级压住，圆角实测仍 2px，改后 6px 生效）；浮层为 Portal，--wb-* 变量经 global.less 的 body:has 桥接解析，深浅色自适应。**验证：** `pnpm typecheck` + `vite build` 通过；实测——分组图标紫/字重 600、连接 500、叶子名称与说明双色、圆角 6px 全部按计算样式确认 ✓；搜索「需求回检记录」过滤后点击叶子，来源落定「回检业务库 · recheck」且模板编辑器出现 ✓。测试观察（非缺陷）：rc-tree 虚拟滚动只渲染视口内行，自动化测试直接按全文找节点会落空，走「搜索过滤→点击」路径即与用户一致；展开状态在 Select 实例存续期内保持（treeDefaultExpandAll 只在首次挂载消费）。

- **2026-09-22（第八十五批）来源树下拉滚动条弱化：** 组长反馈下拉列表滚动条太突兀。按 MessageList 已有的滚动条弱化约定再收敛一档：`.ant-select-tree-list-holder` 默认 thumb 全透明（含 Firefox scrollbar-color），悬停列表时才浮现 6px 圆角细条（var(--wb-control-hover)），轨道恒透明——浮层是临时容器，常驻滚动条没有存在必要，滚动动作必然发生在悬停中，视觉上等于隐藏。**验证：** `vite build` 通过；浏览器实测 scrollbar-width=thin 且 webkit 的 width/thumb/hover 三条规则均已注册生效。

- **2026-09-22（第八十六批）数据源绑定代码局部重构（零行为变更）：** 组长要求对近两日数据源/字段映射相关代码做质量优化与技术局部重构，不得损失交互与功能，最后充分验证。三处拆分 + 一处去重，全部为纯代码移动，不改任何行为：①**AppApis/model.ts（1064 行）拆出两个纯推导模块**——`templates.ts`（契约入参全集 contractRequestParams/contractPathParams、注释相似度列评分 scoreFieldToColumn、增删查改模板 deriveTableOp/buildTableTemplate/LOGIN_SCOPE_PATTERN/columnForTerm/suggestOrderBy）与 `adaptation.ts`（适配行 AdaptationRow/adaptationExpressionKey/externalAdaptations、表达式推荐 recommendAdaptationExpressions/termsOverlap）；私有解析函数 externalNameFromMapping 更名为导出的 mappingSourceName（旧 index.tsx 里的同义 sourceNameOf 一并删除，SQL 预览与适配视图共用一份标签解析）。model.ts 保留类型/匹配/生命周期，头部统一再导出拆分模块的全部公开名——**10 个既有引用方一行未改**；依赖单向（adaptation→templates→model 类型，无运行时环），model 剩 835 行。②**FieldMappingPanel/index.tsx（448 行）拆出 `DatabaseMapping.tsx`（218 行）**——数据库模板编辑器四张分节卡（条件/写入/返回字段/SQL 预览）+ buildSqlPreview，与 ExternalMapping 连线画布对称，index.tsx 只保留目录/契约头/来源选择卡/按类型分发（剩 255 行）。③**来源树构建器抽出 `FieldMappingPanel/sourceTree.tsx`（99 行纯函数）**——buildFieldMappingSourceTree(目录索引) 从 AiChatPanel 迁出（该文件 4069→3994 行）。**验证：** `pnpm typecheck` + `vite build` 通过；浏览器全链路回归（v1.4 实时迭代重走）：直连数据库路径（树三级渲染→选叶→DatabaseMapping 模板编辑器条件/返回字段/SQL 预览→确认绑定→confirmed+五字段收口回填+2 条件）✓；工作流外部路径（类型卡→来源卡→绑定卡→面板自动切→连线自动保存 feeders 落库）✓；同对话 pending 时重跑被正确拦截（「当前有等待确认的工作流节点」保护仍在）✓；工作流内保存并确认→完成→产物 2/4 ✓；已确认视图（连线画布只读可调+fx+保存更新+目录绿点）✓。SQL 预览草稿期「回检单号 未映射」为既有行为（60 分疑似待确认，确认时推荐回填），非本次回归。

### 实体数据层交互草稿（2026-09-10）

- 设计阶段的实体仅存在于需求规格说明书的「实体」分栏，描述用途、需要记录的信息和需要支持的业务操作，不展示字段类型、参数、返回结构或数据实现，也不出现独立实体 Tab。
- 同一实体覆盖完整旅程：需求分析阶段在 RequirementSpec 的「实体」分栏描述用途、业务字段和操作；计划阶段一级保持「技术规划方案」，在其内部子 Tab「实体」细化字段、操作输入输出和数据实现；开发阶段在「开发产物」内部按对象查看操作及实现来源；测试、审查和验收阶段继续只读查看并验证对象操作结果。业务操作描述存于 `entities[].business_operations`，内部技术计划仍可使用 `entities` 字段，但用户投影统一称为实体。
- 页面关联指向「实体.操作()」。对象不持有唯一数据源；客户风险、订单概览属于客户操作，组合结果不自动创建新对象。完整字段关系默认折叠在高级区域。
- 需求描述沿用现有需求表单、AG-UI 保存草稿和需求文档确认流程。计划阶段的数据实现为交互草稿；不读写真实数据库、不调用外部服务、不写回 TechnicalPlan，也不替代原 Workflow 的产物确认门。原型通过 `devagentstudio:prototype:*` 浏览器缓存让实体的数据实现从计划阶段延续到开发阶段，演示辅助的“清理演示数据”会一并复位。正式落地时必须接入既有 AG-UI 产物与确认流程，不能把本机缓存作为生产存储契约。
- 组件职责：`BusinessObjectsPanel` 负责阶段投影与草稿；`BusinessObjectEditor` 负责局部编辑；`OperationDetails` 负责操作与页面关系；`ImplementationPanel` 负责来源选择、AI 匹配、异常处理与确认（2026-09-11 起拆分为“计划意向 / 开发绑定”两种模式）；`DataSourcesPage` 只管理数据库和外部服务目录，不承担绑定。来源目录与实体草稿分别由 `DataSources/catalog.ts`、`BusinessObjects/store.ts` 跨面板同步。样式复用浅色/深色 `--wb-*` 主题变量，并适配窄右侧面板。
- 技术规划方案的用户投影对齐原工程 DocPanel 的五段结构，二级页签为「架构 / 实体 / API 契约 / 页面 / 权限」：架构承载三层技术架构与数据来源目录；实体沿用 TechnicalBusinessObjectsReview（实体投影）；「API 契约」由实体操作实时投影为 REST 资源契约（每对象一个复数资源、每操作一个接口，方法按操作语义映射、内置 CRUD 走模板路径、自定义动作取演示短 id），接口行可点开 Inspector（鉴权/参数/请求/响应/错误码/数据实现意向），页签内交互只改阅读位置；「页面」逐页展示 `页面 → 实体.操作() → METHOD path` 绑定行；「权限」把 requirementSpec.user_roles 投影为角色卡，权限去“查看/查询”前缀后与操作名双向包含匹配到接口，未命中标注“待开发阶段绑定”。审阅面板排版同步收敛：章节标题 20/12px 纵向节奏、卡片 14/16 内距、技术规划方案专属样式拆分到 TechnicalPlanReview.less。底层演示 JSON 仍保留 Endpoint/API Contract 作为内部结构；开发产物目录同样隐藏独立接口分组，用户点击实体后直接在右侧选择操作并绑定数据库或外部服务。
- 原型验证使用 `pnpm typecheck` 和 `vite build --config vite.prototype.config.ts`；`prototype/` 没有 `build` 脚本。验证输出目录 `dist-business-objects/` 已忽略。

本次改版用以下五条新规则替代旧原型规则：

1. **对话不再拥有产物编辑权。** 产物和代码文件由活动 Workflow 的 DAG/Task 范围临时控制写入；对话只负责用户交互、任务发起和状态呈现。
2. **对话严格隶属于阶段，阶段间消息、Workflow 与推进权完全隔离。** 需求分析、项目计划、开发、测试、审查和验收各自有一条默认常规对话；“默认单对话”仅指用户可在当前阶段不拆分上下文地完成该阶段旅程。页面、接口等上下文显著增多时，专业开发者才在当前阶段主动新建常规对话隔离上下文。每个阶段同一时刻只有一条“当前推进对话”。当前推进对话有运行中或待确认任务时禁止新建；空闲时点击“新建”即创建并直接取得推进权，可以马上输入。点击已有对话只切换查看对象，不转移推进权；查看非推进对话时，用户可从顶部提示主动发起并二次确认转移。打开、新建或查看对话不联动产物选择、右侧文件/预览、弹框或其它视图。
3. **业务测试用例提前异步准备。** 需求规格说明书确认后，用户先选择“异步任务”或“潮汐任务”，用例队列整体进入对应任务系统；开发期间持续生成，顶部测试阶段的数量芯片动态绑定用例所在队列并可直接打开该系统抽屉，右侧“测试用例”Tab展示详情。开发完成即可进入测试，单条用例只有在生成并校验后才可执行。
4. **开发阶段代码实现通道化。** 前台 Workflow 的用户产物只分为页面与实体。页面开发负责布局、交互与调用 `实体.操作()`；实体开发负责逐操作绑定数据库/外部服务、AI 字段匹配、多来源关系与数据处理逻辑。内部 Endpoint 可继续作为实现结构存在，但不进入用户产物清单、发起器或开发计数。

5. **初始化规划与工作台状态分离。** 设计阶段只审阅一份需求规格说明书；页面行为、状态和验收以表单字段合并进该说明书，ProductPlan 仅保留为下游生成的内部结构。计划阶段独立审阅 TechnicalPlan，确认后才生成模板并进入开发；设计与规划文档均不再走 Diff。初始化规划记录按应用版本持久化在浏览器缓存，状态转换由领域状态机校验，剧本只负责模拟进度和 AG-UI 快照；演示辅助悬浮球（`show-helper/`，位于 prototype 根目录、与 mock-data 同级的原型辅助区，不属于正式渲染层代码，经 @show-helper 别名挂载）常驻悬浮于界面右下角，可拖拽挪位、点击展开功能菜单，其“清理演示数据”只清理原型缓存（会话、任务与初始化状态），不删除静态存量应用目录。
6. **设计与规划确认均由对话主导，右侧原位编辑。** "确认需求规格说明书""确认 UI 设计稿""进入技术规划方案""重新生成 / 确认技术规划方案"都是各自 Workflow 节点内的连续动作；右侧仅在"预览"和"编辑"两种本机状态之间切换，不使用弹窗或右侧确认按钮。**"修改需求"与右侧面板头部的"编辑"开关只是把右侧切到原位表单的本机动作，不作为工作流答案提交、不消耗确认门**；确认卡保持挂起，用户保存草稿或退出编辑后仍在原卡确认或继续调整。**头部右侧只放两组控件：状态切换开关（预览|编辑，锚定右缘不位移）与保存草稿（仅编辑态出现在开关左侧）**；状态标签（待确认/编辑中/已确认）固定在左侧标题区，不参与右侧操作区的增减。**编辑态与预览态共用同一组分栏（概览、页面与操作、业务流程、实体、权限需求、验收标准），激活分栏在两种状态间保持，观感上是同一份文档**；页面基础信息与页面行为、状态、页面验收合并在"页面与操作"分栏内维护。**UI 设计稿首次生成期间右侧保持需求规格说明书预览（标记已确认），生成完成进入逐页确认时才切到 UI 设计稿；确认阶段内的重新生成（批量选模板）期间右侧停留当前产物不切换**；原位表单编辑期间右侧不自动切换产物，避免丢失编辑中的草稿。活跃门禁卡所在的轨迹节点保持展开，下一步入口不能被节点收起折叠。需求确认前按真实 RequirementSpec 的应用定位、角色、模块、页面、实体、流程、权限、页面行为和验收标准执行完整性检查，生成提示词同步进入原型 AG-UI 状态。右侧自动跟随按“产物里程碑”让位：同一产物审阅期间用户手动切换 Tab 后不抢回；下一个产物就绪待审阅（需求规格说明书 → UI 设计稿 → 技术规划方案）时右侧重新接管并定位到当前门禁产物。任何规划确认、重生成或阶段进入提交成功后立即退出右侧原位编辑态（过程互动静默操作除外）——编辑草稿基于旧版本，不允许继续保存。确认卡进入编辑的按钮与右侧开关统一措辞为“编辑需求”；技术规划方案确认卡的“重新生成”先展开选填修改意见输入，留空才退回默认文案。

旧版以下规则已经废止：

- “一个产物只能由其默认对话编辑”；
- “系统按产物自动创建产物专属对话，并自动投放模板卡推动工作流”（现在的多条开发对话由用户按边界主动创建，产物由用户 @ 提及发起）；
- “点击产物必须跳回所属对话”；
- “静态依赖或输入框附件用于扩张对话写权”；
- “测试阶段只有一份测试报告，不展示用例清单”；
- “普通新建对话可以承接正式代码修改”。

## 二、核心层级

**应用**：用户持续维护和迭代的软件工程，是版本链的根。

**版本**：一次完整制作周期形成的不可变资产快照。版本生成后，当前版本的产物、Workflow 操作和对话输入只读；新变更只能发生在后续迭代版本。

**阶段**：单个版本依次经过设计、计划、开发、测试、审查和验收。设计阶段包含需求规格说明书确认与 UI 设计；计划阶段包含 TechnicalPlan 与模板生成。版本生成是验收后的确定性终态动作，不是第七阶段。

**阶段主对话**：当前阶段的默认正式交互入口。用户可以一直在主对话推进，也可以主动新建额外对话隔离上下文；对话数量不由产物数量决定。它可以先后启动多条 Workflow，汇总阶段状态并引导用户满足准入条件，但它本身不拥有文件或产物写锁。

**Workflow 实例**：一次具有明确目标、DAG、任务范围和终态的正式执行。一个阶段主对话可以承载多条 Workflow；一条 Workflow 可以交付多个强关联产物，并在 DAG 内并行执行路径不冲突的任务。

**产物**：工作区中的长期业务事实。设计阶段产出一份需求规格说明书与可选 UI Manifest；页面行为的内部结构随说明书保存，计划阶段产出 TechnicalPlan 与模板就绪事实，开发阶段交付页面/接口/实体及代码，测试阶段执行测试用例并记录结果与缺陷，审查阶段交付审查报告，验收阶段不新增正式文档。

**代码文件**：实现一个或多个产物的工程事实。文件不属于阶段主对话；每次修改必须归属于当前 Workflow 的具体任务和允许路径。

## 三、对话、Workflow 与产物关系

关系固定为：

```text
一条阶段对话 → 多条 Workflow
一条 Workflow → 一个或多个相关产物
一个产物 → 历史上可被多条 Workflow 依次修改
一个产物在同一时刻 → 只允许一条活动 Workflow 写入
```

每个阶段默认使用自己的主对话，用户需要隔离上下文时才在该阶段新建额外常规对话；开发阶段通常最需要拆分，但对话管理规则对其它阶段保持一致。进入下一阶段时，系统创建或恢复下一阶段的默认对话，不能复用上游消息、Workflow 或编辑权限。开发阶段右侧默认打开“开发产物”Tab；进入编码 Diff 时临时切到“应用文件”，产物审查仍回到“开发产物”内容区，不使用独立浏览器。编辑权限是独立于产物、且以阶段为边界的单一权限，只有用户明确确认后才在当前阶段转移。

产物上下文和文件范围由当前 Workflow 根据正式计划、DAG、目标文件和允许路径确定；对话不维护产物附件、归属或写锁。

## 四、阶段与主对话职责

### 1. 设计阶段 · 产品 Agent

设计阶段主对话澄清业务目标、角色、流程和功能边界，随后生成一份需求规格说明书；用户通过与真实工程对齐的表单维护应用、页面、角色、页面行为、状态、权限和验收标准，确认后才生成 UI 设计稿。UI 设计稿按页面独立展示（顶部页签横排，与本面板其它产物的分栏方式一致）；「确认 UI 设计稿」卡内逐页列出待选版式（未选择前显示“待选择版式”，不展示种子默认模板名）并可展开“选模板”（低保真线框翻看候选，翻看起点定位该页当前版式）；选模板只本地记录不触发生成，全部页面选定后自动一次性批量交后台重画、右侧预览统一刷新；生成中卡片钉住、只加“生成中”标签并禁用动作；生成完成后“确认全部设计稿”才可点击；确认或跳过后卡片转为终态记录——逐页模板名 + 已确认标记，不再渲染动作按钮。右侧 UI 设计稿面板为纯静态呈现（页签、预览、页签上的已确认对勾），不提供任何第二套确认/重画入口。确认或明确跳过后阶段完成：产品 Agent 只输出一句收尾文本（不再渲染阶段门禁大卡），进入计划阶段由统一的 PhaseGateModal 弹框承载——弹框自动弹出一次，可「暂不进入」，之后从顶部阶段条「计划阶段」再次唤起，确认后才生成技术规划方案。正式文档不展示 Diff。

**一问一答与单卡单消息。** 设计/计划阶段对话对齐原工程：不是工作流轨迹驱动，而是一轮对话一条消息，每条 assistant 消息至多直接挂一张交互卡（澄清向导卡或确认卡），不渲染 ProcessSteps，也没有外层汇总壳卡。生成中的轮次展示轻量进度条（“正在生成需求文档 / UI 设计稿 / 技术规划方案…”）；用户在卡片上提交澄清或确认后，答案先以用户消息留痕（澄清/修改意见提交只补一句确认话术，答案由上方转为只读回看的卡片承载，不重复罗列；确认/重生成/阶段进入为一句用户口吻指令，技术规划方案重新生成携带真实修改意见），随后追加一条新的 assistant 消息——对话呈现一轮一轮来回的节奏；上一轮的卡片留在原消息里禁用只读回看。右侧保存草稿等静默续跑只刷新当前消息的快照，不新增对话轮次、不产生用户消息；UI 设计稿确认阶段的过程互动（批量选模板）同样静默续跑——原地刷新当前确认卡与右侧预览，整个确认阶段自始至终只有一张活动卡，直到“确认全部设计稿”或“跳过”；改稿轮生成期间快照短暂不带 clarification，卡片按 ui_confirmation 阶段名兜底钉住（MessageList 与卡片双重判断），绝不卸载重挂——只把动作按钮置为禁用并显示“生成中”状态标签，右侧面板布局恒定、生成状态只出现在预览区；“确认全部设计稿”在所有页面完成版式选择并生成前保持禁用；确认/跳过提交后追加的新 assistant 消息只有一句阶段收尾文本，`planning_stage_entry` 门禁与 `development_entry_confirmation` 一样不在对话区渲染卡片（WorkflowRunCard 返回 null），由 PhaseGateModal 统一弹框承载并支持顶部阶段条再唤起；开发/测试阶段的原地确认（Diff 接受、验收、执行方式选择、用例授权）同样不插用户消息。确认卡不展示「已生成产物」落库路径分区——产物本体由右侧审阅区承载，路径属于内部实现细节（ProductPlan 等内部结构更不得作为用户产物露出）。

**确认卡历史。** 用户提交澄清或确认后，原消息中的确认卡必须保留问题、选项和提交答案。历史卡保持原有向导形态与“第 X / Y 项”步骤指示，通过“上一步 / 下一步”翻页回看每一题的当时选择；控件整体禁用置灰、不再提供提交动作。历史卡不能只显示“已失效”的空提示，也不能把多题平铺成静态长表单。

**产物发起引导卡。** 空开发对话与无目标发送由研发 Agent 投放一次「产物」发起引导；引导消息复用对话区交互卡的卡壳（workflow-run-card：同一套背景、圆角、内距与信号点标题结构），只是卡内内容不同——分“页面 / 实体”两个浅色分组区块（带与产物面板左列同款的类别图标）平铺全部候选产物。行内只保留名称与状态徽标（待实施 / 实施中 / 后台任务 / 已交付 / 继续处理），不展示路由、字段数等补充字段；路由等补充文本仅作为产物面板搜索的匹配数据，不进展示层。发起卡与输入区「产物」按钮共用同一份候选、状态徽标与发起链路：点击“待实施”行直接发起实施（不产生用户消息输入），点击“继续处理”行恢复该产物的后台工作流，禁用行保持静态置灰并以悬浮文案说明原因。旧的“一句话 + 选择产物链接”形态废止；输入区两级选择器保留，用于产物较多时的搜索定位。

### 2. 计划阶段 · 项目 Agent

计划阶段主对话读取已确认的需求规格说明书与 UI Manifest，生成 TechnicalPlan；TechnicalPlan 由开发角色审阅确认后，才进入应用模板生成与开发准入。技术规划方案承载架构、实体、API Contract、Schema、页面技术绑定与默认角色授权（权限页签投影需求角色），不重复维护产品行为。确认技术规划方案后模板生成是计划阶段最后一环：Agent 只补一句「技术规划方案已确认，应用模板已生成，计划阶段完成。」收尾文本，进入开发阶段由统一的开发准入门弹框（TestCaseTaskTypeModal，PhaseGateModal 壳，选择测试用例生成任务类型）承载——领域状态先落到新增的 `awaiting_development_entry` 门禁阶段（快照 mode=`development_entry_confirmation`），弹框可「暂不进入」，之后点击顶部阶段条「开发阶段」再次唤起（workbenchPhase 将该阶段归入计划阶段集合，旅程在确认前不推导进开发）；确认后记录任务类型、推进到 `ready_for_workbench`，自动切换到开发阶段对话并创建测试用例后台任务。

### 3. 开发阶段 · 研发 Agent

开发对话负责页面和实体实现，以及单元测试、调用检查、构建和基础启动验证。进入开发阶段不自动投放模板卡，研发 Agent 先提示用户从「产物」入口选择“页面 / 实体”。页面候选显示页面名与路由，实体候选显示字段数与操作数；二者共用待实施、实施中、后台任务、已交付和继续处理状态。选择页面后进入模板、布局与交互设计；选择实体后右侧直接打开对象工作台，主对话显示“读取实体结构 → 绑定操作的数据实现 → 生成数据适配逻辑”的轨迹。页面对内部接口无感，只调用已经确认的 `实体.操作()`。产物实施时右侧与生成过程配套展示：工作流「生成代码」节点按帧渐进产出 Diff，首个分帧到达即切到右侧源码区并逐帧跟随累计；生成结束落「确认代码变更」时源码区已是完整 Diff，撤销/接受授权仍只保留在对话区改动卡上。页面工作流只交付页面本体（选择页面模板 → 详细设计四步 → 选择页面执行方式 → 生成代码 → 确认代码变更 → 启动产物审查），不再包含「选择接口执行方式 / 接口代码生成」环节；页面数据通过 实体.操作() 提供，构建计划与页面详设数据也不再携带接口单元。

**多条常规任务。** 任务管理抽屉只列出当前阶段的常规任务，并提供“新建任务”。列表不展示消息数、待确认等业务状态，只标识唯一的“当前推进”任务；该任务执行工作流时显示“推进中”，其余任务默认即在行内显示“仅查看”，不依赖选中切换。所有条目同宽等高，用户自建任务的删除按钮常显收在条块右缘槽位内（二次确认，删除后聊天记录一并清除）；当前推进中的自建任务删除按钮置灰不可用（业务上不允许删除推进中的任务，悬浮提示先转移推进权，转移后即可删）；阶段默认任务是该阶段唯一的系统推进入口，不渲染删除入口。新建是明确的工作意图：当前推进任务有真正运行中或仍可操作的待确认事项时，“新建任务”置灰并提示先完成当前任务；阶段准入门（规划/开发）挂起不算未完成事项，不锁新建，且门禁检测跨该阶段全部会话进行，推进权切到新建任务后弹框与顶部入口仍可正常确认；确认一经提交、卡片转为历史记录，就必须立即释放运行占位，不能再阻止新建。空闲时新任务立即成为当前查看上下文并取得推进权，可以直接输入，同时不清空或切换右侧产物、文件、预览和弹框。点击已有任务只打开历史，不弹权限框，且任务管理抽屉保持打开——切换、删除、新建等管理动作连续进行不被打断，条目徽标与选中态随切换即时刷新；查看非推进任务时，原推进任务空闲才可从顶部提示二次确认转移推进权。空开发任务取得推进权后由研发 Agent 投放一次「产物」发起引导，只读期间不投放。开发者可以按自己的认知边界组织上下文，例如把两个相关页面放在一条开发任务中；任务不绑定产物归属，产物写入仍由活动 Workflow 承载。

两套任务系统从存储到调度完全独立：异步任务（常规算力域）与潮汐任务（闲时算力域）各有独立流水、独立入口与抽屉，引擎逐系统推进、互不等待，同一系统内一次只执行一个任务——用例生成与代码实现都在本系统队列里按顺序逐个推进。后台实现任务按“排队 → 执行 Build DAG → 生成代码 → 构建及单元检查 → 启动产物审查 → 完成”无人值守推进；同步任务由剧本在对话内按同一阶段顺序当场推进（生成代码后经「确认代码变更」接受 Diff，再经「启动产物审查」节点把右侧工作区切换到“开发产物”并定位当前产物），引擎不调度同步任务；其记录仅用于产物状态推导，不进入任何队列抽屉，验收入口由对话输入框的「待验收」快速选择承载。

工作流底层采用“一套 DAG、多种表现段”的模型（`mock/workflowGraphs.ts`）：真实工程的底层 DAG 庞大交织，对话面板只呈现本次选中执行的一段。开发工作流注册了一张完整 DAG（页面设计分支与接口设计分支交织，汇聚到“选择执行方式”，再分出同步构建链与后台构建链，完成后进入验收段并预留验收调整回流）；需求分析工作流与项目计划工作流也各自注册了完整 DAG（节点序列对齐真实工程应用规划 Graph 的 requirements / project_planning 段：澄清与确认都是待输入门禁；「需求变更意图分析」借自真实工程的 design_intent_analysis、「映射业务权限规则」借自真实工程 ProductPlan 的权限规则映射），剧本只负责从 DAG 选段播放——详细设计段、同步任务段、派发收口、需求澄清/文档段与产物验收段的节点标题均以 DAG 为唯一来源，因此不要求界面展示的节点一致，但同一种工作流的底层图是一套。验收段回主对话执行：先播放“打开产物审查”节点，再挂“确认验收”待输入节点，确认后节点收口。任务完成即落“已完成”终态；完成后的后续步骤（如产物验收）以入口形式挂在任务条目上，点击在阶段主对话启动对应工作流——验收工作流打开右侧预览并承载确认，确认后产物标记完成、测试依赖解除。任务中心和产物状态是权威入口，完成通知不向对话末尾插入迟到的 Assistant 消息。

开发对象分别维护“未设计 → 设计中/实现排队/实现中/待验收 → 已完成/失败”状态。所有当前开发对象完成（验收通过）并通过开发门禁后即可进入测试阶段；尚未生成的测试用例在测试用例工作台中置灰，直到准备完成。

### 4. 测试阶段 · 测试 Agent

测试主对话从完整应用视角执行已经确认的业务测试用例。测试阶段先运行“非功测试工作流”（启动测试、非功能检查等少量节点），该工作流不选择或改变任何业务用例状态。随后按目录顺序为每条业务用例创建一条“用例测试工作流”。授权卡出现即表示当前用例 Workflow 已启动，目录将对应的单条用例标为紫色进行中；卡片只负责让用户确认是否执行这一条用例。确认后依次执行测试脚本、生成缺陷清单；有缺陷时继续调度返修节点并完成回归测试，无缺陷时直接结束当前 Workflow。当前用例的缺陷、修复状态和执行证据同步显示在右侧详情中。Workflow 完成且缺陷全部关闭后，该用例才标为绿色完成并进入下一条；尚未开始的用例始终保持灰色。用例执行本身不调用模型；执行器按依赖和运行资源进行有界并发，并记录证据。

失败用例生成结构化缺陷，关联用例、证据、受影响开发产物和代码 revision。缺陷修复作为用例测试 DAG 中的返修节点被调度，修复后必须执行对应回归节点；全部必需用例取得终态且阻塞缺陷关闭后，才允许进入审查阶段。

### 5. 审查阶段 · 审查 Agent

审查主对话只在测试门禁通过后进入，独立检查代码规范、安全、健康度、可维护性、计划一致性和交付完整性。审查 Agent 默认只读代码，只维护审查报告；审查问题通过开发主对话创建返修 Workflow。

### 6. 验收阶段 · 产品 Agent

验收主对话辅助用户依据需求文档和应用预览完成确认。验收阶段不新增正式文档；不通过意见由验收主对话判断是否属于当前基线，必要时进入新的需求迭代。

## 五、阶段二级对象清单与左侧导航

页面层级为：

```text
阶段主对话 → 当前阶段对象清单（按需出现） → 右侧文件/预览面板
```

- 设计阶段：右侧按 Tab 展示需求规格说明书和 UI 设计稿；页面行为与验收已合并在需求规格说明书内。
- 计划阶段：右侧展示 TechnicalPlan 的架构、实体、API 和页面绑定，并保留只读的「需求规格说明书」Tab，供审阅技术规划方案（实体、页面绑定）时对照需求来源。
- 开发阶段：展示页面、接口和实体清单及各自完成状态。
- 测试阶段：展示测试用例分组、执行状态和缺陷清单。
- 审查阶段：没有二级清单，右侧直接展示审查报告。
- 验收阶段：没有二级清单，右侧直接展示应用预览和验收动作。

二级清单只用于存在多个需要独立选择、执行或验收对象的阶段。它不是对话目录，也不提供“基于产物新建对话”操作。

二级清单属于阶段状态，不再长期占用左侧会话导航。基础原型的左侧固定为窄功能栏，上下两组：顶部是抽屉类入口「任务管理」和「后台任务」，一道横线收尾；用户功能入口「文件」「技能」「设置」与底部头像贴近、沉底成组。**「任务管理」是第一入口**：抽屉顶部是「常规任务 / 临时任务」固定 Tab 分栏（各带数量角标，默认选中常规任务，交互与异步/潮汐任务抽屉的 Tab 一致），下方单一列表平铺当前 Tab 的条目，条目内以“常规 / 临时”类型标签区分；两个 Tab 各有一行常显说明（位置与样式一致）——常规解释“当前推进”语义（仅当前推进的任务可执行工作流，其余任务仅供查看），临时解释能力边界（可用于讨论问答，无法执行工作流）。Tab 栏右侧「新建任务」按钮跟随当前 Tab——临时 Tab 下新建临时任务，当前推进任务未完成时按钮静态置灰并以提示说明原因。用户自建任务的删除按钮常显在条块右缘槽位内（各行同宽），删除需二次确认且不可恢复；阶段默认任务是唯一推进入口，不提供删除，运行中的任务同样不提供。常规任务支持自主新建、谨慎切换和唯一编辑权限；临时任务可以多开，只提供查阅、解释、分析和 Chat 类输入，不提供产物选择、Workflow、确认卡或写入能力。「后台任务」与顶部测试用例数量芯片是同一队列抽屉的两个入口；存在待验收实现任务时，入口悬浮文案直接给出“N 项待验收”。测试用例明细只通过该抽屉或右侧“测试用例”Tab查看，不在左侧功能栏重复放置入口。对象选择和 Workflow 进度均在中间主工作区或右侧工作台中呈现。

## 六、临时问答与任务管理

临时任务不再单设入口，被整合进左侧「任务管理」抽屉的“临时任务”过滤，并允许用户创建多条；初始不预置任何临时任务，切换到该过滤时列表为空，仅靠过滤标签下方的一行说明交代能力边界。它与常规任务共享管理入口，但保留明确的能力边界：负责代码解释、资料查阅、方案讨论和即时问题分析，不展示产物选择、Workflow、确认卡或写入工具，也不改变阶段、常规任务或产物状态。因为临时任务不占用正式编辑权限，所以即使某条常规任务正在运行或等待确认，用户仍可进入临时任务；这不会为其它常规任务解除输入锁。

如果用户在临时问答中提出修改要求，系统只形成结构化任务建议，并提示转到开发任务用 `@` 选择产物创建正式 Workflow。临时问答不会静默升级权限。

任务管理与临时问答共用统一辅助抽屉：

- 入口位于左侧窄功能栏第一项（「任务管理」）；
- 抽屉与后台任务队列抽屉完全同款同位：420px 面板从左侧菜单栏右侧滑出，头部为图标徽标、标题与一句话说明，不遮挡菜单栏、顶部栏与右侧面板；
- 各抽屉同一时间只能打开一个：打开任一个时自动关闭另一个；
- 抽屉覆盖主任务区域左侧，右侧文件/预览面板保持可见；
- 关闭后恢复主任务原位置和运行状态；
- 任务管理视图内可切换到「临时问答」视图，两者在同一抽屉内互切，临时消息历史保留；
- 临时任务可以多开，每条独立保存自己的受限 Chat 历史；
- 临时问答消息不进入阶段任务的 Workflow 上下文。

## 七、异步业务测试用例准备

### 1. 与单元测试的边界

异步生成的是业务场景、页面与接口组合路径、异常路径和数据校验用例，不替代开发阶段根据代码变更生成并执行的前后端单元测试。

### 2. 触发与执行

1. RequirementSpec 提供业务目标、角色、流程和验收标准。
2. 需求规格说明书带有预计测试用例概要（总量、业务场景和覆盖范围）；说明书确认后弹出任务类型选择，明确展示预计生成的用例总量。用户选择异步任务或潮汐任务后，后台创建独立测试用例准备任务，按业务模块和稳定场景分批调用模型。
3. TechnicalPlan 确认后，补齐页面、Endpoint、Schema 和实体绑定等可执行引用；能够确定性补齐的内容不重复调用模型。
4. 开发阶段正常推进，不等待后台生成完成。
5. 每批完成后立即校验并保存；单批失败只重试该批，不丢失其他结果。
6. 全部用例生成和校验完成后自动进入“已就绪”，不增加用户重复确认步骤。

后台状态为：

```text
排队中 → 生成中 → 校验中 → 已就绪
                    └→ 失败
上游正式产物变化 → 已失效 → 重新排队
```

用例集合绑定需求规格说明书、其内部页面行为和 TechnicalPlan 的当前 revision。上游正式产物修订后，受影响用例不得继续用于测试准入。

### 3. 顶部数量芯片、后台任务抽屉与测试用例工作台

顶部测试阶段只展示用例数量，不再显示“待确认”“已就绪”等状态文案，也不额外发送 Toast 或向阶段主对话重复插入通知：

```text
测试阶段 2/6
```

数量芯片是独立于阶段控制条的任务入口：它渲染在测试阶段按钮之外，即使阶段按钮因旅程未到达而禁用，芯片从计划确认起就始终可点击。悬停数量时提示“测试用例 2/6”；点击芯片动态绑定到用例任务实际所在的任务系统——开发准入门选定异步则打开“异步任务”抽屉，选定潮汐则打开“潮汐任务”抽屉，不触发阶段切换或进入确认。点击测试阶段本身或直接进入测试阶段，才会打开右侧“测试用例”Tab；测试阶段不提供浏览器 Tab。该 Tab 复刻开发产物的“目录 + 内容”布局：左侧只展示业务测试用例树，按场景分组并展示异步生成数量，右侧展示所选用例的前置条件、执行步骤、预期结果和本轮测试结果。

后台任务完成后带后续步骤（如产物验收）时有两类配套入口：任务抽屉条目上的「验收」按钮，以及开发主对话输入框工具栏的工作流入口（仅开发阶段对话提供，不随版本后台任务泄漏到分析/规划等阶段）——点开产物弹层后，待验收产物在自己的行上直接以「继续处理」徽标呈现（按钮化），点击即在主对话启动；工具栏图标保留紫色呼吸圆点提示存在待继续项，仅当开发阶段没有产物列表可承载时才退回独立的待继续列表兜底。两者复用同一条启动链路：定位产物 → 右侧打开产物审查 → 主对话播放验收段。同步任务在对话内确认代码变更后即交付完毕，不产生待验收，也不出现在该入口。

异步任务与潮汐任务从左侧菜单栏就拆成两个独立入口（沙漏/月亮图标；同步任务为闪电图标，仅出现在执行方式选择卡。图标语言：同步=闪电当场执行，异步=沙漏放后台慢慢跑，潮汐=闲时月亮；同步任务的验收入口在对话输入框，不在抽屉），各自滑出一个结构完全相同的队列抽屉：标题、头部徽标与入口共用同一套图标语言，从左侧菜单栏右侧滑出，不遮挡菜单栏、顶部栏与右侧面板，与对话管理/临时问答抽屉同款同位、同一时间互斥打开。抽屉内 Tab 分「进行中 / 已完成」两段：进行中按队列位次编号，一次只执行一个任务、其余按顺序排队；已完成按完成先后编号沉淀。行内不重复展示所属系统（所属关系由入口承载），队列抽屉只承载后台队列任务，同步任务不出现在抽屉中。已完成任务如带后续步骤（如产物验收），条目上提供入口按钮，点击在主会话启动对应工作流。

启动测试和非功能测试属于独立的“非功测试工作流”，但不进入“测试用例”目录。业务用例生成中时，目录使用进行中状态点；未生成的单条用例置灰且不可点击，生成完成后才可逐条查看，执行器运行后将结果更新为执行中、通过或失败。测试阶段不生成测试报告文件。

异步任务系统使用常规算力队列，潮汐任务系统使用低优先级的闲时队列；单条用例依次经过排队、生成、校验三个时段。潮汐系统的总时长刻意长于开发阶段演示时长，用于呈现“开发产物已全部完成，测试用例仍在后台生成”的排队效果。生成节奏由演示常量统一控制，两套系统仅节奏与算力口径不同，交互结构完全一致。

开发产物全部完成且最后一条 Workflow 收尾后，进入测试阶段弹框立即出现，不再额外等待用例准备任务；若用例仍在后台生成，弹框展示当前生成进度并提示可先进入测试阶段，未生成的单条用例在目录中置灰不可用。

用例生成失败或失效时，右侧用例内容区提供重新生成入口；同一时间不叠加测试用例工作台与辅助抽屉。

测试工作流按目录顺序逐条推进时，必须等当前用例在后台生成队列中就绪才会出现执行确认卡；未就绪期间不产生执行状态、不生成用例 Workflow，右侧目录保持置灰。

## 八、阶段准入

阶段是否可查看、是否具备进入条件和当前正在执行哪个阶段必须分开记录。

开发进入测试至少要求：

- 当前开发对象清单全部完成（完成以用户在待验收中接受产物为准）；
- 后台实现任务的构建与单元检查通过（随任务到达待验收即视为通过）；
- 开发阶段单元测试、联调、构建和基础启动检查通过；
- 当前 revision 的业务测试用例总量已确定；未生成的用例在测试工作台中置灰，生成后再开放查看和执行。

测试进入审查至少要求：

- 全部必需用例执行完成；
- 不存在未关闭的阻塞缺陷；

测试发现缺陷时，受影响开发对象回到“进行中/待修复”，但不得抹掉其曾经完成开发的历史。修复后重新执行受影响用例及必要的应用级验证。

## 九、右侧工作区与版本规则

右侧按阶段提供“开发产物”“测试用例”“应用文件”和必要时的“浏览器”能力。“应用文件”在每个阶段始终可用，作为统一项目文件管理系统，汇总已生成的项目文档与已接受的源码；需求规格说明书、UI 清单和技术规划方案在其中只读查看。开发阶段默认进入“开发产物”，测试阶段默认进入“测试用例”，这两个阶段都不提供浏览器 Tab；需求分析/项目计划也不提供浏览器 Tab。开发产物和测试用例都采用“目录 + 内容”双栏，目录宽度受控，层级可折叠，右侧根据当前叶子对象展示页面预览、接口调试或用例详情。只有技术规划方案确认后，顶部开发阶段旁才显示开发产物完成比例，顶部测试阶段旁才显示预计用例生成数量；确认前两个数字均不出现。开发目录中的产物展示领域状态点：未开始（灰）、实现排队（空心紫）、实现中（紫色呼吸）、待验收（紫点外圈光晕）、已完成（绿）、失败（红）；待验收页面即可在内容区打开预览，验收完成后保持可预览。测试目录中的用例展示生成状态点；同一版本内已确认完成的产物状态只增不减，目录刷新或下一条 Workflow 不得回退完成数。点击对象只选择当前 Workflow 或测试执行对象，不切换阶段主对话。开发阶段不再出现代码 Diff 授权页签；审查报告的 Diff 确认可临时切换到“应用文件”。应用文件使用同一源码编辑/只读视图；Markdown 文档不自动切换预览态，阶段主对话始终保持不变。

版本生成后固化当前资产快照并锁定为只读。只有需求和计划有效、开发对象完成、测试门禁通过、审查通过且用户明确验收通过时，才允许生成版本。历史版本不恢复写入型 Workflow，也不允许临时问答获得写权限。

## 十、关键旅程

### 新建应用

新建旅程的工作区目录必须与预置演示应用互斥：mock 目录选择器始终给出全新目录，手动输入预置演示应用的路径会被“已有 DevAgent Studio 应用目录不能复用”拦截；应用索引按工作区路径去重、预置应用优先，因此最近项目里 v1.3 存量应用始终只有一条，专用于演示回退旅程。

创建迭代版本 → 需求分析主对话确认需求文档 → 项目计划主对话确认项目计划并启动后台用例生成 → 开发任务中 `@` 选择产物、确认详细设计并派发实现任务、验收待验收产物 → 进入测试主对话检查已生成用例并持续等待其余用例 → 用例测试 DAG 执行、修复缺陷并回归 → 审查主对话确认审查报告 → 用户验收 → 生成只读版本。

### 多产物开发

进入开发阶段 → 研发 Agent 投放一次 `@` 选择引导 → 用户输入 `@` 从候选中选“我的回检”并发送 → 当前任务投放该页面的模板选择卡 → 用户确认模板 → 前台 Workflow 完成详细设计并在轨迹末尾挂出「选择执行方式」节点 → 用户在内嵌选择卡上选“同步任务 / 异步任务 / 潮汐任务”：选后台通道则在对应任务系统派发后台实现任务、会话锁立即释放；选同步任务则当场在任务内播完生成节点并把记录沉淀到异步系统 → 用户可切到另一条开发任务（或继续在当前任务）`@` “回检介绍”发起下一个产物 → 两套任务系统并行、系统内串行地执行 Build DAG、代码生成、构建/单测和产物审查 → 任务完成后条目提供「验收」入口 → 点击在发起它的任务启动验收工作流，右侧打开产物审查、任务中确认验收 → 产物标记完成；两条实现任务完成后开发门禁开放。全过程不向任务插入迟到的完成通知，产物完成也不会自动触发下一张模板卡——下一个产物始终由用户 `@` 发起。

### 临时问答

用户打开左侧「任务管理」→ 切换到「临时问答」→ 只读询问当前文件或产物 → 返回任务管理切换回开发任务；如果需要修改，生成任务建议并转交开发任务用 `@` 选择产物，临时问答本身不执行 Workflow。

### 异步测试准备

需求规格说明书确认 → 顶部测试阶段显示生成数量 → 用户在开发过程中按需打开“测试用例”Tab查看分组进度 → 后台分批生成和校验 → 用例可逐条查看 → 开发门禁完成后进入测试 → 确定性执行全部用例并记录结果与缺陷。

## 十一、通用交互原则

**过程互动在对话区，右侧面板只承载静态与终态信息。** 用户的交流是连续的，一切操作类的过程互动（选择、授权、确认推进等）都以对话区卡片为主要载体；右侧面板是补充信息的呈现位，尽量只放静态信息与终态信息（产物审阅、预览、完成态）。例如 UI 设计稿的“选模板”在对话区确认卡内逐页完成（展开低保真线框翻看候选、“使用此模板”记录选择，全部选定后统一后台重画），右侧仅保留静态预览与已确认标记，不设第二套确认入口。UI 设计稿的模板预览使用低保真线框（与开发阶段同一模板目录，但只画版式骨架、损失视觉细节）——设计稿阶段只定结构、不定像素。**例外：字段映射工作台（第八十批起）**——应用API的数据绑定允许从右侧直接发起、编辑与确认（直连绑定），与对话区工作流节点并存为两条等效路径，确认动作都是用户显式点击、语义一致。

**禁用态是静态视觉行为，不是悬停后的反馈。** 控件不可用时必须第一时间以静态样式直接呈现——置灰、去强调、去除可点击观感（参照历史澄清卡的按钮、选项与输入框整体灰化）。禁止用鼠标悬停后才出现的信号表达不可用：光标形态切换（如 `not-allowed`）和悬停提示都是滞后的设计，不作为禁用的呈现手段，禁用控件保持默认光标。判断标准：不移动鼠标、不触发悬停，用户扫一眼界面就能分辨哪些可操作、哪些不可操作。
